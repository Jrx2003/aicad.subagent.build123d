# Archived Documentation

This directory holds historical materials that are intentionally kept out of the
live architecture narrative.

Use `docs/cad_iteration/` for current runtime policy and preferred package
surfaces.

Archive contents may preserve old paths, old module names, and old planning
routes without being kept in sync with the current codebase.
