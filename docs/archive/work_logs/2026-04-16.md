# 2026-04-16

## 目标

今天的工作重点有两条：

1. 把 Build123d 幻觉从口头判断变成可落盘、可聚合、可解释的诊断面。
2. 停止围绕固定 benchmark 继续补零散 hard rule，转到 `practice_runs/` 自生成复杂 prompt lane，用真实 LLM run 的中间产物来定位 write/read/repair/validation 四层瓶颈。

## 已完成

### 1. practice lane 已形成稳定入口

新增：

1. `practice/seed_manifest.json`
2. `src/sub_agent_runtime/practice_runner.py`
3. `scripts/run_custom_prompt_practice.py`
4. `scripts/run_topology_targeting_practice.py`

当前 practice 产物结构已经与 benchmark 对齐到同一风格：

1. `prompt.txt`
2. `practice_case.json`
3. `plans/`
4. `actions/`
5. `queries/`
6. `outputs/`
7. `trace/`
8. `evaluation/`
9. `summary.json`
10. `practice_analysis.json`
11. `run_diagnostics.json`

### 2. Build123d 幻觉指标已接入 practice / benchmark 聚合

新增代码：

1. `src/sub_agent_runtime/hallucination.py`

当前事件已经能区分：

1. `write_surface`
2. `read_surface`
3. `repair_surface`
4. `validation_surface`

落盘位置：

1. case summary
2. run summary
3. brief report
4. run diagnostics

### 3. validator grounding 与 feature probe grounding 已接上主路径

新增模块：

1. `src/sandbox_mcp_server/validation_grounding.py`
2. `src/sandbox_mcp_server/feature_probe_grounding.py`

同步扩展：

1. `src/sandbox_mcp_server/contracts.py`
2. `src/sandbox_mcp_server/service.py`
3. `src/sub_agent_runtime/feature_graph.py`
4. `src/sandbox/mcp_runner.py`

重点修复：

1. `validate_requirement` 的 grounding surface 不再在 MCP 边界丢失。
2. `query_feature_probes` 的 family binding / blocker / anchor 信息可以继续写回 domain kernel digest。

### 4. 修复 Kimi tool schema 兼容问题

代码：

1. `src/llm/providers/openai_compatible.py`

新增行为：

1. 对 `kimi-*` 模型发送工具 schema 前，移除 `$ref` 节点上会触发 moonshot flavored schema 冲突的本地 `description`。

对应真实证据：

1. 旧失败目录：`practice_runs/20260416_050500/`
2. 修复后继续推进目录：`practice_runs/20260416_063500/`

### 5. 修复 practice preview 的异步关闭问题

代码：

1. `src/sub_agent_runtime/practice_runner.py`

修复点：

1. 不再在 async context 里嵌套 `asyncio.run(...)`
2. 改为直接 `await render_generated_previews_async(...)`

### 6. 新增两条泛化 API governance 规则

代码：

1. `src/sub_agent_runtime/tool_runtime.py`

新增规则：

1. `invalid_build123d_context.buildpart_topology_access_inside_buildsketch`
2. `invalid_build123d_contract.member_fillet_radius_keyword_conflict`

用途：

1. 把“在 `BuildSketch` 里拿外层 `BuildPart.vertices()/edges()/faces()` 当草图几何”的 builder 生命周期误用前移到 preflight lint。
2. 把成员式 `.fillet(<edge>, radius=...)` 的签名冲突前移到 preflight lint。

对应单测：

1. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`

验证结果：

```bash
uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q
```

```text
68 passed
```

## 今日关键真实产物

### A. topology targeting capability probe

目录：

1. `practice_runs/20260416_043500/topology_targeting_capability_probe/`

价值：

1. 证明 `query_topology` 能返回 `face_ref / edge_refs`
2. 证明 `create_sketch(face_ref=...) -> cut_extrude -> query_topology -> chamfer(edge_refs)` 已真实成功
3. 证明 evaluation 多视图渲染已可落盘：
   - `evaluation/generated_preview_iso.png`
   - `evaluation/generated_preview_front.png`
   - `evaluation/generated_preview_right.png`
   - `evaluation/generated_preview_top.png`

### B. whole-part complex clamshell read chain

目录：

1. `practice_runs/20260416_065500/organic_clamshell_storage_midhigh_v00_0605b8a8/`

价值：

1. 证明 `validate_requirement -> query_feature_probes -> query_kernel_state -> execute_build123d_probe` 已真实被使用。
2. 证明 runtime 已能把复杂 enclosure 的 blocker 收口成 family/blocker/anchor 结构，而不只是报一句“没做好”。

### C. whole-part complex clamshell write hallucination breakdown

目录：

1. `practice_runs/20260416_070500/organic_clamshell_storage_midhigh_v00_0605b8a8/`

价值：

1. 证明当前主瓶颈已经高度集中在 `write_surface`。
2. `practice_analysis.json` 中已经有清晰的 hallucination breakdown：
   - `invalid_api_contract = 1`
   - `write_runtime_exception = 2`

## 当前判断

今天之后，主矛盾已经更清楚了：

1. 当前系统不是“没有反馈机制”，而是 read-model 与 validation grounding 的分辨率还不够均衡。
2. 当前系统也不是“完全不会用 Build123d”，而是 lingering 幻觉集中在 builder 生命周期、sketch/solid 边界、fillet 契约。
3. 继续做 benchmark 拟合的收益已经下降，应该继续用 custom practice lane 压复杂新 prompt。

## 文档更新

今天同步重写：

1. `docs/interview_prep/2026-04-15_反馈机制与泛化瓶颈调查.md`

重点补齐：

1. 幻觉指标说明
2. prompt -> write -> reads -> validate -> kernel -> repair 的链路解释
3. `043500 / 065500 / 070500` 三条真实案例的证据片段
4. face/edge 是否能按 id 选取的明确结论
5. 当前瓶颈到底在 write、read、repair 还是 validation 的分层判断

## 继续推进

### 7. 把 active-builder 幻觉从一类错误拆成两类

本轮继续补强：

1. `invalid_build123d_contract.active_builder_temporary_primitive_transform_rebind`
2. `active_builder_temporary_primitive_transform_contract`
3. `nested_hollow_section_same_builder_subtract_contract`

作用：

1. 不再把 `Rot(...) * solid` / `Pos(...) * solid` 这类 active `BuildPart` 内的 transform 重绑误记成普通 boolean staging。
2. 对 enclosure / shell / lid / base 这类空心结构，给出更具体的 builder-native cavity repair skeleton，而不是只返回泛泛的“不要 temporary arithmetic”。

### 8. validation LLM provider timeout 已接入 diagnostics / hallucination

本轮补齐：

1. `src/sandbox_mcp_server/validation_llm.py`
2. `src/sandbox_mcp_server/service.py`
3. `src/sub_agent_runtime/hallucination.py`

效果：

1. `validation_llm` 超时/无效输出不再只是日志里的一条 warning。
2. 现在会写入 `validate_requirement` 的 `observation_tags / decision_hints`。
3. run-level hallucination 可以把它归到 `validation_surface`，便于和普通 geometry failure 区分。

### 9. 新一轮真实 practice 结果

目录：

1. `practice_runs/20260416_083500/organic_clamshell_storage_midhigh_v00_0605b8a8/`
2. `practice_runs/20260416_091500/organic_clamshell_storage_midhigh_v00_0605b8a8/`

关键观察：

1. `083500` 证明当前高难 clamshell case 的 hallucination 仍以 `write_surface` 为主，`summary.json` 中 `event_count=11`、`weighted_score=11.0`，且主层全部集中在 `write_surface`。
2. `091500` 的 round 1 已不再出现 `ocp_vscode/show`，说明 hygiene bias 已开始生效。
3. `091500` 的 round 2 已成功产出首个 solid，并进入 `validate_requirement -> query_feature_probes` 链路，说明系统已经从“代码经常跑不通”进一步推进到“能产出 base half，但 multi-part enclosure 仍未完全收口”。
4. `091500` 暴露的新主矛盾不是简单 API 猜错，而是 multi-part enclosure 的 envelope / part-count / hinge-bbox 收口，以及 validation 对 hinge / magnet / lid-base separation 的 grounding 仍偏弱。

### 10. 本轮验证

```bash
uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_hallucination.py tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py -q
```

```text
115 passed
```

### 11. validation LLM request-level budget gate

本轮继续把 `validation_llm` 的治理点从 provider timeout 后补救，前移到 `service.py` 的 request 级闸门。

新增：

1. `_VALIDATION_LLM_MAX_ESTIMATED_PROMPT_CHARS = 7000`
2. `_estimate_validation_llm_prompt_chars(...)`
3. `validation_llm_skipped:estimated_prompt_budget_exceeded:<estimated>/<budget>`

意图：

1. 不再让复杂 practice prompt 在 unresolved clause 数不高时，仍然把超大 geometry/topology/process/code surface 一起送进 adjudicator。
2. 保持 `validation_llm.py` 继续只负责 prompt 组装、provider 调用和 JSON 解析；“该不该调用它”留在 `service.py`。

### 12. front-face recess 误判已清掉，剩余瓶颈转成 closure policy

本轮新增修复：

1. `src/sandbox_mcp_server/validation_grounding.py`
2. `src/sandbox_mcp_server/validation_interpretation.py`
3. `src/sub_agent_runtime/feature_graph.py`
4. `src/sub_agent_runtime/agent_loop_v2.py`

核心变化：

1. 新增 `named_face_local_edit_feature` grounding rule，把 `recess / pocket / notch / slot` 这类局部面特征从 hole/layout family 中分离出来。
2. `overall_dimensions` 的 clause match 收窄，不再因为文本里出现 `12 x 6` 这类局部尺寸，就拿整件 bbox 去做“整体尺寸”对比。
3. `clause:body_shape` 不再误标记局部 recess/pocket/notch。
4. `named_face_local_edit` 的 validation signal 不再混入 hole-only anchors。
5. `agent_loop_v2` 新增 last-round local-finish closure 分支：当 `blockers=[]` 但仍有 `insufficient_evidence` 且只剩最后一轮时，policy 不再只开放 read refresh，而是允许“focused read + validate_requirement / finish_run”同轮收口。

直接证据：

1. `practice_runs/20260416_175500/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_06_validate_requirement.json`
2. `practice_runs/20260416_175500/front_face_sketch_recess_focus_v00_194ed1dd/trace/failure_bundle.json`
3. `practice_runs/20260416_175500/front_face_sketch_recess_focus_v00_194ed1dd/trace/domain_kernel_round_08.json`

关键结论：

1. `add_a_centered_rounded_rectangle_recess_on_the_front_face_sized_about_12mm_x_6mm` 已不再被错误归因为 hole-layout 或整体 bbox mismatch。
2. round 4 / round 7 的 `apply_cad_action` 都已成功，说明 topology-targeted local finish 链路本身是通的。
3. 当时最终没有 complete，主因已经不再是 geometry 错误，而是 `blockers=[]` 后的 evidence closure 还会把最后一轮浪费在 read-only refresh 上。

本轮验证：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py tests/unit/sub_agent_runtime/test_feature_graph.py -q
```

```text
47 passed
89 passed
```

### 12. countersink contract 归一化后，局部拓扑链路的幻觉已清零

目录：

1. `practice_runs/20260416_235100/topology_local_finish_focus_v00_95ab3188/`

关键证据：

1. `summary.json` 中 `summary.build123d_hallucination.event_count=0`、`summary.build123d_hallucination.weighted_score=0`，说明这条 topology-aware local finish case 的 Build123d 幻觉已经被压到 0。
2. `queries/round_03_validate_requirement_auto.json` 已把 `feature_target_face_edit`、`feature_target_face_subtractive_merge`、`feature_notch_or_profile_cut` 全部判为 `pass`，证明面向目标面的局部 cut / notch / merge 链路已经开始稳定收口。
3. `queries/final_validate_requirement.json` 仍然保留 `feature_target_face_edit`、`feature_target_face_subtractive_merge`、`feature_notch_or_profile_cut`、`a_front_thumb_notch` 这组 blocker，并带有 `insufficient_evidence` 与 `validation:llm_skipped` 标签，说明末端失败已经不再是写 API 幻觉，而是 stale validation / stale blocker 没有及时回收。
4. `actions/round_08_apply_cad_action.json` 的最后一步 fillet 直接报 `Exit code: 1`，对应 OCP `StdFail_NotDone`，进一步说明最终失败集中在“继续 local finish 的几何稳定性”，而不是“不会写 countersink / hole”。

当前结论：

1. `countersink_radius -> countersink_diameter` 的 canonical 归一化已经把一类 shared hallucination 消掉了。
2. 新主矛盾转为“成功 local finish + 语义刷新之后，没有优先切回 `validate_requirement`，而是继续沿用旧 blocker 进入下一次局部写入”。

### 13. `validate_after_local_finish_semantic_refresh` 已在真实 run 中生效

目录：

1. `practice_runs/20260416_235900/topology_local_finish_focus_v00_95ab3188/`

关键证据：

1. `practice_analysis.json` 的 `runtime_summary.forced_policy_chain` 已包含 `validate_after_local_finish_semantic_refresh`，说明这条 policy 不是只存在于单测里。
2. `trace/failure_bundle.json` 的 `recent_decision_logs` 显示 round 7 确实执行了 `validate_requirement`，不再是 local finish 后直接连写。
3. `queries/round_07_validate_requirement.json` 已返回 `blockers=[]`、`insufficient_evidence=true`、`coverage_confidence=0.8`，且 `two_mounting_holes` 已被判为 `verified`，说明 validation 已进入 evidence-first 判定，不再只是粗粒度 blocker 列表。

当前结论：

1. stale-validation 的第一层问题已经被压下去，runtime 至少会在 local finish 成功后主动重跑一次 validation。
2. 残余问题转到 face-attached local frame 和 count / placement evidence 没有完全闭合。

### 14. 新增 `Countersink(...)` 治理规则后，topology rerun 的首写已稳定成功

目录：

1. `practice_runs/20260416_235930/topology_local_finish_focus_v00_95ab3188/`

关键证据：

1. `actions/round_01_execute_build123d.json` 显示首写 `success=true`，没有再出现上一轮 `Countersink(...)` 的 runtime NameError。
2. `queries/round_06_validate_requirement.json` 已经达到：
   - `summary = Requirement validation has insufficient evidence`
   - `blockers = []`
   - `two_mounting_holes = verified`
   - `a_countersink_on_the_mounting_face_so_that_a_topology_aware_local_finishing_pass_is_useful = insufficient_evidence`
3. run-level 汇总表明 `build123d_hallucination.event_count=0`，说明 write-side hallucination 已从这条 case 中消失。

当前结论：

1. 新的 `invalid_build123d_api.countersink_helper_name` 规则已经把一类共享 helper-name 幻觉前移到治理层。
2. 当前 topology practice 的主矛盾已经进一步收窄到“validation 要更多 geometry/topology evidence，但 domain-kernel / local-finish lane 还会拖着系统进入 read stall”。

### 12. `20260417_064500`：早期 write-surface 幻觉基本被压平，只剩 1 个运行期异常

真实产物：

1. `practice_runs/20260417_064500/summary.json`
2. `practice_runs/20260417_064500/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_03_execute_build123d.json`

关键结果：

1. `hallucination_event_count = 1`
2. `first_write_event_count = 0`
3. `primary_layer = write_surface`
4. 对比前两轮：
   - `20260417_054500: 7 / 4`
   - `20260417_061500: 5 / 1`
   - `20260417_064500: 1 / 0`

剩余唯一异常：

1. round 3 的运行期错误是
   - `Ellipse(major_radius=14, minor_radius=10)`
   - `TypeError: Ellipse.__init__() got an unexpected keyword argument 'major_radius'`

这说明：

1. 前面已经补上的 `case_drift_local_symbol` 与 skill `context_priority` 收口是有效的。
2. 当前 whole-part clamshell lane 的主问题，已经不再是“前几轮经常写出大量 Build123d 幻觉”。
3. 当前最值得继续做的，是把最后这类 helper keyword alias 错误继续前移成 preflight lint，而不是等容器跑炸。

### 13. 新增 generalized fix：把 `Ellipse(major_radius/minor_radius)` 前移成 API governance 规则

实现：

1. `src/sub_agent_runtime/tool_runtime.py`
   - `invalid_build123d_keyword.ellipse_major_radius_alias`
   - `invalid_build123d_keyword.ellipse_minor_radius_alias`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - `test_preflight_lint_rejects_ellipse_major_minor_radius_aliases`

验证：

```bash
uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -k 'ellipse_major_minor_radius_alias or case_drift_local_symbol' -q
```

```text
2 passed, 107 deselected in 1.51s
```

```bash
uv run pytest tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sandbox_mcp_server/test_validation_geometry_fallbacks.py -q
```

```text
223 passed in 1.67s
```

意图：

1. 这不是给 `Ellipse` 这一行打补丁。
2. 这是继续把“Build123d helper 参数别名猜错”这一类共享 write-surface 幻觉，从 runtime exception 前移到更早、更可修的 lint surface。

### 14. `20260417_070500`：local finish lane 已真实进入主路径，但 exact-ref retry 还不够强

真实产物：

1. `practice_runs/20260417_070500/summary.json`
2. `practice_runs/20260417_070500/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_07_apply_cad_action.json`
3. `practice_runs/20260417_070500/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_08_apply_cad_action.json`
4. `practice_runs/20260417_070500/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_06_query_topology.json`

关键结论：

1. 这轮已经不再卡在“永远上不到 local finish”。
2. `forced_policy_chain` 已出现：
   - `local_finish_after_actionable_feature_probe_refresh`
   - `retry_local_finish_with_existing_topology_refs`
3. `query_topology` 已经返回大量真实面/边 ref 与候选集合。
4. 但 round 7 / 8 的 `apply_cad_action` 都失败在同一 exact-ref 契约：
   - `create_sketch must use face_ref from latest query_topology during local_finish`

这说明：

1. 当前瓶颈已经从“有没有 topology targeting / 有没有 local finish lane”，进一步收敛成“失败后如何把 exact ref 强制绑定进下一次局部动作参数”。

### 15. 新增两条 generalized fix：local-finish retry priority + Plane pose contract lint

#### 15.1 local-finish retry priority

实现：

1. `src/sub_agent_runtime/skill_pack.py`
   - 当 `latest_tool == "apply_cad_action"` 时，提升以下 skill 的 `context_priority`：
     - `local_finish_retry_bind_latest_face_ref`
     - `topology_candidate_set_label_is_not_exact_ref`
     - `local_finish_exact_face_ref_contract`
     - `named_face_local_feature_sequence`
2. `tests/unit/sub_agent_runtime/test_context_manager.py`
   - `test_prepare_runtime_skills_payload_keeps_local_finish_retry_guidance_when_truncated`

意图：

1. 不再让失败后的 exact-ref retry guidance 被 generic execute_build123d 技巧挤出前 6 条 runtime skills。
2. 让 bounded local-finish turn 的主提示真正围绕“消费最新 topology refs”组织。

#### 15.2 Plane pose contract lint

实现：

1. `src/sub_agent_runtime/tool_runtime.py`
   - `invalid_build123d_api.plane_located_shape_method_guess`
   - `build123d_plane_translation_contract`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - `test_preflight_lint_rejects_plane_located_shape_method_guess`

真实来源：

1. `practice_runs/20260417_070500/.../actions/round_03_execute_build123d.json`
   - `Plane.XY.located((0, 0, wall))`
   - `AttributeError: 'Plane' object has no attribute 'located'`

验证：

```bash
uv run pytest tests/unit/sub_agent_runtime/test_context_manager.py -k 'local_finish_retry_guidance_when_truncated or high_priority_enclosure_repairs_when_truncated' tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -k 'plane_located_shape_method_guess or ellipse_major_minor_radius_alias or case_drift_local_symbol' -q
```

```text
3 passed, 114 deselected in 1.84s
```

```bash
uv run pytest tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sandbox_mcp_server/test_validation_geometry_fallbacks.py -q
```

```text
225 passed in 2.50s
```

### 16. `20260417_071500` 暴露出 case-drift lint 的一个泛化误伤，并已修正

真实产物：

1. `practice_runs/20260417_071500/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_02_execute_build123d.json`

问题：

1. 这轮没有再复发 `Plane.located(...)`。
2. 但 round 2 被 `case_drift_local_symbol` 错拦：
   - 误把合法的 Build123d helper `Pos` 当成了局部变量 `pos` 的大小写漂移。

根因：

1. 旧规则只看“本地已绑定名称 + 大小写相近”，却不知道 `from build123d import *` 已经把 `Pos` 导入为合法库符号。

修复：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增 `_build123d_exported_symbol_names()`
   - 新增 `_module_imports_build123d_symbols(...)`
   - `case_drift_local_symbol` 只在真正的局部未定义漂移上报，不再误伤 Build123d 导出 helper
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - `test_preflight_lint_does_not_mistake_build123d_pos_for_local_case_drift`

验证：

```bash
uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -k 'case_drift_local_symbol_before_container_runtime or build123d_pos_for_local_case_drift or plane_located_shape_method_guess' -q
```

```text
3 passed, 108 deselected in 4.49s
```

```bash
uv run pytest tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sandbox_mcp_server/test_validation_geometry_fallbacks.py -q
```

```text
226 passed in 5.04s
```

单测：

```bash
uv run pytest tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py -q
```

新增覆盖：

1. `test_validate_requirement_skips_validation_llm_when_estimated_prompt_budget_exceeded`

### 12. practice preview runner lifecycle 修复

并行排查确认：practice lane 结束时的

```text
Attempted to exit cancel scope in a different task than it was entered in
```

不是主 runner 没关，而是 `benchmark/step_similarity_eval.py::_evaluate_step_pair_async()` 新建了短生命周期 `McpSandboxRunner` 但没有显式 `await runner.aclose()`。

本轮修复：

1. 在 `_evaluate_step_pair_async()` 里用 `try/finally` 包住 `runner.execute(...)`
2. `finally` 中强制 `await runner.aclose()`

新增单测：

1. `tests/unit/benchmark/test_step_similarity_eval.py`
2. 覆盖 success / execute 抛异常两条路径都必须显式 close

验证：

```bash
uv run pytest tests/unit/benchmark/test_step_similarity_eval.py tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py -q
```

```text
29 passed
```

### 13. 旧 run 基线症状：`practice_runs/20260416_094500`

这一轮仍是旧代码起跑，因此保留了两条对照基线：

1. `validation_llm_provider_error`
2. preview 完成后的 cross-task cancel-scope 异常

价值：

1. 后续新 run 只要把这两条分别压成 `validation_llm_skipped:estimated_prompt_budget_exceeded` 和干净退出，就能直接证明本轮治理收口确实生效，而不是只停留在单测。

### 14. 新 run 对照结果：`practice_runs/20260416_132500`

这一轮已经把上面的两条旧症状明确压下去：

1. `queries/round_01_validate_requirement_post_write.json`
   - 不再出现 `validation_llm_provider_error`
   - 真实落盘为 `validation_llm_skipped:eligible_clause_budget_exceeded:7`
2. practice preview 正常完成
   - 生成了 `evaluation/generated_preview_{iso,front,right,top}.png`
   - 终端末尾没有再出现 cross-task cancel-scope 异常

这说明两条治理动作已经不只是单测通过，而是进入了真实 LLM practice 链路：

1. validation 复杂度闸门开始在真实 case 上工作
2. preview 后处理 runner 生命周期已经真正收口

但 `132500` 也把新的主瓶颈进一步收窄了：

1. round 1 首写成功，round 2 成功使用 `query_kernel_state + query_feature_probes`
2. round 5 最终失败不再是 provider timeout，也不是 sandbox 关闭异常
3. 最终主失败集中到 `invalid_build123d_contract.active_builder_temporary_primitive_arithmetic`
4. 更具体地说，模型在最后 repair turn 退回了错误的 active builder 心智，开始写 `base.part = ...` / `lid.part += ...` 这类 host-part mutation

### 15. 新增 `active_builder_part_mutation` 治理面

针对 `132500` round 5 暴露出的共享误用，本轮补了一条更直接的治理规则：

1. `invalid_build123d_contract.active_builder_part_mutation`
2. `active_builder_part_mutation_contract`

覆盖模式：

1. `host.part = ...`
2. `host.part += ...`
3. `host.part -= ...`

约束含义：

1. `with BuildPart() as host:` 还没结束时，`host.part` 不是稳定的 detached staging solid
2. active builder 内部应继续用 builder-native `mode=Mode.ADD` / `mode=Mode.SUBTRACT`
3. 如果真的需要 detached boolean，必须先结束 host builder，再在 builder 外做 `result = host.part +/- cutter`

验证：

```bash
uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_skill_pack.py -q
```

```text
90 passed
```

### 16. 继续收紧 artifactless failure 的 probe closure policy

本轮继续把失败后 turn policy 从“模型自行理解 probe 该何时结束”前移成 runtime 显式约束。

新增：

1. `kernel_refresh_after_successful_artifactless_probe`
2. `code_repair_after_first_concrete_code_failure`

含义：

1. 如果 artifactless code failure 后已经有一次成功 `execute_build123d_probe`，下一轮不再允许继续 probe，而是强制 `query_kernel_state` 关闭 probe 链。
2. 如果第一次 whole-part 写已经给出 concrete Build123d contract failure（例如 preflight lint / selector / curve / sweep recipe 等），第二轮直接收口到 `execute_build123d + query_kernel_state`，不再把 probe 当默认下一步。

新增单测：

1. `test_successful_artifactless_probe_requires_kernel_refresh_before_more_probe_turns`
2. `test_first_concrete_build123d_api_lint_failure_stays_on_code_repair_lane`

验证：

```bash
uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_hallucination.py -q
```

```text
109 passed
```

### 17. 新增 `plane_tuple_multiplication` 与 `Loc(...)` 治理规则

继续把高频 Build123d 幻觉从 runtime exception 前移到 preflight lint。

新增：

1. `invalid_build123d_contract.plane_tuple_multiplication`
2. `build123d_plane_tuple_multiplication_contract`
3. `invalid_build123d_api.loc_helper_name`
4. `build123d_location_helper_contract`

对应误用：

1. `Plane.XY * (x, y, z)` 这类把 plane 当 tuple transform surface 的写法
2. `Loc(...)` 这类不存在的 helper alias

目的：

1. 不再等容器抛 `Planes can only be multiplied with Locations or Shapes`
2. 不再等 whole-part repair round 执行到一半再报 `NameError: Loc is not defined`

### 18. 真实 run 证据：`practice_runs/20260416_151500`

这一轮最重要的不是最终通过，而是控制流已经出现了明确改观。

关键链路：

1. round 1：`execute_build123d` 首写被 enclosure shell temporary primitive arithmetic 拦下
2. round 2：模型直接回到 `execute_build123d` 修代码，没有先走 probe
3. round 3：在 artifactless runtime failure 后，模型执行一次成功 `execute_build123d_probe`
4. round 4：runtime 真实落盘 `kernel_refresh_after_successful_artifactless_probe`，只允许 `query_kernel_state`
5. round 5：回到 targeted `execute_build123d` repair，最终生成 `model.step`

直接证据：

1. `practice_runs/20260416_151500/organic_clamshell_storage_midhigh_v00_0605b8a8/trace/events.jsonl`
   - `round 4 turn_tool_policy = kernel_refresh_after_successful_artifactless_probe`
2. `practice_runs/20260416_151500/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_03_execute_build123d_probe.json`
   - probe 成功返回 `model.step + geometry_info.json`
3. `practice_runs/20260416_151500/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_04_query_kernel_state.json`
   - runtime 按 policy 做 semantic refresh，而不是继续 probe
4. `practice_runs/20260416_151500/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_05_execute_build123d.json`
   - round 5 repair write 成功
5. `practice_runs/20260416_151500/organic_clamshell_storage_midhigh_v00_0605b8a8/summary.json`
   - `step_file_exists = true`
   - `validation_complete = false`

结论：

1. 主要矛盾继续从“失败后控制流反复空转”转向“最终几何和 validator evidence 还不够贴合 enclosure 细节要求”。
2. `kernel_refresh_after_successful_artifactless_probe` 已经不是纸面策略，而是进入了真实 LLM run。

### 19. 继续修 DomainKernelState 的陈旧绑定污染

在 `practice_runs/20260416_154500` 与 `practice_runs/20260416_164515` 里都看到一个更细但影响很大的问题：

1. 新一轮 whole-part write 已经把 bbox 修回正确值
2. 但 `query_kernel_state` / `active_feature_instances` 里仍可能混入旧的 `bbox` / `realized_bbox`
3. 在失败写之后，`geometry_summary` 还可能被一个只有 `persisted=false, step_file=null` 的空壳 execution binding 覆盖

本轮新增两条单测：

1. `test_domain_kernel_refreshes_feature_instance_bbox_from_latest_write_after_validation`
2. `test_domain_kernel_keeps_latest_meaningful_geometry_after_failed_write`

对应修复：

1. feature instance 在同步时，会用最新成功写入的 geometry anchors 覆盖旧的 bbox / bbox_min / bbox_max / span
2. `require_geometry=True` 不再把“空壳 geometry_summary”当成有效几何证据

验证：

```bash
uv run pytest tests/unit/sub_agent_runtime/test_feature_graph.py -q
```

```text
3 passed
```

### 20. 真实 run 证据：`practice_runs/20260416_164515`

这轮比前两轮更能说明主路径已经开始收敛。

关键链路：

1. round 1：首写直接成功，但 bbox 是 `78 x 61.6 x 53.0`
2. round 2：按 policy 做 `query_feature_probes + query_kernel_state`
3. round 3：再次 whole-part repair，bbox 修到 `78 x 56 x 32`
4. round 4：继续做一轮 feature probes，聚焦剩余 notch / thickness blocker
5. round 5：runtime 最后一轮明确给出 code repair lane，模型执行 final repair write，但被 `Cylinder(axis=...)` preflight lint 拦下

直接证据：

1. `practice_runs/20260416_164515/organic_clamshell_storage_midhigh_v00_0605b8a8/trace/conversation.jsonl`
   - round 3 已把 bbox 修正到 `78 x 56 x 32`
   - round 5 `turn_tool_policy = code_repair_under_budget_after_repeated_validation_blockers`
2. `practice_runs/20260416_164515/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_05_execute_build123d.json`
   - final failure 为 `invalid_build123d_keyword.cylinder_axis`
3. `practice_runs/20260416_164515/summary.json`
   - `hallucination_event_count = 1`
   - `forced_policy_chain` 已出现预算下的 code repair 收口

结论：

1. 本轮主路径已经不是“不会写 / 写不通”，而是“写得到主体几何，但最后的 slots/notch family repair 仍会触发少量 Build123d API 幻觉”
2. 这类剩余问题更适合继续走 API governance + repair recipe + read-model 分辨率提升，而不是回到 benchmark case 特判

### 21. topology-targeting practice 已打通真实 face/edge id 链路

本轮还修通并运行了：

1. `scripts/run_topology_targeting_practice.py`
2. 产物目录：`practice_runs/20260416_165900/topology_local_targeting_manual`

这条链路的作用不是 whole-part prompt 收敛，而是验证：

1. `query_topology` 能否给出稳定 candidate sets
2. `face_ref` / `edge_refs` 是否真的能驱动局部编辑
3. 局部编辑之后 ref id 是否会按新 step 正确刷新

直接结果：

1. `selected_face_ref = face:1:F_b671e7a44063`
2. `selected_face_ref_after_cut = face:4:F_edd96ad92ed6`
3. `selected_edge_refs` 从 `edge:1:*` 刷新到 `selected_edge_refs_after_cut = edge:4:*`
4. `create_sketch / add_circle / cut_extrude / chamfer` 全部成功
5. `evaluation/generated_preview_{iso,front,right,top}.png` 已生成

对应证据：

1. `queries/round_02_query_topology_initial.json`
2. `queries/round_06_query_topology_after_cut.json`
3. `summary.json`
4. `evaluation/practice_preview.json`

额外发现：

1. 这条 manual practice 的几何动作是通的
2. 外层 `validation_llm` 在这一条链路上出现过 timeout warning，但不影响 topology targeting 能力本身

### 22. 幻觉分类继续细化：把 runtime symbol error 与 Build123d contract error 分开

本轮继续收紧 `build123d_hallucination` 的口径，不再把所有 write runtime exception 都混成一类。

新增分类：

1. `invalid_build123d_keyword.cylinder_axis`
   - 继续视为 Build123d contract 幻觉
   - 对应真实证据：`practice_runs/20260416_164515/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_05_execute_build123d.json`
2. `write_runtime_symbol_error.<symbol>`
   - 用来单独标识 `NameError: name 'part' is not defined` 这一类未定义符号错误
   - 不再和 Build123d API/keyword contract 混在一起

这样做的意义：

1. 继续保留 write-surface 失败的可观察性
2. 但把“库契约幻觉”和“代码结果变量/符号丢失”分层
3. 后续看 weighted score 时，不会把两类完全不同的错误混成同一条 Build123d 能力短板

对应验证：

1. `tests/unit/sub_agent_runtime/test_hallucination.py`
2. 新增单测：`test_build_run_hallucination_summary_separates_runtime_name_error_from_build123d_contract_error`

### 23. DomainKernelState 再收口：清掉陈旧 `realized_bbox`

在前一轮 `practice_runs/20260416_164515` 里已确认：

1. 新 bbox 已在后续 write / probe 中修正
2. 但 `realized_bbox` 可能仍从旧 validation binding 残留在 feature instance 里

本轮对 `feature_graph` 的修复不是加 family 特判，而是收紧 feature instance merge 规则：

1. 只有 incoming binding 带来真正的 fresh geometry 时，才清理旧的几何派生键
2. 清理对象包括 `geometry_summary`、`bbox*`、`realized_*`、`observed_*`
3. 像 `requested_dimensions`、`expected_local_centers` 这种需求侧锚点仍保留

对应验证：

1. `tests/unit/sub_agent_runtime/test_feature_graph.py`
2. `test_domain_kernel_refreshes_feature_instance_bbox_from_latest_write_after_validation`
3. `test_domain_kernel_keeps_latest_meaningful_geometry_after_failed_write`

这样做的收益：

1. `query_kernel_state` 不会继续混入旧 `realized_bbox`
2. failed write 不会再用空壳 `geometry_summary` 覆盖上一次真正成功的几何证据
3. repair bias 与 validator evidence 会更多地建立在“最新成功几何”上

### 24. read-model 开始从“几何候选集”升级到“host-role 候选集”

本轮没有继续给某个 benchmark case 补硬规则，而是先给 `query_topology` 增加一层更通用的 host-role candidate sets。

新增候选：

1. `shell_exterior_faces`
2. `shell_interior_faces`
3. `mating_faces`
4. `split_plane_faces`
5. `opening_rim_edges`

新增 metadata：

1. `host_role`
2. `host_role_confidence`
3. `semantic_host_roles`
4. `semantic_host_digest`

对应代码与测试：

1. `src/sandbox_mcp_server/registry.py`
   - `collect_requirement_topology_hints` 现在能从 `clamshell / enclosure / lid / base / notch / mating surface` 这类要求词汇自动补充 host-role hints
2. `src/sandbox_mcp_server/service.py`
   - `_build_requirement_topology_candidate_sets` 新增 host-role candidate builders
3. `tests/unit/sandbox_mcp_server/test_topology_candidate_sets.py`
   - 新增 hint 收集与 candidate metadata 单测

这一层的目标不是替模型决策，而是：

1. 让 enclosure / half-shell / notch 这类复杂 host 选择，不再只依赖 `top_faces` / `outer_edges`
2. 给后续 read-model、repair packet、validator grounding 一个更稳定的中间语义面

### 25. practice 报表补强：把 host-role / local targeting 从“隐式存在”变成可落盘指标

此前 `practice_runner` 对 read-model 的落盘还偏轻，只能看到：

1. `query_counts`
2. `topology_examples`
3. `topology_targeting_observed`

这不够回答下面这些问题：

1. 本轮到底有没有看到 `mating_faces / split_plane_faces / opening_rim_edges`
2. 有没有真的把 `face_ref / edge_refs` 用到局部修改里
3. topology targeting 是“查过”还是“真正命中并使用过”

本轮已补的字段：

1. `matched_ref_id_count`
2. `candidate_set_count`
3. `candidate_labels`
4. `candidate_host_roles`
5. `local_targeting_action_count`
6. `face_ref_action_count`
7. `edge_ref_action_count`
8. `local_targeting_examples`
9. `host_role_targeting_observed`

并同步写进：

1. `practice_analysis.json / md`
2. `brief_report.md / tsv`
3. `run_diagnostics.json / md`

对应验证：

1. `tests/unit/sub_agent_runtime/test_practice_runner.py`
2. `test_summarize_read_model_usage_captures_host_roles_and_local_targeting`

### 26. validator lane 收口：`blocker_taxonomy` 开始吃 clause grounding surface

本轮在真实产物 `practice_runs/20260416_213728/topology_local_finish_focus_v00_95ab3188/queries/round_06_validate_requirement.json` 里看到一个很关键的不一致：

1. `clause_interpretations` 已经给出：
   - `required_evidence_kinds = ["geometry", "topology"]`
   - `repair_hints = ["query_topology", "query_feature_probes"]`
2. 但 `blocker_taxonomy` 仍把 `feature_countersink` / `two_mounting_holes` 压成：
   - `recommended_repair_lane = "code_repair"`

这不是单 case 问题，而是 validator contract 的控制面没有完全 evidence-first。

本轮已做的收口：

1. `build_validation_blocker_taxonomy(..., clause_interpretations=...)`
2. taxonomy record 会合并相关 clause 的：
   - `observation_tags`
   - `decision_hints`
   - `family_binding`
   - `repair_hints`
3. 对 `explicit_anchor_hole / named_face_local_edit` 这类 topology-aware local feature，
   - 如果 clause grounding 明确指向 `query_topology` 或 `apply_cad_action`
   - lane 允许被 override 成 `local_finish`

对应验证：

1. `tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py`
2. `test_build_validation_blocker_taxonomy_prefers_local_finish_when_clause_grounding_requests_topology_targeting`

这一步的目标是：

1. 不再只靠 blocker id 或 family spec 决定 repair lane
2. 让 validator 真正把 clause grounding surface 传给 runtime

### 27. `cut_extrude` 幻觉进入 write-surface 治理，不再只停留在动作语义层

本轮新增了一个明确的迁移期残留治理点：

1. `execute_build123d` 代码里直接写 `cut_extrude(...)`

此前仓库里 `cut_extrude` 更多只存在于：

1. action surface / registry 语义
2. 技能提示
3. replay helper

但它没有进入 `execute_build123d` preflight lint，因此 real run 里一旦模型把 action surface 误投射成 whole-part code surface，只会在运行期以 `NameError` 暴露，诊断层不够前置。

本轮已做：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增 `legacy_api.cut_extrude_helper`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - 新增对 `cut_extrude(...)` 误写的 lint 单测
   - 并把它接入 `explicit_anchor_hole_countersink_array_safe_recipe`

这一步的意义是：

1. 把一个典型 CadQuery / action-surface 残留，从 runtime exception 前移到 preflight governance
2. 让 hole / countersink family 的 deterministic recipe 能接住这类错误，而不是继续散成 generic runtime failure

### 28. practice diagnostics 误把任意 read 统计成 topology query，已修复并补单测

在 `practice_runs/20260416_214701/run_diagnostics.md` 里看到一个可观测性问题：

1. `brief_report.md` 显示 `query_topology = 0`
2. 但 `run_diagnostics.md` 却把 case 记进了 `topology_query_cases`

根因是：

1. `run_diagnostics` 之前按 `bool(query_counts)` 判断
2. 只要有 `query_kernel_state` / `validate_requirement` 之类 read，也会被误记成 topology query

本轮已修：

1. `src/sub_agent_runtime/practice_runner.py`
   - `topology_query_cases` 改成只认 `query_counts.query_topology > 0`
2. `tests/unit/sub_agent_runtime/test_practice_runner.py`
   - 新增 regression test 锁死这个行为

这一步很关键，因为后面要用 practice 报表判断：

1. 到底有没有真正进入 `query_topology`
2. 还是只是做了 `query_kernel_state` / `validate_requirement`

### 29. 新确认的共享瓶颈：validator lane 已开始收口，但 turn policy 仍会把 `query_topology` 封死

`practice_runs/20260416_215602/topology_local_finish_focus_v00_95ab3188` 给了一个很清晰的链路证据：

1. `queries/round_02_validate_requirement_post_write.json`
   - `blocker_taxonomy` 已给出：
   - `recommended_repair_lane = "local_finish"`
   - `decision_hints = ["repair the contradicted clause before finishing", "query_topology", "query_feature_probes"]`
2. 但同一次 run 的 policy 仍然在：
   - `trace/round_digest.md`
   - `plans/round_04_response.json`
   - `plans/round_05_response.json`
   把允许工具限制成：
   - `query_feature_probes`
   - `query_kernel_state`
   - `validate_requirement`
   明确没有放开 `query_topology`

这说明主要矛盾已经进一步收敛成：

1. validator / taxonomy 这层开始会说“去 topology”
2. 但 runtime policy 还没有把这个信号真正转成 allowed tool set

本轮已修：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - 新增 `_latest_validation_prefers_topology_refresh`
   - semantic refresh / closure policy 在看到 `local_finish + query_topology` 信号时，会把 `query_topology` 放进 `allowed_tool_names`
   - `preferred_validation_assessment_tools` 开始读取 `repair_hints`
2. `src/sub_agent_runtime/turn_state.py`
   - 把 `query_topology` 记入 semantic refresh 工具集合
3. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - 新增 semantic refresh / closure 两个 policy regression tests

### 30. `filter_by_position` 轴带关键词幻觉进入 write-surface 治理

在 `topology_local_finish_focus` 的真实 run 里看到模型把轴带筛选写成：

1. `filter_by_position(Axis.Z, ZMin=..., ZMax=...)`

我核对了 Build123d 真实签名：

1. `ShapeList.filter_by_position(self, axis, minimum, maximum, inclusive=(True, True))`

因此 `ZMin / ZMax` 是典型的轴带关键词幻觉，不应该继续漏到运行时。

本轮已做：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增 `invalid_build123d_api.filter_by_position_keyword_band`
   - 只拦 axis-band alias，不误伤合法的 `minimum / maximum` 或位置带实参写法
2. 复用已有 `build123d_shapelist_axis_filter_contract`
3. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - 新增 `test_preflight_lint_rejects_filter_by_position_keyword_band_aliases`
4. 单测回归：
   - `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
   - `80 passed`

这一步的意义是：

1. 把轴筛选 family 里一类高频 Build123d 幻觉继续前移
2. 避免模型在局部边/面选择阶段，因为关键词猜错而把 topology-aware repair 再次打回 generic code failure

### 31. `220829` 这轮真实 run 证明：validator topology signal 已经存在，但 live action 还没真正进入 topology lane

重新核 `practice_runs/20260416_220829` 后，结论更清楚了：

1. `topology_local_finish_focus_v00_95ab3188`
   - round 1 的 `validate_requirement_post_write` 已明确给出：
   - `recommended_repair_lane = "local_finish"`
   - `decision_hints / repair_hints` 都包含 `query_topology`
2. 但最终 run 统计仍然是：
   - `query_topology = 0`
   - `topology_query_cases: []`
3. 实际发生的是：
   - round 5 执行了 `query_feature_probes + query_geometry`
   - round 6 又回到 write lane，并被 `legacy_api.cut_extrude_helper` 拦住

同时需要特别澄清一件事：

1. `face_ref / edge_refs` 并不是没有
2. 它们已经存在于 `actions/round_*_execute_build123d.json` 的 `topology_index` 里
3. 当前缺口在于：
   - ref 已经能产出
   - validator 已经能提示去读 topology
   - 但真实执行仍没有稳定走到 `query_topology -> targeted local edit`

这意味着当前共享瓶颈已经进一步压缩成：

1. topology read bias 还不够强
2. code-repair 惯性仍强于 topology-aware local finish

### 32. enclosure / clamshell guidance 继续收口：把“保持装配姿态 + 包络内建模”写得更明确

`practice_runs/20260416_222057/organic_clamshell_storage_midhigh_v00_0605b8a8` 的早期产物再次说明一个共享偏差：

1. 模型容易把 lid/hinge 当成“可自由摆放的多个零件”
2. 结果要么 lid 被抬高，要么 hinge 被放到包络外，直接把整体 bbox 拉坏

仓库里原本已经有：

1. separate parts 保持真实装配坐标

但还不够直接。

本轮已补：

1. `src/sub_agent_runtime/skill_pack.py`
   - 明确写入：
   - 如果 requirement 给了 overall bounding box，所有 lid/base/hinge/latch/mating features 都应留在 assembled envelope 内
   - 不要为了可视化把 lid 抬高或把 hinge 放到包络外
2. `tests/unit/sub_agent_runtime/test_skill_pack.py`
   - 增补回归断言
3. 单测回归：
   - `uv run pytest tests/unit/sub_agent_runtime/test_skill_pack.py -q`
   - `19 passed`

### 33. feature probe 不再只“描述需要 topology”，而是开始显式推荐 `query_topology`

`practice_runs/20260416_222057/.../queries/round_03_query_feature_probes.json` 里已经能看到：

1. `anchor_summary.requires_topology_host_ranking = true`
2. `grounding_blockers` 包含 `need_topology_host_selection`

此前这两个信号只停留在 probe 内容里，没有反映到：

1. `recommended_next_tools`
2. runtime 后续 preferred tool 选择

本轮已做：

1. `src/sandbox_mcp_server/feature_probe_grounding.py`
   - 新增 grounded probe recommended tool merge
2. `src/sandbox_mcp_server/service.py`
   - `FeatureProbeRecord.recommended_next_tools` 现在会在 topology host-selection 证据出现时，把 `query_topology` 提到前面
3. `tests/unit/sandbox_mcp_server/test_feature_probe_grounding.py`
   - `slots` family 现在明确断言 `recommended_next_tools[0] == "query_topology"`

### 34. turn policy 开始吃 feature-probe 的 topology host-selection signal，而不只盯 validation hints

此前已经修过一轮：

1. validation / blocker_taxonomy 说 `query_topology`
2. policy 就应该放开 `query_topology`

但 `organic_clamshell` 这轮又暴露出另一层问题：

1. 有时 validation 还没说得那么明确
2. 可是 `query_feature_probes` 已经给出了：
   - `requires_topology_host_ranking`
   - `need_topology_host_selection`

这类信号也应该进入 live control surface。

本轮已补：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - 新增从最新 `query_feature_probes` 结果中提取 topology preference 的 helper
   - semantic refresh allowed tools 会读取这个信号
   - preferred assessment tools 也会把 `query_topology` 提前
2. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - 新增 feature-probe-driven topology preference regression test
3. 单测回归：
   - `uv run pytest tests/unit/sandbox_mcp_server/test_feature_probe_grounding.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
   - `19 passed`

这一步的目标非常明确：

1. 不再只靠 validator 才能把 topology 提到前台
2. 让 read-model 自己产出的 host-selection 证据，也能直接驱动下一轮动作选择

### 35. `223018` 部分 live evidence：feature probe 的 `query_topology` 推荐已经落盘，但全局 geometry blockers 仍可能先把 planner 拉回 whole-part rebuild

在新一轮真实 run `practice_runs/20260416_223018/organic_clamshell_storage_midhigh_v00_0605b8a8` 里，已经能看到刚才的改动开始生效：

1. `queries/round_03_query_feature_probes.json`
   - `slots.recommended_next_tools = ["query_topology", "query_feature_probes", "query_kernel_state"]`
   - `anchor_summary.requires_topology_host_ranking = true`
   - `grounding_blockers` 仍有 `need_topology_host_selection`

这说明：

1. feature probe 层现在已经会把 topology host-selection signal 显式写到推荐工具里
2. 它不再只是埋在 `grounding_blockers` 文本里

但同一条 case 的：

1. `plans/round_04_response.json`

仍然选择了：

1. `execute_build123d`

原因也很清楚：

1. 当时全局 bbox 仍错得很重：
   - `78 x 60 x 49.4` vs `78 x 56 x 32`
2. planner 仍把“先修整体 envelope”判断为比局部 topology targeting 更紧急

这个现象本身是健康的，说明系统已经开始分层：

1. 当 family probe 的主要矛盾是 host selection，它会把 `query_topology` 顶上来
2. 但当 case 同时还存在显著的全局 geometry blocker 时，planner 仍可能先回 whole-part rebuild

接下来更关键的问题，就变成：

1. 当整体 envelope 已经基本稳定后
2. planner 是否会真正利用这个新的 topology signal 去打开 `query_topology -> local finish`

### 36. repair recipe 优先级修正：当 active-builder arithmetic 与 `Loc(...)` 同时出现时，先返回真正的主矛盾修复合同

`practice_runs/20260416_223018/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_01_execute_build123d.json` 暴露出一个很典型的优先级问题：

1. 同时命中了大量 `invalid_build123d_contract.active_builder_temporary_primitive_arithmetic`
2. 也命中了一个 `invalid_build123d_api.loc_helper_name`
3. 但最终 `repair_recipe` 却被 `build123d_location_helper_contract` 抢走

这会把模型的注意力引向次要错误，而不是 enclosure write 最核心的 builder-arithmetic contract。

本轮已修：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 当 `Loc(...)` / `move(...)` 与 active-builder arithmetic 或 transform-rebind 同时出现时，不再让 location helper contract 抢走 recipe
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - 新增 mixed-lint regression test，明确要求 enclosure family 继续返回 `nested_hollow_section_same_builder_subtract_contract`
3. 单测回归：
   - `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
   - `81 passed`

### 37. `Vector` 分量大小写幻觉进入 preflight：`.center().z` 现在会在运行前被拦住

`practice_runs/20260416_223018/organic_clamshell_storage_midhigh_v00_0605b8a8` 的后续 write 又暴露了一类很典型的 Build123d API 误记：

1. `e.center().z`

Build123d 的 vector/point 组件应使用：

1. `.X`
2. `.Y`
3. `.Z`

而不是小写属性。

本轮已补：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增 `invalid_build123d_api.vector_lowercase_component_attribute`
   - 当前先覆盖 `center().x/.y/.z` 这一类高频写法
   - 返回新的 `build123d_vector_component_attribute_contract`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - 新增 `.center().z` regression test
3. 单测回归：
   - `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
   - `82 passed`

### 38. enclosure skill 再补一条：不要默认对整个 hollow enclosure 的全部线性边做 broad fillet

`223018` 的 organic clamshell 在 round 4 whole-part rebuild 里又失败在：

1. `fillet(base.edges().filter_by(GeomType.LINE), R_EDGE)`

这说明模型仍会把：

1. “圆角外观”

误译成：

1. “对整个 shell 的所有线性边一次性 fillet”

而这在有 hinge / seam / notch / cavity 的 enclosure 上非常容易失败。

本轮已补：

1. `src/sub_agent_runtime/skill_pack.py`
   - enclosure skill 明确写入：
   - 不要默认 broad-filleting 全部 `GeomType.LINE` 边
   - 应先缩到外表面 / opening rim / 目标 edge family
2. `tests/unit/sub_agent_runtime/test_skill_pack.py`
   - 新增 guidance 断言
3. 单测回归：
   - `uv run pytest tests/unit/sub_agent_runtime/test_skill_pack.py -q`
   - `19 passed`

### 39. `223018` 真实对照：organic clamshell 首次把整体包络收到了目标尺寸，但 topology-focused case 仍被 write-surface 吃光回合预算

`practice_runs/20260416_223018` 这轮的价值很高，因为它同时给出了一个正向结果和一个尚未收口的反例。

正向结果在：

1. `organic_clamshell_storage_midhigh_v00_0605b8a8`

真实 evidence：

1. final active binding / geometry summary 已到：
   - `bbox = [78.0, 56.0, 32.0]`
   - `solids = 1`
2. 同时 `round_03_query_feature_probes.json` 已写出：
   - `slots.recommended_next_tools = ["query_topology", "query_feature_probes", "query_kernel_state"]`

这说明两件事同时成立：

1. shell/bbox 主链确实在变稳
2. topology host-selection signal 也已经进入 read-model 推荐面

但同一轮的反例也很清楚：

1. `topology_local_finish_focus_v00_95ab3188`

它在 6 回合预算里仍然几乎完全耗在 write-surface 修错上：

1. `feature_probe_count = 0`
2. `read_only_turn_count = 0`
3. `failure_cluster = "tool_gap"`

这说明当前 topology bias 虽然已经接好了代码路径，但如果前面几轮一直在：

1. manual cutter subtract mode
2. `with Rot(...)`
3. active builder mutation / temporary arithmetic
4. `Box(depth=...)`
5. `Plane(...).center_location`

这类 write-surface 错误里循环，仍然会在 6 回合内挤掉真正进入 topology lane 的机会。

因此下一步的验证策略非常明确：

1. 不再只看同一预算下的结果
2. 需要把 topology-focused seed 单独拉高 round budget，验证新的 topology bias 在更合理的回合窗口内是否真的会触发 `query_topology`

### 40. `223917` 真实结果：topology-focused seed 第一次真正打到 read-model，但最后仍被 write-surface 截断

`practice_runs/20260416_223917/topology_local_finish_focus_v00_95ab3188` 这轮是当前最关键的一条真实证据。

它首次出现了：

1. `queries/round_07_query_geometry.json`
2. `queries/round_07_query_feature_probes.json`

其中 `round_07_query_feature_probes.json` 已明确写出：

1. `explicit_anchor_hole.recommended_next_tools = ["query_topology", "execute_build123d"]`
2. `named_face_local_edit.recommended_next_tools = ["query_topology", "apply_cad_action"]`

这说明：

1. 系统已经不只是“知道 topology 很重要”
2. 而是已经能在真实 feature probe 里把下一步具体收口成：
3. `query_topology -> apply_cad_action`

但同一轮最终 summary 也很清楚：

1. `hallucination_event_count = 24`
2. `build123d_hallucination.primary_layer = "write_surface"`
3. `feature_probe_count = 1`
4. `read_only_turn_count = 1`
5. `failure_cluster = "code_path_family_gap"`

这说明当前主瓶颈已经进一步收敛成：

1. read-model 与 topology bias 已经能被触发
2. 但最后一轮仍会被新的 Build123d 参数/selector 幻觉重新拉回 whole-part code rewrite

### 41. 这轮定位出的核心控制流缺口：semantic refresh 已经读到 local-finish 建议，但 turn policy 还没把它提升成 live lane

`223917` 的 round 7 probe 已经把：

1. `query_topology`
2. `apply_cad_action`

写成下一步建议，但 round 8 实际还是回到了 `execute_build123d`。

定位结果很明确：

1. `src/sub_agent_runtime/agent_loop_v2.py`
2. `_latest_actionable_semantic_refresh_since_failed_write(...)`

此前只会从：

1. `probe.blockers -> blocker taxonomy`

推 repair lane，

却不会真正消费：

1. `probe.recommended_next_tools`

因此即使 probe 已经说清楚“应该 `query_topology` 后局部修”，policy 也可能仍回退到 broad code repair。

### 42. 已补控制流修复：feature probe 一旦明确推荐 `query_topology + apply_cad_action`，下一轮允许直接进入 local-finish lane

本轮已改：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - 新增 `_feature_probe_recommends_local_finish(...)`
   - `_latest_actionable_semantic_refresh_since_failed_write(...)` 不再只看 blocker taxonomy
   - 也会读取 `query_feature_probes.probes[*].recommended_next_tools`
2. 当 probe 已明确给出：
   - `query_topology`
   - `apply_cad_action`
   时，会直接把 refresh 结果提升成：
   - `repair_lane = "local_finish"`

对应单测：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - 新增 repeated failed code path + actionable feature probe 的 regression test
2. 回归结果：
   - `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
   - `18 passed`

### 43. 新增一类 write-surface 幻觉治理：`Cylinder(..., taper=...)`

`223917` 的早期 round 里还暴露出新的通用 Build123d 参数幻觉：

1. `Cylinder(..., taper=...)`

这不是 case-specific 问题，而是明确的 API contract 幻觉。

本轮已补：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增 `invalid_build123d_keyword.cylinder_taper`
   - 提示改用：
   - `Cone(bottom_radius=..., top_radius=..., height=...)`
   - 或普通 `Cylinder(...)`
2. 同时把该 lint 纳入 explicit-anchor hole family 的 recipe 归并面
3. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - 新增对应 regression test
4. 回归结果：
   - `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
   - `83 passed`

### 44. 新一轮 `224840` 真实验证已启动，重点观察 repeated-failure 之后是否会真正落到 `query_topology/apply_cad_action`

当前正在运行：

1. `practice_runs/20260416_224840/topology_local_finish_focus_v00_95ab3188`

这轮的起点比 `223917` 更健康：

1. round 1 直接成功写出 `model.step`
2. `round_01_validate_requirement_post_write.json` 已给出：
   - `feature_countersink`
   - `two_mounting_holes`
   - `a_countersink_on_the_mounting_face_so_that_a_topology_aware_local_finishing_pass_is_useful`

它仍会先走一次：

1. `repair_from_actionable_kernel_patch_after_validation_blocker`

但本轮更重要的观察点已经变成：

1. 当后续出现 repeated failure + feature probe refresh 后
2. 新加的 local-finish policy 是否能把最后阶段真正推入：
3. `query_topology` / `apply_cad_action`

### 45. `224840` 的真实结果：first write 明显健康了，但 repeated failure 之后仍没有真正进入 local finish

证据目录：

1. `practice_runs/20260416_224840/topology_local_finish_focus_v00_95ab3188/summary.json`
2. `practice_runs/20260416_224840/topology_local_finish_focus_v00_95ab3188/queries/round_05_query_feature_probes.json`
3. `practice_runs/20260416_224840/topology_local_finish_focus_v00_95ab3188/prompts/round_06_request.json`

这轮最直接的改善是：

1. `round 1` 已经能成功产出可用 STEP
2. `build123d_hallucination.event_count` 从 `223917` 的 `24` 降到 `7`
3. `build123d_hallucination.primary_layer` 仍然是 `write_surface`
4. `feature_probe_count = 1`
5. `read_only_turn_count = 1`

但 `round_05_query_feature_probes.json` 也明确写出了：

1. `holes.recommended_next_tools = ["query_topology", "query_kernel_state"]`
2. `named_face_local_edit.recommended_next_tools = ["query_topology", "apply_cad_action"]`

而 `round_06_request.json` 里的：

1. `turn_tool_policy = null`
2. `preferred_probe_families = null`

说明 feature probe 已经提供了可行动的 topology/local-finish 信号，但 outer turn policy 还没有在 repeated failure 之后把这条 lane 升级成默认主路径。这不是读工具无效，而是 policy 接线还不够彻底。

### 46. 已补第二层控制流修复：repeated failed code path 一旦遇到 local-finish semantic refresh，直接压到 `query_topology/apply_cad_action`

针对 `224840` 暴露出的空档，本轮继续修改了：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - repeated failed `execute_build123d` 分支现在不再只会回到 broad code repair
   - 如果最新 semantic refresh 已经明确指向 `repair_lane = "local_finish"`，则下一轮直接生成：
   - `policy_id = "local_finish_after_actionable_feature_probe_refresh"`
   - `allowed_tool_names = ["query_kernel_state", "query_topology", "apply_cad_action"]`
   - `preferred_tool_names = ["query_topology", "apply_cad_action"]`
2. 对应单测：
   - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - 已回归 `18 passed`

这一步的作用不是去补某个 case，而是把：

1. feature probe 已经确认“下一步该局部修”
2. repeated failed write 说明继续 blind rebuild 收益很低

这两类信号真正合流成 live policy。

### 47. `225419` 的真实结果：`query_topology` 已第一次真正进入 live LLM lane，但 `apply_cad_action` 仍未被触发

证据目录：

1. `practice_runs/20260416_225419/topology_local_finish_focus_v00_95ab3188/plans/round_04_response.json`
2. `practice_runs/20260416_225419/topology_local_finish_focus_v00_95ab3188/queries/round_04_query_topology.json`
3. `practice_runs/20260416_225419/topology_local_finish_focus_v00_95ab3188/queries/round_04_query_feature_probes.json`
4. `practice_runs/20260416_225419/topology_local_finish_focus_v00_95ab3188/plans/round_07_response.json`
5. `practice_runs/20260416_225419/topology_local_finish_focus_v00_95ab3188/summary.json`

这轮的进展比 `224840` 更重要：

1. `plans/round_04_response.json` 已经真实下发 `query_topology + query_feature_probes`
2. `queries/round_04_query_topology.json` 已生成候选集：
   - `top_faces`
   - `top_edges`
   - `upward_planar_faces`
   - `opening_rim_edges`
3. `summary.json`
   - `feature_probe_count = 1`
   - `read_only_turn_count = 3`
   - `validation_call_count = 3`
   - `forced_policy_chain` 已出现：
   - `semantic_refresh_after_validation_assessment_gap_from_code_write`
   - `closure_validation_after_semantic_refresh_from_code_write`
   - `graph_refresh_after_read_stall`

但同一轮的 `query_feature_probes` 里，family 仍只有：

1. `core_geometry`
2. `holes`
3. `pockets`
4. `fillets`
5. `countersinks`
6. `notches`

没有把 `named_face_local_edit` 一起带上，因此虽然 topology read 已经发生，模型仍然更容易把它理解成“补证据后再整体重建”，而不是“拿到 refs 后直接 apply_cad_action”。

### 48. 已补第三层偏置修复：preferred probe families 不再只信 feature graph，额外并入 requirement-driven local-finish family

针对 `225419` 的产物，本轮继续补了两处泛化修复：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - `_preferred_probe_families_for_turn(...)` 不再在 feature graph 已有 families 时提前返回
   - 现在会继续并入 `recommended_feature_probe_families(...)`
2. `src/sub_agent_runtime/skill_pack.py`
   - `recommended_feature_probe_families(...)` 新增 requirement 级 local-finish 提示
   - 对包含 `topology-aware`、`mounting face`、`target face / edge`、`local finish`、`fillet/chamfer/countersink/counterbore/notch` 等语义的需求，会额外加入：
   - `named_face_local_edit`
3. 对应单测：
   - `tests/unit/sub_agent_runtime/test_skill_pack.py`
   - 已回归 `20 passed`

这一层修复的目标是：

1. 不再把 local-finish 家族的进入完全寄托在已有 feature graph 是否先识别成功
2. 而是让 requirement 语义、validation gap、feature probe、query_topology 四层信号能共同把 read-model 偏向真正需要局部 finishing 的方向

### 49. 当前正在发起新一轮真实 practice run，验证最新 family merge 之后是否开始真实出现 `apply_cad_action`

当前已启动：

1. `uv run python scripts/run_custom_prompt_practice.py --seed-ids topology_local_finish_focus --variants-per-seed 1 --max-rounds 8 --runs-root practice_runs`

这轮的主要观察点已经非常明确：

1. `query_topology` 是否继续稳定出现
2. `query_feature_probes` 是否首次把 `named_face_local_edit` 稳定带入 live family list
3. planner 是否开始把 `apply_cad_action` 真正写进 action plan
4. 若仍未触发，剩余缺口究竟是在：
   - family grounding
   - turn policy budget pressure
   - 还是 `apply_cad_action` selector promptability

### 50. 已补一层更具体的 live policy：fresh topology refs + local-finish probe recommendation 不再被 `completion_judge / graph_refresh` 卡在只读回路里

结合 `225419` 的真实产物和进一步的 prompt 检查，本轮补了更细的一层 policy 接线：

1. `practice_runs/20260416_225419/topology_local_finish_focus_v00_95ab3188/prompts/round_04_request.json`
   - `turn_tool_policy.policy_id = semantic_refresh_after_validation_assessment_gap_from_code_write`
   - `blocked_tool_names` 里明确包含 `apply_cad_action`
2. `.../round_05_request.json`
   - `turn_tool_policy.policy_id = closure_validation_after_semantic_refresh_from_code_write`
   - 仍继续 block `apply_cad_action`

这说明此前的问题已经不只是“probe family 不够强”，而是：

1. 即使 topology refresh 已经发生
2. policy 仍会把 `apply_cad_action` 关在门外

本轮已在 `src/sub_agent_runtime/agent_loop_v2.py` 增加：

1. `_latest_feature_probes_recommend_local_finish(...)`
2. `_local_finish_is_actionable_after_semantic_refresh(...)`
3. 新 policy：
   - `local_finish_after_semantic_refresh_from_code_write`
   - `local_finish_after_read_stall_topology_refresh`

核心含义是：

1. 如果最新 feature probe 已明确推荐 `query_topology -> apply_cad_action`
2. 并且当前 write 之后已经拿到了新的 `query_topology`
3. 那下一轮直接开放 local finish，而不是继续停留在 validation-only / graph-refresh-only 车道

对应回归：

1. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
2. `20 passed`

### 51. `230336` 的真实结果：practice lane 已能稳定落盘多视图预览，但这轮主瓶颈前移到了 write-surface

证据目录：

1. `practice_runs/20260416_230336/topology_local_finish_focus_v00_95ab3188/summary.json`
2. `practice_runs/20260416_230336/topology_local_finish_focus_v00_95ab3188/practice_analysis.json`
3. `practice_runs/20260416_230336/topology_local_finish_focus_v00_95ab3188/evaluation/generated_preview_iso.png`
4. `practice_runs/20260416_230336/topology_local_finish_focus_v00_95ab3188/evaluation/generated_preview_front.png`
5. `practice_runs/20260416_230336/topology_local_finish_focus_v00_95ab3188/evaluation/generated_preview_right.png`
6. `practice_runs/20260416_230336/topology_local_finish_focus_v00_95ab3188/evaluation/generated_preview_top.png`

这轮最重要的现象不是“又失败了”，而是失败位置变得更清楚：

1. `executed_action_count = 8`
2. `executed_action_types` 全是 `execute_build123d`
3. `feature_probe_count = 0`
4. `read_only_turn_count = 0`
5. `forced_policy_chain` 主要停在：
   - `code_first_build`
   - `repair_from_actionable_kernel_patch_after_validation_blocker`
   - `code_repair_after_first_concrete_code_failure`
   - `repair_from_actionable_kernel_patch_after_repeated_validation_blocker`

这说明：

1. 新增的 topology/local-finish live policy 还没机会发挥
2. 因为这轮在到达 read-model 之前，就被更前置的 Build123d contract 幻觉反复截断了

`summary.json` 也印证了这一点：

1. `build123d_hallucination.event_count = 10`
2. `primary_layer = "write_surface"`
3. `categories = { invalid_api_contract: 7, write_runtime_exception: 3 }`

其中最主要的几个真实问题是：

1. round 2:
   - `invalid_build123d_contract.explicit_anchor_manual_cutter_requires_subtract_mode`
2. round 3:
   - `Cylinder(..., length=...)` 运行时失败
3. round 6:
   - `filter_by_position(Plane.XY, ...)` 运行时失败
4. round 7:
   - broad edge-set `fillet(...)` 触发 OCC `StdFail_NotDone`

### 52. 针对 `230336` 已新增两类 API governance 规则，继续把 write-surface 幻觉收进统一治理层

这轮产物里暴露出的两个问题都不是 case-specific：

1. `Cylinder(..., length=...)`
2. `filter_by_position(Plane.XY, min, max)`

它们本质上都是：

1. 模型在猜 Build123d contract
2. 不是 benchmark case 的局部特判问题

因此本轮已在 `src/sub_agent_runtime/tool_runtime.py` 新增：

1. `invalid_build123d_keyword.cylinder_length_alias`
2. `invalid_build123d_api.filter_by_position_plane_axis`

并把它们并入已有治理/recipe 体系：

1. `build123d_shapelist_axis_filter_contract`
2. explicit directional hole / slot recipe 的 lint 归并面

对应回归：

1. `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
2. `86 passed`

### 53. 已把 latest repair packet recipe 正式接进 runtime skill notes，而不是只停留在 domain kernel digest

这轮补的不是又一条 case prompt 文案，而是把已经存在于语义内核里的 repair recipe 明确暴露给当前回合：

1. 代码：
   - `src/sub_agent_runtime/skill_pack.py`
   - `tests/unit/sub_agent_runtime/test_skill_pack.py`
2. 新增 skill：
   - `kernel_repair_packet_recipe`
3. 暴露内容：
   - `latest_repair_packet_family_id`
   - `latest_repair_packet_recipe_id`
   - `latest_repair_packet_recipe_summary`
   - `latest_repair_packet_recipe_skeleton`

这层修改的目的很明确：

1. 让模型在看到 concrete repair packet 时，优先走 packet-aligned 修复车道
2. 不再让 repair recipe 只存在于 `domain_kernel_digest`，却在 runtime skill notes 里基本不可见
3. 对 explicit-anchor hole / countersink family，进一步强化 helper-first、host-face-first、avoid-manual-cutter 的共享偏置

对应回归：

1. `uv run pytest tests/unit/sub_agent_runtime/test_skill_pack.py -q`
2. `21 passed`

### 54. `231347` 的双 seed 真实结果：read-model 已经能持续给证据，但主瓶颈仍集中在 write-surface

证据目录：

1. `practice_runs/20260416_231347/summary.json`
2. `practice_runs/20260416_231347/organic_clamshell_storage_midhigh_v00_0605b8a8/practice_analysis.json`
3. `practice_runs/20260416_231347/topology_local_finish_focus_v00_95ab3188/practice_analysis.json`

run-level 结果：

1. `total_cases = 2`
2. `hallucination_event_count = 8`
3. `hallucination_primary_layer_counts = { write_surface: 2 }`

其中 organic case 的关键信号是：

1. `query_feature_probes = 3`
2. `query_kernel_state = 3`
3. `validate_requirement = 2`
4. `topology_targeting_observed = false`
5. `hallucination.event_count = 3`
6. 主 rule 变成：
   - `invalid_build123d_context.transform_context_manager`
   - `invalid_build123d_api.loc_helper_name`

这说明：

1. organic enclosure 这条线已经不是“完全没有读反馈”
2. 当前更前置的问题是 Build123d transform / location contract 仍会把 whole-part write 打断

topology case 的关键信号是：

1. `query_topology = 2`
2. `matched_ref_id_count = 96`
3. `candidate_set_count = 10`
4. `candidate_labels` 已包含：
   - `Primary Outer Faces`
   - `Top Faces`
   - `Top Edges`
   - `Top Outer Edges`
   - `Upward Planar Faces`
5. 但：
   - `face_ref_action_count = 0`
   - `edge_ref_action_count = 0`
   - `local_targeting_action_count = 0`
6. `hallucination.event_count = 5`
7. 主 rule 仍是：
   - `invalid_build123d_contract.explicit_anchor_manual_cutter_requires_subtract_mode`

这说明：

1. topology evidence 已经能真实进入 live lane
2. 但 packet-aligned local finish 还没有稳定压过 explicit-anchor manual cutter 的 whole-part code path

### 55. 新一轮真实 practice run 已再次发起，开始验证 `kernel_repair_packet_recipe` 是否能改变 live 行为

新的真实 session：

1. `session_id = 23285`
2. 命令：
   - `uv run python scripts/run_custom_prompt_practice.py --seed-ids topology_local_finish_focus,organic_clamshell_storage_midhigh --variants-per-seed 1 --max-rounds 8 --runs-root practice_runs`

本轮重点观察项：

1. topology case 是否会更早把 `query_topology` 之后的 lane 切到 packet-aligned repair / local finish
2. organic case 是否继续被 transform / Location contract 截断
3. `kernel_repair_packet_recipe` 是否会在真实 prompt 中减少 manual cone/cylinder hole repair 的反复出现

### 56. 已补一层更通用的 repair-packet 几何归一化：没有 `geometry_summary` 时，也能从顶层 bbox 生成 host frame

这轮继续补的不是 case 文案，而是 repair packet 自身的几何底座：

1. 代码：
   - `src/sub_agent_runtime/feature_graph.py`
   - `tests/unit/sub_agent_runtime/test_feature_graph.py`
2. 新增行为：
   - 当 `parameter_bindings` 没有 `geometry_summary`
   - 但存在顶层 `bbox / bbox_min / bbox_max`
   - 仍然合成 `_repair_packet_geometry_summary(...)`
   - 继续生成 `centered_bbox_xy` host frame

这层收益不只属于 explicit-anchor：

1. explicit-anchor local anchor / countersink family 能更稳定拿到 host frame
2. spherical / host-face local family 也能复用同一层 bbox synthesis

对应回归：

1. `uv run pytest tests/unit/sub_agent_runtime/test_feature_graph.py -q`
2. `5 passed`

### 57. `232454` 的早期真实信号：organic case 已经主动走到 `execute_build123d_probe`

当前还在跑的真实目录：

1. `practice_runs/20260416_232454/`

截至目前 organic case 已经落盘到：

1. `queries/round_03_validate_requirement_post_write.json`
2. `queries/round_04_query_kernel_state.json`
3. `queries/round_07_execute_build123d_probe.json`
4. `queries/round_07_query_kernel_state.json`

这比上一轮更健康的地方在于：

1. 不再只是“写失败 -> 继续盲修”
2. 已经开始真实切到 `execute_build123d_probe + query_kernel_state` 的诊断链
3. 说明 runtime 对复杂 enclosure 的 probe lane 已经更积极，不再完全依赖 broad rewrite

### 58. 已把两类高频 lint family 提升成更强的 runtime skill notes

本轮新增：

1. `execute_build123d_active_builder_authority_repair`
2. `execute_build123d_explicit_anchor_helper_first_repair`

位置：

1. `src/sub_agent_runtime/skill_pack.py`
2. `tests/unit/sub_agent_runtime/test_skill_pack.py`

这两条 skill 的目标都不是 case-specific：

1. 第一条针对 active `BuildPart` 内 temporary primitive arithmetic / part mutation / transform rebind 这类共享 contract 误用
2. 第二条针对 explicit-anchor hole / countersink family 反复退回 manual `Cylinder/Cone` cutter 的共享偏差

对应回归：

1. `uv run pytest tests/unit/sub_agent_runtime/test_skill_pack.py -q`
2. `23 passed`

### 59. 已补一条更关键的 turn-policy gate：under-grounded kernel patch 不再抢走 local-feature evidence lane

这轮把 `232454/topology_local_finish_focus_v00_95ab3188` 的真正根因锁清了：

1. `queries/round_04_validate_requirement_post_write.json` 到 `round_07_validate_requirement_post_write.json`
2. 每轮都在重复：
   - `feature_countersink`
   - `two_mounting_holes`
   - `a_countersink_on_the_mounting_face_so_that_a_topology_aware_local_finishing_pass_is_useful`
3. `decision_hints / repair_hints` 已经连续明确要求：
   - `query_topology`
   - `query_feature_probes`
   - `inspect count or placement with geometry/topology evidence`
4. 但 `prompts/round_05_request.json` 到 `round_08_request.json` 里实际 policy 却被锁成：
   - `repair_from_actionable_kernel_patch_after_validation_blocker`
   - 允许面只剩 `execute_build123d`

继续往下查 `trace/domain_kernel_round_08.json`，发现所谓“actionable patch”其实还没真正可执行：

1. `latest_repair_packet_family_id = explicit_anchor_hole`
2. `latest_repair_packet_recipe_id = explicit_anchor_hole_helper_contract_fallback`
3. `latest_repair_packet_target_anchor_summary = {}`
4. `repair_packets[0].anchor_keys = []`
5. `repair_packets[0].parameter_keys` 只有 coarse bbox keys
6. `recipe_skeleton.center_source_key = derive_from_requirement_or_validation`

也就是说：

1. validator 在说“还缺 topology / feature grounding”
2. kernel packet 却只是一个“还没落到可执行锚点”的半成品
3. runtime 以前会把这个半成品错当成可以直接 whole-part repair 的 lane

本轮已在 `src/sub_agent_runtime/agent_loop_v2.py` 补上显式 gate：

1. `_validation_has_evidence_gap(...)`
2. `_validation_requests_localized_evidence_refresh(...)`
3. `_kernel_patch_is_under_grounded_for_local_feature_gap(...)`
4. `_kernel_patch_should_yield_semantic_refresh(...)`

新的语义是：

1. validation 若明确还缺 localized geometry/topology evidence
2. 且 latest kernel patch 只有 coarse bbox 参数、没有 anchor grounding、recipe center 还依赖 requirement/validation 推断
3. 那么该 patch 不能直接抢走控制面
4. turn policy 必须先回到 semantic refresh

对应新增回归：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
2. 新测试：
   - `test_under_grounded_kernel_patch_yields_semantic_refresh_for_local_feature_evidence_gap`
3. 回归结果：
   - `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
   - `21 passed`

### 60. 新一轮真实 practice run 已再次发起，开始验证新的 under-grounded patch gate

新的真实 session：

1. `session_id = 9329`
2. 命令：
   - `uv run python scripts/run_custom_prompt_practice.py --seed-ids topology_local_finish_focus,organic_clamshell_storage_midhigh --variants-per-seed 1 --max-rounds 8 --runs-root practice_runs`

本轮重点观察项：

1. `topology_local_finish_focus` 是否不再出现 `round_04 ~ round_07` 那种纯 `validate_requirement_post_write` 循环
2. validator 已经要求 `query_topology / query_feature_probes` 的时候，runtime 是否真的把这两个 read lane 打开
3. `organic_clamshell_storage_midhigh` 是否会因为这条新 gate 出现副作用回退

### 61. 已把 explicit-anchor helper contract skill 前移：不再等 lint 之后才提醒 `CounterSinkHole(...)` 正确关键字

新的 topology real run 很快暴露出另一层共享偏差：

1. `practice_runs/20260416_234357/topology_local_finish_focus_v00_95ab3188/queries/round_02_query_feature_probes.json`
2. `practice_runs/20260416_234357/topology_local_finish_focus_v00_95ab3188/queries/round_02_query_topology.json`
3. `practice_runs/20260416_234357/topology_local_finish_focus_v00_95ab3188/actions/round_03_execute_build123d.json`

这次 runtime 已经成功把 lane 从：

1. `repair_from_actionable_kernel_patch_after_validation_blocker`

拉回到：

1. `semantic_refresh_before_under_grounded_kernel_patch_for_local_feature_gap`

并且 round 02 已真实使用：

1. `query_feature_probes`
2. `query_topology`

但 round 03 的新失败表明另一个共享问题还在：

1. `failure_kind = execute_build123d_api_lint_failure`
2. `lint rule = invalid_build123d_keyword.countersink_angle_alias`
3. 模型已经愿意采用 helper-first rebuild
4. 但还是把 `CounterSinkHole(...)` 的关键字写成了 guessed alias

这说明旧的 `execute_build123d_explicit_anchor_helper_first_repair` 还不够前置，因为它此前只会在：

1. 已经出现 explicit-anchor 相关 lint 之后

才进入 runtime skills。

本轮已改成：

1. 只要 `latest_repair_packet_family_id = explicit_anchor_hole`
2. 且 `latest_repair_packet_recipe_id = explicit_anchor_hole_helper_contract_fallback`
3. 就主动把 helper-first guidance 放进 skill pack

对应改动：

1. `src/sub_agent_runtime/skill_pack.py`
2. `tests/unit/sub_agent_runtime/test_skill_pack.py`

新增回归：

1. `test_build_runtime_skill_pack_proactively_adds_explicit_anchor_helper_guidance_from_repair_packet`

回归结果：

1. `uv run pytest tests/unit/sub_agent_runtime/test_skill_pack.py -q`
2. `24 passed`

### 62. 已补 `topology targeting -> apply_cad_action` 的强制收口 gate，开始验证 local-finish read stall 是否真正被切断

新一轮回归里又暴露出更细的一层 closure 问题：

1. `practice_runs/20260416_234357/topology_local_finish_focus_v00_95ab3188/queries/round_02_query_topology.json`
2. `practice_runs/20260416_234357/topology_local_finish_focus_v00_95ab3188/plans/round_05_response.json`
3. `practice_runs/20260416_234357/topology_local_finish_focus_v00_95ab3188/plans/round_08_response.json`

问题不是 runtime 没拿到 topology 锚点，而是：

1. `query_topology` 已经返回大量 `matched_ref_ids`
2. `candidate_sets` 也已明确给出 `Top Faces / Opening Rim Edges`
3. `query_feature_probes` 同时推荐 `query_topology + apply_cad_action`
4. 但后续 policy 仍只是宽松地允许 `query_topology`
5. 模型继续消耗读回合，没有把这些 ref 真正喂进 `apply_cad_action`

本轮已在 `src/sub_agent_runtime/agent_loop_v2.py` 加入新的证据驱动 gate：

1. `_latest_feature_probes_recommend_apply_local_finish(...)`
2. `_has_actionable_topology_targeting_since_round(...)`
3. `_local_finish_should_force_apply_after_topology_targeting(...)`

新的规则是：

1. 只要 latest `query_feature_probes` 已明确推荐 `apply_cad_action`
2. 且 latest `query_topology` 已返回非空 `matched_ref_ids` 或带 `ref_ids` 的 `candidate_sets`
3. 那么下一轮 policy 不再继续开放 `query_topology`
4. 直接切到：
   - `policy_id = apply_local_finish_after_topology_targeting`
   - `allowed_tool_names = ["apply_cad_action"]`

对应新增 TDD 回归：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
2. `test_local_finish_lane_forces_apply_cad_action_after_topology_candidates_exist`

回归结果：

1. targeted:
   - `3 passed, 19 deselected`
2. full:
   - `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
   - `22 passed`

新的真实验证 session 已发起：

1. `session_id = 45179`
2. 命令：
   - `uv run python scripts/run_custom_prompt_practice.py --seed-ids topology_local_finish_focus --variants-per-seed 1 --max-rounds 8 --runs-root practice_runs`
3. run root：
   - `practice_runs/20260416_235841/`

### 63. `235841` 真实 run 已完成：新的强制 local-finish gate 还没被触发，当前瓶颈上移到 feature probe family 暴露不稳定

`practice_runs/20260416_235841/topology_local_finish_focus_v00_95ab3188/` 的结果说明：

1. 新的 `apply_local_finish_after_topology_targeting` gate 没有误触发
2. 但也没有被真正命中
3. 原因不是 topology read 没做好，而是 `query_feature_probes` 没继续暴露 `named_face_local_edit`

关键证据：

1. `queries/round_03_query_topology.json`
   - `matched_ref_ids = 43`
   - `candidate_sets` 已包含：
     - `primary_outer_faces`
     - `top_outer_edges`
     - `top_faces`
     - `opening_rim_edges`
2. `queries/round_04_query_feature_probes.json`
   - 只剩：
     - `explicit_anchor_hole`
     - `core_geometry`
   - 没有：
     - `named_face_local_edit`
3. `summary.json`
   - `failure_cluster = read_stall_gap`
   - `forced_policy_chain` 仍停在：
     - `semantic_refresh_before_under_grounded_kernel_patch_for_local_feature_gap`
     - `graph_refresh_after_read_stall`
     - `semantic_refresh_after_validation_assessment_gap_from_code_write`

这说明当前主瓶颈已经进一步收敛为：

1. read-model 其实能给出可操作 topology refs
2. 但 feature-probe / domain-kernel 没把 local-finish family 稳定保留下来
3. 所以 control surface 仍然以 `explicit_anchor_hole subtree_rebuild` 为主线

### 64. 已把 `Rectangle(length=...)` 纳入 preflight governance：避免 2D sketch primitive keyword 幻觉继续漏到容器里

`235841` 这轮 run 在：

1. `practice_runs/20260416_235841/topology_local_finish_focus_v00_95ab3188/actions/round_06_execute_build123d.json`

暴露出新的共享 API 幻觉：

1. stderr:
   - `TypeError: Rectangle.__init__() got an unexpected keyword argument 'length'`
2. 当时 `lint_rule_ids = []`
3. 说明 preflight 还没有覆盖：
   - `Rectangle(width=..., length=...)`

这类错误不该等到容器运行后才暴露，因此本轮已在：

1. `src/sub_agent_runtime/tool_runtime.py`

新增 rule：

1. `invalid_build123d_keyword.rectangle_length_alias`

对应 TDD：

1. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
2. `test_preflight_lint_rejects_rectangle_length_keyword_alias`

回归结果：

1. targeted:
   - `1 passed`
2. subset:
   - `2 passed`
3. full:
   - `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
   - `87 passed`

### 65. 已把 `preferred_probe_families` 从软提示提升为 runtime-managed probe governance，防止模型漏掉 `named_face_local_edit`

`235841` 的核心证据已经说明：

1. `plans/round_04_response.json`
   - `query_feature_probes.arguments.families = ["explicit_anchor_hole", "core_geometry"]`
2. 但同轮 `turn_tool_policy.preferred_probe_families` 其实还包含：
   - `named_face_local_edit`

也就是说，当前 semantic refresh lane 已经知道应该补哪几个 family，但 `query_feature_probes` 仍允许模型自己漏掉它们。这会导致：

1. topology evidence 明明已经存在
2. probe lane 却只盯着 `explicit_anchor_hole`
3. `named_face_local_edit` 被意外丢失
4. 后续 local-finish gate 无法触发

本轮已在 `src/sub_agent_runtime/tool_runtime.py` 做一条治理层修复：

1. `query_feature_probes` 的 runtime-managed fields 不再只 `setdefault("families", [])`
2. 而是把：
   - 模型显式传入的 `families`
   - 当前 `turn_tool_policy.preferred_probe_families`
3. 做去重合并

这条修复不依赖具体 case，只依赖：

1. 当前回合是否存在 turn policy
2. 该 policy 是否已经显式给出 preferred probe families

对应新增回归：

1. `tests/unit/sub_agent_runtime/test_tool_runtime_execution.py`
2. `test_query_feature_probes_injects_preferred_probe_families_from_turn_policy`

回归结果：

1. targeted:
   - `1 passed`
2. full:
   - `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_execution.py -q`
   - `6 passed`

### 66. `000536` 真实 rerun 已证明两条治理修复生效：family 丢失问题消失，write-surface 幻觉归零

`practice_runs/20260417_000536/topology_local_finish_focus_v00_95ab3188/` 的关键变化非常明确：

1. `plans/round_03_response.json`
   - `query_feature_probes.arguments.families = ["explicit_anchor_hole", "named_face_local_edit", "core_geometry"]`
2. `queries/round_03_query_feature_probes.json`
   - `detected_families` 真实包含：
     - `named_face_local_edit`
   - 对应 probe 已给出：
     - `recommended_next_tools = ["query_topology", "apply_cad_action"]`
3. 整轮 `hallucination_event_count = 0`
4. 没再出现：
   - `Rectangle(length=...)`
   - `CounterSinkHole keyword alias`
   - 其他 write-surface API contract 爆炸

这说明：

1. `preferred_probe_families` 注入修复已经有效
2. `Rectangle(length=...)` 的 preflight governance 也已经避免了 runtime exception 继续漏到容器里

### 67. 剩余主瓶颈继续收敛：local-finish 已经 ready，但 read-stall exit 分支还没强制 apply

`000536` 还没有收敛完成，但失败形态已经进一步变“健康”：

1. `forced_policy_chain` 从 round 04 开始反复进入：
   - `local_finish_after_read_stall_topology_refresh`
2. `plans/round_04_response.json`
   - 模型已经明确说“then use apply_cad_action”
3. 但 run 启动时还没有我刚补上的新 gate，所以 allowed tools 仍是：
   - `query_kernel_state`
   - `query_topology`
   - `apply_cad_action`
4. 最终还是被模型拖成：
   - `inspection_only_rounds = 7`

针对这一点，本轮已继续补第二条 closure gate：

1. `src/sub_agent_runtime/agent_loop_v2.py`
2. 新 policy：
   - `apply_local_finish_after_topology_targeting_from_read_stall`

规则改成：

1. 如果已经进入 read-stall exit 的 local-finish lane
2. 且 topology refs 已 actionable
3. 且 feature probe 已明确推荐 `apply_cad_action`
4. 下一轮直接只开放：
   - `apply_cad_action`

对应新增回归：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
2. `test_read_stall_with_actionable_topology_targets_forces_apply_cad_action`

回归结果：

1. subset:
   - `3 passed, 20 deselected`
2. full:
   - `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
   - `23 passed`

新的真实验证 session 已再次发起：

1. `session_id = 16322`
2. 目标：
   - 验证 `local_finish_after_read_stall_topology_refresh` 是否被新的 apply-only gate 替代

### 68. `000852` 说明 local-finish lane 已经被重复 code failure 正常抬起，但 actionable feature-probe refresh 仍需 apply-only 收口

`practice_runs/20260417_000852/topology_local_finish_focus_v00_95ab3188/` 提供了一个更进一步的健康信号：

1. `queries/round_06_query_feature_probes.json`
   - `detected_families` 继续稳定包含：
     - `explicit_anchor_hole`
     - `named_face_local_edit`
   - `named_face_local_edit` probe 继续给出：
     - `recommended_next_tools = ["query_topology", "apply_cad_action"]`
     - `grounding_blockers = []`
2. `prompts/round_07_request.json`
   - policy 已经进入：
     - `local_finish_after_actionable_feature_probe_refresh`

也就是说：

1. repeated code failure -> semantic refresh -> local_finish promotion
2. 这条链已经打通
3. 但该 policy 当时仍只允许：
   - `query_kernel_state`
   - `query_topology`
   - `apply_cad_action`

结果：

1. `plans/round_07_response.json`
   - 仍选择了 `query_topology`
2. `queries/round_07_query_topology.json`
   - 依旧返回了 `matched_ref_ids = 46`
3. 但没有真正消费这些 ref 进入局部修改

### 69. 已给 `local_finish_after_actionable_feature_probe_refresh` 补 apply-only 收口，避免 local-finish 各入口继续重复读 topology

本轮已继续在 `src/sub_agent_runtime/agent_loop_v2.py` 增加：

1. `apply_local_finish_after_actionable_feature_probe_refresh`

新的逻辑是：

1. 如果 repeated code failure 之后已经通过 feature probes 抬起了 local-finish lane
2. 且 topology read 已经给出 actionable refs
3. 那么不要再给模型 `query_topology`
4. 直接只开放：
   - `apply_cad_action`

对应新增回归：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
2. `test_actionable_feature_probe_refresh_forces_apply_after_topology_targeting`

回归结果：

1. subset:
   - `3 passed, 21 deselected`
2. full:
   - `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
   - `24 passed`

新的真实验证 session 已再次发起：

1. `session_id = 96372`
2. 目标：
   - 验证 `local_finish_after_actionable_feature_probe_refresh` 是否被 apply-only 版本取代

### 70. 已补 `followup_after_semantic_refresh_before_closure_validation_from_code_write`，避免 budget-skipped validation 过早抢回 closure lane

`practice_runs/20260417_001422/topology_local_finish_focus_v00_95ab3188/` 暴露了一个新的主瓶颈：

1. `queries/round_06_validate_requirement_post_write.json`
   - 主体几何 checks 已大多通过
   - 但 `a_front_thumb_notch` 与 `a_countersink_on_the_mounting_face_so_that_a_topology_aware_local_finishing_pass_is_useful` 仍是：
     - `insufficient_evidence`
   - `decision_hints` 同时给出：
     - `inspect count or placement with geometry/topology evidence`
     - `validation_llm_skipped:estimated_prompt_budget_exceeded:7821/7000`
2. `queries/round_07_query_feature_probes.json`
   - `named_face_local_edit` 已明确推荐：
     - `query_topology`
     - `apply_cad_action`
3. 但旧逻辑下 round 8 仍会进入：
   - `closure_validation_after_semantic_refresh_from_code_write`
   - 导致最后一轮继续偏向 `validate_requirement`

这说明：

1. validator 已经说“当前缺的是 placement / topology 级证据”
2. semantic refresh 也已经给出了 grounded follow-up
3. 但 closure lane 仍把这类状态当作“可以直接复验”的收尾态

本轮在 `src/sub_agent_runtime/agent_loop_v2.py` 新增：

1. `_latest_validation_has_budget_skipped_hint(...)`
2. `_post_semantic_refresh_followup_tools_for_turn(...)`
3. `_semantic_refresh_followup_should_preempt_closure_validation(...)`
4. 新 policy：
   - `followup_after_semantic_refresh_before_closure_validation_from_code_write`

新逻辑只依赖通用信号：

1. 最新 validation 仍是 evidence gap
2. semantic refresh 已成功执行
3. 最新 feature probes 已经给出具体 follow-up tools
4. 当前还没有新的 validation / finish turn

满足这些条件时：

1. 不再开放 `validate_requirement`
2. 直接沿着 feature-probe 给出的 grounded next tools 继续推进
3. 如果尚未拿到 fresh topology refs，就优先：
   - `query_topology`
4. 如果 topology refs 已经可消费，前面的 apply-only gate 会继续接管到：
   - `apply_cad_action`

对应新增回归：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
2. `test_budget_skipped_validation_after_semantic_refresh_keeps_followup_lane_open`

回归结果：

1. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
2. `25 passed`

新的真实验证 session 已再次发起：

1. `session_id = 99219`
2. 目标：
   - 验证 round 8 是否会从过早 closure 转成 grounded follow-up lane

### 71. `002446` 已证明 closure bug 被压下去，但主瓶颈正式切换到 `explicit_anchor_hole` repair quality

`practice_runs/20260417_002446/topology_local_finish_focus_v00_95ab3188/` 已完成，本轮最重要的结果不是分数，而是 failure shape 的变化：

1. `forced_policy_chain` 不再以：
   - `closure_validation_after_semantic_refresh_from_code_write`
   收尾
2. 相反，后半段已经被稳定锁到：
   - `repair_from_actionable_kernel_patch_after_validation_blocker`
   - `repair_from_actionable_kernel_patch_after_repeated_validation_blocker`
3. `read_only_turn_count` 从上一轮 `001422` 的 `3` 降到：
   - `1`
4. `feature_probe_count` 从 `2` 降到：
   - `1`
5. `build123d_hallucination.event_count` 从 `5` 降到：
   - `1`

也就是说：

1. closure lane 过早抢回控制权的问题已经被压下去
2. runtime 不再把最后几轮浪费在 `query_feature_probes -> validate_requirement` 的空转链里
3. 新的主瓶颈已经非常明确地切换到：
   - `explicit_anchor_hole` family 的 whole-part repair quality

本轮失败的真实形态在：

1. `queries/round_06_validate_requirement_post_write.json`
2. `queries/round_07_validate_requirement_post_write.json`
3. `summary.json`

核心信号是：

1. `feature_hole = pass`
2. 但 `feature_countersink = fail`
3. `two_mounting_holes = fail`
4. `a_countersink_on_the_mounting_face_so_that_a_topology_aware_local_finishing_pass_is_useful = fail`
5. evidence 里反复出现：
   - `hole_feature=True`
   - `snapshot_countersink_geometry=False`
   - `cone_like_face_present=False`

这说明：

1. 当前主问题已经不是“没有孔”
2. 而是 helper-first repair 还没有稳定地产生 countersink 的锥面几何

### 72. 本地 Build123d 最小实验已确认：`CounterSinkHole` 的关键契约不是参数名，而是 host-face plane 的法向朝向

为了确认 `feature_hole=true but countersink=false` 的真正原因，本轮做了本地最小实验：

1. `CounterSinkHole` 签名确认：
   - `(radius, counter_sink_radius, depth=None, counter_sink_angle=82, mode=Mode.SUBTRACT)`
2. 在底面使用常见的 inward-normal 风格时：
   - 结果只有 `PLANE + CYLINDER`
   - 没有 `CONE`
3. 在顶面，或者在底面显式使用 outward-normal plane 时：
   - 会出现 `CONE`

实验结果摘要：

1. `top_plane`
   - `CONE = 2`
   - `CYLINDER = 2`
2. `bottom_inward_custom`
   - `CONE = 0`
   - `CYLINDER = 2`
3. `bottom_outward_custom`
   - `CONE = 2`
   - `CYLINDER = 2`

这条证据直接说明：

1. 当前 explicit-anchor family 的瓶颈不只是 helper 名称或 keyword alias
2. 更关键的是模型还没有稳定掌握：
   - `CounterSinkHole` 必须绑定到目标 host-face plane
   - 且要沿该 face 的 outward normal 使用
3. 如果 plane 法向错了，就会出现：
   - 有孔
   - 但没有 visible countersink cone

基于这条结论，本轮已继续加强两层资产：

1. `src/sub_agent_runtime/feature_graph.py`
   - 给 `explicit_anchor_hole_helper_contract_fallback` 增加：
     - `workplane_normal_strategy = host_face_outward_normal`
     - `center_frame_kind = host_face_local_2d`
     - `center_count_hint`
     - `center_count_source`
     - `helper_contract = CounterSinkHole(...)`
2. `src/sub_agent_runtime/skill_pack.py`
   - 给 `execute_build123d_explicit_anchor_helper_first_repair` 增加：
     - outward-normal guidance
     - 2D host-face center-set guidance
     - preserve realized center count guidance

对应回归：

1. `uv run pytest tests/unit/sub_agent_runtime/test_feature_graph.py tests/unit/sub_agent_runtime/test_skill_pack.py -q`
2. `29 passed`

新的真实验证 session 已再次发起：

1. `session_id = 45335`
2. 目标：
   - 验证新的 repair packet contract 与 skill guidance 是否会把 countersink family 的 whole-part repair 再往前推一档

### 73. `003311` 暴露出另一条真实根因：`previous_tool_failure_summary.repair_recipe` 进入 prompt 时被截断，repair lane 的关键步骤没有高显著暴露给模型

本轮在复盘 `practice_runs/20260417_003311/topology_local_finish_focus_v00_95ab3188/` 时，确认了一个比“模型不听 guidance”更扎实的系统根因：

1. `prompts/round_02_request.json`
2. `plans/round_02_response.json`

直接对照后可以看到：

1. prompt 里的 `previous_tool_failure_summary.repair_recipe.recipe_skeleton.steps` 被截断成 `...[truncated ...]`
2. 但 round 2 以后模型又重新退回：
   - `Plane.rotated(..., origin_tuple)` 猜测
   - nested subtractive `BuildPart`
   - manual cutter 回潮

这说明：

1. preflight repair recipe 虽然已经生成出来
2. 但进入 planner prompt 时没有被保留成完整、可消费的控制面
3. 模型实际看到的是“半截 recipe + 一堆泛化 hygiene 文案”

对应修复：

1. `src/sub_agent_runtime/context_manager.py`
   - `build_previous_tool_failure_summary(...)` 不再粗暴压扁 `repair_recipe`
   - 现在会保留：
     - `recipe_id`
     - `repair_family`
     - `recipe_summary`
     - `recipe_skeleton.steps`
2. `src/sub_agent_runtime/skill_pack.py`
   - 新增 `execute_build123d_failure_recipe_focus`
   - 直接把上一轮失败生成出的 recipe id / family / skeleton / steps 抬成高显著 runtime skill

对应回归：

1. `uv run pytest tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_skill_pack.py -q`
2. `29 passed`

### 74. fresh rerun `20260416_235900` 证明两条泛化修复已经见到效果，但主瓶颈切到 local action contract

最新真实 run：

1. `practice_runs/20260416_235900/topology_local_finish_focus_v00_95ab3188/`

当前观察到的正向变化：

1. `build123d_hallucination.event_count`
   - 从 `003311` 的 `15`
   - 降到 `11`
2. `invalid_build123d_keyword.slot_center_point_radius_alias`
   - 在这条新 run 中不再出现
   - 说明 AST 级 lint 修复已经生效
3. `forced_policy_chain` 首次真的走到了：
   - `apply_local_finish_after_topology_targeting`

但新的真实瓶颈也更清楚了：

1. round 5 已经成功使用：
   - `query_feature_probes`
   - `query_topology`
2. round 6 也已经切到了：
   - `apply_cad_action`
3. 但模型发出的 action 仍是：
   - `hole(diameter=..., depth=..., position={x,y,z}, direction=[...])`
   - 没有消费 `face_ref`
4. 最终在 replay 里触发：
   - `hole requires an existing solid result`

这说明：

1. 当前 read-model 与 policy 已经开始把 lane 推向 local finish
2. 但 `apply_cad_action.HOLE` 的 face-attached workplane contract 之前没有被服务层强制执行
3. 所以模型还能发出一个“裸 xyz 打孔”的 action，最后拖到 runtime exception 才暴露

对应修复：

1. `src/sandbox_mcp_server/service.py`
   - `_validate_action_contract(...)` 现在新增 `HOLE` 的 face-frame 契约：
     - 如果没有 `face_ref`
     - 也没有 `_face_candidate_hint`
     - 且当前没有 face-attached sketch context
     - 直接返回 `invalid_request`
2. suggestions 也同步改成：
   - 先 `query_topology` 拿 `face_ref`
   - 或先 `create_sketch(face_ref=...)`
   - 再用 hole 的 2D local centers

对应新增回归：

1. `tests/unit/sandbox_mcp_server/test_action_contracts.py`
2. `uv run pytest tests/unit/sandbox_mcp_server/test_action_contracts.py tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_skill_pack.py -q`
3. `29 passed`
4. `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
5. `89 passed`

### 75. 新一轮真实 rerun 已再次发起，目标是验证 local `hole` action 是否从 runtime exception 前移成可修复 contract failure

当前最新 session：

1. `practice_runs/20260417_000800/`

本轮重点不是看 benchmark 分数，而是看三件事：

1. `apply_local_finish_after_topology_targeting` 之后，模型是否会开始显式消费 `face_ref`
2. 如果仍然没有 `face_ref`，是否会被服务层更早地拒绝，而不是再炸成 `hole requires an existing solid result`
3. `build123d_hallucination` 的 `write_runtime_exception` 是否继续下降

### 76. `000800` 真实结果继续证明：主路径健康度在上升，hallucination 已从 `15 -> 11 -> 5`

新一条真实 run：

1. `practice_runs/20260417_000800/topology_local_finish_focus_v00_95ab3188/`

这条 run 的关键信号已经很明确：

1. `hallucination_event_count = 5`
2. `hallucination_weighted_score = 5.0`
3. `forced_policy_chain` 仍然稳定包含：
   - `apply_local_finish_after_topology_targeting`
4. `executed_action_types` 已经变成：
   - `execute_build123d`
   - `execute_build123d`
   - `apply_cad_action`
   - `execute_build123d`
   - `execute_build123d`
   - `execute_build123d`

和之前两条对照：

1. `003311`
   - `event_count = 15`
2. `235900`
   - `event_count = 11`
3. `000800`
   - `event_count = 5`

可以直接看出：

1. AST lint 误报修复
2. failure recipe 显著性提升
3. local hole contract 前移

这三条泛化修复叠加后，已经把 write-surface 噪音明显压下来了。

### 77. `000800` 也给出了更精确的下一条修复目标：topology 已经给出 `Opening Rim Edges`，但 local fillet 还没有消费 `edge_refs`

对应真实证据：

1. `practice_runs/20260417_000800/topology_local_finish_focus_v00_95ab3188/queries/round_03_query_topology.json`
2. `practice_runs/20260417_000800/topology_local_finish_focus_v00_95ab3188/plans/round_04_response.json`
3. `practice_runs/20260417_000800/topology_local_finish_focus_v00_95ab3188/actions/round_04_apply_cad_action.json`

这组产物说明：

1. `query_topology` 已经明确返回：
   - `Opening Rim Edges`
   - `Top Edges`
   - 以及对应 `edge_refs`
2. 但 round 4 的 planner 仍然只发了：
   - `apply_cad_action(fillet, selection_criteria=...)`
   - 没有 `edge_refs`
3. 最终在 Build123d 里触发：
   - `StdFail_NotDone`
   - `Failed creating a fillet with radius of 1.5`

因此本轮又新增一条 runtime preflight：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 当最近一次 `query_topology` 已经存在可消费的 `edge` candidate sets 时
   - `fillet/chamfer` 如果还不带 `edge_refs`
   - 就直接返回：
     - `apply_cad_action_contract_failure`
     - 并提示消费最新的 `edge_refs`

对应新增回归：

1. `tests/unit/sub_agent_runtime/test_tool_runtime_execution.py`
2. `test_apply_cad_action_fillet_requires_edge_refs_once_topology_candidates_exist`
3. `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_execution.py -q`
4. `7 passed`

### 78. `001200` 已再次发起，专门验证 local fillet 是否会从 runtime exception 前移成 `missing edge_refs` 的可修复反馈

最新真实验证目录：

1. `practice_runs/20260417_001200/`

本轮只看两件事：

1. round 4 左右如果再次进入 `apply_local_finish_after_topology_targeting`
   - 是否还会发出不带 `edge_refs` 的 fillet/chamfer
2. 如果仍然不带
   - 是否会被 tool runtime 更早拦下
   - 并把 failure surface 从 Build123d runtime exception 改成结构化 contract failure

### 79. `001200` 的真实结果说明：`missing edge_refs` 已经成功前移，但 local-finish repair lane 还会被预算策略截断

最新真实产物：

1. `practice_runs/20260417_001200/topology_local_finish_focus_v00_95ab3188/actions/round_05_apply_cad_action.json`
2. `practice_runs/20260417_001200/topology_local_finish_focus_v00_95ab3188/plans/round_06_response.json`
3. `practice_runs/20260417_001200/topology_local_finish_focus_v00_95ab3188/prompts/round_07_request.json`
4. `practice_runs/20260417_001200/topology_local_finish_focus_v00_95ab3188/summary.json`

这组证据已经明确证明：

1. round 5 的 local fillet 不再掉进 Build123d 几何内核异常
2. 而是被 runtime 更早拦成：
   - `apply_cad_action_contract_failure`
   - `missing edge_refs for local fillet/chamfer`
3. round 6 也确实按照建议去做了：
   - `query_topology`
4. 但 round 7 又被：
   - `code_first_after_semantic_refresh`
   - 提前切回 `execute_build123d`

所以主瓶颈已经进一步收缩成：

1. local contract failure 之后的 repair lane 没有被持续保活
2. runtime 仍会让 budget/policy 在 topology refresh 之后抢先把 lane 切回 whole-part rebuild

### 80. 本轮继续补了两条泛化修复：一条保 local retry lane，一条把 topology refs 真正放进 prompt 证据面

对应代码：

1. `src/sub_agent_runtime/agent_loop_v2.py`
2. `src/sub_agent_runtime/context_manager.py`

新增行为：

1. 当最近一次 `apply_cad_action` 失败属于：
   - `apply_cad_action_contract_failure`
2. 且后续已经通过 `query_topology` 拿到了可消费的 `matched_ref_ids/candidate_sets`
3. runtime 会优先返回：
   - `retry_local_finish_after_topology_contract_repair`
4. 不再让 `code_first_after_semantic_refresh` 抢先截断这条 local repair chain

同时，prompt evidence 也不再只给：

1. `Top Edges`
2. `Opening Rim Edges`

而是额外显式暴露：

1. `topology_targeting_summary`
2. `matched_ref_ids`
3. `candidate_sets[].ref_ids`

这样模型在下一轮不是只看到“有这个标签”，而是能直接消费真实 `edge_refs`。

### 81. 这两条修复都已补上单测，并已通过

对应新增回归：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
2. `test_apply_contract_failure_keeps_local_finish_retry_lane_after_topology_refresh`
3. `tests/unit/sub_agent_runtime/test_context_manager.py`
4. `test_prompt_payload_keeps_topology_candidate_refs_visible_for_local_finish_retry`

验证结果：

1. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
   - `26 passed`
2. `uv run pytest tests/unit/sub_agent_runtime/test_context_manager.py -q`
   - `2 passed`

### 82. 新一轮真实 rerun `002200` 已再次发起，专门验证这两条修复是否真的打通 `edge_refs` 消费链

当前运行目录：

1. `practice_runs/20260417_002200/`

这轮主要看三件事：

1. `retry_local_finish_after_topology_contract_repair` 是否真实触发
2. `round_07_request.json` 一类 prompt 是否已经能看到真实 `edge_refs`
3. planner 是否终于会把 `Opening Rim Edges` 的 `ref_ids` 带进 `apply_cad_action`

### 83. `002200` 已经给出正向证据：topology refs 进入 prompt 之后，模型真实消费了 `edge_refs`

关键产物：

1. `practice_runs/20260417_002200/topology_local_finish_focus_v00_95ab3188/prompts/round_08_request.json`
2. `practice_runs/20260417_002200/topology_local_finish_focus_v00_95ab3188/queries/round_07_query_topology.json`
3. `practice_runs/20260417_002200/topology_local_finish_focus_v00_95ab3188/plans/round_08_response.json`
4. `practice_runs/20260417_002200/topology_local_finish_focus_v00_95ab3188/actions/round_08_apply_cad_action.json`
5. `practice_runs/20260417_002200/topology_local_finish_focus_v00_95ab3188/practice_analysis.md`

这轮已经可以下两个明确结论：

1. `prompt` 里现在不只是有 `Top Edges` 这类标签，而是真的有：
   - `topology_targeting_summary`
   - `candidate_sets[].ref_ids`
   - 例如 `edge:1:E_fb2f396d2fb3`
2. round 8 的 planner 也确实消费了这些 ref，并发出了：
   - `apply_cad_action(fillet, edge_refs=[...], radius=1.5)`

对应结果：

1. `actions/round_08_apply_cad_action.json`
   - `success = true`
2. `practice_analysis.md`
   - `topology_targeting_observed: True`
   - `local_targeting_action_count: 1`
   - `local_targeting_examples` 中已记录真实 `edge_refs`

因此这条链路已经从：

1. “能识别 top edges”
2. “能把 failure 前移成 missing edge_refs”
3. 进一步推进成：
   - “真实把 topology refs 填进 local finish action 并成功执行”

### 84. 主瓶颈继续前移：现在更值得压的是 validator 的局部完成度 grounding，而不是继续给 local fillet 加硬规则

`002200` 的 stop reason 仍是：

1. `trace/stop_reason.json`
   - `max_rounds_reached`
   - `validation_complete = false`

同时：

1. `queries/round_08_validate_requirement_post_write.json`
   - 核心 feature checks 多数已 pass
   - 但要求级 clause 仍保留 `unknown`
2. `practice_analysis.md`
   - `status: partial_geometry`
   - `issue: Requirement validation has insufficient evidence`

这说明当前最值得继续收敛的共享 family 已经不是：

1. `missing edge_refs`
2. `local finish lane 被过早切回 code write`

而是：

1. read-model 对局部完成度的证据组织还不够强
2. validator 对这些局部证据的 clause-level grounding 还不够稳定

后续继续做复杂 custom practice 时，应该优先压：

1. `validator.insufficient_evidence` 的共享 closure
2. 局部 feature 与要求 clause 之间的 grounding registry
3. `query_kernel_state / query_feature_probes / query_topology` 三者的局部完成度摘要能力

### 85. 本轮补了两条更通用的 validator/read-model 收敛面：方向性 feature target 语义抽取，以及 precise feature grounding 放行

对应代码：

1. `src/sandbox_mcp_server/registry.py`
2. `src/sandbox_mcp_server/validation_interpretation.py`

新增收敛点：

1. `registry.py` 不再只识别显式的：
   - `select the top face`
   - `top-face recess`
2. 现在也会把这类相对位置语义吸进 `face_targets`：
   - `front thumb notch`
   - `holes on the right side`
   - `top pocket`
3. `validation_interpretation.py` 不再把所有 precise clause 一律打成：
   - `Feature-level evidence exists, but count or placement is still under-specified`
4. 当已经同时具备：
   - feature 本身存在
   - target-face grounding 已存在
   - 或更强的 notch/hole alignment 已存在
5. 会允许 notch / hole 这类局部 feature clause 直接判成 `VERIFIED`

这两条改动的目标不是修某个 prompt 字样，而是让：

1. 相对位置语义更稳定进入 validator / read-model
2. host-face grounding 不再被 precise-gating 白白浪费掉

### 86. 对应新增回归已通过

新增测试：

1. `tests/unit/sandbox_mcp_server/test_registry_semantics.py`
   - `test_analyze_requirement_semantics_detects_directional_feature_targets`
2. `tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py`
   - `test_interpret_requirement_clauses_verifies_directional_notch_when_feature_and_host_face_grounding_exist`

验证结果：

1. `uv run pytest tests/unit/sandbox_mcp_server/test_registry_semantics.py -q`
   - `3 passed`
2. `uv run pytest tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py -q`
   - `29 passed`
3. 随后又跑了合并回归：
   - `uv run pytest tests/unit/sandbox_mcp_server/test_registry_semantics.py tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py -q`
   - `32 passed`

### 87. 复杂 whole-part practice `010756` 再次证明：高难 enclosure 的主导失败仍是 write-surface Build123d 契约幻觉

真实产物：

1. `practice_runs/20260417_010756/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_01_execute_build123d.json`
2. `practice_runs/20260417_010756/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_02_execute_build123d.json`
3. `practice_runs/20260417_010756/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_05_execute_build123d.json`
4. `practice_runs/20260417_010756/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_08_execute_build123d.json`
5. `practice_runs/20260417_010756/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_07_query_feature_probes.json`
6. `practice_runs/20260417_010756/organic_clamshell_storage_midhigh_v00_0605b8a8/practice_analysis.json`

这轮的结论很明确：

1. `query_feature_probes` 已经能把 `slots` family 标成：
   - `requires_topology_host_ranking = true`
   - `need_topology_host_selection`
2. 但整条高难 whole-part case 仍主要死在 code write 契约上
3. 具体代表性幻觉包括：
   - `offset(opening=...)`
   - `Loc(...)`
   - `Cylinder(..., axis=...)`
   - `RuntimeError: Nothing to subtract from`

因此当前复杂 enclosure 主路径最该继续压的，不是 benchmark score，而是：

1. Build123d API governance
2. 让这些 whole-part shell / cutter / local subtraction 误写更早前移成结构化 repair recipe

### 88. 本轮又补了一条新的 whole-part shell 契约 lint：`offset(opening=...)`

对应代码：

1. `src/sub_agent_runtime/tool_runtime.py`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`

新增行为：

1. 如果 `execute_build123d` 里出现：
   - `offset(..., opening=...)`
2. runtime 会直接前移成：
   - `invalid_build123d_keyword.offset_opening_singular`
3. 并回给统一的：
   - `build123d_shell_offset_contract`

意义：

1. 复杂 enclosure case 里常见的 shell 幻觉又少了一条 runtime exception
2. 它会更早变成结构化 `repair_recipe`
3. 有利于继续把 high-difficulty whole-part case 从“容器里炸掉”推进成“runner 可解释、可修复”

对应验证：

1. `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
   - `90 passed`

### 89. 本轮把 `Rot/Pos/Location` 误当 context manager 的 whole-part 幻觉正式收编成 repair recipe

对应代码：

1. `src/sub_agent_runtime/tool_runtime.py`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`

新增资产：

1. `build123d_transform_placement_contract`

作用：

1. 以前这类错误只会留下：
   - `invalid_build123d_context.transform_context_manager`
2. 现在除了 lint 本身，还会落一个统一的结构化 recipe
3. 后续 whole-part 高难 prompt 再撞到：
   - `with Rot(...):`
   - `with Pos(...):`
   - `with Location(...):`
4. runtime 不再只是说“错了”，而是会明确要求：
   - active builder 内优先用 `Locations(...)`
   - 真要先变换再布尔，就先做 detached solid，再 `Rot(...) * solid`

对应回归：

1. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - `test_preflight_lint_rejects_transform_helper_as_context_manager`

### 90. 真实 practice `012453` 进一步把瓶颈钉死：旧 policy 会把 topology-sensitive blocker 继续压回 whole-part repair

对应产物：

1. `practice_runs/20260417_012453/summary.json`
2. `practice_runs/20260417_012453/run_diagnostics.json`
3. `practice_runs/20260417_012453/organic_clamshell_storage_midhigh_v00_0605b8a8/prompts/round_03_request.json`
4. `practice_runs/20260417_012453/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_02_validate_requirement_post_write.json`

这轮最重要的结论：

1. round 2 已经成功写出正体：
   - `actions/round_02_execute_build123d.json`
   - `solids=4`
   - `bbox=[78.0, 57.8, 32.75]`
2. validator 也已经把 blocker 收缩成更结构化的局部问题：
   - `feature_target_face_subtractive_merge`
   - `feature_notch_or_profile_cut`
   - `keep_wall_thickness_near_2_4mm`
   - `add_four_corner_magnet_recesses_on_the_mating_faces`
   - `a_front_thumb_notch_about_10mm_wide`
   - `one_side_plug_pocket`
3. 但 `round_03_request.json` 里的 policy 仍然是：
   - `repair_from_actionable_kernel_patch_after_validation_blocker`
4. 并且把：
   - `query_topology`
   - `query_feature_probes`
   全部挡掉，只允许继续 `execute_build123d`

这说明当时的真正主矛盾已经不是“模型不会想读”，而是：

1. runtime 在“已有几何 + 已有 blocker + family 明显 topology-sensitive”的情况下
2. 仍然过早把路径锁死到 whole-part rebuild

这轮最终的 run-level 结果也印证了这个判断：

1. `hallucination_event_count = 21`
2. `query_feature_probes = 2`
3. `query_topology = 0`
4. `forced_policy_chain` 里连续出现两次：
   - `repair_from_actionable_kernel_patch_after_validation_blocker`

### 91. 针对 `012453` 暴露的 gating 问题，新加了一条更泛化的 policy：先做 family probe assessment，再决定要不要 whole-part repair

对应代码：

1. `src/sub_agent_runtime/agent_loop_v2.py`
2. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`

新增判断面：

1. `_kernel_patch_should_yield_feature_probe_assessment(...)`
2. `feature_probe_assessment_before_actionable_kernel_patch_repair`

它解决的问题不是某一个 prompt，而是一类共享场景：

1. 最新 whole-part write 已成功
2. validator 已经给出 concrete blockers
3. kernel patch 看上去“可修”
4. 但 blocker family 明显落在：
   - `named_face_local_edit`
   - `slots`
   - `explicit_anchor_hole`
   - `half_shell`
   - `nested_hollow_section`
5. 这时不应该立刻再 whole-part rewrite
6. 而应该先打开：
   - `query_feature_probes`
   - `query_kernel_state`
   - 必要时再让 `query_topology` 提前进场

对应新增回归：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - `test_first_concrete_build123d_api_lint_failure_can_refresh_topology_when_latest_feature_probe_requires_host_selection`
   - `test_successful_whole_part_write_with_local_topology_sensitive_blockers_prefers_feature_probe_assessment`

对应验证：

1. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
   - `28 passed`

### 92. `013316` 的真实结果把当前瓶颈进一步收窄：topology 已进入 live lane，但进入得太晚

对应产物：

1. `practice_runs/20260417_013316/summary.json`
2. `practice_runs/20260417_013316/run_diagnostics.json`
3. `practice_runs/20260417_013316/organic_clamshell_storage_midhigh_v00_0605b8a8/plans/round_04_response.json`
4. `practice_runs/20260417_013316/organic_clamshell_storage_midhigh_v00_0605b8a8/plans/round_08_response.json`
5. `practice_runs/20260417_013316/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_08_query_topology.json`

这轮最关键的结论：

1. 幻觉主层仍是：
   - `write_surface`
2. 但 read lane 已经不再是空白：
   - round 4 真实执行了 `query_feature_probes`
   - round 8 真实执行了 `query_kernel_state + query_topology`
3. `query_topology` 已经返回：
   - 非空 `matched_ref_ids`
   - `candidate_sets_count = 13`
   - `Primary Outer Faces / Front Faces / Top Edges / Upward Planar Faces`
4. 所以问题已经不再是“进不了 topology”
5. 而是“topology 进入 live lane 的时机仍然偏晚，最后一轮只够读，不够改”

### 93. 基于 `013316`，继续把 `topology_refresh_after_first_concrete_code_failure` 从“允许 topology”收紧成“先执行 topology refresh”

对应代码：

1. `src/sub_agent_runtime/agent_loop_v2.py`
2. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`

具体改动：

1. 当最新 whole-part code failure 已经是 concrete Build123d contract failure
2. 且 freshest feature probe 已明确要求：
   - `need_topology_host_selection`
   - 或 `query_topology` 前置
3. policy 不再继续开放 `execute_build123d`
4. 而是先收紧成：
   - `mode = "graph_refresh"`
   - `allowed_tool_names = ["query_kernel_state", "query_topology"]`

对应回归：

1. 先把测试改成红灯，要求这一场景不能继续允许 `execute_build123d`
2. 再改生产代码
3. 验证：
   - `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
   - `28 passed`

这条收口仍然是共享控制流规则，不是 prompt/case 级 if/else。

### 94. `014253` 的真实结果说明：控制流比上一轮更愿意 probe-first，但 high enclosure 首轮 builder-native bias 仍明显不够

对应产物：

1. `practice_runs/20260417_014253/summary.json`
2. `practice_runs/20260417_014253/run_diagnostics.json`
3. `practice_runs/20260417_014253/organic_clamshell_storage_midhigh_v00_0605b8a8/plans/round_03_response.json`
4. `practice_runs/20260417_014253/organic_clamshell_storage_midhigh_v00_0605b8a8/plans/round_05_response.json`
5. `practice_runs/20260417_014253/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_05_query_feature_probes.json`

这轮的积极面：

1. round 3 已经真实执行：
   - `execute_build123d_probe + query_kernel_state`
2. round 5 已经真实执行：
   - `query_feature_probes`
3. 说明控制流已经不再只会一头扎进 repeated whole-part rewrite

但更大的问题也更清楚了：

1. `hallucination_event_count = 18`
2. `hallucination_weighted_score = 18.0`
3. `first_write_event_count = 16`
4. 首轮主导 rule id 集中在：
   - `invalid_build123d_contract.active_builder_part_mutation`
   - `invalid_build123d_contract.active_builder_temporary_primitive_arithmetic`
   - `invalid_build123d_context.sketch_primitive_requires_buildsketch`

所以当前 high enclosure 的主要收口方向之一，已经明确是 first-turn builder-native bias。

### 95. 两类 broad fillet runtime exception 已正式并入 `execute_build123d_selector_failure`

对应代码：

1. `src/sub_agent_runtime/context_manager.py`
2. `tests/unit/sub_agent_runtime/test_context_manager_failures.py`

新增收编：

1. `There are no suitable edges for chamfer or fillet`
2. `StdFail_NotDone: BRep_API: command not done` 且调用栈仍在 `fillet(...)` / `chamfer(...)`

这样 whole-part broad fillet 失败不再只是黑箱 runtime exception，而会自然带出：

1. selector/local-finish recovery bias
2. `query_feature_probes`
3. 后续 `query_topology`

对应验证：

1. `uv run pytest tests/unit/sub_agent_runtime/test_context_manager_failures.py -q`
   - `6 passed`

### 96. enclosure/clamshell 的 builder-native cavity guidance 已经前移到 first turn

对应代码：

1. `src/sub_agent_runtime/skill_pack.py`
2. `tests/unit/sub_agent_runtime/test_skill_pack.py`

本轮调整：

1. `nested_hollow_section_builder_native_cavity` 不再等到 `latest_tool == execute_build123d` 或失败后才出现
2. 只要 requirement 本身已经明显是：
   - `enclosure`
   - `shell`
   - `clamshell`
   - `lid/base`
3. first turn 也会直接给：
   - builder-native cavity
   - 不要 active builder part mutation
   - 不要 detached primitive arithmetic
   - 不要对整个 enclosure 宽泛全局 fillet

对应新增回归：

1. `test_build_runtime_skill_pack_adds_nested_hollow_section_cavity_guidance_on_first_turn`

对应整体验证：

1. `uv run pytest tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_context_manager_failures.py -q`
   - `60 passed`

### 97. `015055` 真实证明：first-turn builder-native cavity guidance 前移已经开始改变 whole-part 首轮形态

对应产物：

1. `practice_runs/20260417_015055/summary.json`
2. `practice_runs/20260417_015055/run_diagnostics.json`
3. `practice_runs/20260417_015055/organic_clamshell_storage_midhigh_v00_0605b8a8/prompts/round_01_request.json`
4. `practice_runs/20260417_015055/organic_clamshell_storage_midhigh_v00_0605b8a8/plans/round_02_response.json`

和 `014253` 对比，最关键的变化：

1. `014253` 的首轮 prompt 没有 `nested_hollow_section_builder_native_cavity`
2. `015055` 的首轮 prompt 已经带上这条 skill
3. 结果是 `015055` 的 round 2 whole-part 代码明显更 builder-native：
   - `BuildPart`
   - `Locations(...)`
   - `mode=Mode.SUBTRACT`
4. run-level 指标也对应收缩：
   - `hallucination_event_count: 18 -> 12`
   - `first_write_event_count: 16 -> 1`

### 98. `015055` 进一步把剩余瓶颈收缩到 placement / transform / context 契约

当前主导 failure family：

1. `invalid_build123d_api.loc_helper_name`
2. `invalid_build123d_context.transform_context_manager`
3. `invalid_build123d_context.sketch_primitive_requires_buildsketch`
4. 以及 whole-part broad fillet 的 runtime exception

这说明问题已经从“大块 enclosure 结构完全写错”继续收缩到：

1. `Locations/Location/Pos/Rot/scale(...)`
2. `BuildSketch` vs `BuildPart`
3. fillet selector / local-finish 切换

### 99. `Scale.by(...)` 已被正式收编进 preflight helper-contract 治理面

对应代码：

1. `src/sub_agent_runtime/tool_runtime.py`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`

新增：

1. `invalid_build123d_api.scale_helper_case`
2. `build123d_scale_helper_contract`

正确收口：

1. 不再写 `Scale.by(...)` / `Scale(...)`
2. 改成 lowercase：
   - `scale(shape, by=(sx, sy, sz))`

对应整体验证：

1. `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_context_manager_failures.py -q`
   - `153 passed`

### 100. `015906` 把当前主瓶颈进一步钉到 topology evidence 的消费阶段，而不是生成阶段

对应产物：

1. `practice_runs/20260417_015906/summary.json`
2. `practice_runs/20260417_015906/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_01_execute_build123d.json`
3. `practice_runs/20260417_015906/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_06_query_topology.json`
4. `practice_runs/20260417_015906/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_06_query_feature_probes.json`
5. `practice_runs/20260417_015906/organic_clamshell_storage_midhigh_v00_0605b8a8/plans/round_07_response.json`

关键事实：

1. `summary.json` 已经是：
   - `build123d_hallucination.event_count = 0`
   - `weighted_score = 0`
   - `step_file_exists = true`
2. round 6 已真实拿到：
   - `matched_ref_ids_count = 75`
   - `candidate_sets_count = 12`
3. `query_feature_probes` 仍明确建议：
   - `query_topology`
   - `apply_cad_action`
4. 但 round 7 还是回退到了 `execute_build123d`

这说明当前问题已经不是：

1. Build123d helper/API 幻觉
2. 也不是 read-model 完全缺席

而是：

1. topology 证据已经足够
2. 但 validation 里还残留 blocker 时，local-finish lane 的准入条件过窄
3. runtime 会因此错过一次本该直接消费 refs 的 `apply_cad_action`

本轮对应修复：

1. `src/sub_agent_runtime/agent_loop_v2.py`
2. 只要最近一次成功 `execute_build123d` 之后已经满足：
   - feature probe 推荐 `apply_cad_action`
   - topology 已给出可消费 refs
   - 当前还没进入 read stall
3. 就允许 local finish 覆盖“validation 仍有 blocker”的保守 veto

新增回归：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
2. 新增：
   - `test_local_finish_lane_forces_apply_even_with_remaining_validation_blockers`
3. 当前验证：
   - `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
   - `29 passed`

### 101. `021258` 真实证明：`apply_local_finish_after_topology_targeting` 已进入 live prompt，但 `apply_cad_action` 仍可能用 `rollback` 逃逸

对应产物：

1. `practice_runs/20260417_021258/organic_clamshell_storage_midhigh_v00_0605b8a8/prompts/round_07_request.json`
2. `practice_runs/20260417_021258/organic_clamshell_storage_midhigh_v00_0605b8a8/plans/round_07_response.json`
3. `practice_runs/20260417_021258/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_07_apply_cad_action.json`

这轮最关键的正向变化：

1. round 6 已真实执行 `query_topology + query_kernel_state`
2. round 7 的 `turn_tool_policy` 已经变成：
   - `apply_local_finish_after_topology_targeting`
   - `allowed_tool_names = ["apply_cad_action"]`

这证明 topology evidence 已能接管下一轮控制流。

但同一轮也暴露出新的 action-surface 漏洞：

1. `plans/round_07_response.json` 虽然调用了 `apply_cad_action`
2. 但实际参数是：
   - `action_type = rollback`
3. `actions/round_07_apply_cad_action.json` 里 rollback 真实成功

因此本轮继续新增：

1. `src/sub_agent_runtime/tool_runtime.py`
2. 在 `turn_tool_policy.mode == local_finish` 时，禁止 `apply_cad_action` 使用：
   - `rollback`
   - `clear_session`
   - `get_history`
   - `modify_action`
   - `snapshot`

新增回归：

1. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
2. `test_apply_cad_action_preflight_rejects_rollback_escape_under_local_finish_policy`
3. `test_apply_cad_action_preflight_allows_topology_anchored_create_sketch_under_local_finish_policy`
4. `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
   - `93 passed`

### 102. `021258` 还暴露了 feature probe 重复而 topology 进入过慢，因此继续补了 probe -> topology 的 anti-stall gate

对应产物：

1. `practice_runs/20260417_021258/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_02_query_feature_probes.json`
2. `practice_runs/20260417_021258/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_04_query_feature_probes.json`

现象：

1. round 2 probe 已建议：
   - `query_topology`
   - `apply_cad_action`
2. round 4 probe 仍重复建议相同 lane
3. 但 topology 直到 round 6 才真正执行

因此本轮新增：

1. `src/sub_agent_runtime/agent_loop_v2.py`
2. 新 policy：
   - `topology_refresh_after_feature_probe_assessment_from_code_write`
3. 含义：
   - 成功 whole-part 写之后
   - 如果已经成功跑过一次 feature probe
   - 且 probe 仍明确要求 `query_topology`
   - 同时尚未真正进入 `query_topology` / `apply_cad_action`
   - 下一轮直接收窄到：
     - `query_topology`
     - `query_kernel_state`

新增回归：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
2. `test_feature_probe_assessment_escalates_to_topology_refresh_before_repeating_probe`
3. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
   - `123 passed`

### 103. `022533` 真实 practice 证明：`topology_local_finish_focus` 这类练习当前仍可能先被 `Slot*` builder 参数契约幻觉截断，所以要继续把 write-surface contract 收编进治理层

对应产物：

1. `practice_runs/20260417_022533/topology_local_finish_focus_v00_95ab3188/summary.json`
2. `practice_runs/20260417_022533/topology_local_finish_focus_v00_95ab3188/actions/round_02_execute_build123d.json`
3. `practice_runs/20260417_022533/topology_local_finish_focus_v00_95ab3188/actions/round_05_execute_build123d.json`
4. `practice_runs/20260417_022533/topology_local_finish_focus_v00_95ab3188/actions/round_08_execute_build123d.json`
5. `practice_runs/20260417_022533/topology_local_finish_focus_v00_95ab3188/plans/round_03_response.json`
6. `practice_runs/20260417_022533/topology_local_finish_focus_v00_95ab3188/plans/round_06_response.json`
7. `practice_runs/20260417_022533/topology_local_finish_focus_v00_95ab3188/queries/round_04_query_kernel_state.json`

关键现象：

1. 这轮是专门验证 `local_topology_targeting_expected = true` 的 practice case
2. 但最终：
   - `step_file_exists = false`
   - `build123d_hallucination.event_count = 5`
   - `primary_layer = write_surface`
3. 真实错误依次是：
   - `SlotCenterPoint(center_point=...)`
   - `SlotCenterToCenter(center_to_center=...)`
   - `SlotCenterToCenter(width=...)`
4. round 3 / round 6 虽然真实用了 `execute_build123d_probe`
5. round 4 / round 7 也真实用了 `query_kernel_state`
6. 但因为没有成功 solid，`query_topology` 和 face/edge targeting 根本没有进入 live path

因此本轮继续新增：

1. `src/sub_agent_runtime/tool_runtime.py`
2. 新 rule：
   - `invalid_build123d_keyword.slot_center_to_center_alias`
3. 当前收编：
   - `center_to_center -> center_separation`
   - `width -> height`
4. 新增回归：
   - `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - `test_preflight_lint_rejects_slot_center_to_center_center_to_center_alias`
   - `test_preflight_lint_rejects_slot_center_to_center_width_alias`
5. 验证：
   - `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
   - `96 passed`
   - `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
   - `30 passed`

### 104. `184100` 真实 rerun 说明：现在链路已经能走到 `feature_probes -> query_topology -> apply_cad_action`，新的主瓶颈变成 local finish 没有消费 exact `face_ref`

对应产物：

1. `practice_runs/20260417_184100/topology_local_finish_focus_v00_95ab3188/summary.json`
2. `practice_runs/20260417_184100/topology_local_finish_focus_v00_95ab3188/queries/round_03_query_topology.json`
3. `practice_runs/20260417_184100/topology_local_finish_focus_v00_95ab3188/prompts/round_04_request.json`
4. `practice_runs/20260417_184100/topology_local_finish_focus_v00_95ab3188/plans/round_04_response.json`
5. `practice_runs/20260417_184100/topology_local_finish_focus_v00_95ab3188/actions/round_04_apply_cad_action.json`
6. `practice_runs/20260417_184100/topology_local_finish_focus_v00_95ab3188/actions/round_08_execute_build123d.json`

关键现象：

1. 这轮已经比 `022533` 健康很多：
   - `step_file_exists = true`
   - `feature_probe_count = 1`
   - `executed_action_types` 已包含 `apply_cad_action`
   - `forced_policy_chain` 已真实出现：
     - `topology_refresh_after_feature_probe_assessment_from_code_write`
     - `apply_local_finish_after_topology_targeting_from_read_stall`
2. `query_topology` 已经真实返回：
   - `face_ref`
   - `edge_refs`
   - `matched_ref_ids`
   - `candidate_sets`
3. round 4 也已经真实被 runtime 强制到：
   - `turn_tool_policy.mode = local_finish`
   - `allowed_tool_names = ["apply_cad_action"]`

但新的 action-surface 缝也被钉得很清楚：

1. `plans/round_04_response.json`
   - 模型选择的是 `apply_cad_action`
2. `actions/round_04_apply_cad_action.json`
   - runtime 直接拒绝：
     - `hole on an existing solid needs a face-attached local frame; provide face_ref from query_topology...`
3. 这说明模型虽然已经进了 local finish
4. 但仍然没有消费 exact `face_ref`
5. 而是把它降级成 broad alias `face = "bottom"`

因此本轮继续新增一层更泛化的 prompt contract，而不是再补 case 规则：

1. `src/sub_agent_runtime/context_manager.py`
2. system prompt 新增统一约束：
   - 当 `query_topology` 已经返回 exact refs 时，`apply_cad_action` 必须直接消费这些 refs
3. prompt payload 新增：
   - `local_finish_contract`
   - `must_consume_exact_topology_refs`
   - `preferred_face_refs`
   - `preferred_edge_refs`
   - `candidate_sets`
   - `instructions`
4. 新增回归：
   - `tests/unit/sub_agent_runtime/test_context_manager.py`
   - `test_prompt_payload_adds_local_finish_contract_when_topology_refs_exist`
5. 验证：
   - `uv run pytest tests/unit/sub_agent_runtime/test_context_manager.py -q`
   - `3 passed`
   - `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_context_manager.py -q`
   - `129 passed`

### 105. `184900` 进一步证明：有一部分 local-finish 失败不是模型不想用 refs，而是 prompt compaction 把 `topology_targeting_summary/local_finish_contract` 压没了

对应产物：

1. `practice_runs/20260417_184900/topology_local_finish_focus_v00_95ab3188/queries/round_04_query_topology.json`
2. `practice_runs/20260417_184900/topology_local_finish_focus_v00_95ab3188/prompts/round_05_request.json`
3. `practice_runs/20260417_184900/topology_local_finish_focus_v00_95ab3188/plans/round_05_response.json`
4. `practice_runs/20260417_184900/topology_local_finish_focus_v00_95ab3188/actions/round_05_apply_cad_action.json`

关键现象：

1. `query_topology` 的落盘产物已经有：
   - `face_ref`
   - `edge_refs`
   - `matched_ref_ids`
   - `candidate_sets`
2. 但 `round_05_request.json` 里：
   - `topology_targeting_summary = null`
   - `local_finish_contract = null`
   - `freshest_evidence.query_topology` 只剩一个被压扁的壳
3. 结果模型 round 5 继续输出：
   - `face = "bottom"`
   - 而不是 `face_ref = "face:..."`

因此本轮继续修的是 runtime compaction fidelity，而不是 case prompt：

1. `src/sub_agent_runtime/compact.py`
2. 新策略：
   - 把
     - `topology_targeting_summary`
     - `local_finish_contract`
     提升为 hard-budget priority sections
   - 压缩 `freshest_evidence` 时，若 `query_topology.targeting_summary` 已存在，则回填到压缩后的 evidence
3. 新增回归：
   - `tests/unit/sub_agent_runtime/test_context_manager.py`
   - `test_compacted_prompt_payload_keeps_topology_targeting_and_local_finish_contract`
4. 验证：
   - `uv run pytest tests/unit/sub_agent_runtime/test_context_manager.py -q`
   - `4 passed`
   - `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_context_manager.py -q`
   - `130 passed`

### 106. `20260416_221500` 把 local-finish 的剩余主矛盾继续收紧成两层：action contract 还不够硬，contract failure 后 policy 还会松回大回环

对应产物：

1. `practice_runs/20260416_221500/topology_local_finish_focus_v00_95ab3188/prompts/round_04_request.json`
2. `practice_runs/20260416_221500/topology_local_finish_focus_v00_95ab3188/plans/round_04_response.json`
3. `practice_runs/20260416_221500/topology_local_finish_focus_v00_95ab3188/actions/round_04_apply_cad_action.json`
4. `practice_runs/20260416_221500/topology_local_finish_focus_v00_95ab3188/trace/tool_timeline.jsonl`
5. `practice_runs/20260416_221500/topology_local_finish_focus_v00_95ab3188/practice_analysis.json`

这轮 run 已经明确证明：

1. compaction 修复是真实生效的
   - `round_04_request.json` 里：
     - `topology_targeting_summary` 存在
     - `local_finish_contract` 存在
     - `freshest_evidence.query_topology.targeting_summary` 也存在
2. 但 `round_04_response.json` 里模型仍输出：
   - `action_type = "hole"`
   - `action_params.face = "front"`
   - 没有 `face_ref`
3. `round_04_apply_cad_action.json` 仍被 runtime 拒绝：
   - `invalid_request: hole on an existing solid needs a face-attached local frame...`
4. 更关键的是 `trace/tool_timeline.jsonl` 显示：
   - round 5 走成了 `query_geometry`
   - round 6 又回到 `execute_build123d`
   - 也就是 `apply_cad_action 失败 -> query_geometry -> execute_build123d` 的大回环

因此本轮继续做了两层更泛化的修复，而不是补 case prompt：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增 local-finish face-target contract gate
   - 当最新 `query_topology` 已经给出 actionable `face_ref` 候选时：
     - `hole`
     - `sphere_recess`
     - 无 `path_ref` 的 `create_sketch`
     不能再退化成 broad `face` / `plane` alias
2. `src/sub_agent_runtime/agent_loop_v2.py`
   - 新增 policy：
     - `retry_local_finish_with_existing_topology_refs`
   - 当 `apply_cad_action` 只是因为没消费 exact refs 而失败，且当前几何状态并未变化时：
     - 下一轮继续收紧到 `apply_cad_action`
     - 不先松回 `query_geometry`

新增回归：

1. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - `test_apply_cad_action_preflight_rejects_broad_face_alias_for_hole_under_local_finish_policy`
2. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - `test_apply_contract_failure_keeps_local_finish_retry_lane_with_existing_topology_refs`
3. 回归验证：
   - `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_context_manager.py -q`
   - `132 passed`

### 107. `20260416_223300` 已经开始验证新约束的真实效果：首轮 whole-part write 更健康，round 4 再次确认 exact topology refs 确实进入 local-finish prompt

当前已落盘的关键产物：

1. `practice_runs/20260416_223300/topology_local_finish_focus_v00_95ab3188/actions/round_01_execute_build123d.json`
2. `practice_runs/20260416_223300/topology_local_finish_focus_v00_95ab3188/queries/round_03_query_topology.json`
3. `practice_runs/20260416_223300/topology_local_finish_focus_v00_95ab3188/prompts/round_04_request.json`
4. `practice_runs/20260416_223300/topology_local_finish_focus_v00_95ab3188/trace/conversation.jsonl`

这轮 run 到目前为止已经能确认：

1. 首轮 whole-part write 直接成功：
   - 没有先撞到 Build123d API 幻觉
2. round 3 已真实进入：
   - `query_topology`
   - `query_kernel_state`
3. round 4 的 prompt 再次确认：
   - `turn_tool_policy.mode = local_finish`
   - `topology_targeting_summary` 存在
   - `local_finish_contract` 存在
   - `preferred_face_refs` 已真实注入

这条 run 还在继续，下一步重点观察的是：

1. `round_04_response.json`
   - 模型是否开始真正消费 `face_ref`
2. 若仍失败：
   - 新 preflight gate 是否把 broad alias 更早拦下
3. 若再次 contract failure：
   - 新 policy 是否阻止它立刻松回 `query_geometry -> execute_build123d`

### 108. `20260416_223300` 已经在真实 LLM run 上验证了“preflight 早拦截 + bounded retry policy”这组组合修复

对应产物：

1. `practice_runs/20260416_223300/topology_local_finish_focus_v00_95ab3188/plans/round_04_response.json`
2. `practice_runs/20260416_223300/topology_local_finish_focus_v00_95ab3188/actions/round_04_apply_cad_action.json`
3. `practice_runs/20260416_223300/topology_local_finish_focus_v00_95ab3188/prompts/round_05_request.json`
4. `practice_runs/20260416_223300/topology_local_finish_focus_v00_95ab3188/plans/round_05_response.json`
5. `practice_runs/20260416_223300/topology_local_finish_focus_v00_95ab3188/trace/tool_timeline.jsonl`

这轮 run 里最关键的真实变化是：

1. round 4 模型仍然先犯老错误
   - `plans/round_04_response.json`
   - 仍输出：
     - `action_type = "hole"`
     - `face = "bottom"`
2. 但这次 runtime 不再把错误拖到 sandbox 深处
   - `actions/round_04_apply_cad_action.json`
   - preflight 直接拒绝：
     - `hole must use face_ref from latest query_topology instead of face='bottom' during local_finish`
3. 更重要的是 round 5 没再掉回：
   - `query_geometry -> execute_build123d`
4. `prompts/round_05_request.json`
   - 仍然保持：
     - `turn_tool_policy.mode = local_finish`
     - `previous_tool_failure_summary.failure_kind = apply_cad_action_contract_failure`
5. `plans/round_05_response.json`
   - 模型明确读取了失败反馈和 topology evidence
   - 改成：
     - `face_ref = "face:1:F_ee1b0c3da42c"`
6. `trace/tool_timeline.jsonl`
   - round 4：
     - `apply_cad_action` 在 preflight 失败
   - round 5：
     - `apply_cad_action(face_ref=...)` 成功

这说明本轮两层修复已经在真实链路上共同起作用：

1. action-surface gate
   - 把 broad alias 早拦下
2. retry policy
   - 让 runtime 在同一几何状态下继续 bounded local-finish
   - 而不是立刻松回 whole-part repair

这是目前最重要的一次真实收敛证据，因为它证明系统已经开始具备：

1. 先暴露 exact-ref 约束
2. 再把错误局限在 bounded retry lane 内修正
3. 最终真正消费 `face_ref`

### 109. `20260416_230500` 证明 `semantic_admission_after_first_stable_solid` 已经在真实 LLM run 中取代旧的预算误判分流

对应产物：

1. `practice_runs/20260416_230500/topology_local_finish_focus_v00_95ab3188/prompts/round_06_request.json`
2. `practice_runs/20260416_230500/topology_local_finish_focus_v00_95ab3188/plans/round_06_response.json`
3. `practice_runs/20260416_230500/topology_local_finish_focus_v00_95ab3188/trace/tool_timeline.jsonl`
4. `practice_runs/20260416_230500/topology_local_finish_focus_v00_95ab3188/practice_analysis.json`

这轮 run 的核心意义是：

1. round 5 的 `apply_cad_action(face_ref=...)` 成功之后
2. round 6 不再直接跳回：
   - `code_first_after_feature_budget_risk`
3. 而是首次真实进入：
   - `semantic_admission_after_first_stable_solid`
   - `query_kernel_state + query_feature_probes`
4. 这说明 `agent_loop_v2` 里原来把所有 post-solid admission 都误当成 budget risk 的问题已经被修掉

对应新增回归：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - `test_post_solid_semantic_admission_prefers_graph_refresh_before_code_escape_when_budget_allows`
   - `test_post_solid_semantic_admission_reopens_code_escape_when_budget_is_tight`

### 110. `20260416_232000` 证明 prompt 层的 `objective_health` 已经和 admission policy 对齐

对应产物：

1. `practice_runs/20260416_232000/topology_local_finish_focus_v00_95ab3188/prompts/round_06_request.json`
2. `practice_runs/20260416_232000/topology_local_finish_focus_v00_95ab3188/trace/tool_timeline.jsonl`
3. `practice_runs/20260416_232000/topology_local_finish_focus_v00_95ab3188/practice_analysis.json`

这轮 run 的关键变化是：

1. `objective_health.status`
   - 从旧的 `feature_budget_constrained`
   - 改成新的 `semantic_admission_required`
2. `objective_health.recommended_bias`
   - 从旧的 `prefer_execute_build123d_over_local_feature_continuation`
   - 改成新的 `refresh_semantic_state_before_reopening_whole_part_write`
3. `objective_health.recommended_next_tools`
   - 已改成：
     - `query_kernel_state`
     - `query_feature_probes`

对应新增回归：

1. `tests/unit/sub_agent_runtime/test_context_manager.py`
   - `test_objective_health_prefers_semantic_admission_before_code_escape_after_first_stable_local_finish`

### 111. 新增 `global fillet helper` preflight 规则，并在高难 practice seed 上观察到 failure family 正从 API 幻觉转向更真实的几何选择失败

对应改动：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增 `invalid_build123d_contract.global_fillet_helper_argument_contract`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - `test_preflight_lint_rejects_global_fillet_helper_with_host_shape_argument`

这一规则针对的是：

1. `fillet(shape, edges, radius)` 这类三参误用
2. 本地确认的真实签名是：
   - 全局 helper：`fillet(objects, radius)`
   - member style：`shape.fillet(radius, edge_list)`

对应真实 run 证据：

1. `practice_runs/20260416_232000/topology_local_finish_focus_v00_95ab3188/plans/round_07_response.json`
   - 模型曾生成：
     - `fillet(base_box, pocket_top_edges, FILLET_RADIUS)`
2. 这类错误此前只会在 runtime 里炸成普通异常
3. 现在已经被前移为可结构化治理的 lint 面

对应高难 seed 的最新信号：

1. `practice_runs/20260416_233500/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_01_execute_build123d.json`
   - 首轮失败不是 API 名字瞎写
   - 而是 broad fillet edge selection 引发：
     - `StdFail_NotDone`
2. `practice_runs/20260416_233500/organic_clamshell_storage_midhigh_v00_0605b8a8/plans/round_02_response.json`
   - 下一轮已自动转成 cleaner builder-first rebuild
   - 明确“defer fillets to a later local finishing step”

这说明当前系统状态继续在变健康：

1. 中难 case 上，主矛盾已从 policy 误判转成 whole-part rewrite 质量
2. 高难 case 上，主矛盾开始从“库不会用”继续往“几何 edge set 太宽、局部宿主不明确”迁移

### 112. `apply_cad_action.cut_extrude` 现在也有前置 contract gate，不再把“没有 active profile sketch”的错误留到 runtime 深处

对应改动：

1. `src/sub_agent_runtime/tool_runtime.py`
   - `_preflight_gate_apply_cad_action(...)`
   - 新增对 `cut_extrude` 的 active profile sketch 检查
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - `test_apply_cad_action_preflight_rejects_cut_extrude_without_active_profile_sketch`
   - `test_apply_cad_action_preflight_allows_cut_extrude_with_active_profile_sketch`

这次治理的核心不是加一个 case patch，而是把已有 write-surface 契约继续统一到 action-surface：

1. 如果 `query_sketch.sketch_state.profile_refs` 已经明确为空
2. 或当前 action history 只停留在 `create_sketch`
3. 那么 `apply_cad_action(cut_extrude)` 会直接返回：
   - `apply_cad_action_contract_failure`
4. 不再把错误拖到 sandbox 里炸成：
   - `cut_extrude requires an active profile sketch`

它补上的其实是和 `hole must use face_ref`、`fillet/chamfer must use edge_refs` 同一层的治理：

1. 先要求 local window 自身满足最小执行契约
2. 再允许进入几何执行

对应真实旧证据：

1. `practice_runs/20260416_233500/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_05_apply_cad_action.json`
   - 这里曾真实掉成：
     - `cut_extrude requires an active profile sketch`
2. 这次改动的目标就是把这类错误前移成可解释、可修复、可统计的 preflight failure

### 113. 两个常见 Build123d write-surface 幻觉 family 已补成结构化 `repair_recipe`，不再让 round 2/3 只拿到空壳 failure summary

对应改动：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增 `build123d_sketch_primitive_builder_contract`
   - 新增 `build123d_cylinder_axis_transform_contract`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - `test_preflight_lint_rejects_sketch_primitive_inside_buildpart_without_buildsketch`
   - `test_preflight_lint_rejects_unsupported_cylinder_axis_keyword`
   - 同时保留：
     - `test_preflight_lint_prefers_directional_hole_recipe_for_explicit_anchor_cylinder_axis`

这次补的不是新的 lint，而是把已存在的 lint family 变成能驱动下一轮 repair 的结构化资产：

1. `invalid_build123d_context.sketch_primitive_requires_buildsketch`
   - 以前能报错，但 `repair_recipe` 为空
   - 现在会明确给出：
     - sketch primitive 应进入 `BuildSketch(target_plane)`
     - 完成 2D profile 后再 `extrude/revolve`
2. `invalid_build123d_keyword.cylinder_axis`
   - 以前只有 explicit-anchor directional hole family 才能拿到专门 recipe
   - 现在通用 whole-part / subtree rebuild 也会拿到：
     - 先 `Cylinder(radius=..., height=...)`
     - 再用 `Rot(...)` / `Pos(...)` / `Locations(...)` 做朝向和放置

这一步对 high-difficulty prompt 很重要，因为它把：

1. “模型知道错了”
2. 升级成
3. “runtime 能把错法压缩成下一轮可复用的修复骨架”

这比继续只堆 skill 文案更接近长期可维护的治理层。

### 114. 修补 `code_first_after_semantic_refresh` 的过早逃逸：如果一次 local-finish 已经成功且语义刷新后仍指向局部修改，就继续留在 local-finish lane

这次修的是一个更深的调度问题，不是新的 Build123d API lint。

真实根因来自：

1. `practice_runs/20260417_000500/topology_local_finish_focus_v00_95ab3188/queries/round_02_query_topology.json`
   - 已经拿到 actionable `face_ref`
2. `practice_runs/20260417_000500/topology_local_finish_focus_v00_95ab3188/actions/round_04_apply_cad_action.json`
   - 已经成功做出一次 `face_ref` 驱动的 local hole/countersink
3. `practice_runs/20260417_000500/topology_local_finish_focus_v00_95ab3188/plans/round_06_response.json`
   - 旧策略却仍然把下一轮推回 `execute_build123d`
4. `practice_runs/20260417_000500/topology_local_finish_focus_v00_95ab3188/trace/tool_timeline.jsonl`
   - whole-part rewrite 随后在真实几何上撞到 `fillet(...)` 的 `StdFail_NotDone`

因此当前瓶颈已经非常明确：

1. 不是 topology id 根本读不出来
2. 也不是 `apply_cad_action` 完全做不了基于 `face_ref` 的局部修改
3. 而是一次局部成功之后，只要 semantic refresh 看到还有 unsatisfied feature，旧策略就会过早触发 `code_first_after_semantic_refresh`

这次改动对应：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - 新增：
     - `continue_local_finish_after_semantic_refresh`
     - `refresh_topology_for_continued_local_finish_after_semantic_refresh`
2. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - 新增：
     - `test_successful_local_finish_semantic_refresh_stays_on_local_finish_lane`

新的策略约束是：

1. 如果最近一次结构化成功写是 `apply_cad_action`
2. 且之后已经发生 semantic refresh
3. 且未满足 feature 仍明显属于 `local_finish` family
4. 那么：
   - 如果 topology refs 还新鲜，就继续 `apply_cad_action`
   - 如果 refs 已 stale / missing，就先 `query_topology`
5. 只有当这些条件都不成立时，才允许重新打开 whole-part code

单测验证：

1. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q -k 'successful_local_finish_semantic_refresh_stays_on_local_finish_lane or post_solid_semantic_admission'`
   - `3 passed`
2. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
   - `134 passed`

这一步的意义在于把当前系统的主矛盾继续往更健康的方向推：

1. 不再把已经建立起来的 topology-aware local finish 轻易打断
2. 尽量先耗尽局部修改 lane
3. 只有在 evidence 明确不足时才回到 whole-part rewrite

### 115. 修补 `query_topology` 的 face-hint 规范化缺口：`bottom` 这类显式 hint 以前不会自动映射到 `bottom_faces`

这是这轮新 real run 里继续挖出来的 read-model 基础问题。

真实证据来自：

1. `practice_runs/20260417_001500/topology_local_finish_focus_v00_95ab3188/plans/round_04_response.json`
   - 模型明确请求的是：
     - `selection_hints = ["bottom", "mounting", "primary_outer_faces"]`
2. `practice_runs/20260417_001500/topology_local_finish_focus_v00_95ab3188/queries/round_04_query_topology.json`
   - 返回的 candidate sets 却主要是：
     - `Primary Outer Faces`
     - `Top Faces`
     - `Front Faces`
     - `Upward Planar Faces`
   - 没有把 `Bottom Faces` 顶到前排
3. 新增单测：
   - `tests/unit/sandbox_mcp_server/test_topology_candidate_sets.py`
   - `test_normalize_topology_selection_hints_maps_generic_face_aliases_to_candidate_sets`
4. 红灯直接证明旧行为是：
   - `["bottom", "front", "top_faces"]`
   - 而不是：
   - `["bottom_faces", "front_faces", "top_faces"]`

根因在：

1. `src/sandbox_mcp_server/service.py`
   - `_normalize_topology_selection_hints(...)`
2. 这里之前只是把原始 hint 做 lower/underscore
3. 但不会把：
   - `bottom -> bottom_faces`
   - `front -> front_faces`
   - `top -> top_faces`
4. 后面的 candidate builder 认识的是 `*_faces`
5. 所以显式 face hint 会在规范化阶段就被弱化掉

这次修复后：

1. `top / bottom / front / back / left / right`
2. 以及对应的 `*_face`
3. 都会被统一映射到 canonical candidate ids：
   - `top_faces`
   - `bottom_faces`
   - `front_faces`
   - `back_faces`
   - `left_faces`
   - `right_faces`

回归结果：

1. `uv run pytest tests/unit/sandbox_mcp_server/test_topology_candidate_sets.py -q`
   - `3 passed`
2. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sandbox_mcp_server/test_topology_candidate_sets.py -q`
   - `137 passed`

这一步的意义不是修一个 `bottom` case，而是把“自然语言里的宿主面方向词”稳定映射到 read-model 真正消费的 candidate-set 语义面，减少 topology 目标在摘要和 prompt 压缩阶段被 requirement 派生 hint 稀释掉。

### 116. 修补 `post_solid_semantic_admission` 的“首个稳定实体”误判：已经存在 whole-part stable solid 时，不再把后续 local-finish 误判成第一颗稳定 solid

这次修的是另一个更隐蔽的 budget escape 根因。

红灯回归是：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
2. 新增：
   - `test_followup_local_finish_after_existing_whole_part_solid_does_not_retrigger_first_solid_code_escape`
3. 失败现象明确是：
   - round 1 已经有成功的 `execute_build123d` stable solid
   - round 2 后续又有一次成功的 `apply_cad_action`
   - 旧逻辑却仍然触发 `code_first_after_feature_budget_risk`

根因在：

1. `src/sub_agent_runtime/turn_state.py`
2. `build_post_solid_semantic_admission_signal(...)`
3. 旧版本只会向前检查“之前是否有别的 `apply_cad_action` stable solid”
4. 但不会识别“更早其实已经有 `execute_build123d` stable solid”

因此它会把：

1. later local finish success
2. 错当成
3. first stable solid admission

这正是 `practice_runs/20260417_002500/topology_local_finish_focus_v00_95ab3188/prompts/round_07_request.json`
里那种过早 `feature_budget_constrained -> code_first_after_feature_budget_risk`
的上游原因。

修复后：

1. 只要更早任意 write turn 已经产出 stable solid
2. 后续 local-finish 成功就不再重新触发“首个 stable solid 入场”这套 budget 信号

验证：

1. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q -k 'followup_local_finish_after_existing_whole_part_solid_does_not_retrigger_first_solid_code_escape or post_solid_semantic_admission'`
   - `3 passed`
2. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sandbox_mcp_server/test_topology_candidate_sets.py -q`
   - `137 passed`

这一步的价值在于：

1. 不再把“已经有 host solid 的局部修补”误当成“刚刚第一次建出可用实体”
2. `local_finish` lane 因此能在更合理的 budget 条件下继续延伸
3. whole-part rewrite 的时机更接近真实风险，而不是被错误 admission 信号提前触发

### 117. 修补 `apply_cad_action` 的 exact-ref 字段归一：`face -> face_ref`、`edges -> edge_refs`

这次不是加新的 hard rule，而是把 topology-aware local finish 的一个共享契约问题收成 runtime 治理层。

真实问题来自两类 live 产物：

1. `practice_runs/20260417_010500/topology_local_finish_focus_v00_95ab3188/actions/round_04_apply_cad_action.json`
   - 模型已经拿到了 `face:1:F_...`
   - 但把它写进了 `face` 字段
2. `practice_runs/20260417_010500/topology_local_finish_focus_v00_95ab3188/actions/round_08_apply_cad_action.json`
   - 模型已经拿到了 `edge:2:E_...`
   - 但把它写进了 `edges` 字段

也就是说，问题不再是：

1. ref 根本没读到

而是：

1. ref 已经读到
2. 但在 `apply_cad_action` 的字段契约上发生了轻微漂移

这次改动对应：

1. `src/sub_agent_runtime/tool_runtime.py`
2. 在 `_preflight_gate_apply_cad_action(...)` 中新增两条轻量归一：
   - `face='face:...'` -> `face_ref='face:...'`
   - `edges=['edge:...', ...]` -> `edge_refs=['edge:...', ...]`

TDD：

1. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
2. 新增：
   - `test_apply_cad_action_preflight_normalizes_exact_face_ref_passed_via_face_field`
   - `test_apply_cad_action_preflight_normalizes_exact_edge_refs_passed_via_edges_field`
3. 两条测试都先红后绿

回归：

1. `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'normalizes_exact_face_ref_passed_via_face_field or normalizes_exact_edge_refs_passed_via_edges_field or rejects_broad_face_alias_for_hole_under_local_finish_policy'`
   - `3 passed`
2. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sandbox_mcp_server/test_topology_candidate_sets.py -q`
   - `139 passed`

这一步的意义是：

1. runtime 不再把“已经得到 exact topology refs，但字段名轻微漂移”的情况粗暴算成失败
2. local finish 的成功率更依赖真实几何可行性，而不是 JSON 字段名偶然写错
3. 这类治理可以覆盖新 prompt / 新 family，不依赖固定 benchmark case

### 118. 修补 `face='bottom'` 这类 broad alias 的唯一候选收束：只有在 candidate set 唯一时才自动转成 `face_ref`

在 `practice_runs/20260417_011500/topology_local_finish_focus_v00_95ab3188/`
里，最新 live run 暴露出的下一层瓶颈是：

1. `queries/round_03_query_topology.json`
   - 已经返回：
     - `bottom_faces -> face:1:F_cd469ff74c98`
2. `prompts/round_04_request.json`
   - prompt 中已经明确出现：
     - `When query_topology has already returned exact face_ref ... pass those exact refs into apply_cad_action`
     - `preferred_face_refs`
     - `Bottom Faces`
3. 但 `round_04_apply_cad_action`
   - 仍然写成：
     - `face = "bottom"`

也就是说，当前还有一种更前面的契约漂移：

1. 模型没有把唯一候选的 broad alias 收束成 exact ref
2. 虽然 read-model 和 prompt 已经把唯一候选说得很清楚

因此这次再新增一条更保守的归一：

1. 仅当：
   - `turn_tool_policy.mode == local_finish`
   - action 需要 `face_ref`
   - `face` 是 `top/bottom/front/back/left/right`
   - 对应 candidate set 只有一个 `face:` ref
2. 才把 broad alias 自动改写成该唯一 `face_ref`
3. 如果 candidate set 里有多个 face refs，仍然继续严格拒绝

TDD：

1. 新增：
   - `test_apply_cad_action_preflight_resolves_unambiguous_broad_face_alias_to_single_candidate_ref`
2. 同时把旧测试收紧成“多候选 broad alias 仍应拒绝”
   - `test_apply_cad_action_preflight_rejects_broad_face_alias_for_hole_under_local_finish_policy`

回归：

1. `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'resolves_unambiguous_broad_face_alias_to_single_candidate_ref or normalizes_exact_face_ref_passed_via_face_field or normalizes_exact_edge_refs_passed_via_edges_field or rejects_broad_face_alias_for_hole_under_local_finish_policy'`
   - `4 passed`
2. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sandbox_mcp_server/test_topology_candidate_sets.py -q`
   - `141 passed`

这一步的意义在于：

1. 把 read-model 已经给出的“唯一候选面”真正接到 write-model
2. 继续减少 topology-local-finish 中“知道该改哪一面，但字段没收束”的无效损耗
3. 同时保留多候选时的严格性，不把 runtime 变成无约束猜测器

### 119. `012500` 证明最新治理已经把主导问题压到单一 read-surface alias：同一条 run 中连续两次 local finish 成功

这条 run 的证据非常强：

1. `practice_runs/20260417_012500/topology_local_finish_focus_v00_95ab3188/actions/round_03_apply_cad_action.json`
2. `practice_runs/20260417_012500/topology_local_finish_focus_v00_95ab3188/actions/round_06_apply_cad_action.json`
3. `practice_runs/20260417_012500/topology_local_finish_focus_v00_95ab3188/actions/round_08_apply_cad_action.json`
4. `practice_runs/20260417_012500/topology_local_finish_focus_v00_95ab3188/summary.json`
5. `practice_runs/20260417_012500/topology_local_finish_focus_v00_95ab3188/trace/tool_timeline.jsonl`

关键变化有三层：

第一层，前面几轮的 broad face alias 已经不再成为主导失败：

1. round 3 的 `apply_cad_action`
   - `face_ref = "face:1:F_dc834b0bfaea"`
   - `success = true`
2. round 6 的 `apply_cad_action`
   - `face_ref = "face:2:F_321cdb5542b4"`
   - `success = true`

第二层，forced policy chain 继续保持在 local-finish 语义下：

1. `summary.json`
   - `forced_policy_chain`
   - 仍包含：
     - `apply_local_finish_after_topology_targeting`
     - `refresh_topology_for_continued_local_finish_after_semantic_refresh`
     - `continue_local_finish_after_semantic_refresh`

第三层，整条 run 只剩下一个 hallucination 事件：

1. `build123d_hallucination.event_count = 1`
2. `build123d_hallucination.weighted_score = 0.6`
3. `build123d_hallucination.primary_layer = "read_surface"`
4. 唯一问题是 round 8 的局部 fillet：
   - `missing edge_refs for local fillet/chamfer`

这说明当前主导问题已经从：

1. whole-part build123d code 不会写

收敛成：

1. final local-fillet turn 上的 topology ref alias hygiene

### 120. 继续收口 local-finish alias：新增 `face_reference -> face_ref`、`target_edges -> edge_refs`

`012500` 的 round 8 暴露出的最后一层噪音是：

1. `target_edges = ["edge:3:E_6be4722df9d9"]`
2. `face_reference = "face:3:F_82e829026abc"`
3. 但 canonical contract 需要的是：
   - `edge_refs`
   - `face_ref`

因此本轮继续把这层 alias 也纳入 `_preflight_gate_apply_cad_action(...)` 的统一治理。

新增 TDD：

1. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
2. 新增：
   - `test_apply_cad_action_preflight_normalizes_exact_face_reference_alias`
   - `test_apply_cad_action_preflight_normalizes_exact_target_edges_alias`
3. 先红后绿

实现对应：

1. `src/sub_agent_runtime/tool_runtime.py`
2. 新增两条 exact-ref 归一：
   - `face_reference='face:...'` -> `face_ref='face:...'`
   - `target_edges=['edge:...', ...]` -> `edge_refs=['edge:...', ...]`

回归：

1. `uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'normalizes_exact_face_reference_alias or normalizes_exact_target_edges_alias or normalizes_exact_edge_refs_passed_via_edges_field or normalizes_exact_face_ref_passed_via_face_field'`
   - `4 passed`
2. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sandbox_mcp_server/test_topology_candidate_sets.py -q`
   - `143 passed`

这一步的意义在于：

1. 当前 local-finish lane 中常见的 exact topology ref 别名已经基本收进同一层治理
2. 后续真实 run 如果仍失败，就更接近真实几何可行性，而不是字段别名噪音
3. 这比继续针对单个 benchmark case 调 prompt 更接近长期可维护的系统修复面

### 121. `014500` 暴露出的更深层 runtime 问题已经定位并修复：solid-like host 在 replay helper 中被错误降级成空 `Part`

这条问题不是新的 benchmark patch，而是一个更底层的 continuity bug。

真实产物：

1. `practice_runs/20260417_014500/topology_local_finish_focus_v00_95ab3188/queries/round_04_query_topology.json`
   - `topology_index_present = true`
   - `top_faces.ref_ids = ["face:1:F_7e3afe9a032c"]`
2. `practice_runs/20260417_014500/topology_local_finish_focus_v00_95ab3188/actions/round_05_apply_cad_action.json`
   - `action_type = "hole"`
   - `face_ref = "face:1:F_7e3afe9a032c"`
   - `success = false`
3. `practice_runs/20260417_014500/topology_local_finish_focus_v00_95ab3188/queries/round_07_query_topology.json`
   - `candidate_sets = []`
   - `topology_index_present = false`

helper 级复现实验把根因钉死为：

1. `_aicad_result_has_positive_solid(solid) == True`
2. 但旧版 `_aicad_as_part(solid)` 之后
3. `_aicad_result_has_positive_solid(...) == False`

也就是说，真实 host 并不是一开始就没了，而是在 replay helper 里为了统一成 `Part(...)` 被错误降级成了“没有正体积实体的 Part”。

修复：

1. `src/sandbox_mcp_server/build123d_replay_helpers.py`
2. `_aicad_as_part(...)` 现在只有在 `Part(target)` 转换后仍保住正体积实体时才返回 `Part`
3. 否则保留原始 solid-like target，避免 `_aicad_apply_holes(...)` / `_aicad_apply_sphere_recesses(...)` 把有效 host 误判为空模型

### 122. 对应 TDD 与回归已补齐，local hole helper 现在能真实接受 solid-like host

新增 TDD：

1. `tests/unit/sandbox_mcp_server/test_build123d_replay_helpers.py`
   - `test_aicad_as_part_preserves_positive_solid_for_solid_result`
   - `test_apply_holes_accepts_solid_like_current_result`

同时保留上一轮的 failed-write snapshot fallback 回归：

1. `tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py`
   - `test_validate_requirement_defaults_to_latest_successful_snapshot_after_failed_write`
   - `test_query_topology_defaults_to_latest_successful_snapshot_after_failed_write`

验证：

1. `uv run pytest tests/unit/sandbox_mcp_server/test_build123d_replay_helpers.py -q`
   - `6 passed`
2. `uv run pytest tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py -q -k 'latest_successful_snapshot or query_topology_defaults_to_latest_successful_snapshot_after_failed_write'`
   - `2 passed`

这一步的价值在于：

1. 把 `014500` 这类局部减材失败，从“看起来像 feedback 没给清楚”收口成了一个确定的 runtime continuity bug
2. 把问题从模糊的“局部 hole 为什么有时不行”推进成明确的 helper 契约修复
3. 后续真实 practice run 如果仍失败，就更能代表真正的 write/read/validation 瓶颈，而不是被 replay helper 自己误伤

### 123. `020500` 进一步暴露出 policy 层计数语义问题：失败的 local retry 被误算成连续 structured writes，导致 round 8 过早逃回 `execute_build123d`

真实证据：

1. `practice_runs/20260417_020500/topology_local_finish_focus_v00_95ab3188/actions/round_07_apply_cad_action.json`
   - `success = true`
   - 已经真实完成一次 `face_ref` 驱动的 local hole/countersink
2. `practice_runs/20260417_020500/topology_local_finish_focus_v00_95ab3188/prompts/round_08_user_prompt.txt`
   - `policy_id = "code_first_after_feature_budget_risk"`
   - `reason` 中明确写出 `after 3 consecutive structured writes`
3. 但这条 run 的实际 structured write 序列是：
   - round 5 失败
   - round 6 失败
   - round 7 成功

根因：

1. `src/sub_agent_runtime/turn_state.py`
2. 旧版 `count_consecutive_write_turns(...)`
   - 只看 `turn.write_tool_name`
   - 不区分 write 成功还是失败
3. `build_feature_chain_budget_risk(...)`
   - 直接消费这个计数
   - 因此把“失败重试 + 成功一次”误判成“3 次连续 local-finish 写入”

修复：

1. `src/sub_agent_runtime/turn_state.py`
2. 新增 `count_consecutive_successful_write_turns(...)`
3. `build_feature_chain_budget_risk(...)` 改为只统计成功的 `apply_cad_action`

回归：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - `test_failed_local_action_attempts_do_not_count_as_feature_budget_escape_writes`
   - 按真实 `020500` 的 7-turn 结构复现
2. `uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q -k 'successful_local_finish_semantic_refresh_stays_on_local_finish_lane or code_first_after_feature_budget_risk or failed_local_action_attempts_do_not_count_as_feature_budget_escape_writes'`
   - `2 passed, 34 deselected`
3. `uv run pytest tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sandbox_mcp_server/test_build123d_replay_helpers.py tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py tests/unit/sandbox_mcp_server/test_action_contracts.py -q`
   - `70 passed`

这一步的意义在于：

1. 当前系统剩余瓶颈继续从“Build123d API 幻觉”后移到“runtime policy 是否会过早离开 bounded local-finish lane”
2. 修复保持在控制流语义层，没有往 `agent_loop` 再塞新的单 case 特判
3. 修完以后，新一轮真实 practice run 才更适合继续判断 read-model / validator / repair packet 的真实瓶颈

### 124. `023500` 真实证明 `feature_budget_risk` 修复已经进入 live policy：organic case 的 round 8 不再被赶回 `execute_build123d`

本轮真实 run：

1. `practice_runs/20260417_023500/organic_clamshell_storage_midhigh_v00_0605b8a8`
2. `practice_runs/20260417_023500/topology_local_finish_focus_v00_95ab3188`

最关键的新证据在 organic case：

1. `practice_runs/20260417_023500/organic_clamshell_storage_midhigh_v00_0605b8a8/prompts/round_08_user_prompt.txt`
   - `policy_id = "continue_local_finish_after_semantic_refresh"`
2. 对比前一轮 `020500/topology_local_finish_focus`
   - round 8 是 `code_first_after_feature_budget_risk`
3. 说明这次修复已经从单测进入了真实 LLM live lane

同时 `summary.json` 里的 policy 链也更健康：

1. `apply_local_finish_after_topology_targeting_from_read_stall`
2. `retry_local_finish_with_existing_topology_refs`
3. `refresh_topology_for_continued_local_finish_after_semantic_refresh`
4. `continue_local_finish_after_semantic_refresh`

这一步的意义在于：

1. `feature_budget_risk` 的修复不是纸面上的
2. runtime 已经不再因为“失败 retry 被误计数”而提前放弃 local-finish lane
3. 后续剩余问题终于可以更干净地归因到 exact-ref 消费、read-model ranking、以及 face-local action contract

### 125. `023500` 还把主导幻觉再次压窄：organic case 只剩 exact-ref contract，topology case 则主要剩 explicit-anchor first-write 长尾

organic case 的新结论：

1. `practice_runs/20260417_023500/organic_clamshell_storage_midhigh_v00_0605b8a8/summary.json`
   - `build123d_hallucination.primary_layer = "read_surface"`
   - `categories = {"local_action_contract_missing_target_refs": 2}`
2. `actions/round_08_apply_cad_action.json`
   - `error_message = "create_sketch must use face_ref from latest query_topology during local_finish"`
3. 这说明 whole-part code 和 policy 主链已经不是主矛盾，新的主要问题是：
   - exact `face_ref` 没有被消费进 local face edit

topology case 的新结论：

1. `practice_runs/20260417_023500/topology_local_finish_focus_v00_95ab3188/summary.json`
   - `build123d_hallucination.primary_layer = "write_surface"`
   - `rule_id = "invalid_build123d_contract.explicit_anchor_manual_cutter_requires_subtract_mode"`
2. 说明当前这条 case 的主导问题仍是 explicit-anchor hole family 在 first write 上的 manual-cutter contract 长尾

因此，这轮 run 把两个剩余方向分得更清楚了：

1. 一条是 `organic_clamshell` 这类 local-finish tail，要继续增强 exact-ref contract / planar host selection
2. 一条是 `explicit_anchor_hole` 这类 first-write family，要继续把 helper-first / subtract-mode contract 稳定前置

### 126. 为了压低 local-finish exact-ref 失误，新加了一个通用 runtime skill，而不是继续补 case-specific 规则

实现：

1. `src/sub_agent_runtime/skill_pack.py`
   - 新增 `local_finish_exact_face_ref_contract`
2. `tests/unit/sub_agent_runtime/test_skill_pack.py`
   - 新增 `test_build_runtime_skill_pack_adds_local_finish_exact_face_ref_contract_guidance`

这条 skill 的目标非常单一：

1. 当系统已经进入 `named_face_local_edit / local_finish` 语义面
2. 并且 query_topology 已经成为正确读路径时
3. 明确告诉模型：
   - `face_ref='face:...'`
   - `create_sketch(face_ref=...)`
   - 不要再回到 `plane='XY'` 或 broad face alias

回归：

1. `uv run pytest tests/unit/sub_agent_runtime/test_skill_pack.py -q`
   - `29 passed`

这一步的意义在于：

1. 不去把 `organic_clamshell` 特判写死
2. 而是把 “local-finish 是 exact-ref contract，不是 broad plane sketch” 升级成一个跨 case 的 runtime guidance
3. 随后已经立即启动新的真实 run `practice_runs/20260417_024200`，专门验证这条 skill 是否真的能改善 organic case 的 round 8 行为

### 127. `024200` 真实证明：`query_topology -> create_sketch(face_ref=...)` 已经在 organic case 中真实打通

目录：

1. `practice_runs/20260417_024200/organic_clamshell_storage_midhigh_v00_0605b8a8`

关键证据：

1. `queries/round_06_query_topology.json`
   - `matched_ref_ids[0] = "face:1:F_f9b9ba94399c"`
2. `actions/round_07_apply_cad_action.json`
   - `failure_kind = "apply_cad_action_contract_failure"`
   - `error_message = "cut_extrude requires an active profile sketch with non-empty profile_refs"`
   - `suggestions` 已明确提示：
   - `create_sketch(face_ref=...)`
   - `add_circle/add_rectangle/add_polygon`
3. `plans/round_08_response.json`
   - 模型下一轮真实选择：
   - `apply_cad_action`
   - `action_type = "create_sketch"`
   - `face_ref = "face:1:F_f9b9ba94399c"`
4. `actions/round_08_apply_cad_action.json`
   - `success = true`
5. `practice_analysis.md`
   - `topology_targeting_observed = True`
   - `local_targeting_action_count = 1`
   - `local_targeting_examples[0].file = "actions/round_08_apply_cad_action.json"`

这轮的意义在于：

1. 真实 LLM 路径里，`query_topology` 已经能给出后续局部 sketch 需要消费的 exact `face_ref`
2. `face_ref` 已经不是“摘要里存在”，而是已经进入成功的 `create_sketch(face_ref=...)`
3. 当前剩余主矛盾继续后移到：
   - local sketch profile bootstrap
   - `cut_extrude` 的 active profile contract

### 128. 基于 `023500 / 024200` 的共同证据，`create_sketch` 现在开始吃 bootstrap profile 参数，而不是只开一个空 sketch

根因：

1. `023500` 的 organic case 已经暴露出一个共性问题：
   - 模型在 `create_sketch` 里会带 `width/height` 这类 profile hint
   - 但 runtime 以前只创建 sketch context，没有把这些 hint 变成真正的 closed profile
2. `024200` 的 round 7 / round 8 又进一步证明：
   - 当前主矛盾已经从 exact `face_ref` 获取
   - 后移到 local sketch 是否形成 `profile_refs`

本轮补强：

1. `src/sandbox_mcp_server/service.py`
   - `CADActionType.CREATE_SKETCH` 现在会在创建 sketch 后：
   - 如果带 `width/height`，自动 bootstrap rectangle profile
   - 如果带 `radius/diameter`，自动 bootstrap circle profile
2. `tests/unit/sandbox_mcp_server/test_action_contracts.py`
   - 新增：
   - `test_create_sketch_bootstraps_rectangle_profile_from_dimensions`
   - `test_create_sketch_bootstraps_circle_profile_from_diameter`

本轮验证：

1. `uv run pytest tests/unit/sandbox_mcp_server/test_action_contracts.py -q`
   - `7 passed`
2. `uv run pytest tests/unit/sandbox_mcp_server/test_action_contracts.py tests/unit/sandbox_mcp_server/test_build123d_replay_helpers.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q`
   - `79 passed, 11 warnings`

这一步的意图不是为了某一个 notch / thumb cut case 写 shortcut，而是把一个更泛化的 local-finish 能力补齐：

1. 模型如果已经知道 host face，并给出了最小 profile hint
2. runtime 就不应该把这条信息静默丢掉
3. 而应该尽量把它编译成真正可消费的 closed profile，继续推进 `cut_extrude / hole / pocket` 一类局部链路

### 129. `030500` 真实证明 host-role targeting 已经进入 read-model，而且 exact-ref 三段式失败链条已经被清楚落盘

目录：

1. `practice_runs/20260417_030500/organic_clamshell_storage_midhigh_v00_0605b8a8`

关键证据：

1. `practice_analysis.md`
   - `topology_targeting_observed = True`
   - `host_role_targeting_observed = True`
   - `candidate_host_roles = ['closure_landing', 'inner_planar_host', 'mating_face']`
   - `local_targeting_action_count = 1`
2. `queries/round_05_query_topology.json`
   - `matched_ref_ids[0] = "face:1:F_a6f816301bdc"`
3. `plans/round_06_response.json`
   - 仍然先尝试 detached `create_sketch(plane='XY', ...)`
4. `plans/round_07_response.json`
   - 又尝试一次 detached `create_sketch(plane='XY', origin=[0, 0, 0])`
5. `actions/round_06_apply_cad_action.json`
6. `actions/round_07_apply_cad_action.json`
   - 都报：
   - `create_sketch must use face_ref from latest query_topology during local_finish`
7. `plans/round_08_response.json`
   - 终于改成 `create_sketch(face_ref='face:1:F_a6f816301bdc', ...)`
8. `actions/round_08_apply_cad_action.json`
   - `success = true`

这轮的意义在于：

1. read-model 已经不只是返回 face ids，而是开始显式返回 host-role 候选
2. 现在的主矛盾不是“没有 topology”或“没有 host-role”
3. 而是 exact-ref contract 已经失败过一次以后，模型仍会多浪费 1 到 2 个 retry

### 130. 新增两条更泛化的修复：`.geometry` API 治理 + failure-driven exact-ref retry skill

这两条改动都直接来自 `030500` 暴露的共享失败模式。

#### A. `.geometry` 进入 API governance

实现：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增 `invalid_build123d_api.topology_geometry_attribute`
   - 新增 repair recipe `build123d_topology_geometry_attribute_contract`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - 新增 `test_preflight_lint_rejects_topology_geometry_attribute_access`

作用：

1. 把 `Edge.geometry` / `Face.geometry` 这类纯库级 hallucination 前移成 preflight contract
2. 明确引导改用 `.geom_type`、`.length`、`.radius`、`.bounding_box()`、`.center()` 这类 typed topology evidence

#### B. failure-driven exact-ref retry skill

实现：

1. `src/sub_agent_runtime/skill_pack.py`
   - 新增 `local_finish_retry_bind_latest_face_ref`
2. `tests/unit/sub_agent_runtime/test_skill_pack.py`
   - 新增 `test_build_runtime_skill_pack_adds_failure_driven_exact_face_ref_retry_guidance`

触发条件：

1. 仅在上一轮 `previous_tool_failure_summary.error`
2. 已明确出现：
   - `create_sketch must use face_ref from latest query_topology during local_finish`

作用：

1. 下一轮直接强调 `create_sketch(face_ref=...)`
2. 不再允许继续浪费在 detached `plane='XY'` sketch 上

回归：

1. `uv run pytest tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'local_finish_exact_face_ref_contract or local_finish_retry_bind_latest_face_ref or topology_geometry_attribute or lowercase_vector_component_attribute or vector_component_indexing or builder_method_reference_assignment'`
   - `6 passed, 131 deselected`

这一步的意义在于：

1. 新增修复面继续保持在“治理契约 + failure-driven guidance”层
2. 没有围绕某个具体 clamshell case 写 prompt patch
3. 已立即再次发起新一轮真实 run `practice_runs/20260417_031500`，继续验证 live LLM 是否会少走 detached sketch 弯路

### 131. `032500` 完整跑完以后，新的主证据已经足够清楚：write-surface governance 覆盖不均匀，且 read-stall policy 会在 whole-part rebuild 语义还没收敛时过早强推 local finish

本轮重点检查了：

1. `practice_runs/20260417_032500/summary.json`
2. `practice_runs/20260417_032500/organic_clamshell_storage_midhigh_v00_0605b8a8/practice_analysis.json`
3. `practice_runs/20260417_032500/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_01_execute_build123d.json`
4. `actions/round_02_execute_build123d.json`
5. `actions/round_03_execute_build123d.json`
6. `trace/events.jsonl`

这轮 run 给出的关键信号：

1. round 1
   - `.z` / `.Z` 被 preflight lint 拦住
   - `build123d_hallucination.categories.invalid_api_contract = 6`
2. round 2
   - `Box.__init__() got an unexpected keyword argument 'radius'`
   - 说明 `Box(radius=...)` 当时还没被前移进 preflight governance
3. round 3
   - whole-part code write 成功
   - 但 geometry 仍然是 `bbox = [78.0, 56.0, 66.0]`
4. run root `summary.json`
   - `forced_policy_chain` 里明确出现：
   - `apply_local_finish_after_topology_targeting_from_read_stall`
5. 最终 stop reason 仍是：
   - `Requirement validation has 6 core blocker(s)`

这说明当时已经同时存在两类共享问题：

1. write-surface 仍有未纳入 registry 的 primitive keyword 幻觉
2. policy 在 DomainKernel 仍持续产出 `whole_part_rebuild` patch 时，还是可能因为 topology read stall 而提前转向 local finish

### 132. 基于 `032500`，补了三条泛化修复：`Box(radius=...)` preflight governance、rebuild 优先级门槛、runtime hallucination 分类

#### A. `Box(radius=...)` 进入 preflight governance

实现：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增 `invalid_build123d_keyword.box_radius_alias`
   - 把 `Box(radius=...)` 与已有 `Box(depth=...)` 放到同一层 keyword contract
2. 同时扩展 `build123d_box_keyword_contract`
   - 明确指向两条合法修复：
   - `Box(length, width, height)` + 后续 fillet
   - `RectangleRounded(...)` + `extrude(...)`
3. 单测：
   - `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - `test_preflight_lint_rejects_unsupported_box_radius_keyword_and_surfaces_contract`

#### B. read-stall 不再无条件压到 local finish

实现：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - 新增 `_local_finish_should_defer_to_actionable_rebuild_patch(...)`
2. 当前规则：
   - 只要最新 actionable kernel patch 明确是 `whole_part_rebuild` / `subtree_rebuild`
   - `_local_finish_should_force_apply_after_topology_targeting(...)`
   - `_local_finish_is_actionable_after_semantic_refresh(...)`
   - 都不能再直接强推 `apply_cad_action`
3. 单测：
   - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - `test_read_stall_with_actionable_rebuild_patch_does_not_force_apply_cad_action`

这一步是把“DomainKernelState 是唯一语义 authority”真正落实到 turn policy 上，而不是继续让 topology read stall 抢走 whole-part rebuild 的优先级。

#### C. runtime hallucination 不再把 constructor keyword 错误都压成 generic exception

实现：

1. `src/sub_agent_runtime/hallucination.py`
   - 新增 `unexpected keyword argument` 识别
   - 归类为 `invalid_constructor_keyword_contract`
2. 单测：
   - `tests/unit/sub_agent_runtime/test_hallucination.py`
   - `test_build_run_hallucination_summary_classifies_unexpected_constructor_keyword_error`

这样像：

1. `TypeError: Box.__init__() got an unexpected keyword argument 'radius'`

这类错误以后不会只落成 `write_runtime_exception`，而会变成更明确的 write-surface constructor contract 问题。

### 133. 本轮回归结果

针对新增改动，先跑了增量回归，再跑了完整相关文件回归：

```bash
uv run pytest tests/unit/sub_agent_runtime/test_hallucination.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q
```

结果：

```text
154 passed in 1.54s
```

这意味着：

1. 新增 `Box(radius=...)` governance 没破坏已有 preflight lint 面
2. rebuild-vs-local-finish 的分流门槛没有打坏 turn policy 既有行为
3. hallucination 指标的分类能力继续增强，但没有影响已有统计面

### 134. `20260417_033500` 暴露的新共性问题：active-builder 幻觉与 multi-part host 污染

新一轮真实 practice：

1. `practice_runs/20260417_033500/organic_clamshell_storage_midhigh_v00_0605b8a8`

最终结论：

1. `summary.json` 显示 `build123d_hallucination.event_count = 31`
2. `primary_layer = write_surface`
3. 主要集中在：
   - `invalid_build123d_contract.active_builder_temporary_primitive_arithmetic`
   - `invalid_build123d_contract.active_builder_temporary_primitive_transform_rebind`
4. 最后一轮虽然成功写出 STEP，但几何仍然不对：
   - bbox 变成 `78 x 62 x 37.2`
   - 说明模型在 builder-first repair 下开始把多物理件塞进一个 shared `BuildPart`，导致 lid/base/hinge 的 host 边界互相污染

这说明当前主矛盾已经进一步收敛为两点：

1. active builder authoritative contract 仍需要更强的生成约束
2. multi-part enclosure 需要更明确的“每个物理件单独闭合建模”约束

### 135. 新增收口：lane 规范化 + multi-part BuildPart 约束升级

实现：

1. `src/sub_agent_runtime/feature_graph.py`
   - 新增 `_canonical_recommended_repair_lane(...)`
   - 当 taxonomy / binding 把 `general_geometry` 这类 whole-part 问题错误标成 `local_finish` 时，强制收口成：
     - `code_repair`（whole-part）
     - `probe_first`（subtree）
   - 保留真正 `named_face_local_edit` 的 `local_finish`
2. `src/sub_agent_runtime/skill_pack.py`
   - `execute_build123d_active_builder_authority_repair`
   - `nested_hollow_section_builder_native_cavity`
   - `enclosure_local_feature_placement_contract`
   - 都补上“多物理件一件一个 closed BuildPart，不要把 lid/base/hinge 全塞进一个 shared active host”这条约束
3. `src/sub_agent_runtime/tool_runtime.py`
   - `nested_hollow_section_same_builder_subtract_contract`
   - `active_builder_temporary_primitive_boolean_contract`
   - repair skeleton 同步补上 one-physical-part-per-builder 的修复骨架
4. `src/sub_agent/prompts/codegen.md`
   - 补上 Build123d 生成约束：如果 requirement 明确要求 lid/base 或 body/cover 等 separate parts，必须分开建模，builder 关闭后再 `Compound(...)`

对应测试：

1. `tests/unit/sub_agent_runtime/test_feature_graph.py`
   - `test_domain_kernel_canonicalizes_general_geometry_local_finish_lane_to_code_repair`
   - `test_domain_kernel_preserves_named_face_local_finish_lane`
2. `tests/unit/sub_agent_runtime/test_skill_pack.py`
   - 补强 active-builder / nested-hollow / enclosure placement guidance 断言

### 136. 本轮新增回归结果

```bash
uv run pytest tests/unit/sub_agent_runtime/test_feature_graph.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q
```

结果：

```text
183 passed in 1.68s
```

### 137. 下一轮真实 practice 已启动

为验证这次收口是否真的降低 write-surface 幻觉，并减轻 whole-part / local-finish lane 混乱，已再次启动：

1. `uv run python scripts/run_custom_prompt_practice.py --seed-ids organic_clamshell_storage_midhigh --variants-per-seed 1 --max-rounds 8 --run-id 20260417_042500`

当前继续监控：

1. `practice_runs/20260417_042500/...`

### 138. `20260417_042500` 新证据：主矛盾已从 Build123d API 幻觉转向 whole-part geometry 与 policy precedence 脱节

关键产物：

1. `practice_runs/20260417_042500/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_02_query_feature_probes.json`
2. `practice_runs/20260417_042500/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_07_query_topology.json`
3. `practice_runs/20260417_042500/organic_clamshell_storage_midhigh_v00_0605b8a8/trace/events.jsonl`
4. `practice_runs/20260417_042500/organic_clamshell_storage_midhigh_v00_0605b8a8/trace/domain_kernel_round_08.json`
5. `practice_runs/20260417_042500/organic_clamshell_storage_midhigh_v00_0605b8a8/summary.json`

结论：

1. 本轮 `build123d_hallucination.event_count = 0`，说明已经不再主要卡在 API/构造器幻觉
2. 但第一次成功写后，`general_geometry` probe 仍把 `solids=3`、`bbox=78x58.5x35.5` 视作 success，导致 runtime 过早进入 topology/local family 路线
3. round 6/7 已经读到了 local-finish 所需的 feature probe 和 topology 证据，但 round 8 仍被 `code_repair_under_budget_after_repeated_validation_blockers` 拉回 `query_kernel_state`
4. 因此新的共享瓶颈已经更明确：
   - read-model 对 whole-part geometry mismatch 过于乐观
   - final-round code repair policy 给读逃逸口过大

### 139. 新增收口：feature probe 明确暴露 whole-part geometry grounding gap 时，优先 whole-part repair，不再默认升级 topology refresh

实现：

1. `src/sandbox_mcp_server/service.py`
   - `general_geometry` probe 新增对 expected bbox / expected part count 的 grounding blocker 检查
2. `src/sandbox_mcp_server/feature_probe_grounding.py`
   - common anchor summary 暴露 `expected_bbox`、`expected_part_count`
3. `src/sub_agent_runtime/agent_loop_v2.py`
   - 新增 `_feature_probe_payload_has_general_geometry_grounding_gap(...)`
   - 新增 `_latest_feature_probes_have_general_geometry_grounding_gap(...)`
   - 当 latest feature probe 已经明确表明 whole-part geometry 仍错时，直接回到 `code_repair_after_feature_probe_detected_whole_part_geometry_gap`
   - 不再默认升级成 `topology_refresh_after_feature_probe_assessment_from_code_write`

对应测试：

1. `tests/unit/sandbox_mcp_server/test_validation_geometry_fallbacks.py`
   - `test_query_feature_probes_general_geometry_flags_part_count_and_bbox_mismatch`
2. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - `test_feature_probe_topology_refresh_does_not_preempt_code_repair_when_general_geometry_probe_has_grounding_blockers`

### 140. 新增收口：repeated blocker 的最后一轮只允许写，不再把最后一轮浪费在 `query_kernel_state`

实现：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - `code_repair_under_budget_after_repeated_validation_blockers`
   - `allowed_tool_names` 收紧为仅 `execute_build123d`
   - `preferred_tool_names` 收紧为仅 `execute_build123d`
2. 保留 `code_repair_last_round_after_validation_evidence_gap` 的旧语义，不把 evidence-gap 分支误收紧

对应测试：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - `test_last_round_code_repair_after_repeated_validation_blockers_disallows_kernel_refresh`

### 141. 当前最新回归结果

```bash
uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q
uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_feature_graph.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sandbox_mcp_server/test_validation_geometry_fallbacks.py -q
```

结果：

```text
39 passed in 1.40s
222 passed in 1.81s
```

### 142. 新一轮真实 practice 已启动

为验证上述 generalized fixes 是否真的改变真实 run 行为，已再次启动：

1. `uv run python scripts/run_custom_prompt_practice.py --seed-ids organic_clamshell_storage_midhigh --variants-per-seed 1 --max-rounds 8 --run-id 20260417_054500`

当前继续监控：

1. `practice_runs/20260417_054500/...`

### 143. `20260417_054500` 已完成：最后一轮 write-only policy 生效，但 write-surface 幻觉仍偏高

关键结果：

1. `practice_runs/20260417_054500/summary.json`
   - `hallucination_event_count = 7`
   - `failure_cluster = code_path_family_gap`
   - `validation_complete = false`
2. `practice_runs/20260417_054500/.../trace/events.jsonl`
   - round 8 policy 为 `code_repair_under_budget_after_repeated_validation_blockers`
   - `allowed_tool_names = ["execute_build123d"]`
   - 说明最后一轮已经不再浪费到 `query_kernel_state`
3. 但这一轮仍暴露出新的 write-surface 问题：
   - round 1/2: `SlotOverall` / `Rot(...)` / nested subtractive `BuildPart`
   - round 8: `mag_Z` vs `mag_z` 这类本地变量大小写漂移，最终在容器里以 `NameError` 暴露

### 144. 新增 generalized fix：本地变量大小写漂移前移到 preflight lint

实现：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增 `_find_case_drift_local_symbol_hits(...)`
   - 新增 `_collect_module_local_binding_names(...)`
   - 对 `mag_Z` vs `mag_z` 这类“本地已绑定符号的大小写漂移”直接产出
     - `invalid_build123d_identifier.case_drift_local_symbol`
   - 避免继续等到容器运行后才以 runtime `NameError` 形式暴露

对应测试：

1. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - `test_preflight_lint_rejects_case_drift_local_symbol_before_container_runtime`

### 145. 新增 generalized fix：runtime skill 截断改成尊重 context priority，不再机械取前 6 条

问题根因：

1. `20260417_054500` 的 round 2 真实产物表明
   - `nested_hollow_section_builder_native_cavity` 已经存在于 skill registry
   - 但在 `V2ContextManager._prepare_runtime_skills_payload()` 中被 6 条上限直接截掉

实现：

1. `src/sub_agent_runtime/skill_pack.py`
   - 给 skill 注入 `context_priority`
   - 把 `execute_build123d_failure_lint_contract`
   - `execute_build123d_api_lint_repair_first`
   - `execute_build123d_failure_recipe_focus`
   - `execute_build123d_active_builder_authority_repair`
   - `nested_hollow_section_builder_native_cavity`
   - `enclosure_local_feature_placement_contract`
   - `local_finish_exact_face_ref_contract`
     这些 canonical hot-path skill 提升到更靠前的位置
2. `src/sub_agent_runtime/context_manager.py`
   - `_prepare_runtime_skills_payload()` 改成按 `context_priority` 排序后再截断
   - 不再机械 `runtime_skills[:6]`

对应测试：

1. `tests/unit/sub_agent_runtime/test_context_manager.py`
   - `test_prepare_runtime_skills_payload_keeps_high_priority_enclosure_repairs_when_truncated`

### 146. 收紧 enclosure / local-finish skill trigger，避免误伤普通 base / recess whole-part case

问题：

1. 提升 enclosure skill 优先级后，回归暴露两个 trigger 本身过宽：
   - 裸 `base` 会误触 enclosure family
   - 一般性的 top-face recess / pattern case 会误触 `local_finish_exact_face_ref_contract`

实现：

1. `src/sub_agent_runtime/skill_pack.py`
   - 新增 `_requirement_mentions_enclosure_host(...)`
   - enclosure skill 改成依赖更明确的 enclosure/lid/base/shell/clamshell 组合语义
   - `_requirement_prefers_named_face_local_feature_sequence(...)` 去掉过宽的 `recess` 触发

结果：

1. hot-path suite 重新通过
   - `222 passed in 1.50s`

### 147. `20260417_061500` 已完成：早期 write-surface 幻觉继续下降

关键结果：

1. `practice_runs/20260417_061500/summary.json`
   - `hallucination_event_count = 5`
   - 相比 `20260417_054500` 的 `7` 继续下降
   - `first_write_event_count = 1`
   - 相比 `20260417_054500` 的 `4` 明显下降
2. `forced_policy_chain`
   - `code_first_build`
   - `feature_probe_assessment_before_actionable_kernel_patch_repair`
   - `code_repair_after_feature_probe_detected_whole_part_geometry_gap`
   - `semantic_refresh_after_validation_evidence_gap_from_code_write`
   - `code_repair_after_first_concrete_code_failure`
   - `probe_first_after_repeated_code_failure`
3. 当前主要残留瓶颈已更集中在 enclosure whole-part write 细节：
   - `invalid_build123d_keyword.cylinder_axis`
   - `invalid_build123d_contract.active_builder_part_mutation`
   - `invalid_build123d_contract.active_builder_temporary_primitive_arithmetic`
   - `invalid_build123d_keyword.offset_opening_singular`

### 148. 新一轮真实 practice 已启动

为了验证刚落地的 case-drift lint 与 context-priority skill truncation fix 是否继续改变真实行为，再次启动：

1. `uv run python scripts/run_custom_prompt_practice.py --seed-ids organic_clamshell_storage_midhigh --variants-per-seed 1 --max-rounds 8 --run-id 20260417_064500`

当前继续监控：

1. `practice_runs/20260417_064500/...`

### 149. `20260417_072500` 暴露出的最新主矛盾：multi-part enclosure 已不再主要卡在 API 幻觉，而是卡在 assembled pose / overall bbox 语义

关键证据：

1. `practice_runs/20260417_072500/organic_clamshell_storage_midhigh_v00_0605b8a8/evaluation/geometry_info.json`
   - `difference_notes = ["STEP geometric signatures are closely aligned"]`
   - `passed = true`
   - `scores.final_score = 1.0`
2. `practice_runs/20260417_072500/organic_clamshell_storage_midhigh_v00_0605b8a8/evaluation/practice_preview.json`
   - `generated_stats.solids = 2`
   - `generated_stats.faces = 56`
   - `generated_stats.edges = 140`
   - `generated_stats.bbox = [78.0, 56.0, 70.0]`
3. `practice_runs/20260417_072500/organic_clamshell_storage_midhigh_v00_0605b8a8/queries/round_08_validate_requirement_post_write.json`
   - `feature_target_face_edit: pass`
   - 但仍失败于：
     - `feature_target_face_subtractive_merge`
     - `feature_notch_or_profile_cut`
     - `create_a_two_part_rounded_clamshell_storage_enclosure_with_overall_dimensions_78mm_x_56mm_x_32mm`
   - 最直接的几何证据是：
     - `requested_dimensions=[78.0, 56.0, 32.0], realized_bbox=[78.0, 56.0, 70.0]`

判断：

1. 这轮最值得记录的不是“又有一个 case 没过”。
2. 真正重要的是：
   - runtime 已经能稳定写出两个 solids
   - 也能保留相当多的局部 feature
   - 但模型仍会把 lid/base 解释成“可以为了展示而分开摆放”的对象
3. 所以当前的 shared bottleneck 已经从基础 API 契约，前移到 multi-part assembled pose / bbox 语义。

### 150. 找到更具体的根因：assembled-pose guidance 早就存在，但在真实 request 中被截断掉了

关键证据：

1. `src/sub_agent_runtime/skill_pack.py`
   - 早已有：
     - multi-part 要保持 `real assembly/closed coordinate frame`
     - 有 overall bbox 时不要把 lid/base 摆成 exploded layout
2. 但 `practice_runs/20260417_072500/organic_clamshell_storage_midhigh_v00_0605b8a8/prompts/round_08_request.json`
   - `execute_build123d_minimal_script_hygiene` 只保留了前 6 条 guidance
   - 与 assembled pose 直接相关的那几条没有进入最终 request
3. 同一轮模型返回则明确写了：
   - `practice_runs/20260417_072500/organic_clamshell_storage_midhigh_v00_0605b8a8/plans/round_08_response.json`
   - `practice_runs/20260417_072500/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_08_execute_build123d.json`
   - 包含：
     - `# Position lid above base for visualization`
     - `lid_moved = lid.part.moved(Pos(0, 0, base_h + 0.5))`

这说明：

1. 问题不是知识缺失。
2. 更准确地说，是关键知识还没有以最高优先级、最不容易被截断的形式进入 prompt。

### 151. 已落地 generalized 收口：multi-part assembled pose / overall bbox 约束升级成独立高优先级 skill，并抬进 system prompt

代码：

1. `src/sub_agent_runtime/skill_pack.py`
   - 新增 `multi_part_assembled_pose_bbox_contract`
   - 触发条件基于 enclosure/lid-base + overall dimensions / bounding box 语义
2. `src/sub_agent_runtime/context_manager.py`
   - system prompt 新增：
     - separate parts + overall dimensions 时必须保持 shared assembled coordinate frame

测试：

1. `tests/unit/sub_agent_runtime/test_skill_pack.py`
   - `test_build_runtime_skill_pack_adds_multi_part_assembled_pose_bbox_contract`
2. `tests/unit/sub_agent_runtime/test_context_manager.py`
   - truncation 相关断言已更新，确认新 skill 能进入最终 payload

验证：

```bash
uv run pytest tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sandbox_mcp_server/test_validation_geometry_fallbacks.py -q
```

结果：

```text
227 passed, 11 warnings in 6.16s
```

意义：

1. 这不是围绕某个固定 benchmark 的硬补丁。
2. 这是把“多零件装配姿态必须服从整体包络”的知识，从长 guidance 列表里抽出来，升级成可维护的 canonical runtime asset。

### 152. `073500` 证明 assembled-pose 修复已经真实改变几何失败模式，但后半段被 Docker 基础设施故障打断

关键证据：

1. `practice_runs/20260417_073500/organic_clamshell_storage_midhigh_v00_0605b8a8/prompts/round_01_request.json`
   - 已带入 `multi_part_assembled_pose_bbox_contract`
2. `practice_runs/20260417_073500/organic_clamshell_storage_midhigh_v00_0605b8a8/actions/round_04_execute_build123d.json`
   - `solids = 3`
   - `bbox = [78.0, 69.0, 27.0]`
3. 相比 `072500` 的 `bbox = [78.0, 56.0, 70.0]`
   - 可以确认“lid 被抬高导致 Z 向包络失真”的问题已经不再是主导矛盾
4. 但同一 run 后半段出现：
   - `Container wait failed`
   - `Docker API error`
   - `Docker Desktop is unable to start`

结论：

1. assembled-pose / overall-bbox 高优先级 skill 已经在真实 LLM run 里起作用。
2. 当前新的阻塞，一部分是 detached subtractive builder family，另一部分是基础设施稳定性，而不是旧的 exploded lid 布局。

### 153. 已收紧 hallucination 口径：Docker / container 层故障不再记入 Build123d 幻觉指标

改动：

1. `src/sub_agent_runtime/hallucination.py`
   - 新增基础设施错误过滤
   - `Docker API error` / `Container wait failed` / `Docker Desktop is unable to start` 不再记入 `build123d_hallucination`
2. `tests/unit/sub_agent_runtime/test_hallucination.py`
   - 新增 `test_build_run_hallucination_summary_ignores_infrastructure_write_errors`

验证：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_hallucination.py tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_skill_pack.py -q
```

```text
53 passed in 1.29s
```

意义：

1. `build123d_hallucination` 以后更准确衡量 Build123d 相关幻觉，而不是所有 write 失败的总和。
2. practice / benchmark 报告里的 write_surface 统计不会再被 Docker 守护进程异常污染。

### 154. general-geometry gap 不再一刀切压回 whole-part rebuild，新增 hybrid topology refresh 分流

问题：

1. 之前只要 `query_feature_probes` 里出现 `general_geometry` grounding gap
2. runtime 就会把后续 turn 固定到 `code_repair_after_feature_probe_detected_whole_part_geometry_gap`
3. 这会让“整体包络已中度接近目标，但 probe 已经明确要求 topology host selection”的 run 也失去 `query_topology` 机会

实现：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - 新增 `_latest_feature_probes_allow_topology_refresh_despite_general_geometry_gap`
   - 新增 `_feature_probe_payload_allows_hybrid_topology_refresh`
   - 新增 `_feature_probe_payload_has_topology_refresh_signal`
   - 新增 `_latest_write_geometry_is_close_enough_for_topology_refresh`
2. moderate geometry gap + 明确 topology host selection 信号
   - 允许先走一次 `query_topology`
3. severe envelope drift
   - 仍保留 whole-part rebuild 优先

验证：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -k 'feature_probe_topology_refresh_can_preempt_code_repair_when_geometry_gap_is_moderate_and_topology_host_selection_is_needed or feature_probe_general_geometry_gap_stays_on_code_repair_when_bbox_is_far_from_expected_envelope' -q
```

```text
2 passed, 38 deselected in 1.36s
```

意义：

1. 这条改动直接服务于 complex custom prompt 的 read-model 收口。
2. runtime 现在更愿意在“整体几何基本成形”后读取 topology host 证据，而不是盲目重写整段代码。

### 155. `230500` 与 `231500` 进一步把问题从“有没有读链路”收窄到“精确 ref 有没有被稳定消费”

新增真实对照：

1. `practice_runs/20260416_230500/topology_local_finish_focus_v00_95ab3188/`
2. `practice_runs/20260416_231500/topology_local_finish_focus_v00_95ab3188/`

关键结论：

1. `230500` 已真实走过：
   - `execute_build123d`
   - `query_feature_probes`
   - `query_topology`
   - `apply_cad_action`
   - `query_kernel_state`
2. `query_topology` 已经返回 step-local 的精确 `face:<step>:<entity_id>` refs，不再只是 `"top"`、`"bottom"` 这类宽泛标签。
3. `230500/actions/round_04_apply_cad_action.json` 仍出现 `face='bottom'`，但同一条 run 的 `round_05_apply_cad_action.json` 已经成功消费了一个真实 `face_ref`。
4. `231500/actions/round_08_apply_cad_action.json` 则进一步退化成 `candidate:mating_faces`，并在 `trace/failure_bundle.json` 中被明确判成 malformed `face_ref`。

意义：

1. 当前读链路已经真实工作。
2. 当前主瓶颈不再是“读不到 topology”。
3. 当前更深一层的共享问题，是 topology evidence 生成以后，write lane 还没有稳定强制消费精确 ref；能力已经存在，但还不够稳。

### 156. 修复 turn-policy：与 local finish family 无关的 subtree rebuild patch 不再抢走 `apply_cad_action`

根因：

1. `src/sub_agent_runtime/agent_loop_v2.py` 之前会把任意 `whole_part_rebuild / subtree_rebuild` actionable patch 都视为 local finish 的抢占信号。
2. 这会导致：
   - 即使 `query_feature_probes` 已明确建议 `named_face_local_edit -> apply_cad_action`
   - 只要最新还有一个不相关 family 的 rebuild patch
   - local finish lane 仍可能被整体重建 lane 抢走

本轮改动：

1. 新增 `_latest_feature_probe_apply_local_finish_families`
2. 新增 `_actionable_patch_family_ids`
3. `_local_finish_should_defer_to_actionable_rebuild_patch(...)` 改成 family-aware：
   - 若 latest feature probes 推荐 local finish 的 family 与 rebuild patch family 不重叠
   - 不再 defer rebuild，允许继续走 `apply_cad_action`

新增回归测试：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
2. `test_successful_host_write_keeps_local_finish_apply_even_with_unrelated_subtree_rebuild_patch`

验证：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_context_manager.py -q
```

```text
49 passed in 1.30s
```

意义：

1. runtime 已不再把所有 rebuild patch 一概当成更高优先级。
2. 这条修复面向的是 family 级控制流泛化，而不是某个单 case 的 prompt 微调。

### 157. `231500` 的终局失败不是模型逻辑单点崩溃，而是宿主磁盘打满导致 sandbox / evaluation 写入失败

真实报错：

1. `/var/folders/.../build123d-sandbox-*` 创建临时目录时报：
   - `OSError: [Errno 28] No space left on device`
2. `practice_runs/20260416_231500/.../evaluation/benchmark_eval.json` 写入时报：
   - `OSError: [Errno 28] No space left on device`

定位过程：

1. `df -h / /System/Volumes/Data`
   - 当时可用空间只剩约 `323MiB`
2. `docker system df`
   - 未使用镜像与 build cache 仍有大量可回收空间
3. `~/Library/Containers/com.docker.docker/Data/vms/0/data/Docker.raw`
   - 宿主占用已到 `460.43 GiB`

处理：

```bash
docker system prune -af --volumes
```

结果：

1. 回收 `8.24GB`
2. `df -h` 恢复到约 `15GiB` 可用空间

意义：

1. `231500` 的失败里掺入了明确的基础设施因素，不能直接算作 Build123d 能力退化。
2. 但这次排查也反向证明：practice lane 的真实运行已经足够长、足够重，必须把宿主资源状况纳入持续诊断。

### 158. 清理完 Docker 空间后，已重新启动真实 topology-focused practice run

已启动：

```bash
uv run python scripts/run_custom_prompt_practice.py --seed-ids topology_local_finish_focus --variants-per-seed 1 --max-rounds 8 --force-post-convergence-round --run-id 20260416_233900
```

目的：

1. 验证第 156 条 family-aware local finish policy 修复是否能在真实 LLM run 中体现出来。
2. 排除磁盘打满后，继续观察当前 live bottleneck 是否仍主要集中在：
   - exact `face_ref` consumption
   - `explicit_anchor_hole / feature_hole / feature_countersink` 的局部写入闭环

### 159. `build123d-runtime:latest` 镜像已从“磁盘清理后丢失”恢复成可重建状态，Dockerfile 也回正到当前生态窗口

本轮真实阻塞并不只是一句“镜像没了”，而是暴露出 Docker runtime 依赖面已经落后于仓库当前实际运行面：

1. `docker system prune -af --volumes` 清空间后，`build123d-runtime:latest` 被一并回收
2. 直接重建时，旧的 `docker/build123d-runtime.Dockerfile` 仍锁定：
   - `build123d==0.8.0`
   - `ocp-gordon==0.1.18`
3. 在当前 Linux arm64 环境下，这组版本已经不可解析

本轮修复：

1. 对齐本地真实运行面与可用 wheel：
   - `build123d==0.10.0`
   - `ocp-gordon==0.2.0`
   - `cadquery-ocp==7.9.3.1`
   - `cadquery-ocp-proxy==7.9.3.1`
2. 将 Dockerfile 改成“两阶段安装”：
   - 先装 Linux arm64 可解析的 OCP 与基础依赖
   - 再对 `build123d / ocp-gordon / ocpsvg` 走 `--no-deps`
3. 同时把 py311 不可用的 `ipython==9.12.0` 回退到可安装的 `ipython==8.39.0`

验证：

1. `docker build -t build123d-runtime:latest -f docker/build123d-runtime.Dockerfile .`
2. 构建最终成功，镜像重新恢复

意义：

1. 这不是临时补一个缓存文件，而是把 sandbox runtime 的依赖窗口真正拉回到当前 Build123d practice lane 可持续运行的状态。
2. 后续真实 LLM run 不会再因为“清空间之后镜像不可重建”而被基础设施卡住。

### 160. local-finish preflight 新增“unique candidate face set -> exact face_ref” 归一化，解决 `candidate:mating_faces` 这一类 live failure

根因：

1. `tool_runtime._preflight_gate_apply_cad_action(...)` 之前只会处理：
   - `face='bottom'`
   - `face='front'`
   - `face='face:...'`
2. 但不会处理：
   - `face_ref='candidate:mating_faces'`
   - `face_ref='candidate:top_faces'`
3. 结果是：
   - 即使最新 `query_topology` 已经给出唯一 candidate set
   - live run 里仍会把这个 candidate label 原样带进 service
   - 最终在更后面才炸成 `invalid_reference: malformed face_ref`

本轮改动：

1. `src/sub_agent_runtime/tool_runtime.py`
2. local-finish preflight 现在会：
   - 先把 `candidate:<candidate_id>` / label token 规范化
   - 若 candidate set 唯一，则直接解析成 concrete `face_ref`
   - 若 candidate set 含多个面，则在 preflight 立刻给出结构化 contract failure，而不是拖到 service 层再炸

新增测试：

1. `test_apply_cad_action_preflight_normalizes_unique_candidate_face_ref_under_local_finish`
2. `test_apply_cad_action_preflight_rejects_ambiguous_candidate_face_ref_under_local_finish`

验证：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -k 'candidate_face_ref or face_reference or face_alias' -q
```

```text
5 passed
```

以及：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_context_manager.py -q
```

```text
121 passed
```

意义：

1. 这条修复不是 case hardcode，而是把 latest `query_topology` 的 read evidence 更稳定地编译进 write lane。
2. 当前系统已经从“完全不会消费 ref”进一步推进到“能在 preflight 阶段自动接住一类常见的 evidence-to-action 丢失”。 

### 161. `feature_graph` 现在会回收已经失效的 `named_face_local_edit` placeholder，不再把 family-level evidence gap 扩大成历史残影

根因：

1. 之前 `feature_graph.sync_domain_kernel_state(...)` 对 feature node 的收口几乎只看：
   - `is_complete`
   - `blocked_feature_ids`
2. `_sync_feature_instances_and_patches(...)` 虽然会把旧 blocker 清掉，但不会把这类变化继续回写到 feature node。
3. 结果就是：
   - `validate_requirement` 已经变成 `blockers=[]`
   - 但只要仍有 `insufficient_evidence`
   - `feature.named_face_local_edit` 这类 placeholder 仍会继续留在 `unsatisfied_feature_ids`
4. 这不是几何真的还缺这个 family，而是 domain kernel 同步面没有把 family-level validation hint 吃进去。

本轮修复：

1. `src/sub_agent_runtime/feature_graph.py`
2. 新增 `validation family status hint` 提取：
   - 从 `requirement_checks`
   - 从 `clause_interpretations`
   - 提取 `family_binding -> {verified / contradicted / insufficient_evidence}`
3. `_sync_feature_instances_and_patches(...)` 现在会：
   - 在 blocker 清空后回收无效 placeholder instance
   - 对仍有 `family_binding=... insufficient_evidence` 的 family 保持 active
   - 不再让 `named_face_local_edit.primary` 这种仅用于 local-finish admission 的实例长期悬挂
4. 新增 `_reconcile_feature_node_statuses(...)`
   - 把 feature instance 状态和 validation family hint 重新汇总回 feature node
   - 让 `unsatisfied_feature_ids` 反映真实剩余工作，而不是历史残影

新增测试：

1. `tests/unit/sub_agent_runtime/test_feature_graph.py`
   - `test_domain_kernel_resolves_local_finish_placeholder_when_only_explicit_anchor_evidence_gap_remains`
2. 回归：
   - `tests/unit/sub_agent_runtime/test_feature_graph.py`
   - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`

验证：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_feature_graph.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q
```

```text
51 passed in 1.30s
```

意义：

1. 这一步解决的不是某一个 case 的 prompt 文案，而是 domain kernel 对 validator 结果的收口粒度。
2. 后续 policy 不会再因为 `named_face_local_edit` 历史残影而错误判断“还有两个 feature 没做完”。
3. 这为 read-stall / evidence-gap 的后续治理腾出了更干净的控制面。

### 162. `20260417_001300` 真实 rerun 已验证 placeholder 残影被压下去，主矛盾正式收敛到 `explicit_anchor_hole` 证据闭环

关键产物：

1. `practice_runs/20260417_001300/topology_local_finish_focus_v00_95ab3188/trace/domain_kernel_round_08.json`
2. `practice_runs/20260417_001300/topology_local_finish_focus_v00_95ab3188/practice_analysis.json`
3. `practice_runs/20260417_001300/topology_local_finish_focus_v00_95ab3188/summary.json`

关键结果：

1. `domain_kernel_round_08.json`
   - `completed_node_ids = ["feature.named_face_local_edit"]`
   - `unsatisfied_feature_ids = ["feature.explicit_anchor_hole"]`
   - `active_node_ids = ["feature.explicit_anchor_hole"]`
2. 这说明 local-finish placeholder 已经真正退出主未完成集，不再和真实 countersink/hole family 混在一起。
3. `runtime_summary.failure_cluster = "read_stall_gap"`
   - 说明主导问题已不再是旧的 placeholder/status 同步错误
   - 而是 `explicit_anchor_hole` 家族的证据闭环仍不够强
4. `runtime_summary.forced_policy_chain` 已出现：
   - `apply_local_finish_after_topology_targeting_from_read_stall`
   - `refresh_topology_for_continued_local_finish_after_semantic_refresh`
5. 这表示 runtime 已经把后半段决策切进“读证据补闭环”而不是“再被历史语义残影拖回 broad rewrite”

仍然存在的问题：

1. `build123d_hallucination.event_count = 3`
2. 其中 round 1 还是出现了：
   - `Plane(front_face).shift_origin((0, 0, notch_center_z))`
   - 触发 `ValueError: (...) is not located within plane`
3. 这说明 write-side hallucination 还没有完全结束，只是已经从 helper 名称错误进一步下沉到 host-plane / local-frame 契约错误。

因此本轮继续补了下一条治理：

1. `src/sub_agent_runtime/tool_runtime.py`
2. 新增 rule：
   - `invalid_build123d_contract.face_plane_shift_origin_global_coordinate_guess`
3. 新增 recipe：
   - `build123d_face_plane_shift_origin_contract`
4. 新增测试：
   - `test_preflight_lint_rejects_face_plane_shift_origin_global_coordinate_guess`

大回归：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_feature_graph.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q
```

```text
166 passed in 4.92s
```

当前结论：

1. `feature_graph/domain kernel` 残影问题已经实质修复。
2. 当前最值得继续压的不是 `named_face_local_edit` placeholder，而是：
   - `explicit_anchor_hole` 的 count / placement / countersink 证据闭环
   - 以及 face-derived plane 的 local-frame contract hallucination。

### 163. `20260417_001900` 进一步确认 face-plane 幻觉已退出主导面，write-side hallucination 从 `3` 降到 `1`

关键产物：

1. `practice_runs/20260417_001900/topology_local_finish_focus_v00_95ab3188/summary.json`
2. `practice_runs/20260417_001900/topology_local_finish_focus_v00_95ab3188/practice_analysis.json`
3. `practice_runs/20260417_001900/topology_local_finish_focus_v00_95ab3188/actions/round_01_execute_build123d.json`
4. `practice_runs/20260417_001900/topology_local_finish_focus_v00_95ab3188/trace/domain_kernel_round_08.json`

对比 `001300` 的核心变化：

1. `build123d_hallucination.event_count`
   - `001300 = 3`
   - `001900 = 1`
2. `001300` 的三个事件里，仍包含：
   - round 1 face-plane `shift_origin(...)` runtime exception
   - round 2 manual cutter subtract contract
   - round 2 legacy `cut_extrude(...)`
3. `001900` 则只剩：
   - round 1 一个 `fillet(...)` 几何失败

这说明：

1. 新补的 `face_plane_shift_origin_global_coordinate_guess` 治理，已经把上一轮最典型的 host-plane 坐标幻觉从真实 LLM run 中挤掉了。
2. 现在 write-side 的主导早期错误，已经进一步收缩到“几何可行性 / broad fillet selection”这一层，而不是 workplane contract。

更重要的是，`domain kernel` 收口继续保持稳定：

1. `trace/domain_kernel_round_08.json`
   - `completed_node_ids = ["feature.named_face_local_edit"]`
   - `unsatisfied_feature_ids = ["feature.explicit_anchor_hole"]`
2. 也就是说，上一条修复并不是偶然跑对一次，而是在后续 rerun 里仍能稳定把 placeholder family 退出未完成集。

当前剩余主问题：

1. `failure_cluster = "read_stall_gap"`
2. `coverage_confidence = 0.8`
3. `insufficient_evidence = true`
4. 所以现在最该继续压的是：
   - `explicit_anchor_hole` 的 geometry/topology evidence closure
   - 以及首轮 broad fillet 选择导致的 `StdFail_NotDone`

### 164. `explicit_anchor_hole` 新增 count-based grounding，不再把“只有数量约束、没有显式坐标”的孔类需求一概打成 `missing_expected_local_centers`

这轮继续沿着 `20260417_001900` 的真实产物往下追，发现 `query_feature_probes` 明确给出了：

1. `grounding_blockers = ["feature_countersink", "missing_expected_local_centers"]`
2. `expected_local_center_count = 0`
3. `realized_local_center_count = 4`

也就是说，当前系统并不是“看不到孔”，而是：

1. 能看到真实减材中心；
2. 但只会从显式坐标、pattern、pitch-circle 这类文本里提 `expected_local_centers`；
3. 面对 `two mounting holes` 这类只有数量约束、没有显式坐标的自然语言时，read-model 仍然缺一条中间层。

本轮补上的不是 case hardcode，而是一条更通用的数量级 grounding lane：

1. `src/sandbox_mcp_server/service.py`
   - 新增 `_infer_expected_local_feature_count(...)`
   - `query_feature_probes` 的 `explicit_anchor_hole` family 现在会同时输出：
     - `expected_local_centers`
     - `expected_local_center_count`
2. `src/sandbox_mcp_server/feature_probe_grounding.py`
   - `anchor_summary` 支持在无显式坐标时仍保留 `expected_local_center_count`
   - 当 `realized_count > expected_count` 时，grounding blocker 不再是模糊的 `missing_expected_local_centers`，而是更可操作的 `center_count_mismatch`
3. `src/sandbox_mcp_server/service.py`
   - `validate_requirement` 新增 `feature_local_anchor_count_alignment`
4. `src/sandbox_mcp_server/validation_interpretation.py`
   - 把 `feature_local_anchor_count_alignment` 接进 hole family 的 precise grounding / contradiction path

这条改动的直接意义：

1. `two mounting holes` 这类需求，后续不再只能退成 `insufficient_evidence`
2. 当系统实际做出了 `4` 个孔时，validator 可以把它压成 concrete contradiction
3. 这比补某个 benchmark case 的 prompt 文案更泛化，因为真实需求里大量孔/槽/磁铁位约束都只有“数量 + 对称/角点”而没有显式坐标

新增验证：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sandbox_mcp_server/test_validation_geometry_fallbacks.py tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py -q
```

```text
71 passed in 2.46s
```

新增关键测试：

1. `tests/unit/sandbox_mcp_server/test_validation_geometry_fallbacks.py`
   - `test_query_feature_probes_tracks_expected_hole_count_without_explicit_coordinates`
   - `test_validate_requirement_surfaces_count_mismatch_for_holes_without_explicit_coordinates`

### 165. local-finish 新增“preserve existing local centers”显式语义面，避免补 countersink 时重新发明坐标

继续分析 `practice_runs/20260417_082501/topology_local_finish_focus_v00_95ab3188` 后，当前主问题已经非常具体：

1. `round_07_validate_requirement_auto.json` 里，`feature_local_anchor_count_alignment` 一度已经变成 `pass`
2. `realized_centers` 当时是 `[-23, -13]`、`[-23, 13]`
3. 但 `round_08_apply_cad_action.json` 仍然重新提交了
   - `[5.0, -30.0, -9.5]`
   - `[50.0, -5.0, -9.5]`
4. 结果最后把已经对齐好的 2 孔布局重新污染成 4 个中心

这说明问题不再是“没有 face_ref / 没有 evidence”，而是：

1. runtime 已经能把 local-finish 收敛到 exact `face_ref`
2. 但 prompt/context 还没有把“已有稳定局部中心布局必须复用”提升成一等约束

这轮补的不是新的 case rule，而是一条更通用的 local-finish 语义面：

1. `src/sub_agent_runtime/context_manager.py`
   - `local_finish_contract` 新增 `preserve_existing_local_layout`
   - 当 domain kernel 的 active feature instance 已经带有：
     - `expected_local_center_count`
     - `realized_centers`
     - 且两者数量已对齐
   - prompt 会把这组 centers 直接抬升成显式 contract，并追加约束：
     - “reuse that exact center set”
     - 不要为了补 countersink / host-face-local detail 再猜一套新坐标
2. `src/sub_agent_runtime/skill_pack.py`
   - 新增 `local_finish_preserve_existing_local_centers`
   - 把这组 preserved centers 直接写成 runtime skill note
   - 并在 `latest_tool == apply_cad_action` 时给到高优先级，避免在 skill truncation 时被挤掉

这条改动的设计意图：

1. 不去改 public tool contract
2. 也不去给某个 benchmark case 写硬编码 positions
3. 而是把 domain-kernel 已经有的稳定局部布局，显式暴露给 final local-finish turn
4. 让模型在“只剩局部细节”的阶段更像是在补完同一组 feature，而不是重新发明 feature

本地验证：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_skill_pack.py -q
```

```text
44 passed in 1.29s
```

新增关键测试：

1. `tests/unit/sub_agent_runtime/test_context_manager.py`
   - `test_local_finish_contract_surfaces_preserved_local_centers_from_domain_kernel`
2. `tests/unit/sub_agent_runtime/test_skill_pack.py`
   - `test_build_runtime_skill_pack_adds_local_finish_preserved_center_guidance`

下一步：

1. 已经启动新的真实 rerun：
   - `practice_runs/20260417_103501/...`
2. 重点看它在 final local-finish 阶段是否还会重新猜 `positions`
3. 如果这轮仍然回退，再继续补 policy / read-model，而不是回到 case-specific hard rule

### 166. `20260417_103501`：topology local-finish case 已真实收口，最终 turn 不再回到明显错误的世界坐标猜测

这轮真实 rerun 已经给出比单测更强的证据：

1. 目录：
   - `practice_runs/20260417_103501/topology_local_finish_focus_v00_95ab3188/`
2. 关键产物：
   - `plans/round_08_response.json`
   - `queries/round_07_validate_requirement.json`
   - `summary.json`
   - `practice_analysis.json`

和旧失败 run `practice_runs/20260417_082501/...` 相比，最关键的变化是最终 local-finish action：

1. 旧 run 的 `round_08` 还在猜明显错误的位置：
   - `[5.0, -30.0, -9.5]`
   - `[50.0, -5.0, -9.5]`
2. 新 run 的 `plans/round_08_response.json` 已变成 host-face-local 风格的二维中心：
   - `{"x": -23, "y": -13}`
   - `{"x": 23, "y": -13}`
3. 同一轮最终结果已经变成：
   - `converged = true`
   - `validation_complete = true`
   - `unsatisfied_feature_ids = []`

这说明新加的 `preserve_existing_local_layout` / `local_finish_preserve_existing_local_centers` 没有停留在 prompt 文案层，而是已经改变了真实 LLM 在 final local-finish turn 的行为。

还需要注意一个 reporting 问题：

1. 这条 run 虽然已经收口，但 case-level 汇总里仍然出现了 `failure_cluster = "read_stall_gap"`
2. 这更像是 practice reporting 的 stale classification，而不是 runtime 实际失败
3. 这个问题单独进入后续修复面，不和 local-finish 主路径混在一起

### 167. `20260417_104500`：organic clamshell 当前主瓶颈重新被压缩回 shared write-surface contract，而不是 read-model / validator

新的中高难 clamshell practice run：

1. 目录：
   - `practice_runs/20260417_104500/organic_clamshell_storage_midhigh_v00_0605b8a8/`
2. 关键产物：
   - `summary.json`
   - `trace/stop_reason.json`
   - `actions/round_05_execute_build123d.json`
   - `actions/round_06_execute_build123d.json`

这轮 run 的价值在于，它把当前主矛盾重新钉得非常清楚：

1. `stop_reason = max_rounds_reached`
2. `converged = false`
3. `validation_complete = false`
4. 但 `build123d_hallucination.event_count = 10`
5. 且 `primary_layer = write_surface`
6. `categories.invalid_api_contract = 10`

也就是说，这轮失败并不是 validator 判断不清，也不是 query/read 链路没被使用，而是 whole-part rebuild 在 round 5/6 连续命中了共享 Build123d contract：

1. `invalid_build123d_keyword.cylinder_axis`
2. `invalid_build123d_contract.active_builder_part_mutation`
3. `invalid_build123d_contract.active_builder_temporary_primitive_arithmetic`

更重要的是，旧 repair recipe 在这类混合 lint 下存在一个结构性问题：

1. 它容易把模型继续推向 slot/cylinder 的局部修辞
2. 但没有把“先关闭 host builder，再做 detached boolean”提升成更高优先级的 structural contract
3. 结果就是模型修了 `axis=` 这类表面 API 问题后，依然会继续写出不可执行的 active-builder boolean

因此这轮紧接着补了两条 generalized 修复：

1. `src/sub_agent_runtime/tool_runtime.py`
   - mixed lint 同时出现时，structural builder contract 的 repair recipe 不再被 generic `cylinder_axis` transform recipe 抢走
   - `explicit_cylindrical_slot_boolean_safe_recipe` 也被改成更明确的 builder-closure wording
2. `src/sub_agent_runtime/skill_pack.py`
   - `execute_build123d_failure_lint_contract` 新增 mixed lint guidance：
   - 当 feature-level keyword 错误与 active-builder 结构错误同时存在时，先修 builder-authority

本地回归：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_skill_pack.py -q
```

```text
152 passed in 3.09s
```

新增关键测试：

1. `test_preflight_lint_prefers_structural_builder_contract_over_cylinder_axis_fix_when_lints_are_mixed`
2. `test_build_runtime_skill_pack_surfaces_structural_priority_for_mixed_failure_lints`

下一步：

1. 已再次启动真实 rerun：
   - `practice_runs/20260417_114500/...`
2. 重点看 clamshell 是否会从 round 5/6 的连续 write-surface hallucination，转成更稳定的 builder-native rebuild / detached-after-close contract

### 168. success-state reporting 不再保留历史 `read_stall_gap`：`failure_cluster` 对完成态 run 直接清空

在补 `20260417_103501` 的成功证据时，还顺手确认了一个独立的 reporting 问题：

1. `practice_runs/20260417_103501/topology_local_finish_focus_v00_95ab3188/summary.json`
2. `practice_runs/20260417_103501/topology_local_finish_focus_v00_95ab3188/practice_analysis.json`

这条 run 同时满足：

1. `converged = true`
2. `validation_complete = true`

但汇总里仍然带着：

1. `failure_cluster = "read_stall_gap"`

继续追代码后发现，这是 `_infer_runtime_failure_cluster(...)` 的顺序问题：

1. `agent_loop_v2` 在写 summary 时，把 `validation_complete` 与 `failure_cluster` 分开计算
2. 原逻辑会先命中：
   - `inspection_only_rounds >= max(2, len(turns)//2)`
3. 因此即使最终已经完成，历史 read-stall 仍会残留到完成态 summary

本轮补上的修复非常小，但语义很关键：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - `_infer_runtime_failure_cluster(...)` 顶部新增成功态短路
   - 当 `_is_successful_validation(latest_validation)` 为真时，直接返回 `None`
2. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - 新增 `test_infer_runtime_failure_cluster_ignores_read_stall_history_after_successful_validation`

相关回归：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_skill_pack.py -q
```

```text
196 passed in 5.23s
```

这条修复的意义：

1. `failure_cluster` 重新回到“失败原因分类”而不是“历史中途轨迹标签”
2. practice / benchmark 汇总不会再把已经完成的 case 错判成 `read_stall_gap`
3. 后续比较版本时，完成态 case 的 failure 统计会更干净

### 169. `20260417_114500` 暴露了新的 mixed lint 争抢：`nested subtractive BuildPart` 不能再被 `Location helper` 表层修复面抢走

新的 clamshell rerun：

1. `practice_runs/20260417_114500/organic_clamshell_storage_midhigh_v00_0605b8a8/`

这轮有两个重要信息：

1. 它还没吃到最后补上的 nested-subtractive precedence 修复
2. 但它已经把下一层共享瓶颈暴露得很清楚

从 `actions/round_02_execute_build123d.json` 可以看到，这轮 mixed lint 组合是：

1. `invalid_build123d_context.nested_subtractive_buildpart_inside_active_builder`
2. `invalid_build123d_context.sketch_primitive_requires_buildsketch`
3. `invalid_build123d_api.loc_helper_name`

旧逻辑在这类组合下，会把 repair recipe 退成：

1. `build123d_location_helper_contract`

这显然不对，因为：

1. `Loc(...)` 只是表层 helper 问题
2. 真正让代码不可执行的是 nested subtractive builder / host-subtractive contract 本身
3. 只修 `Loc(...)`，下一轮仍然很容易继续写出不可执行的 host-local subtractive pattern

因此本轮继续补了一条 generalized precedence：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 当 `nested_hollow_section` family 与
     - `invalid_build123d_context.nested_subtractive_buildpart_inside_active_builder`
     - 或 `invalid_build123d_api.nested_buildpart_cutter_part_arithmetic`
     同时出现时
   - repair recipe 直接回到 `nested_hollow_section_same_builder_subtract_contract`
   - 而不是再让 `build123d_location_helper_contract` 抢走主导权
2. 同时把 `nested_hollow_section_same_builder_subtract_contract` 的 skeleton 再补强了一步：
   - 如果局部 cut 依赖 `Rectangle / SlotOverall / Circle / Ellipse`
   - 先 `BuildSketch(target_plane)` 建 2D profile
   - 再 extrude / cut against host

新增测试：

1. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - `test_preflight_lint_prefers_nested_hollow_structural_recipe_over_location_helper_when_nested_subtractive_builder_is_present`

完整相关回归：

```bash
TMPDIR=$PWD/.tmp uv run pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q
```

```text
197 passed in 3.16s
```

这条修复的目的仍然不是按某个 clamshell case 写补丁，而是把 mixed lint 下的 repair surface 再往 structural contract 收口一层。

下一步：

1. 已再次启动真实 rerun：
   - `practice_runs/20260417_121500/...`
2. 重点看 round 2 是否仍然退成 `build123d_location_helper_contract`
3. 如果不再退化，再继续看后续 bottleneck 会不会继续上浮到更高层的 host-frame / part-separation / geometry contradiction
