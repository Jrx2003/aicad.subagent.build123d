# 2026-04-20

1. 清理和收纳产物目录，先把空目录和无引用噪声 run 从主视图里移走：
   - 删除空 practice roots：
     - `practice_runs/20260419_143017`
     - `practice_runs/20260419_153808`
     - `practice_runs/20260419_170548`
     - `practice_runs/20260419_195441`
     - `practice_runs/20260419_203500`
     - `practice_runs/20260419_210500`
   - 删除空 test roots：
     - `test_runs/20260413_094501`
     - `test_runs/20260413_094502`
     - `test_runs/20260413_094650`
     - `test_runs/20260413_095100`
     - `test_runs/20260413_102400`
     - `test_runs/20260420_101900`
     - `test_runs/20260420_stage1_probe_docker_check`
   - 归档无根级 `brief_report.md/summary.json` 且未被文档引用的 practice roots 到：
     - `practice_runs/archive/incomplete_unreferenced_20260420/`
   - 归档无根级 `summary.json` 且未被文档引用的 legacy raw test roots 到：
     - `test_runs/archive/raw_legacy_20260420/`
   - 新增收纳索引：
     - `practice_runs/CURATED_INDEX.md`
     - `test_runs/CURATED_INDEX.md`
     - `practice_runs/archive/incomplete_unreferenced_20260420/README.md`
     - `test_runs/archive/raw_legacy_20260420/README.md`

2. 确认昨天 STEP / 渲染缺失的根因是 Docker sandbox 镜像缺失，不是 daemon 挂掉：
   - `docker info` 正常
   - 重建镜像：
     - `build123d-runtime:latest`
   - 真实 probe 成功产出 STEP 和渲染：
     - `test_runs/20260420_stage1_probe_docker_check_rebuilt/summary.json`
   - 该 probe 的关键信号：
     - `step_file = model.step`
     - `render_file = render_view.png`
     - `action_success = true`
     - `render_success = true`

3. 落地 packet truth surface 和第一版 packet observability：
   - 代码：
     - `src/sub_agent_runtime/tool_adapters.py`
     - `src/sub_agent_runtime/agent_loop_v2.py`
     - `src/sub_agent_runtime/skill_pack.py`
     - `src/sub_agent_runtime/feature_graph.py`
     - `src/sub_agent_runtime/tool_runtime.py`
     - `src/sub_agent_runtime/practice_runner.py`
     - `benchmark/run_prompt_benchmark.py`
     - `src/sub_agent_runtime/contracts.py`
   - 行为变化：
     - packet skill text 现在区分 runtime-supported vs descriptive-only
     - benchmark/practice summary 新增：
       - `repair_packet_exposed_count`
       - `repair_packet_supported_count`
       - `repair_packet_compile_success_count`
       - `repair_packet_compile_failure_count`
       - `repair_packet_fallback_count`
       - `repair_packet_fallback_reasons`
       - `execute_build123d_preflight_fail_count`
       - benchmark 额外新增 `mean_turns_per_pass`
   - trace 层新增 packet 事件：
     - `repair_packet_exposed`
     - `repair_packet_supported`
     - `repair_packet_compile_succeeded`
     - `repair_packet_compile_failed`
     - `repair_packet_fallback`
     - `build123d_preflight_failed`

4. 扩展第一批 executable packet family：
   - 新增 deterministic compile 支持：
     - `explicit_anchor_hole_centered_host_frame_array`
     - `explicit_anchor_hole_local_anchor_array`
   - 暂不执行：
     - `explicit_anchor_hole_helper_contract_fallback`
   - 对应测试覆盖：
     - `tests/unit/sub_agent_runtime/test_tool_adapters.py`
     - `tests/unit/sub_agent_runtime/test_tool_runtime_execution.py`

5. 修正文档 drift，明确 Build123d canonical story：
   - 更新：
     - `docs/cad_iteration/TOOL_SURFACE.md`
     - `docs/cad_iteration/ITERATION_PROTOCOL.md`
     - `docs/OBSERVABILITY.md`
     - `CODEX.md`
   - 核心修正：
     - `execute_build123d` 是默认 first write
     - `execute_repair_packet` 是 runtime-supported packet 的首选 narrow repair lane
     - `apply_cad_action` 仍只用于 bounded local finish
     - `family_repair_packet_hit` 降级为兼容口径，不再代表 packet 真执行成功

6. 用真实 benchmark 轨迹定位新 routing 缺口，并立刻修复：
   - 真实 before 证据：
     - `benchmark/runs/20260420_110500/L2_172/summary.json`
   - 该 case 在修复前已经暴露：
     - `repair_packet_exposed_count = 5`
     - `repair_packet_supported_count = 5`
     - `repair_packet_compile_success_count = 0`
     - `repair_packet_fallback_count = 5`
     - `repair_packet_fallback_reasons.turn_policy_disallowed_execute_repair_packet = 5`
   - 根因：
     - `code_repair_after_repeated_concrete_code_failure` 分支没有切到 actionable packet lane
   - 修复：
     - `src/sub_agent_runtime/agent_loop_v2.py`
     - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - 新增测试：
     - repeated concrete code failure + supported packet -> policy 必须优先 `execute_repair_packet`

7. 当前测试状态：
   - focused regression：
     - `tests/unit/benchmark/test_run_prompt_benchmark.py`
     - `tests/unit/sub_agent_runtime/test_practice_runner.py`
     - `tests/unit/sub_agent_runtime/test_tool_runtime_execution.py`
   - broader regression：
     - `tests/unit/sub_agent_runtime/test_tool_adapters.py`
     - `tests/unit/sub_agent_runtime/test_tool_runtime_execution.py`
     - `tests/unit/sub_agent_runtime/test_skill_pack.py`
     - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
     - `tests/unit/sub_agent_runtime/test_feature_graph.py`
     - `tests/unit/benchmark/test_run_prompt_benchmark.py`
     - `tests/unit/sub_agent_runtime/test_practice_runner.py`
   - 最新结果：
     - `172 passed`

8. 进行中的真实 rerun：
   - 修复前 benchmark 证据 run：
     - `benchmark/runs/20260420_110500/`
   - 已确认 `L2_172` 不再是空跑：
     - `benchmark/runs/20260420_110500/L2_172/outputs/model.step`
     - `benchmark/runs/20260420_110500/L2_172/summary.json`
   - 下一步：
     - 等当前旧进程跑完，立即用新 routing 修复重跑 `L2_172 / L2_130 / L2_149`
     - 输出 before/after packet routing 对照

9. 继续收纳 benchmark 目录，避免主视图继续堆满半成品 run：
   - 新增：
     - `benchmark/runs/CURATED_INDEX.md`
     - `benchmark/runs/archive/incomplete_unreferenced_20260420/README.md`
   - 归档无根级 `summary.json` 且未被文档引用的 benchmark roots 到：
     - `benchmark/runs/archive/incomplete_unreferenced_20260420/`
   - 本轮归档的 benchmark roots：
     - `20260413_124628`
     - `20260413_125232`
     - `20260413_132346`
     - `20260413_142830`
     - `20260413_164340`
     - `20260413_165148`
     - `20260414_135310`
     - `20260414_140057`
     - `20260414_143112`
     - `20260414_143113`
     - `20260415_162530`
     - `20260415_162908`
     - `20260415_170500`
     - `20260415_190000`
     - `20260415_201500`
     - `20260420_111500`
     - `20260420_112500`
     - `20260420_150500`
   - 特别说明：
     - `20260420_111500` 和 `20260420_112500` 是我手动中止后的 stale rerun，已移出主视图。
     - `20260420_150500` 是 `openai/gpt-5.2-pro` provider 超时后的 stale rerun，已移出主视图。
     - `20260413_142700`、`20260413_170212`、`20260414_093041`、`20260414_140058`、`20260420_110500` 虽然没有 root summary，但仍被现有文档引用，所以暂时保留在主目录。
     - `benchmark/runs/by_practice/` 不是空 run root，而是 legacy symlink 索引，本轮不删除。

10. 用 `openai/gpt-5.2` 做三条真实 rerun，确认 Docker 正常、STEP/渲染恢复，并把 packet lane 的现状压实到具体产物：
   - `L2_172`：
     - run：`benchmark/runs/20260420_150900/L2_172/`
     - 结果：3 轮收敛，`step_file_exists = true`，`validation_complete = true`
     - 关键产物：
       - `benchmark/runs/20260420_150900/L2_172/outputs/model.step`
       - `benchmark/runs/20260420_150900/L2_172/evaluation/generated_preview_iso.png`
       - `benchmark/runs/20260420_150900/L2_172/summary.json`
     - 结论：
       - Docker / evaluator 链路正常
       - countersink case 现在能靠 code repair 收敛
       - 但本条 run `repair_packet_exposed_count = 0`，说明 explicit-anchor-hole packet 还没有真正进入这条主路径
   - `L2_130`：
     - run：`benchmark/runs/20260420_151300/L2_130/`
     - 结果：`step_file_exists = true` 但最终 `EVAL_FAIL`
     - 关键产物：
       - `benchmark/runs/20260420_151300/L2_130/actions/round_04_execute_repair_packet.json`
       - `benchmark/runs/20260420_151300/L2_130/actions/round_07_execute_repair_packet.json`
       - `benchmark/runs/20260420_151300/L2_130/outputs/model.step`
       - `benchmark/runs/20260420_151300/L2_130/summary.json`
     - 关键指标：
       - `repair_packet_exposed_count = 7`
       - `repair_packet_supported_count = 7`
       - `repair_packet_compile_failure_count = 2`
       - `repair_packet_fallback_count = 3`
       - `repair_packet_fallback_reasons.turn_policy_disallowed_execute_repair_packet = 3`
     - 结论：
       - half-shell packet lane 是活的
       - 当前瓶颈已经收缩为 packet contract / anchor completeness，而不是 packet tool 根本没接上
   - `L2_149`：
     - run：`benchmark/runs/20260420_152100/L2_149/`
     - 结果：2 轮收敛，`step_file_exists = true`，`validation_complete = true`
     - 关键产物：
       - `benchmark/runs/20260420_152100/L2_149/outputs/model.step`
       - `benchmark/runs/20260420_152100/L2_149/evaluation/generated_preview_iso.png`
       - `benchmark/runs/20260420_152100/L2_149/summary.json`
     - 结论：
       - path-sweep 仍然是 code-first 修复即可收敛的 family
       - 本条 run `repair_packet_exposed_count = 0`，符合“暂不强推 packet 化 path_sweep”的预期

11. 经过今天的真实 rerun，现阶段最明确的后续方向：
   - `explicit_anchor_hole`：
     - 继续追 `L2_172` 为什么没有 surfacing actionable packet；这不是 provider 问题，也不是 Docker 问题，而是 family surfacing / packet exposure 条件还不够稳
   - `half_shell / axisymmetric_profile`：
     - 优先修 `half_shell_profile_global_xz_lug_hole_recipe` 的 contract miss，把 `L2_130` 从“packet lane 活着但编译 miss”推到真正 compile success
   - `path_sweep`：
     - 保持 code-first，不急着 packetize；现有 rerun 已证明它能用较小的 preflight-guided code repair 在 2 轮内通过

12. 继续沿着 `L2_130` 的真实失败做 root-cause 调试，并修掉 half-shell packet contract drift：
   - root cause 证据：
     - `benchmark/runs/20260420_151300/L2_130/actions/round_04_execute_repair_packet.json`
   - 直接定位到的问题：
     - packet payload 给的是 `family_id = axisymmetric_profile`
     - recipe contract 只接受 `half_shell_profile / directional_hole`
     - 同时 recipe contract 还错误地要求 `outer_radius / inner_radius / hole_centers_xz` 等 packet parameters
     - 但实际 compiler `_compile_half_shell_profile_repair_packet(...)` 是从 `requirement_text` 解析这些值，而不是从 packet parameter bindings 读取
   - 最小修复：
     - `src/sub_agent_runtime/tool_adapters.py`
       - `half_shell_profile_global_xz_lug_hole_recipe.supported_family_ids` 加入 `axisymmetric_profile`
       - `required_parameters` 改为空，避免在 compiler 前就被错误 contract 卡死
   - TDD：
     - 新增单测：
       - `test_describe_runtime_repair_packet_support_accepts_axisymmetric_half_shell_recipe`
       - `test_compile_runtime_repair_packet_execution_compiles_axisymmetric_half_shell_recipe`
     - 相关回归：
       - `tests/unit/sub_agent_runtime/test_tool_adapters.py`
       - `tests/unit/sub_agent_runtime/test_tool_runtime_execution.py`
       - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
     - 最新结果：
       - `93 passed`

13. 修复后追加真实 `L2_130` rerun，看 live behavior 是否变化：
   - run：`benchmark/runs/20260420_153000/L2_130/`
   - 结果：
     - 2 轮收敛到 `validation_complete = true`
     - `step_file_exists = true`
     - `execute_build123d_preflight_fail_count = 1`
     - `repair_packet_exposed_count = 0`
     - evaluator 仍是 `geometry_mismatch`
   - 关键产物：
     - `benchmark/runs/20260420_153000/L2_130/outputs/model.step`
     - `benchmark/runs/20260420_153000/L2_130/evaluation/generated_preview_iso.png`
     - `benchmark/runs/20260420_153000/L2_130/summary.json`
     - `benchmark/runs/20260420_153000/L2_130/evaluation/benchmark_eval_summary.txt`
   - 对照结论：
     - `20260420_151300` 证明 packet lane 之前是真活的，但被 contract miss 和 fallback 拖住
     - `20260420_153000` 说明修复后，至少不会再因为同一类 contract drift 阻塞 live run；这次模型直接在 code-first repair 里收敛，没有再触发 packet lane
     - 当前 `L2_130` 的剩余问题已经从 “packet compile miss” 收缩成 “geometry mismatch / evaluator fail”，而不是 runtime contract 直接错配
     - evaluator 摘要表明：
       - bbox 几乎完全对齐
       - 但 `volume deviation 0.332`、`face count relative deviation 0.857`、`edge count relative deviation 0.762` 仍偏大
       - 下一步应优先对半壳剖面与孔/内腔布置的拓扑复杂度做对照，而不是继续处理 packet contract

14. 继续把 packet-first surface 做模块化，而不是把逻辑继续堆进大文件：
   - 新增模块：
     - `src/sub_agent_runtime/repair_packet_support.py`
     - `src/sub_agent_runtime/skill_runtime_guidance.py`
   - 作用：
     - `repair_packet_support.py`
       - 收拢 runtime packet support matrix / recipe contract / packet preference 选择逻辑
       - 让 `agent_loop_v2.py` 不再从 `tool_adapters.py` 间接读 packet 能力
     - `skill_runtime_guidance.py`
       - 收拢 repair packet / failure recipe / lint failure 对应的 runtime skill 生成逻辑
       - 让 `skill_pack.py` 继续专注 requirement heuristics 和主 skill pack 编排
   - 这次没有盲拆 `tool_runtime.py` 的 preflight lint 大段逻辑；那块仍然是下一阶段更大的专项，单独拆才有意义
   - 当前行数变化：
     - `src/sub_agent_runtime/skill_pack.py`: `2905 -> 2504`
     - `src/sub_agent_runtime/tool_adapters.py`: `1072 -> 942`

15. 不再把 repair packet 截成单个 latest stub，而是保留少量高优先 packet：
   - 代码：
     - `src/sub_agent_runtime/feature_graph.py`
   - 行为变化：
     - `DomainKernelState` 现在最多保留 4 个高优先 active repair packets，而不是 `[:1]`
     - `build_domain_kernel_digest(...)` 继续只给一个 `latest_repair_packet_*` 视角，但这个 latest 现在来自排序后的最佳 packet，而不是字典插入副作用
     - `to_query_payload(include_bindings=True)` 里的 `repair_packets` 改为展示 active prioritized packets，而不是混着 stale packet 的旧插入序列
   - 新增测试：
     - `tests/unit/sub_agent_runtime/test_feature_graph.py::test_replace_repair_packets_retains_multiple_prioritized_packets`
   - 这条测试最初失败点就是：
     - `repair_packet_count == 1`
   - 修完后通过，说明“只保留一个 packet”的老行为已经被替换掉

16. 本轮相关回归测试：
   - 第一组：
     - `tests/unit/sub_agent_runtime/test_feature_graph.py`
     - `tests/unit/sub_agent_runtime/test_tool_adapters.py`
     - `tests/unit/sub_agent_runtime/test_skill_pack.py`
     - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
     - 结果：`153 passed`
   - 第二组：
     - `tests/unit/sub_agent_runtime/test_tool_runtime_execution.py`
     - `tests/unit/benchmark/test_run_prompt_benchmark.py`
     - `tests/unit/sub_agent_runtime/test_practice_runner.py`
     - 结果：`23 passed`
   - 本轮新增/相关 targeted total：`176 passed`

17. 用真实 benchmark case 验证“多 packet surface”已经进入真实产物，不只是单测结论：
   - run：
     - `benchmark/runs/20260420_155050/L2_130/`
   - 有效产物：
     - `benchmark/runs/20260420_155050/L2_130/outputs/model.step`
     - `benchmark/runs/20260420_155050/L2_130/evaluation/generated_preview_iso.png`
     - `benchmark/runs/20260420_155050/L2_130/actions/round_03_execute_repair_packet.json`
     - `benchmark/runs/20260420_155050/L2_130/trace/domain_kernel_round_03.json`
     - `benchmark/runs/20260420_155050/L2_130/summary.json`
   - 真实证据：
     - `trace/domain_kernel_round_03.json` 里：
       - `repair_packet_count = 4`
       - `latest_repair_packet_family_id = explicit_anchor_hole`
       - `repair_packets` 列表里已经同时保留多个 active packet，而不是只剩一个
     - `actions/round_03_execute_repair_packet.json` 里：
       - runtime 确实选择了 `explicit_anchor_hole_centered_host_frame_array`
       - 这次 compile miss 的原因已经收缩成 `repair_packet_missing_hole_diameter`
   - run-level 结果：
     - `step_file_exists = true`
     - `repair_packet_exposed_count = 6`
     - `repair_packet_supported_count = 6`
     - `repair_packet_compile_failure_count = 1`
     - `repair_packet_fallback_count = 4`
     - `execute_build123d_preflight_fail_count = 3`
     - 最终仍是 `EVAL_FAIL`
   - 当前 live bottleneck：
     - packet surface 已经不再是“只有一个 latest stub”
     - 但模型在后续 round 8 仍把 topology evidence 错写成非法 `apply_cad_action` 参数
     - `trace/stop_reason.json` 明确记录了最后失败是 `invalid_tool_arguments:apply_cad_action`
     - 所以下一层该修的是 local-finish action contract / topology-to-action translation，而不是继续压缩 packet surface

18. 改动后再跑一条真实 probe，确认本地 test_runs 链路没回归成空产物：
   - run：
     - `test_runs/20260420_155324/`
   - 关键产物：
     - `test_runs/20260420_155324/model.step`
     - `test_runs/20260420_155324/render_view.png`
   - 关键信号：
     - `action_success = true`
     - `render_success = true`
     - `validate_success = true`
     - `topology_ref_ids = ["face:4:F_12964442fec6"]`
   - 备注：
     - 脚本尾部仍有 `anyio` shutdown warning，但不影响 STEP / render / validate 实际成功落盘；这属于 probe 脚本退出清理面的独立小问题

19. 修补 `apply_cad_action` 的 model-facing canonical contract，并把这部分边界逻辑从大文件里抽出来：
   - 新增模块：
     - `src/sub_agent_runtime/cad_action_interface.py`
   - 本轮做的事：
     - 抽出 `preflight_gate_apply_cad_action(...)`
     - 新增 `normalize_model_facing_apply_cad_action_arguments(...)`
   - 行为变化：
     - model-facing `apply_cad_action` 现在会先把误放到顶层的 `face_ref / edge_refs / diameter / positions / through_all ...` 折回 `action_params`
     - `params` alias 也会并回 `action_params`
     - 已有的 nested `action_params` 保持优先，不会被顶层漂移字段粗暴覆盖
   - 同步规范化：
     - `src/sub_agent_runtime/tool_runtime.py`
       - tool description / follow-up recommendation 明确改成 canonical `action_type + action_params`
     - `src/sub_agent_runtime/context_manager.py`
       - local-finish contract 改成明确提 `action_params.face_ref / action_params.edge_refs`
     - `src/sub_agent_runtime/skill_pack.py`
       - exact-ref guidance 改成 canonical `action_params.*` 写法
     - `docs/cad_iteration/TOOL_SURFACE.md`
     - `docs/cad_iteration/ITERATION_PROTOCOL.md`
     - `docs/work_logs/2026-04-19.md`
     - `docs/interview_prep/2026-04-15_反馈机制与泛化瓶颈调查.md`
       - 把最容易误导后续 reader 的扁平 `apply_cad_action(face_ref=...)` 表述改成 canonical 写法

20. 针对 `apply_cad_action` canonical contract 的回归：
   - 相关测试：
     - `tests/unit/sub_agent_runtime/test_tool_runtime_execution.py`
       - 新增：flattened top-level -> nested `action_params`
       - 新增：flat + nested merge，不覆盖已有 nested keys
     - `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
     - `tests/unit/sub_agent_runtime/test_skill_pack.py`
     - `tests/unit/sub_agent_runtime/test_context_manager.py`
   - 结果：
     - `241 passed`

21. 修补后重新跑真实 benchmark，确认 `invalid_tool_arguments:apply_cad_action` 不再复现：
   - run：
     - `benchmark/runs/20260420_162900/L2_130/`
   - 关键信号：
     - `status = PASS`
     - `planner_rounds = 4`
     - `step_file_exists = true`
     - `repair_packet_compile_success_count = 1`
     - `repair_packet_fallback_count = 0`
   - 关键产物：
     - `benchmark/runs/20260420_162900/L2_130/summary.json`
     - `benchmark/runs/20260420_162900/L2_130/actions/round_04_execute_repair_packet.json`
     - `benchmark/runs/20260420_162900/L2_130/trace/stop_reason.json`
   - 结论：
     - 上一条 run 里的 `invalid_tool_arguments:apply_cad_action` 已经消失
     - 这条 case 现在直接稳定收敛到 `execute_repair_packet`，并在 round 4 PASS

22. 再补一条真实 local-finish practice，确认 canonical `action_params.*` 在 exact-ref 局部修改链上也成立：
   - run：
     - `practice_runs/20260420_163800/topology_local_finish_focus_v00_95ab3188/`
   - 关键信号：
     - `status = complete`
     - `validation_complete = true`
     - `step_file_exists = true`
     - `exact_ref_consumption_rate = 1.0`
     - `face_ref_action_count = 2`
     - `local_targeting_action_count = 2`
   - 关键证据：
     - `practice_runs/20260420_163800/topology_local_finish_focus_v00_95ab3188/actions/round_07_apply_cad_action.json`
     - `practice_runs/20260420_163800/topology_local_finish_focus_v00_95ab3188/trace/round_digest.md`
     - `practice_runs/20260420_163800/topology_local_finish_focus_v00_95ab3188/practice_analysis.md`
   - 最关键的一条 live 证据：
     - `trace/round_digest.md` / tool timeline 里，round 7 的真实 tool arguments 已经是：
       - `action_type = "hole"`
       - `action_params.face_ref = "face:2:F_26cd36a56463"`
     - `action_params.positions = [[-20, 0], [20, 0]]`
     - `action_params.hole_type = "countersink"`
     - 说明 canonical `action_params.*` 已经真实进入 live local-finish lane，不只是单测修复

23. 再补一条 rollout-family benchmark，确认当前 `L2_172` 的剩余问题已经不是工具契约，而是 geometry/context：
   - run：
     - `benchmark/runs/20260420_165500/L2_172/`
   - 关键产物：
     - `benchmark/runs/20260420_165500/L2_172/summary.json`
     - `benchmark/runs/20260420_165500/L2_172/outputs/model.step`
     - `benchmark/runs/20260420_165500/L2_172/evaluation/generated_preview_iso.png`
     - `benchmark/runs/20260420_165500/L2_172/trace/round_digest.md`
     - `benchmark/runs/20260420_165500/L2_172/actions/round_04_execute_build123d.json`
   - 结果：
     - `status = EVAL_FAIL`
     - `validation_complete = true`
     - `step_file_exists = true`
     - `first_solid_success = true`
     - `repair_packet_exposed_count = 0`
     - `execute_build123d_preflight_fail_count = 0`
   - 关键结论：
     - 这条 case 现在已经不是 packet/tool-surface 卡死，也不是 invalid-tool-arguments
     - 它能在 round 4 产出有效 STEP 和四视图渲染，并且 validator 判 complete
     - 但 evaluator 仍给 `geometry_mismatch`
     - benchmark analysis 里给出的主原因已经收缩到：
       - `centroid offset 0.499 indicates pose difference`
       - `local feature-anchor deviation 0.625 is high`
   - 这说明下一阶段在 `explicit_anchor_hole` / countersink 家族上的重点，不是继续补 `apply_cad_action` 契约，而是：
     - whole-part code lane 对 prompt 坐标系和 pose 的 literalness
     - 或者把这类低方差 countersink array 更早推到 packet / stricter recipe lane

24. 完成第一轮运行时分层收口后的测试边界重画，并把 `tooling.lint` 的主 orchestration 从旧大文件里真正抽离：
   - 新增并启用的 `tooling.lint` 子模块：
     - `src/sub_agent_runtime/tooling/lint/preflight.py`
     - `src/sub_agent_runtime/tooling/lint/recipes.py`
     - `src/sub_agent_runtime/tooling/lint/plane_rules.py`
     - `src/sub_agent_runtime/tooling/lint/builder_rules.py`
   - `tool_runtime.py` / `tooling.executor` 现在只保留兼容入口和薄封装；preflight orchestrator、repair recipe 选择、plane/builder family collectors 已迁出旧中心实现面
   - 按新边界拆掉旧聚合测试文件，改为主题文件：
     - `test_tool_runtime_preflight_lint_*.py`
     - `test_tool_runtime_execution_*.py`
     - `test_semantic_kernel_sync_*.py`
     - `test_context_manager_*.py`
     - `test_skill_pack_*.py`
     - `test_agent_loop_policy_*.py`
   - 验证结果：
     - `uv run pytest tests/unit/sub_agent_runtime -q`
     - `412 passed`
     - `uv run pytest tests/unit/benchmark/test_run_prompt_benchmark.py -q`
     - `5 passed`

25. 用 Docker + Kimi 跑了一条真实 benchmark，确认 provider/model -> runtime -> sandbox 链路正常：
   - run：
     - `benchmark/runs/20260420_222135/L1_20/`
   - 调用：
     - `./benchmark/run_prompt_benchmark.sh --cases L1_20 --reasoning-provider kimi --reasoning-model kimi-k2.5-thinking --batch-actions --max-rounds 2`
   - 运行环境确认：
     - `docker info` 正常
     - `ServerVersion = 29.1.5`
   - 关键结果：
     - `status = PASS`
     - `provider = kimi`
     - `model = kimi-k2.5-thinking`
     - `planner_rounds = 1`
     - `validation_complete = true`
     - `step_file_exists = true`
     - `evaluation_passed = true`
     - `token_usage.total_tokens = 5609`
   - 关键产物：
     - `benchmark/runs/20260420_222135/L1_20/outputs/model.step`
     - `benchmark/runs/20260420_222135/L1_20/summary.json`
     - `benchmark/runs/20260420_222135/L1_20/trace/events.jsonl`
