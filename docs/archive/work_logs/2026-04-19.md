# 2026-04-19

## 4/19 深夜追加：把 `RectangleRounded` 外轮廓误解和 active-builder transform rebind 提升为共享 contract

### 今天又做了什么

1. 继续沿同一个高难 seed `clamshell_enclosure_highwater` 做真实 Kimi practice，不再围 benchmark case 补规则，而是把两个最新暴露出来的共享 write-side contract 提升到 prompt/system/skill 三层：
   - `src/sub_agent/prompts/codegen.md`
     - 新增 `RectangleRounded(width, depth, radius=...)` 外轮廓语义说明
     - 明确如果 requirement 给的是 overall outer width/depth，不能再手动改写成 `width - 2*radius` / `depth - 2*radius`
   - `src/sub_agent_runtime/context_manager.py`
     - system prompt 同步补上同一条 rounded-envelope contract
   - `src/sub_agent_runtime/skill_pack.py`
     - 在 `clamshell_split_axis_and_hinge_contract` 里补上 rounded outer footprint 约束
     - 在 `execute_build123d_failure_lint_contract` 里补上 `active_builder_temporary_primitive_transform_rebind` 的明确 repair lane：
       - active host 内用 `Locations(...)` 直接落位
       - detached solid 只能在 builder 关闭后再做 `Rot(...) * part` / `Pos(...) * Rot(...) * part`
2. 相关红绿测试已补齐并通过：
   - `tests/unit/sub_agent_runtime/test_codegen_prompt_contract.py`
   - `tests/unit/sub_agent_runtime/test_context_manager.py`
   - `tests/unit/sub_agent_runtime/test_skill_pack.py`

### 为什么要补这两条 contract

1. 真实 run `practice_runs/20260419_211649/clamshell_enclosure_highwater_v00_355620b2/` 最关键的退化发生在两处：
   - round 4：
     - `actions/round_04_execute_build123d.json`
     - 命中 `invalid_build123d_contract.active_builder_temporary_primitive_transform_rebind`
   - round 9：
     - `actions/round_09_execute_build123d.json`
     - 整体 bbox 退化为 `66.0 x 58.0 x 71.3`
     - 说明模型把圆角半径错误地当成了要从 outer envelope 里额外扣掉的量，同时 hinge lane 也重新漂移
2. 这两个问题都不是单 case 特例：
   - 前者是 Build123d active-builder contract 的共享幻觉
   - 后者是 rounded-rect outer envelope 的共享语义误解

### 如何证明这次改动有效

1. focused 红绿测试：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_codegen_prompt_contract.py tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_skill_pack.py -q -k 'rectangle_rounded_outer_envelope or system_prompt_surfaces_core_build123d_contracts or transform_rebind_repair_lane_for_failure_lint or adds_clamshell_split_axis_and_hinge_contract'`
   - 结果：`4 passed`
2. 再补一圈相关回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_codegen_prompt_contract.py tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_skill_pack.py -q -k 'semicircle_arc_contract or discourages_nested_buildpart_cutter_arithmetic or centered_clamshell_split_pose_and_boundaries or rectangle_rounded_outer_envelope or system_prompt_surfaces_core_build123d_contracts or transform_rebind_repair_lane_for_failure_lint or adds_clamshell_split_axis_and_hinge_contract or frontloads_clamshell_host_local_cut_contract_on_first_turn'`
   - 结果：`8 passed`
3. 新一轮真实 rerun 已经启动：
   - `practice_runs/20260419_212907/clamshell_enclosure_highwater_v00_355620b2/`
4. 到目前为止最直接的早期证据来自 round 1：
   - `practice_runs/20260419_212907/clamshell_enclosure_highwater_v00_355620b2/actions/round_01_execute_build123d.json`
     - `success=true`
     - `bbox=[72.0, 64.0, 20.0]`
   - `practice_runs/20260419_212907/clamshell_enclosure_highwater_v00_355620b2/queries/round_01_validate_requirement_post_write.json`
     - 当前 blocker 已缩成：
       - `feature_notch_or_profile_cut`
       - `create_a_snap_clamshell_enclosure_with_overall_dimensions_72mm_x_64mm_x_26mm`
     - 旧 run 常见的 `use_a_pin_hinge` 首轮 blocker 这次没有再出现
5. 这说明新增 contract 至少已经把同一条 seed 的第一轮主导失败继续往后推：
   - 没有重新跌回 `detached_subtractive_builder_without_host`
   - 没有在首轮把 overall outer footprint 直接写成 `66 x 58`

### 后续方向

1. 继续监控 `practice_runs/20260419_212907/` 的后续 round：
   - 看新规则能否把 round 4 的 transform-rebind 命中压掉
   - 看是否还能避免 `211649` round 9 那种 rounded-envelope 退化
2. 如果这轮 live rerun 继续保持：
   - 首写 `success=true`
   - no hinge blocker in early validation
   - no `66 x 58` outer-envelope collapse
   那下一层最值得继续收口的共享问题就会更明确地收缩到：
   - notch/profile cut 的几何表达与 validator grounding
   - wall-thickness / shell evidence 读模型
   - magnet slot clause 的 geometry-grounded interpretation

## 4/19 午夜追加：`212907` live rerun 继续证明早期幻觉被压缩，multi-part validator 改成 exact-count

### 今天又做了什么

1. 持续监控新 run：
   - `practice_runs/20260419_212907/clamshell_enclosure_highwater_v00_355620b2/`
2. 在 runtime 侧确认了一条新的正向趋势：
   - round 1 直接成功
   - round 3 再次 whole-part rewrite 也成功
   - 旧 run `211649` round 4 命中的 `active_builder_temporary_primitive_transform_rebind` 这次没有复现
3. 同时顺手修了一条 validator grounding 缺口：
   - `src/sandbox_mcp_server/validation_interpretation.py`
   - 以前对于 `two-part` / `lid and base` 一类 clause，只要 `solids >= 2` 就直接 `VERIFIED`
   - 现在改成：
     - 优先读取 `expected_part_count`
     - 对 `two-part` / `lid and base` 默认按 exact `2` 处理
     - `solids != expected_part_count` 时直接 `CONTRADICTED`

### 如何证明有效

1. `212907` round 1 的真实证据：
   - `practice_runs/20260419_212907/clamshell_enclosure_highwater_v00_355620b2/actions/round_01_execute_build123d.json`
     - `success=true`
     - `bbox=[72.0, 64.0, 20.0]`
   - `practice_runs/20260419_212907/clamshell_enclosure_highwater_v00_355620b2/queries/round_01_validate_requirement_post_write.json`
     - blocker 只有：
       - `feature_notch_or_profile_cut`
       - `create_a_snap_clamshell_enclosure_with_overall_dimensions_72mm_x_64mm_x_26mm`
     - 说明首写没有重新跌回旧的 builder-contract / hinge-geometry 早期崩点
2. `212907` round 3 / round 6 的真实证据：
   - `practice_runs/20260419_212907/clamshell_enclosure_highwater_v00_355620b2/actions/round_03_execute_build123d.json`
     - `success=true`
     - `bbox=[72.0, 64.0, 19.5]`
   - `practice_runs/20260419_212907/clamshell_enclosure_highwater_v00_355620b2/actions/round_05_execute_build123d.json`
     - 被更窄的 shared lint 前移拦下：
       - `invalid_build123d_keyword.cylinder_axis`
   - `practice_runs/20260419_212907/clamshell_enclosure_highwater_v00_355620b2/actions/round_06_execute_build123d.json`
     - `success=true`
     - `bbox=[72.0, 64.5, 41.0]`
   - 这说明当前主导问题已经继续从“首轮不可执行”收缩到“后续 rebuild 里的 cylinder orientation / detached fragment 控制”
3. `212907` 的最终 summary 也已经落盘：
   - `practice_runs/20260419_212907/summary.json`
   - 对比 `practice_runs/20260419_211649/summary.json`：
     - `hallucination.event_count: 5 -> 1`
     - `hallucination.weighted_score: 5.0 -> 1.0`
     - `events_per_write: 0.8333 -> 0.1667`
     - `first_write_event_count: 4 -> 0`
   - `212907` 唯一剩余 write-side hallucination 只剩：
     - round 5 `invalid_build123d_keyword.cylinder_axis`
4. 这说明本轮共享改动至少已经把同一条高难 seed 的主导 write-side 幻觉，从：
   - detached subtractive builder
   - active-builder transform rebind
   - rounded-envelope collapse
   继续压缩成：
   - 一个更窄的 cylinder helper contract
3. validator 修正的红绿测试：
   - 新增：
     - `tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py::test_two_part_lid_base_clause_rejects_extra_detached_solids`
   - 先失败，后通过：
     - `./.venv/bin/pytest tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py -q -k 'multi_part_clause_verifies_from_solid_count or two_part_lid_base_clause_rejects_extra_detached_solids'`
     - 结果：`2 passed`
   - 再回归整个 suite：
     - `./.venv/bin/pytest tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py -q`
     - 结果：`41 passed`

### 后续方向

1. 继续盯住 `212907` 是否还能在后半程保持：
   - 不复现 `active_builder_temporary_primitive_transform_rebind`
   - 不复现 `66 x 58` outer-envelope collapse
   - 把主导 failure 压在 notch / detached fragment / final bbox closure
2. 下一轮 live rerun 要验证 validator exact-count 修正是否真的改变后半程决策：
   - `two-part lid/base` 不再因为 `solids >= 2` 被误判完成
   - 多余 detached hinge/magnet fragments 会被更早地保持在 blocker surface
3. validator 这次的 exact-count 修正只是第一步，下一步更值得收口的是：
   - `corner magnet slots`
   - `thumb notch`
   - `smooth printable outer form`
   这几类 clause 什么时候应该 `INSUFFICIENT_EVIDENCE`，什么时候才允许直接 `VERIFIED/CONTRADICTED`

## 4/19 夜间追加：clamshell practice lane 从“首轮炸掉”推进到“后段几何偏差”

### 今天做了什么

1. 沿着同一个高难 seed `clamshell_enclosure_highwater` 连续做了两轮真实 Kimi practice，对共享 write-surface 幻觉做了前移治理，而不是补 case rule：
   - `src/sub_agent_runtime/tool_runtime.py`
     - 新增 preflight 规则：`invalid_build123d_contract.circle_make_face_trim_profile`
     - 专门拦截 `Circle(...) + BuildLine + make_face()` 伪半圆/伪圆角缺口写法
   - `src/sub_agent_runtime/context_manager.py`
     - system prompt 前移三条共享规则：
       - 禁止 `Circle + BuildLine + make_face` 伪半圆
       - 禁止 active host 内 `host.part -= cutter.part`
       - 明确 clamshell front/back seam 与 pin hinge axis 语义
   - `src/sub_agent/prompts/codegen.md`
     - 同步补齐同一批共享约束，避免只靠 post-write lint 兜底
2. 相关红绿测试已补齐：
   - `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py::test_preflight_lint_rejects_circle_plus_make_face_trim_pattern_and_surfaces_arc_profile_contract`
   - `tests/unit/sub_agent_runtime/test_context_manager.py::test_build_messages_system_prompt_surfaces_core_build123d_contracts`
   - `tests/unit/sub_agent_runtime/test_codegen_prompt_contract.py`

### 如何证明有效

1. 前一轮真实基线：
   - `practice_runs/20260419_205848/clamshell_enclosure_highwater_v00_355620b2/summary.json`
2. 新一轮真实 rerun：
   - `practice_runs/20260419_210938/clamshell_enclosure_highwater_v00_355620b2/summary.json`
3. 直接可讲的变化：
   - `hallucination.event_count: 5 -> 3`
   - `hallucination.weighted_score: 5.0 -> 3.0`
   - `events_per_write: 0.7143 -> 0.5`
   - first write 从 runtime exception 退化为可验证几何：
     - 旧证据：`practice_runs/20260419_205848/.../actions/round_01_execute_build123d.json`
       - `ValueError: Cannot build face(s): wires not planar`
     - 新证据：`practice_runs/20260419_210938/.../actions/round_01_execute_build123d.json`
       - `success=true`
       - `bbox=[72.0, 70.0, 29.0]`
       - validator 已能直接指出真实 blocker，而不是停在 runtime 崩溃
4. 这说明新增的 arc-profile / host-local-cut governance 至少已经把主导失败从：
   - `round 1 write_runtime_exception`
   - 推进成
   - 后续 repair 回合中的更窄 Build123d contract 幻觉与几何偏差
5. 新 run 中被成功压掉的老问题：
   - `205848` 的 round 1：
     - `wires not planar`
   - `210938` 中不再出现
6. 新 run 中仍然主导的剩余问题也已经明确：
   - `practice_runs/20260419_210938/.../actions/round_03_execute_build123d.json`
     - `invalid_build123d_keyword.center_arc_arc_angle_alias`
   - `practice_runs/20260419_210938/.../actions/round_06_execute_build123d.json`
     - 几何成功，但 `bbox.z=68.0`
   - `practice_runs/20260419_210938/.../trace/tool_timeline.jsonl`
     - 对应 round 6 代码里 hinge pin 仍被写成默认 Z 轴长柱

### 后续方向

1. 已继续启动下一轮真实 rerun：
   - `practice_runs/20260419_211649/clamshell_enclosure_highwater_v00_355620b2/`
2. 这轮才会真正吃到最新补上的 hinge-axis system prompt 约束，验证目标很具体：
   - 首写继续保持 `success=true`
   - 不再把 pin/barrel 留在默认 Z 轴
   - 继续避免退回 `nested_buildpart_cutter_part_arithmetic` / `detached_subtractive_builder_without_host`
3. 如果 `211649` 再次把事件数压低，下一层最值得收口的共享问题就是：
   - centered clamshell split pose 的 Z 区间收敛
   - hinge boundary cylindrical face 的 validator grounding
   - notch / magnet slot 的 evidence coverage，而不是再去补 API 幻觉 hard rule

## 4/19 深夜补充：practice analysis 展示层已和真实 action 链路对齐

### 今天又做了什么

1. 修了 `practice_runner` 的一个分析层错误：
   - `src/sub_agent_runtime/practice_runner.py`
   - 旧逻辑会逐个 action 文件扫描整段 cumulative `action_history`
   - 结果是 round 6/7 这种 sketch continuation 会把历史里的 `create_sketch` 重复计入当前文件
   - 导致 `practice_analysis.md` 把真实链路错误写成 3 次 `create_sketch`
2. 这次改法不是简单“只取最后一个动作”，而是：
   - 当前 round 的 `action_type` 取最新实际动作
   - targeting 上下文允许沿同一条 sketch lane 继承最近一次 concrete `face_ref/edge_refs`
   - 所以 round 6/7 现在可以正确显示为：
     - `add_circle`
     - `cut_extrude`
3. 对应回归已补在：
   - `tests/unit/sub_agent_runtime/test_practice_runner.py::test_summarize_read_model_usage_tracks_latest_action_type_in_cumulative_sketch_lane`

### 如何证明这次改动有效

1. focused 回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_practice_runner.py -q -k 'cumulative_sketch_lane or captures_host_roles_and_local_targeting or marks_stale_and_nonconcrete_refs'`
   - 结果：`3 passed`
2. 真实 artifact 已经重算：
   - `practice_runs/20260419_203038/hinged_pillbox_magnet_medium_v00_e34c3c99/practice_analysis.md`
3. 现在其中 `local_targeting_examples` 已明确变成：
   - round 5: `create_sketch`
   - round 6: `add_circle`
   - round 7: `cut_extrude`
4. 这说明当前 practice lane 的报告已经能更准确地说明：
   - 模型是否真的进入了 bounded local finish
   - exact `face_ref` 是如何在 sketch lane 中被持续消费的

### 后续方向

1. 继续把这条更可信的 artifact 链路接到汇报页里：
   - `report-20260419-build123d-practice-progress/index.html`
   - `report-20260419-build123d-practice-progress/shareable-report.md`
2. 然后继续起更高难 custom practice seed：
   - `clamshell_enclosure_highwater`
   - `organic_clamshell_storage_midhigh`
3. 下一轮真实 run 的重点不再是“报告对不对”，而是看这套 shared policy + read-model 改动是否还能在更难 prompt 下保持：
   - 低 hallucination
   - 少 whole-part fallback
   - clear local-targeting evidence

## 4/19 深夜补充：successful local finish 之后先做 semantic refresh

### 今天又做了什么

1. 新增了一条共享 turn policy，不再让 runtime 在 `apply_cad_action` 已经成功完成一个实体局部修改之后，直接因为短预算掉回 `code_first_after_feature_budget_risk`。
2. 具体改动落在：
   - `src/sub_agent_runtime/agent_loop_v2.py`
   - 新 policy：`semantic_refresh_after_successful_local_finish`
3. 这条 policy 的含义很直接：
   - 如果最新一次成功写入来自 `apply_cad_action`
   - 且当前不在 open-sketch 连续编辑窗口里
   - 且这次写入之后还没有 fresh validation / semantic refresh
   - 那下一轮先强制做一次 `query_feature_probes/query_kernel_state/query_topology` 级别的语义刷新
   - 再决定是否继续 local finish，或者是否真的需要 whole-part rewrite
4. 这不是为了 `hinged_pillbox` 单独加规则，而是为了修一类共享失真：
   - geometry 已经变了
   - 但 semantic state 还停留在上一轮
   - runtime 却已经开始按 stale blocker 计算 feature-chain budget risk

### 如何证明这次改动有效

1. 先写了贴近真实 run 形态的红测：
   - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py::test_successful_local_finish_under_short_budget_prefers_semantic_refresh_before_code_escape`
2. 这条测试复现的正是共享问题：
   - round 1 whole-part host 成功
   - round 2 已完成 `query_feature_probes + query_topology`
   - round 3-5 连续做了 `create_sketch -> add_circle -> cut_extrude`
   - geometry 已经发生变化
   - 但 feature graph 还没刷新
   - 旧逻辑会直接返回：
     - `policy_id = code_first_after_feature_budget_risk`
3. 红测失败后，补上新 policy 分支，再回跑：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q -k 'successful_local_finish_under_short_budget_prefers_semantic_refresh_before_code_escape'`
   - 结果：`1 passed`
4. 再补一圈相关回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q -k 'semantic_refresh_after_successful_local_finish or local_finish or feature_chain_budget_risk or open_sketch_window_under_critical_budget_exits_to_code_first_escape'`
   - 结果：`21 passed`
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_turn_state.py -q`
   - 结果：`1 passed`

### 后续方向

1. 当前正在用真实 Kimi run 验证这条 policy 是否真的改变 live 路径：
   - `practice_runs/20260419_203038/hinged_pillbox_magnet_medium_v00_e34c3c99/`
2. 这条 run 最值得继续盯的是：
   - 如果它先完成一个 front-face local cut
   - 下一轮是否先进入 semantic refresh
   - 而不是再次直接退回 whole-part `execute_build123d`
3. 如果 live 也验证通过，下一层最值得继续压的共享问题会变成：
   - `RectangleRounded` 局部 label-recess radius 失配
   - front-face local feature 做完一个之后，第二个 feature 的 topology/semantic closure
   - `practice_analysis` 对 local action 类型的归类是否需要同步修正


## 4/19 深夜补充：named-face plane-family guidance 提前到共享主面

### 今天又做了什么

1. 没有继续补单 case rule，而是把 `named-face -> plane-family` 这层共享 guidance 往前提了一层。
2. 这次改动分成三部分：
   - `src/sub_agent_runtime/skill_pack.py`
     - 把 `execute_build123d_clamshell_host_local_cut_contract` 的优先级提前到 generic enclosure guidance 前面
     - 把 `code_first_local_finish_tail_contract` 里的模糊表述改成显式映射：
       - `top/bottom -> Plane.XY`
       - `front/back -> Plane.XZ`
       - `left/right -> Plane.YZ`
   - `src/sub_agent/prompts/codegen.md`
     - 同步补上 named host faces 的 plane-family 映射，避免 whole-part first write 只靠后置 repair 才得到这层约束
   - `tests/unit/sub_agent_runtime/test_skill_pack.py`
     - 新增 red/green 测试，约束这两层 shared surface 不再退化
3. 这不是为了某一个 clamshell case 做硬规则，而是为了把 “front/back local edit 不能漂回 `Plane.YZ`” 固化成更前置、更泛化的治理层。

### 如何证明这次改动有效

1. 新增红绿测试已经先失败再转绿：
   - `test_build_runtime_skill_pack_prioritizes_clamshell_host_local_cut_guidance_ahead_of_generic_enclosure_placement`
   - `test_build_runtime_skill_pack_generic_local_finish_tail_maps_named_faces_to_plane_families`
2. 相关 suite 当前结果：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_skill_pack.py -q`
   - 结果：`54 passed`
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'named_face_plane_family_mismatch or clamshell_host_local_cut_recipe'`
   - 结果：`10 passed`
3. 最新真实 rerun 证据已经落盘：
   - 新 run：`practice_runs/20260419_201455/front_face_sketch_recess_focus_v00_194ed1dd/summary.json`
   - 旧对照：`practice_runs/20260419_201000/front_face_sketch_recess_focus_v00_194ed1dd/summary.json`
4. 关键变化很清楚：
   - 旧对照里，唯一 hallucination 是 `invalid_api_contract`，直接来自 round 1 的 `named_face_plane_family_mismatch`
   - 新 rerun 里，`build123d_hallucination.event_count = 1` 仍然成立，但类别已经变成：
     - `write_runtime_exception`
     - 也就是 broad fillet 失败，而不是 front-face plane-family 幻觉
5. 对应 round 1 工件也能直接说明这一点：
   - `practice_runs/20260419_201455/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_01_execute_build123d.json`
   - 现在 round 1 失败原因是：
     - `Failed creating a fillet with radius of 1.0`
   - 不再是：
     - `invalid_build123d_contract.named_face_plane_family_mismatch`
6. 这说明本次改动至少已经把同一条 practice seed 的主导 hallucination，从 front-face plane-family 错误推进到了更后面的 shared geometry issue。

### 后续方向

1. 当前同一批 live rerun 还在继续：
   - `practice_runs/20260419_201455/hinged_pillbox_magnet_medium_v00_e34c3c99/`
2. 这条 run 现在最值得观察的是：
   - `hinged_pillbox_magnet_medium` 是否也不再把 front label recess 写回 `Plane.YZ`
   - 如果这层收益能跨 seed 复用，就说明这次改的是共享 control surface，而不是 prompt 偶然漂移
3. 下一层共享瓶颈也已经变清楚：
   - whole-part first write 仍可能把“本该留给 local finish 的 broad fillet”提前塞进 round 1
   - local finish 之后剩余的 countersink closure 仍需继续收口
4. 也就是说，当前主矛盾已经进一步收缩成：
   - first-write 的 broad fillet / selector 治理
   - local-finish 后的 countersink / validation closure
   - 而不是 named-face plane-family 主导失败


## 4/19 晚间补充：`apply_cad_action` no-effect 假成功识别

### 今天又做了什么

1. 在 `134500` 卡死修掉之后，继续追 `practice_runs/20260419_195039/` 的真实后续残差。
2. 这条 run 把新的共享问题钉得很清楚：
   - round 4 已经真实跑到了 `query_topology + query_kernel_state`
   - round 5-7 也已经真实消费了 exact `face_ref`
   - 但 round 7 的 `cut_extrude` 虽然返回 `success=true`，几何却完全没变
3. 这说明当前主矛盾已经不是“面选不中”，而是：
   - `apply_cad_action` 的实体写链里存在 no-effect 假成功
   - runtime 会把这种没有几何效果的 local finish 误当成真实进展
4. 已经在 `src/sandbox_mcp_server/service.py` 落地一层通用保护：
   - 对 `cut_extrude / extrude / hole / fillet / chamfer / trim / revolve / loft / sweep / sphere_recess` 这类实体写动作，
   - 如果 action 返回成功但 geometry 没有 material delta，
   - 直接改判为结构化失败，不再继续伪装成成功写入
5. 同时补上了 runtime 侧的可观察面：
   - `src/sub_agent_runtime/turn_state.py` 新增 `no_op_action_count`
   - `src/sub_agent_runtime/agent_loop_v2.py` 已把这个值写进 run summary

### 如何证明这次修复有效

1. 真实 case 证据链：
   - `practice_runs/20260419_195039/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_05_response.json`
   - `practice_runs/20260419_195039/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_06_response.json`
   - `practice_runs/20260419_195039/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_07_response.json`
   - `practice_runs/20260419_195039/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_07_apply_cad_action.json`
   - `practice_runs/20260419_195039/hinged_pillbox_magnet_medium_v00_e34c3c99/queries/round_07_validate_requirement_post_write.json`
   - `practice_runs/20260419_195039/hinged_pillbox_magnet_medium_v00_e34c3c99/practice_analysis.md`
2. 这些工件共同说明：
   - topology targeting 已经是真的，`exact_ref_consumption_rate = 1.0`
   - validator 也明确说 `feature_notch_or_profile_cut` 仍未实现
   - 更关键的是，round 5 -> 7 的 snapshot geometry 一直没变
   - 也就是说旧逻辑里这是典型的“假成功”
3. 红绿回归已经补齐：
   - `tests/unit/sandbox_mcp_server/test_action_contracts.py::test_apply_cad_action_rejects_no_effect_cut_extrude`
   - `tests/unit/sub_agent_runtime/test_turn_state.py::test_run_state_counts_no_effect_apply_action_as_no_op`
4. focused 结果：
   - `./.venv/bin/pytest tests/unit/sandbox_mcp_server/test_action_contracts.py -q -k no_effect_cut_extrude`
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_turn_state.py -q`
   - 都已通过
5. 合并相关 suite：
   - `./.venv/bin/pytest tests/unit/sandbox_mcp_server/test_action_contracts.py tests/unit/sub_agent_runtime/test_tool_runtime_execution.py tests/unit/sub_agent_runtime/test_turn_state.py -q`
   - 结果：`22 passed`

### 后续方向

1. 继续盯新的真实 rerun：
   - `practice_runs/20260419_200420/`
2. 这条 run 现在验证的是：
   - 如果 local finish 再次走到 no-effect cut/extrude，
   - runtime 是否会更早把它识别成失败，
   - 而不是再浪费后续 round 去验证一个根本没生效的实体写
3. 如果这条保护在 live lane 生效，下一层更值得继续压的共享瓶颈就是：
   - whole-part rebuild 的 `transform_context_manager`
   - validator 对 notch/profile cut 的几何 grounding
   - local finish 后的更短 closure policy

## 4/19 晚间补充：`134500` 卡死根因、修复、验证

### 今天又做了什么

1. 把 `practice_runs/20260419_134500/` 的 round 1 卡死从“live run 没动静”继续压到了具体代码行。
2. 真实根因已经定位清楚：
   - 不是 Kimi 没返回
   - 不是 Build123d 几何执行卡住
   - 不是 MCP `execute_build123d` 本身卡住
   - 而是 `execute_build123d` 进入 sandbox 之前，preflight lint 的 `RectangleRounded` radius guard 自己卡死
3. 具体卡点是：
   - `src/sub_agent_runtime/tool_runtime.py::_preflight_lint_execute_build123d`
   - `src/sub_agent_runtime/tool_runtime.py::_find_rectanglerounded_radius_bounds_hits`
   - `src/sub_agent_runtime/tool_runtime.py::_collect_numeric_assignment_env`
4. 根因模式也已经查明：
   - 同一个变量名在不同 loop 中被重复赋值
   - 旧实现用无界 fixed-point 扫描整个 AST
   - 结果会在两个值之间来回振荡，永远收不敛
5. 已把 numeric env 求值改成：
   - 按源码顺序收集赋值
   - 有界 pass 收敛
   - 允许 later assignment 覆盖 earlier assignment
   - 不再因为重复临时变量名进入无限 preflight scan

### 如何证明这次修复有效

1. `practice_runs/20260419_134500/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_01_response.json`
   - 这就是触发卡死的真实 round 1 代码
2. `/tmp/preflight_lint_stack.txt`
   - traceback 已明确指向：
   - `_collect_numeric_assignment_env -> _find_rectanglerounded_radius_bounds_hits -> _preflight_lint_execute_build123d`
3. 复现实验已经证明旧实现会振荡：
   - `magnet_z` 在这个真实 case 里会在 `1.0` 和 `0.8` 之间反复切换
4. 修复后直接重放真实 preflight lint：
   - 命令：直接把 `134500` 的 round 1 源码重新喂给 `_preflight_lint_execute_build123d(...)`
   - 结果：`elapsed_sec 2.504`
   - 说明 preflight lint 已经能返回，不再卡死
5. focused 单测已补齐并通过：
   - `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py::test_numeric_assignment_env_converges_when_same_name_is_reassigned_in_multiple_loops`
   - `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py::test_preflight_lint_rejects_rectanglerounded_radius_that_exceeds_half_of_height`
   - 命令：
     - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'rectanglerounded_radius_that_exceeds_half_of_height or numeric_assignment_env_converges_when_same_name_is_reassigned_in_multiple_loops'`
   - 结果：`2 passed`
6. 新的真实 rerun 已继续启动：
   - `practice_runs/20260419_195039/`
   - 目标不是只看单测，而是确认 live round 1 不再死在 `tool_batch_started`

### 后续方向

1. 先继续盯 `practice_runs/20260419_195039/`，确认它是否越过旧的 round 1 preflight 死锁点。
2. 如果这条 run 顺利越过，当前 enclosure seed 的主矛盾就会重新回到更健康的两类共享问题：
   - `named_face_plane_family_mismatch`
   - clamshell / local-finish 后续 closure
3. 这次修复的意义在于：
   - 当前 practice lane 的瓶颈再次从“runtime 自己卡死”回到“可观察、可修复的 geometry / policy family”
   - 这比继续堆零散 case 规则更有价值

## 现场汇报入口

如果只需要先快速回答“今天做了什么、怎么证明有效、后续方向是什么”，优先使用：

1. `report-20260419-build123d-practice-progress/index.html`
2. `report-20260419-build123d-practice-progress/shareable-report.md`
3. `practice_runs/20260419_234500/hinged_pillbox_magnet_medium_v00_e34c3c99/summary.json`
4. `practice_runs/20260419_234500/hinged_pillbox_magnet_medium_v00_e34c3c99/practice_analysis.md`
5. `practice_runs/20260419_184731/hinged_pillbox_magnet_medium_v00_e34c3c99/summary.json`
6. `practice_runs/20260419_184731/hinged_pillbox_magnet_medium_v00_e34c3c99/practice_analysis.md`
7. `practice_runs/20260419_185457/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`
8. `practice_runs/20260419_185457/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_02_execute_build123d.json`
9. `practice_runs/20260419_185457/hinged_pillbox_magnet_medium_v00_e34c3c99/queries/round_03_query_geometry.json`

一句话版本：

1. 今天继续沿 practice lane 收口 live runtime，主线已经从“Build123d API 大量幻觉”收窄到“clamshell/hinge family 的 plane-family 误伤 + 写成功后的 read/repair closure”。
2. 最强证据链已经形成：`234500 -> 184731 -> 185457`，分别对应“write-surface 幻觉仍高”、“幻觉已压到 1 个事件且 first solid 成功”、“新 run 已真实走到 query_topology + exact-ref local finish，但还剩 2 个共享 write-surface family”。
3. 下一步优先继续压 `185457` 暴露出来的两个残余 family：`named_face_plane_family_mismatch` 与 `detached_subtractive_builder_without_host`，同时保持它已经跑出来的 topology-aware local finish 链。

## 4/19 当前主线直答

### 今天做了什么

1. 把 clamshell / living hinge 这条 shared family 再收紧一层，不再只说“别写 detached hinge”，而是明确：
   - living hinge 默认是 host-owned back-edge strip / flexure
   - seam coordinate 只属于 back edge，不允许把整个 lid / base 平移到 seam 上
   - local finish 里 `create_sketch(face_ref=...)` 之后不该再去 `get_history`
2. 把 open sketch window 的 closure policy 再往前推了一步：
   - 之前已经修过“fresh profile 之后优先 materialize，不再先 `query_sketch`”
   - 今天又新增“fresh `create_sketch(face_ref=...)` 之后，如果 sketch 还是空的，下一轮只允许 `apply_cad_action` 继续写第一笔草图几何，不再把 `query_sketch` 暴露成同级选项”
3. 连续跑了两条真实 Kimi practice：
   - `practice_runs/20260419_124200/`
   - `practice_runs/20260419_130800/`
4. 已继续启动带最新 apply-only sketch policy 的 live rerun：
   - `practice_runs/20260419_134500/`

### 如何证明这些改动有效

1. `practice_runs/20260419_124200/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_01_response.json`
   - first write 已不再写 detached `hinge_barrel` / `hinge_pin`
   - 也不再把整个 lid 平移到 back-edge seam 上
2. `practice_runs/20260419_124200/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_04_response.json`
3. `practice_runs/20260419_124200/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_04_apply_cad_action.json`
   - local finish 已真实消费 exact `face_ref`
   - 不再浪费在 `get_history`
4. `practice_runs/20260419_124200/hinged_pillbox_magnet_medium_v00_e34c3c99/practice_analysis.md`
   - `hallucination: events=0`
   - `topology_targeting_observed: True`
   - `exact_ref_consumption_rate: 1.0`
5. `practice_runs/20260419_130800/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_05_response.json`
6. `practice_runs/20260419_130800/hinged_pillbox_magnet_medium_v00_e34c3c99/queries/round_05_query_sketch.json`
   - 这条 live run 暴露出新的共享 closure gap：
   - `create_sketch(face_ref=...)` 成功后，round 5 仍然去看了一个空 sketch
7. `practice_runs/20260419_130800/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_06_response.json`
8. `practice_runs/20260419_130800/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_08_response.json`
   - 说明这条 run 的主矛盾已经不是 Build123d 幻觉，而是 sketch closure 多浪费了一轮
   - 后半段仍能走出 `add_rectangle -> cut_extrude`
9. `practice_runs/20260419_130800/hinged_pillbox_magnet_medium_v00_e34c3c99/practice_analysis.md`
   - `hallucination: events=0`
   - `local_targeting_action_count: 3`
   - `exact_ref_consumption_rate: 1.0`
   - 剩余 blocker 已收窄成整体验证：`create_a_two_part_rounded_pillbox_enclosure_sized_64mm_x_48mm_x_24mm_with_a_living_hinge_at_the_back`
10. 今天新增的 apply-only policy 已经有 focused 红绿测保护：
    - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py::test_open_sketch_window_after_empty_query_sketch_still_prefers_apply_only`
    - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q -k 'open_sketch_window_after_empty_query_sketch_still_prefers_apply_only or continue_open_sketch_window_after_apply_action or open_sketch_window_with_fresh_profile_prefers_apply_over_query_sketch or open_sketch_window_under_critical_budget_exits_to_code_first_escape or open_sketch_window_with_empty_sketch_and_two_rounds_left_exits_to_code_first_escape'`
    - 结果：`4 passed`

### 后续方向

1. 先看 `practice_runs/20260419_134500/` 是否成功省掉 `130800` round 5 那次空 `query_sketch`
2. 如果这一步生效，当前 enclosure/clamshell family 的主矛盾就会更纯粹地收缩到：
   - first write 的 whole-part 两段壳体/hinge 组织
   - validator 对 two-part + living hinge 关系的 geometry grounding
3. 后续继续优先做共享 policy 和 shared family contract，不回到单 case helper rule

## 4/19 下午最新直答

### 今天做了什么

1. 把 clamshell/hinge family 的 skill guidance 再收紧一层，明确：
   - back-edge hinge seam 是 `y = ±depth/2`
   - hinge 轴向单独用 transform 决定
   - 不允许把 back-edge seam 误写成 `Plane.YZ` family
2. 把 detached positive hinge helper 的 lint 误伤修掉，不再把“正确的独立铰链圆柱 builder”误判成 named-face plane-family mismatch。
3. 启动新的真实 Kimi rerun `practice_runs/20260419_185457/`，用 live run 验证：
   - round 5 的旧假阳性是否已经消失
   - topology-aware local finish 能不能重新跑起来

### 如何证明有效

1. `practice_runs/20260419_234500/hinged_pillbox_magnet_medium_v00_e34c3c99/summary.json`
   - `planner_rounds = 8`
   - `executed_action_count = 5`
   - `build123d_hallucination.event_count = 10`
   - `primary_layer = write_surface`
2. `practice_runs/20260419_184731/hinged_pillbox_magnet_medium_v00_e34c3c99/summary.json`
   - `planner_rounds = 8`
   - `executed_action_count = 3`
   - `build123d_hallucination.event_count = 1`
   - `last_error = null`
3. `practice_runs/20260419_184731/hinged_pillbox_magnet_medium_v00_e34c3c99/practice_analysis.md`
   - `status = partial_geometry`
   - `query_topology = 3`
   - `query_kernel_state = 3`
   - 剩余 blocker 已收窄为 `feature_notch_or_profile_cut`
4. `practice_runs/20260419_185457/hinged_pillbox_magnet_medium_v00_e34c3c99/summary.json`
   - `build123d_hallucination.event_count = 2`
   - `executed_action_count = 5`
   - `forced_policy_chain` 已出现 `apply_local_finish_after_topology_targeting_from_read_stall`
5. `practice_runs/20260419_185457/hinged_pillbox_magnet_medium_v00_e34c3c99/practice_analysis.md`
   - `local_targeting_action_count = 1`
   - `fresh_targeting_action_count = 1`
   - `exact_ref_consumption_rate = 1.0`
6. `practice_runs/20260419_185457/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_08_apply_cad_action.json`
   - 最新 run 已真实消费 exact `face_ref`
   - 当前剩余 blocker 仍收窄在 `feature_notch_or_profile_cut`

### 后续方向

1. 继续比对 `184731` 与 `185457`，保持 `185457` 已经跑出来的 topology-aware local finish 链，不让系统重新退回纯 read-only stall。
2. 下一刀优先压 `185457` 暴露出来的两个残余 write-surface family：
   - `named_face_plane_family_mismatch`
   - `detached_subtractive_builder_without_host`
3. 在不破坏泛化的前提下，把后续优化重点放在“host-owned local cut contract + exact-ref local finish closure”，而不是回去写更多单 case helper 规则。

## 今日简版汇报

### 三句话结论

1. 今天最重要的推进，不是继续围 benchmark 加规则，而是把 practice lane 补成了无 Docker 也能真实跑通的 live lane。
2. 两条真实 run `practice_runs/20260419_143759` 与 `practice_runs/20260419_144300` 已经证明：主矛盾重新收敛到 write-side Build123d contract。
3. `Plane(...).moved(...)` 这类共享 API 幻觉已经退出首轮主导面，当前下一层共享瓶颈是 `broad_shell_axis_fillet_on_fresh_host`。

### 最新追加结论

1. `practice_runs/20260419_154837 -> 20260419_155523 -> 20260419_155831` 三条真实 Kimi run 已经把问题逐层压缩：先解决 graph-refresh 卡死，再重开 round 4 的 code repair，最后第一次真实走到 `query_topology -> apply_cad_action(action_params.face_ref=...)`。
2. `practice_runs/20260419_155831/front_face_sketch_recess_focus_v00_194ed1dd/practice_analysis.md` 已证明：
   - `local_targeting_action_count = 1`
   - `fresh_targeting_action_count = 1`
   - `exact_ref_consumption_rate = 1.0`
3. 当前最明确剩余瓶颈已经收窄到 `explicit_anchor_hole` 的 countersink 几何承认链，而不是“前表面 ref 取不到”。
   - 直接证据：
     - `practice_runs/20260419_155831/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_06_validate_requirement.json`
4. 今天又补了一条 budget closure：
   - `repair_after_local_finish_semantic_refresh_under_budget`
   - 作用：successful local finish 之后如果又做过 semantic refresh，且只剩最后一轮，不再把最后一轮浪费在 `validate_requirement`，而是直接重开 actionable repair lane。
   - 代码：
     - `src/sub_agent_runtime/agent_loop_v2.py`
     - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
5. 新 rerun `practice_runs/20260419_160521/` 已进一步证明：最后一轮可以真实切回修复，而不是纯 validation。
   - 直接证据：
     - `practice_runs/20260419_160521/front_face_sketch_recess_focus_v00_194ed1dd/plans/round_06_response.json`
     - `practice_runs/20260419_160521/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_06_execute_build123d.json`
     - `practice_runs/20260419_160521/front_face_sketch_recess_focus_v00_194ed1dd/trace/failure_bundle.json`
6. 新增一条 family-level local-finish bias：
   - 当 exact `face_ref` 已经存在且目标就是 `hole/countersink` 这类直接特征时，优先 direct `apply_cad_action(hole|countersink, face_ref=...)`，不要先开 `create_sketch(face_ref=...)`。
   - 代码：
     - `src/sub_agent_runtime/context_manager.py`
   - 单测：
     - `tests/unit/sub_agent_runtime/test_context_manager.py::test_prompt_payload_adds_local_finish_contract_when_topology_refs_exist`
7. 今天又补齐了另一半尾段 closure：
   - `code_escape_after_local_finish_semantic_refresh_under_budget`
   - 作用：successful local finish 之后如果又做过 semantic refresh、只剩最后一轮、blocker 还在、但拿不到 actionable patch，就直接放行 `execute_build123d` 做 whole-part escape，而不是退回 `validate_after_local_finish_semantic_refresh`。
   - 代码：
     - `src/sub_agent_runtime/agent_loop_v2.py`
   - 红绿测：
     - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py::test_last_round_after_successful_local_finish_semantic_refresh_falls_back_to_code_escape_without_patch`
8. 新 rerun `practice_runs/20260419_163503/` 说明 first-write 仍有两类共享 Build123d helper 幻觉残差，但已经被清楚压缩到更窄的 write-surface：
   - round 1：`invalid_build123d_keyword.countersink_angle_alias`
   - round 2：`invalid_build123d_api.workplanes_helper_name`
   - 直接证据：
     - `practice_runs/20260419_163503/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_01_execute_build123d.json`
     - `practice_runs/20260419_163503/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_02_execute_build123d.json`
     - `practice_runs/20260419_163503/brief_report.md`
9. 针对 `163503`，我继续把首轮 focused skill 收紧成更明确的 helper contract：
   - `CounterSinkHole(...)` 明确要求 `counter_sink_angle=...`
   - 明确禁止发明 `Workplanes(...)`
   - 代码：
     - `src/sub_agent_runtime/skill_pack.py`
   - 回归：
     - `tests/unit/sub_agent_runtime/test_skill_pack.py`
10. `162814` 的 local-anchor-count 偏大问题已经进一步定位为 validator 聚合过宽，而不是实际几何真的做出了那么多底面孔位。
    - 直接证据：
      - `practice_runs/20260419_162814/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_06_validate_requirement.json`
    - 当前处理方式：
      - 对 `explicit_anchor_hole` 新增 family-specific requirement slice，只把它用于 hole/count 的 face-target 与 count inference
      - 不再让 front-face recess 之类的其他子句污染底面孔位计数
    - 代码：
      - `src/sandbox_mcp_server/service.py`
    - 新增回归：
      - `tests/unit/sandbox_mcp_server/test_validation_geometry_fallbacks.py::test_explicit_anchor_hole_family_text_prefers_hole_clause_and_bottom_face_target`
      - `tests/unit/sandbox_mcp_server/test_validation_geometry_fallbacks.py::test_validate_requirement_uses_hole_clause_face_targets_for_local_anchor_count_on_mixed_face_requirement`
11. 上述 validator 收窄已经过一轮更宽的验证回归：
    - `./.venv/bin/pytest tests/unit/sandbox_mcp_server/test_validation_geometry_fallbacks.py tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_context_manager.py -q`
    - 结果：`151 passed`
12. 新增 centered-host face-plane 治理，开始直接拦截“默认 centered `Box(...)` 上用整长 `Plane.offset(...)` 去找 named face”这一类共享 Build123d 幻觉。
    - 代码：
      - `src/sub_agent_runtime/tool_runtime.py`
      - `src/sub_agent_runtime/skill_pack.py`
    - 单测：
      - `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py::test_preflight_lint_rejects_full_span_face_plane_offset_on_centered_box`
      - `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py::test_preflight_lint_allows_half_span_face_plane_offset_on_centered_box`
13. `practice_runs/20260419_164939/` 证明 hole/count face-target 已恢复正确：
    - `expected_local_center_count = 2`
    - `realized_local_center_count = 2`
    - 当前失败已经不再是 front-face recess 污染底面孔位计数，而是明确收窄到 `feature_countersink`
    - 直接证据：
      - `practice_runs/20260419_164939/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_03_query_feature_probes.json`
      - `practice_runs/20260419_164939/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_06_validate_requirement.json`
14. `practice_runs/20260419_165846/` 说明问题已经继续前移：
    - round 1：bottom-face half-span 已被模型主动修正
    - round 3：已真实消费 exact `bottom face_ref` 做 `apply_cad_action(hole, hole_type='countersink')`
    - round 6：`feature_countersink` 与 `two_mounting_holes_on_the_bottom_face` 均已通过
    - 剩余问题已经转成 validator 的 `insufficient_evidence`
    - 直接证据：
      - `practice_runs/20260419_165846/brief_report.md`
      - `practice_runs/20260419_165846/front_face_sketch_recess_focus_v00_194ed1dd/practice_analysis.md`
      - `practice_runs/20260419_165846/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_06_validate_requirement_post_write.json`
      - `practice_runs/20260419_165846/front_face_sketch_recess_focus_v00_194ed1dd/plans/round_03_response.json`
15. 针对 `165846` 暴露出来的 validator closure 问题，本轮又补了两类共享 clause 处理：
    - `2mm deep` 这类孤立 short depth modifier，在前一个 feature clause 已验证时不再继续单独阻塞 completion
    - `so that a topology-aware local finishing pass ... is useful` 这类过程性元子句，降为 process-only / diagnostic
    - 代码：
      - `src/sandbox_mcp_server/validation_interpretation.py`
    - 单测：
      - `tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py::test_interpret_requirement_clauses_treats_local_finish_usefulness_clause_as_process_only`
      - `tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py::test_interpret_requirement_clauses_demotes_short_depth_modifier_fragment_after_verified_feature`
16. 截止当前，这一轮 validator/runtime 宽回归已经过：
    - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py tests/unit/sandbox_mcp_server/test_validation_geometry_fallbacks.py -q`
    - 结果：`271 passed`

### 今天做了什么

1. 把 validator 的 grounding 缺口从隐式状态补成结构化 surface。
   - 代码：
     - `src/sandbox_mcp_server/contracts.py`
     - `src/sandbox_mcp_server/validation_grounding.py`
     - `src/sandbox_mcp_server/service.py`
   - 新字段：
     - `grounding_gap_reasons`
     - `overclaim_guard`
     - `required_evidence_kinds`

2. 把 repair packet 的 recipe contract 校验正式前移到 runtime adapter。
   - 代码：
     - `src/sub_agent_runtime/tool_adapters.py`
   - 新返回面：
     - `repair_packet_contract_miss`
     - `missing_anchor_keys`
     - `missing_parameters`
     - `recipe_contract`

3. 把 practice lane 的 read-model 使用质量补成可统计指标，而不再只看“有没有 query_topology”。
   - 代码：
     - `src/sub_agent_runtime/practice_runner.py`
   - 新指标：
     - `fresh_targeting_action_count`
     - `stale_ref_action_count`
     - `nonconcrete_ref_action_count`
     - `candidate_label_ref_action_count`
     - `exact_ref_consumption_rate`

4. 补了 `local-process` sandbox fallback，让 practice lane 在本机没有 Docker daemon 时仍能真实执行 Build123d。
   - 代码：
     - `src/common/config.py`
     - `src/sandbox/__init__.py`
     - `src/sandbox/local_process_runner.py`
     - `src/sandbox/docker_runner.py`

5. 把 `Plane(...).moved(...)` 收进 API governance 和首轮 hygiene，作为共享 Build123d 幻觉处理，而不是继续靠 case 内自然碰撞。
   - 代码：
     - `src/sub_agent_runtime/tool_runtime.py`
     - `src/sub_agent_runtime/skill_pack.py`

### 如何证明有效

1. 今天新增与回归相关单测已经通过。
   - 执行：
     - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_practice_runner.py tests/unit/sub_agent_runtime/test_tool_adapters.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sandbox/test_local_process_runner.py tests/unit/sandbox/test_docker_runner.py tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py tests/unit/sub_agent_runtime/test_hallucination.py -q`
   - 结果：
     - `238 passed`

2. `local-process` 不是只存在于单测，已经真实进入 practice live lane。
   - 直接证据：
     - `practice_runs/20260419_143759/practice_manifest.json`
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`
   - 关键信息：
     - round 1 stderr 已出现 `build123d-local-process-...`
     - run 最终成功落出 `model.step` 与四视图图片，而不是再次死在 MCP server 启动

3. 第一条真实 practice run 已经完整跑完，说明当前项目在 practice lane 上是可运行的。
   - run：
     - `practice_runs/20260419_143759/`
   - 汇总：
     - `practice_runs/20260419_143759/brief_report.md`
     - `practice_runs/20260419_143759/run_diagnostics.md`
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/practice_analysis.md`
   - 关键信息：
     - `status = partial_geometry`
     - `step_file_exists = True`
     - `hallucination.event_count = 3`
     - `hallucination.primary_layer = write_surface`
     - `local_targeting_action_count = 1`
     - `fresh_targeting_action_count = 1`
     - `stale_ref_action_count = 0`
     - `exact_ref_consumption_rate = 1.0`

4. 第二条真实 rerun 进一步证明：今天新增治理已经改变了首轮错误形态。
   - run：
     - `practice_runs/20260419_144300/`
   - 汇总：
     - `practice_runs/20260419_144300/brief_report.md`
     - `practice_runs/20260419_144300/run_diagnostics.md`
     - `practice_runs/20260419_144300/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`
   - 对比结论：
     - `hallucination.event_count: 3 -> 2`
     - `writes: 4 -> 3`
     - 首轮主失败从 `Plane.YZ.moved(...)` 变成 `invalid_build123d_contract.broad_shell_axis_fillet_on_fresh_host`
   - 解释：
     - 这不代表任务已经完成，而是说明今天新增的共享治理确实把错误从 API 猜错推进到了更窄的 shell host 组织问题。

5. 当前主瓶颈已经被进一步收窄，而不是停留在“环境不通”。
   - 直接证据：
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_02_execute_build123d.json`
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_03_execute_build123d.json`
   - 关键信息：
     - round 1：`Plane.YZ.moved(...)` 触发真实 Build123d API 幻觉
     - round 2：`SlotOverall(...)` 在 active `BuildPart` 中被 preflight lint 直接拦下
     - round 3：修完后成功产出 `model.step`

6. read-model 与 topology-aware local finish 不是空转，已经在真实 case 中被消费。
   - 直接证据：
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/queries/round_05_query_topology.json`
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_06_apply_cad_action.json`
   - 关键信息：
     - `preferred_ref_id = face:1:F_bf763cd5c387`
     - round 6 已真实消费这个 `face_ref`
     - `fresh_targeting = true`

### 当前有效结论

1. practice lane 现在已经能在无 Docker daemon 的开发机上跑出真实 Build123d 几何、render 图和完整工件链。
2. 当前复杂新 prompt 的主瓶颈不是 read-model 缺席，而是 write-side Build123d contract 仍有共享幻觉残差。
3. read-model 的提升方向现在更明确：不是继续加“有没有读”的统计，而是继续提升“读到的 ref 是否被新鲜、具体地消费”。
4. validator / repair / read-model 的分层改造已经不再只是设计，今天的 diff 已经把三条关键 surface 真正落盘。

### 下一步

1. 继续用同一个 seed rerun，验证新加的 `Plane(...).moved(...)` guidance / preflight 是否能把首轮失败再向后推。
   - 正在运行：
     - `practice_runs/20260419_144300/`

2. 继续压 `feature_notch_or_profile_cut` 这条共享 family，不针对单 case 写硬规则，而是继续增强：
   - `BuildSketch` / host-plane contract
   - host-owned local cut lane
   - topology-aware local finish closure

3. 继续把 today 的 surface 往汇报与诊断面贯通：
   - `grounding_gap_reasons`
   - `repair_packet_contract_miss`
   - `fresh_targeting_action_count`
   - `stale_targeting_cases`

## 补充入口

1. 汇报页：
   - `report-20260419-build123d-practice-progress/index.html`
   - `report-20260419-build123d-practice-progress/shareable-report.md`
2. 重点 run：
   - `practice_runs/20260419_143759/`
3. 进行中的 rerun：
   - `practice_runs/20260419_144300/`

## 2026-04-19 第二阶段追加进展

### `practice_runs/20260419_145502/` 证明：first-write 已继续收紧，但 read-model 仍有一层共享误导

这条 run 是今天后半段最关键的一条真实对照，因为它把新加的 rounded-envelope guidance 和 read-model 缺口都一起照出来了。

直接证据：

1. `practice_runs/20260419_145502/brief_report.md`
2. `practice_runs/20260419_145502/run_diagnostics.md`
3. `practice_runs/20260419_145502/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`
4. `practice_runs/20260419_145502/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_02_execute_build123d.json`
5. `practice_runs/20260419_145502/hinged_pillbox_magnet_medium_v00_e34c3c99/queries/round_02_validate_requirement_post_write.json`
6. `practice_runs/20260419_145502/hinged_pillbox_magnet_medium_v00_e34c3c99/queries/round_04_query_topology.json`
7. `practice_runs/20260419_145502/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_05_apply_cad_action.json`

这条 run 的三个关键信号：

1. round 1 的首轮主失败已不再是 `Plane(...).moved(...)`，也不再是 fresh-host broad fillet，而是更窄的：
   - `invalid_build123d_contract.detached_subtractive_builder_without_host`
2. round 2 whole-part 写已经真实成功：
   - `actions/round_02_execute_build123d.json -> success = true`
3. round 5 失败不是 write-side API 幻觉，而是 read-side target 误导：
   - `invalid_reference: face_ref 'face:1:F_1e5e00c2830d' is not planar (geom_type=CYLINDER)`

这条 run 的汇总结论：

1. `build123d_hallucination.event_count = 2`
2. `build123d_hallucination.weighted_score = 1.6`
3. 分层从只剩 `write_surface`，变成：
   - `write_surface = detached subtractive builder without host`
   - `read_surface = non-planar face_ref consumed by create_sketch`

这说明当前瓶颈已经进一步细化：

1. first-write 侧已经不再主要卡在最浅层 API 猜错
2. 但 `query_topology -> apply_cad_action` 之间仍可能因为 candidate 排序不够好，把圆柱面放到 sketch host 前面

### 针对 `145502` 已新增的 read-model 修复

为了避免 `front_faces` / `back_faces` / `left_faces` / `right_faces` / `top_faces` / `bottom_faces` 默认把不可 sketch 的曲面排到前面，本轮已补一条共享 read-model 排序修复：

1. `src/sandbox_mcp_server/service.py`
   - 方向面 candidate sets 现在默认 `planar first`，再按面积降序
2. `tests/unit/sandbox_mcp_server/test_topology_candidate_sets.py`
   - 新增：当 `front_faces` 同时含平面与圆柱面时，默认首选平面

对应回归：

1. `./.venv/bin/pytest tests/unit/sandbox_mcp_server/test_topology_candidate_sets.py -q`
   - `6 passed`
2. `./.venv/bin/pytest tests/unit/sandbox_mcp_server/test_action_contracts.py -q -k 'query_topology or face_ref'`
   - `4 passed, 6 deselected`

### 当前继续进行中的验证

1. 我已经基于这条 planar-first 排序修复继续发起下一条真实 rerun：
   - `practice_runs/<next timestamp>/...`
2. 目标不是提高单 case 分数，而是验证：
   - `query_topology` 是否会把可 sketch 的 planar `front_face` 排到前面
   - `apply_cad_action(create_sketch(face_ref=...))` 是否能少一次 read-surface invalid_reference

### `practice_runs/20260419_150602/` 进一步证明：focused local-finish 链路已经完整进入 live run，但末端 contract 还需继续收紧

这条 run 的价值比 `150141` 更高，因为它专门回答了 front-face local finish 这条链到底有没有真实跑起来。

直接证据：

1. `practice_runs/20260419_150602/brief_report.md`
2. `practice_runs/20260419_150602/run_diagnostics.md`
3. `practice_runs/20260419_150602/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_03_query_feature_probes.json`
4. `practice_runs/20260419_150602/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_04_query_topology.json`
5. `practice_runs/20260419_150602/front_face_sketch_recess_focus_v00_194ed1dd/plans/round_05_response.json`
6. `practice_runs/20260419_150602/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_05_apply_cad_action.json`
7. `practice_runs/20260419_150602/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_06_apply_cad_action.json`

这条 run 证明了四件事：

1. round 2 whole-part 写已经稳定成功。
2. round 3 已真实进入 `query_feature_probes`。
3. round 4 已真实进入 `query_topology`，而且 `Front Faces` 候选已经收缩成单一 ref。
4. round 5 / 6 没有再卡在“没有 topology evidence”，而是暴露出更末端的两个 local-finish contract：
   - round 5：`apply_cad_action preflight failed: get_history is not allowed while the turn_tool_policy is in local_finish mode`
   - round 6：`apply_cad_action preflight failed: cut_extrude requires an active profile sketch with non-empty profile_refs`

这说明 focused local-finish 主链其实已经打通：

1. `execute_build123d`
2. `validate_requirement`
3. `query_feature_probes`
4. `query_topology`
5. `apply_cad_action`

但末端还有两层 shared contract 需要继续收：

1. 当 exact refs 已经存在时，禁止把 `apply_cad_action` 浪费在 `get_history` 这类 session-control 逃逸动作上。
2. 当 local edit 需要 `cut_extrude` 时，必须先有 active sketch / profile_refs，而不是直接跳到 cut。

### 基于 `150602` 已新增的 local-finish contract 修复

针对 round 5 的 `get_history` 逃逸动作，本轮已继续前移一条共享 contract：

1. `src/sub_agent_runtime/context_manager.py`
   - local-finish instructions 新增：
     - `do not spend apply_cad_action on get_history or any other session-control escape action`
2. `tests/unit/sub_agent_runtime/test_context_manager.py`
   - 新增断言，确保这条约束进入 prompt payload

## 2026-04-19 第三阶段追加进展

### `practice_runs/20260419_151916/` 证明：exact topology ref 已经从“读得到”推进到“真实写得到”

这条 run 是今天最关键的 topology-targeting 证据，因为它第一次把之前修的两层改动同时跑进了真实 Kimi live lane：

1. `candidate_set.preferred_ref_id` 优先于杂乱 `matched_ref_ids`
2. local-finish prompt 里新增了可直接消费的 exact ref 文本提示

直接证据：

1. `practice_runs/20260419_151916/brief_report.md`
2. `practice_runs/20260419_151916/run_diagnostics.md`
3. `practice_runs/20260419_151916/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_03_query_topology.json`
4. `practice_runs/20260419_151916/front_face_sketch_recess_focus_v00_194ed1dd/plans/round_04_response.json`
5. `practice_runs/20260419_151916/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_04_apply_cad_action.json`
6. `practice_runs/20260419_151916/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_05_query_sketch.json`

这条 run 给出了两个明确结论：

1. round 4 已真实消费平面 face ref：
   - `face_ref = face:1:F_4514e213348b`
   - `actions/round_04_apply_cad_action.json -> success = true`
2. 主瓶颈已经不再是“选错 non-planar 面”，而是“在只剩 1-2 轮时仍被困在 open sketch tail”

对应汇总面：

1. `hallucination.event_count = 1`
2. `hallucination.weighted_score = 0.6`
3. 唯一 hallucination 已不是 topology targeting 失败，而是：
   - `apply_cad_action preflight failed: clear_session is not allowed while the turn_tool_policy is in local_finish mode`

这说明今天这一轮 local-finish 改造已经把主问题继续前推了一层：

1. 从 “query_topology 之后仍选错曲面”  
2. 变成 “runtime 没给 sketch tail 一个正式的 budget escape lane”

### 针对 `151916` 已新增的 policy 修复

这条 run 直接促成了一个共享 policy 级改动：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - 新增 open sketch tail 的 budget 风险判断
   - 当 `remaining_rounds < min_write_steps_remaining` 时，退出 `continue_open_sketch_window_after_apply_action`
   - 正式切到：
     - `policy_id = code_escape_after_open_sketch_window_under_budget`
     - `mode = code_first`
2. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - 新增 critical budget 场景回归测试

对应回归：

1. `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q -k 'open_sketch_window_under_critical_budget_exits_to_code_first_escape or continue_open_sketch_window_after_apply_action or open_sketch_window_persists_after_query_sketch_when_profile_is_ready'`
   - 通过

### `practice_runs/20260419_152405/` 证明：critical-budget escape 已经进入真实 live lane

这条 run 的价值不在于最后完全收敛，而在于它给出了 policy 改动已经生效的真实证据。

直接证据：

1. `practice_runs/20260419_152405/brief_report.md`
2. `practice_runs/20260419_152405/run_diagnostics.md`
3. `practice_runs/20260419_152405/front_face_sketch_recess_focus_v00_194ed1dd/summary.json`
4. `practice_runs/20260419_152405/front_face_sketch_recess_focus_v00_194ed1dd/plans/round_06_response.json`
5. `practice_runs/20260419_152405/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_06_execute_build123d.json`

这条 run 明确证明了两件事：

1. `forced_policy_chain` 已真实出现：
   - `code_escape_after_open_sketch_window_under_budget`
2. 第 6 轮不再退化成 `clear_session`，而是真的切回：
   - `execute_build123d`

也就是说，今天新增的 budget escape 不是只在单测里通过，而是已经进入真实 Kimi practice run。

### `152405` 暴露出来的下一层共享写侧瓶颈

第 6 轮虽然成功切回了 whole-part rewrite，但新的失败也很有价值：

1. `actions/round_06_execute_build123d.json`
   - `AttributeError: 'Plane' object has no attribute 'rotate'. Did you mean: 'rotated'?`

这不是 case-specific 问题，而是和更早的 `Plane(...).moved(...)` 同类型的共享 Build123d plane transform 幻觉。

因此本轮又继续补了一层共享 governance：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增：
     - `invalid_build123d_api.plane_rotate_shape_method_guess`
   - 并把它纳入现有：
     - `build123d_plane_rotation_contract`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - 新增对应单测

对应回归：

1. `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'plane_rotate_shape_method_guess or plane_rotated_origin_guess'`
   - 通过
2. `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'preferred_face_refs or local_finish or plane_moved_shape_method_guess or plane_located_shape_method_guess'`
   - 通过

### 今天收口后的最新判断

到当前为止，4/19 的真实 practice 结论已经很明确：

1. `query_topology -> apply_cad_action(create_sketch(face_ref=...))` 这条 exact ref lane 已经真实打通。
2. open sketch tail 在 critical budget 下现在已经有了正式 code escape，不再只能退化成 `clear_session`。
3. 主瓶颈再次向前推进，已经变成更窄的共享 write-side plane transform 幻觉：
   - `Plane(...).moved(...)`
   - `Plane....rotate(...)`
4. 这正符合当前目标：不是围某个 benchmark case 写硬规则，而是持续把共享 hallucination family 收编进 governance / skill / policy / read-model 这几层明确边界里。
3. 回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_context_manager.py -q -k 'local_finish_contract or topology_targeting_summary or system_prompt_surfaces_core_build123d_contracts'`
   - `4 passed, 9 deselected`
4. 相关策略回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q -k 'local_finish or query_topology'`
   - `18 passed, 37 deselected`

### 下一条 focused rerun 的验证目标

1. 继续用 `front_face_sketch_recess_focus`
2. 验证刚新增的 local-finish contract 是否能让 round 5 直接消费 exact ref，而不再浪费在 `get_history`

## 2026-04-19 第四阶段追加进展

### `practice_runs/20260419_173500/` 暴露旧的 completion closure 缺口

这条 run 把问题定位得很明确：

1. `queries/round_07_validate_requirement.json`
   - `is_complete = true`
   - `blockers = []`
2. 但 runtime 仍然继续进入了 round 8：
   - `plans/round_08_response.json`

因此本轮在 `src/sub_agent_runtime/agent_loop_v2.py` 里正式补上：

1. `_turn_has_successful_validation_completion(...)`
2. 当前回合已出现 successful complete validation 时，直接 `stop_reason=validated_complete`
3. `latest_validation` 已 complete 时，下一轮 policy 只允许 `finish_run`

对应回归：

1. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`

### `practice_runs/20260419_181500/` 说明 exact ref 已有，但 local finish 仍误走 sketch chain

这条 run 的价值在于，它说明主矛盾已经不再是 write-surface API 幻觉：

1. `brief_report.md`
   - `hallucination_events = 0`
2. 但 `plans/round_04_response.json` 里，模型在 exact bottom `face_ref` 已存在时，仍选择：
   - `apply_cad_action(action_type="create_sketch", action_params={"face_ref": ...})`

因此本轮继续在 prompt/runtime 侧补了共享偏置：

1. `src/sub_agent_runtime/context_manager.py`
   - `local_finish_contract.preferred_action_types = ["hole", "countersink", "counterbore"]`
   - focus attachment 明确写入：如果目标动作已经能直接表示成 host-face hole/countersink，就先 direct action，再考虑 `create_sketch(face_ref=...)`
2. `src/sub_agent_runtime/skill_pack.py`
   - `local_finish_exact_face_ref_contract` guidance 同步强调：
     - direct host-face hole/countersink 优先于 `create_sketch(face_ref=...)`

对应回归：

1. `tests/unit/sub_agent_runtime/test_context_manager.py`
2. `tests/unit/sub_agent_runtime/test_skill_pack.py`

### `practice_runs/20260419_184800/` 证明 direct hole/countersink 偏置已进入真实 live lane

这条 rerun 是今天最关键的正向证据。

直接证据：

1. `brief_report.md`
   - `status = complete`
   - `validation = 1`
2. `plans/round_04_response.json`
   - round 4 不再先开 `create_sketch`
   - 而是直接选择：
     - `apply_cad_action(action_type="hole", action_params={"hole_type": "countersink", "face_ref": ...})`
3. `queries/round_07_validate_requirement_post_write.json`
   - `is_complete = true`
   - `blockers = []`
4. `practice_analysis.md`
   - `local_targeting_action_count = 3`
   - `fresh_targeting_action_count = 2`
   - `stale_ref_action_count = 1`

这说明今天的共享改动已经在真实 Kimi run 里改变了 local finish 的动作选择，而不是只停留在单测。

### 本轮宽回归

执行：

1. `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py -q`

结果：

1. `304 passed, 11 warnings`

### 当前收口后的最新判断

到当前为止，今天最清楚的结论已经变成：

1. closure bug 已经被正式修掉，`validate_requirement complete` 不再继续空转。
2. local finish 的主要矛盾已经从 “exact ref 取不到” 推进到 “exact ref 已有时，动作优先级要不要先 direct hole/countersink”。
3. 这一层偏置已经在 `184800` 的真实 run 中生效。
4. 下一步最值得继续压的，不再是这条 family 的 sketch chain，而是：
   - first-write helper / plane contract 幻觉残差
   - local-targeting 跑起来后的 stale-ref repeat

### `practice_runs/20260419_201500/` 追加证明：今天这组修复已经跨 seed 复用

在补完 `validated_complete` closure 与 direct hole/countersink bias 之后，我又跑了一条不同 seed 的真实 Kimi case：

1. `practice_runs/20260419_201500/brief_report.md`
2. `practice_runs/20260419_201500/topology_local_finish_focus_v00_95ab3188/practice_analysis.md`
3. `practice_runs/20260419_201500/topology_local_finish_focus_v00_95ab3188/plans/round_04_response.json`
4. `practice_runs/20260419_201500/topology_local_finish_focus_v00_95ab3188/actions/round_04_apply_cad_action.json`

这条 run 的结论很直接：

1. `status = complete`
2. `validation = 1`
3. `hallucination_events = 0`
4. `local_targeting_action_count = 1`
5. `fresh_targeting_action_count = 1`
6. `stale_ref_action_count = 0`
7. `exact_ref_consumption_rate = 1.0`

最重要的是，round 4 同样已经直接选择：

1. `apply_cad_action(action_type="hole", action_params={"hole_type": "countersink", "face_ref": ...})`

这说明今天新增的 local-finish 偏置与 closure 纪律，不只是对 `front_face_sketch_recess_focus` 的同 prompt rerun 生效，而是至少已经在第二条 practice seed 上复用成功。

### `practice_runs/20260419_203500/` 继续暴露下一层共享瓶颈：named-face plane mismatch 的 preflight 分辨率不够

为了继续压 half-shell / enclosure 这一侧，我又启动了：

1. `practice_runs/20260419_203500/hinged_pillbox_magnet_medium_v00_e34c3c99/`

这条 run 的中间工件已经把当前剩余问题继续收窄：

1. `actions/round_01_execute_build123d.json`
   - round 1 的真实失败是：
     - `invalid_build123d_contract.named_face_plane_family_mismatch`
   - 直接错误形态是 front-face label recess 被写成 `Plane.YZ`
2. `plans/round_02_response.json`
   - round 2 已经开始主动复述正确映射：
     - `top/bottom -> Plane.XY`
     - `front/back -> Plane.XZ`
     - `left/right -> Plane.YZ`
3. `actions/round_02_execute_build123d.json` 与 `actions/round_05_execute_build123d.json`
   - 暴露出另一个更重要的问题：
   - preflight 还会把 base/lid 的 bare `Plane.XY` host-profile 草图也一并判成 front-face mismatch

这不是新的 case rule 入口，而是一个更共享的治理问题：`named_face_plane_family_mismatch` 的规则分辨率不够，误把普通 host-profile 草图和真正的 named-face local edit 混在一起了。

因此我在这一轮又继续做了两件事：

1. `src/sub_agent_runtime/skill_pack.py`
   - `enclosure_local_feature_placement_contract` 新增更明确的 named enclosure face -> plane family mapping：
     - `top/bottom -> Plane.XY`
     - `front/back -> Plane.XZ`
     - `left/right -> Plane.YZ`
   - 并明确指出：
     - front label / front notch / front recess 不该落到 `Plane.YZ`
2. `src/sub_agent_runtime/tool_runtime.py`
   - 收窄 `_find_named_face_plane_family_mismatch_hits(...)`
   - 当脚本里已经出现了正确的 allowed plane family 时，不再把 bare named-plane host-profile 草图一起误报成 mismatch

对应红绿测：

1. `tests/unit/sub_agent_runtime/test_skill_pack.py::test_build_runtime_skill_pack_adds_enclosure_local_feature_placement_contract_on_first_turn`
   - 先红后绿
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py::test_preflight_lint_ignores_bare_xy_host_profiles_when_front_face_local_edit_uses_xz`
   - 先红后绿

当前下一步非常明确：

1. 等 `203500` 旧代码 run 完整结束
2. 立即用同 seed rerun，验证新的 plane-family guidance + lint 分辨率修正，是否能把首轮失败从这层 false positive 再继续前移

### `practice_runs/20260419_210500/` 与离线 preflight 复算，进一步定位出真正的 matcher 根因

在 `203500` 跑完后，我又用当时的代码启动了同一 seed 的 rerun：

1. `practice_runs/20260419_210500/hinged_pillbox_magnet_medium_v00_e34c3c99/`

这条 run 的价值不是结果变好，而是把 matcher 的误伤范围暴露得更清楚：

1. `actions/round_01_execute_build123d.json`
   - round 1 一共被打出了 6 个 `invalid_build123d_contract.named_face_plane_family_mismatch`
2. 这些 hit 同时覆盖了：
   - 真正错误的 front-face local edit：`line 40`、`line 67`
   - 不该一起报错的 base/lid host profile：`line 20`、`line 26`、`line 48`、`line 54`
3. 这说明旧逻辑的问题不只是“没看懂 front/back plane family”，而是：
   - 它根本没有区分“局部前表面编辑 sketch”和“壳体主体/内腔 profile sketch”

因此我又补了一条更贴近真实 run 的红测：

1. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py::test_named_face_plane_family_mismatch_only_hits_local_front_face_sketches_not_shell_profiles`
   - 这个测试直接要求：
     - `named_face_plane_family_mismatch` 只能命中真正的 front-face local sketches
     - 不能把 `Plane.XY` 的 base/lid shell profile 也一起算进去

对应实现：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 给 `_find_named_face_plane_family_mismatch_hits(...)` 增加了 host-profile 识别层
   - 新逻辑会先识别 enclosing `BuildPart` 的 host span ids
   - 然后把“尺寸仍明显属于主体 profile”的 `BuildSketch` 从 mismatch 候选里剔除

这一轮的三层证据已经齐：

1. 新红测先失败，再转绿：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k only_hits_local_front_face_sketches_not_shell_profiles`
2. 同 family 聚焦回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'only_hits_local_front_face_sketches_not_shell_profiles or named_front_face_plane_family_mismatch or ignores_bare_xy_host_profiles_when_front_face_local_edit_uses_xz or named_front_face_xz_plane_family'`
   - 结果：`4 passed`
3. 宽回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_skill_pack.py -q`
   - 结果：`187 passed`

我还对 `210500` 的 round 1 源码做了离线 preflight 复算，确认这个修复已经能真实改变同一份旧代码的 lint 结果：

1. 复算对象：
   - `practice_runs/20260419_210500/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_01_response.json`
2. 当前代码下的复算结果：
   - `named_face_plane_family_mismatch` 命中数已经从 `6 -> 2`
   - 只剩真正该保留的 `line 40` 与 `line 67`

### `practice_runs/20260419_221500/` 已启动，正在验证这次 matcher 收窄是否能把 enclosure seed 推进到下一层真实瓶颈

在完成上面的红绿测、宽回归和离线复算之后，我又立即启动了同一 seed 的真实 Kimi rerun：

1. `practice_runs/20260419_221500/hinged_pillbox_magnet_medium_v00_e34c3c99/`

这条 run 的目标很明确：

1. 不再看 matcher 在本地是不是“理论上变好了”
2. 而是看 live lane 里，这次 plane-family matcher 收窄之后，系统能否真正越过旧的 6-hit preflight 误伤面
3. 如果能越过去，下一层更可能暴露的共享瓶颈将是：
   - `repair_surface` 上的 `clamshell_host_local_cut_contract` 粘性不够
   - `validation_surface` 上的 front/back datum 漂移
   - `write_surface` 上 detached hinge transform 表达不稳定

### practice seed 继续扩展：新增 `clamshell_host_local_cut_focus`

为了避免 enclosure 侧后续验证只能继续围着 `hinged_pillbox_magnet_medium` 单条 seed 打转，我又在：

1. `practice/seed_manifest.json`

里新增了一条更专门的中难度 practice seed：

1. `clamshell_host_local_cut_focus`

这条 seed 的设计目的不是复刻当前 case，而是把下一层更可能暴露出来的 enclosure 共享问题单独拉出来练：

1. front/back datum 稳定性
2. shell local cut 是否落在正确 host face
3. detached hinge / living hinge 的表达是否稳定
4. side cable exit 与 front-face features 是否会相互干扰

我已经用 practice runner 入口验证过 manifest 仍可正常加载：

1. `seed_count = 7`
2. `last_seed = clamshell_host_local_cut_focus`

### `practice_runs/20260419_221500/ -> 20260419_223900/`：enclosure/clamshell 侧已经从 write-only 盲修推进到 topology-aware local finish

这组对比是今天最重要的新证据，因为它证明刚才补的 clamshell plane-family guidance 不是只让单测更好看，而是真的改变了 live Kimi run 的前半段轨迹。

先看旧 run：

1. `practice_runs/20260419_221500/brief_report.md`
2. `practice_runs/20260419_221500/hinged_pillbox_magnet_medium_v00_e34c3c99/summary.json`
3. `practice_runs/20260419_221500/hinged_pillbox_magnet_medium_v00_e34c3c99/practice_analysis.md`

关键事实：

1. `hallucination_events = 26`
2. `executed_action_types = [execute_build123d x 8]`
3. `query_topology = 0`
4. `local_targeting_action_count = 0`
5. round 1 直接同时撞上：
   - `invalid_build123d_contract.named_face_plane_family_mismatch`
   - `invalid_build123d_context.transform_context_manager`

对应最值得直接打开的文件：

1. `practice_runs/20260419_221500/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`

再看新 run：

1. `practice_runs/20260419_223900/brief_report.md`
2. `practice_runs/20260419_223900/hinged_pillbox_magnet_medium_v00_e34c3c99/summary.json`
3. `practice_runs/20260419_223900/hinged_pillbox_magnet_medium_v00_e34c3c99/practice_analysis.md`

关键变化已经很具体：

1. `hallucination_events: 26 -> 5`
2. `writes: 8 -> 5`
3. `query_topology: 0 -> 1`
4. `query_kernel_state: 0 -> 1`
5. `local_targeting_action_count: 0 -> 1`
6. `fresh_targeting_action_count: 0 -> 1`
7. `exact_ref_consumption_rate: 0.0 -> 1.0`

更重要的是 round 1 的错误形态确实被前移收窄了：

1. `221500` round 1：
   - 5 个 lint hit
   - 包含 `named_face_plane_family_mismatch`
2. `223900` round 1：
   - 2 个 lint hit
   - 只剩 `transform_context_manager`

对应直接证据：

1. `practice_runs/20260419_221500/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`
2. `practice_runs/20260419_223900/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`

`223900` 这条 run 的中段轨迹也很有价值，因为它说明系统已经不再只会一轮轮 whole-part 重写：

1. `queries/round_02_validate_requirement_post_write.json`
2. `queries/round_03_query_feature_probes.json`
3. `queries/round_03_query_kernel_state.json`
4. `queries/round_04_query_topology.json`
5. `actions/round_05_apply_cad_action.json`

这条链路说明：

1. round 2 已经先写出一个可验证的 whole-part 几何
2. 然后 runtime 进入：
   - `validate_requirement`
   - `query_feature_probes`
   - `query_kernel_state`
   - `query_topology`
   - `apply_cad_action(create_sketch, face_ref=...)`
3. 也就是说，enclosure/clamshell 侧已经被推进到了 topology-aware local finish，而不是继续卡在 front/back plane family 误伤

### 新暴露出的共享瓶颈：open sketch window 在只剩 2 轮时还会把模型锁死在 local_finish

`223900` 的 round 7/8 又暴露出另一条很明确的 runtime policy closure 问题：

1. `plans/round_07_response.json`
   - 模型已经明确判断要 whole-part fallback
2. 但 turn policy 仍然把它锁在 local_finish
3. 最后它只能发：
   - `apply_cad_action(action_type="snapshot")`
4. 被 preflight 明确拒绝：
   - `snapshot is not allowed while the turn_tool_policy is in local_finish mode`

直接证据：

1. `practice_runs/20260419_223900/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_07_response.json`
2. `practice_runs/20260419_223900/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_07_apply_cad_action.json`

这不是模型“笨”，而是 policy 太乐观：

1. `create_sketch -> query_sketch(empty)` 之后
2. 只剩 2 轮
3. 当前代码原本还认为 local_finish 可以继续

所以我又继续补了 runtime closure：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - `_open_sketch_window_requires_code_escape(...)`
2. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - `test_open_sketch_window_with_empty_sketch_and_two_rounds_left_exits_to_code_first_escape`

新规则：

1. 如果 latest action 还是 `create_sketch`
2. `query_sketch` 看到的仍然是空 sketch
3. 只剩 2 轮

那么就直接走：

1. `policy_id = code_escape_after_open_sketch_window_under_budget`
2. `mode = code_first`

而不是继续把模型锁死在 `apply_cad_action`。

### 新增 clamshell transform lane skill：把 `with Rot(...):` 这个共享残差继续收窄

`223900` 的 round 1 还剩 2 个 `transform_context_manager`，这说明 plane-family false positive 已经压下去后，真正下一层 first-write 矛盾就是：

1. clamshell front/back local cut
2. detached hinge helper
3. 仍然容易被写成 `with Rot(...):`

所以我又补了一个更窄的 family skill：

1. `src/sub_agent_runtime/skill_pack.py`
   - `execute_build123d_clamshell_transform_lane_contract`
2. `tests/unit/sub_agent_runtime/test_skill_pack.py`
   - `test_build_runtime_skill_pack_adds_clamshell_transform_lane_guidance_after_rot_context_failure`

它做的不是 case patch，而是把 clamshell repair turn 里两条 lane 分清：

1. front-face notch / label recess
   - 回到 host-native `BuildSketch(Plane.XZ.offset(±depth/2))`
2. hinge bar / hinge pin
   - 回到 detached positive solid -> `Rot(...) * part`

### `practice_runs/20260419_224800/` 已启动：用最新代码验证这两条收口是否能继续压低后半段空转

在补完：

1. `clamshell_transform_lane_contract`
2. `open_sketch_window` 的 2-round code escape closure

之后，我已经启动了新的真实 Kimi rerun：

1. `practice_runs/20260419_224800/hinged_pillbox_magnet_medium_v00_e34c3c99/`

这条 run 的目的非常明确：

1. 看最新 skill 是否能继续压低 round 1/2 的 `transform_context_manager`
2. 看最新 policy 是否能避免 `223900` 里 round 7 那种 `snapshot` 误逃生

当前它仍在运行中，后续会继续把结果同步回这份日志和汇报页。

### `practice_runs/20260419_224800/` 暴露的最新根因，以及当前已经落地的修复

`224800` 这条 live rerun 没有把 enclosure seed 再推进一层，但它把问题钉得更准了：

1. 它不是又冒出新的 family。
2. 它是把现有 `named_face_plane_family_mismatch` 的最后一层误伤面直接暴露出来了。
3. 具体表现是：
   - round 1 仍把 `inner_w = width - 2 * wall_thick`
   - `inner_d = depth - 2 * wall_thick`
   - 这种典型 host-profile alias 当成了 front-face local cut 的错误 plane family
   - 从而把本该允许的 `BuildSketch(Plane.XY.offset(...))` 壳体中空草图误判成 `expected_planes = XZ`

直接证据：

1. `practice_runs/20260419_224800/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_01_response.json`
2. `practice_runs/20260419_224800/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`

这次根因不是靠目测猜出来的，而是我直接用当前 lint 函数对 `224800` 的 round 1 代码做了离线复算，并进一步打到内部 helper 上：

1. 复算前：
   - `_find_named_face_plane_family_mismatch_hits(...)` 返回：
     - `line 45 -> XY`
     - `line 61 -> XY`
2. 继续下钻后看到：
   - `host spans = {'width', 'depth'}`
   - `sketch ids = {'inner_w', 'inner_d'}`
   - 但 `host_profile_alias_ids = set()`
3. 真正原因是：
   - `_collect_buildpart_host_profile_alias_ids(...)` 之前只按“精确命名”过滤 modifier
   - `wall_thick` 没有被当成 host-profile modifier
   - 所以 `inner_w/inner_d` 无法被识别成 width/depth 的派生 alias

对应修复已经落地到：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增 `_identifier_tokens(...)`
   - 新增 `_is_host_profile_modifier_id(...)`
   - 新增 `_strip_host_profile_modifier_ids(...)`
   - 把 host-profile modifier 识别从“精确名字匹配”升级成“token 归一 + alias 友好识别”
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - 新增：
     - `test_named_face_plane_family_mismatch_ignores_host_profile_aliases_derived_via_wall_thick_names`

这一轮红绿证据很完整：

1. 新测试先红：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k wall_thick_names`
   - 失败时命中：
     - `line 15 -> XY`
     - `line 18 -> YZ`
2. 修复后转绿：
   - 同一测试只剩真正该保留的 `line 18 -> YZ`
3. 宽回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
   - 结果：`142 passed`

更重要的是，这次修复已经命中真实产物链，而不是只停在单测：

1. 我把 `224800` 的 round 1 真实代码重新喂给当前 `_find_named_face_plane_family_mismatch_hits(...)`
2. 当前结果已经从旧的 2 个误伤 hit 降到：
   - `[]`
3. 也就是说，`224800` 暴露出来的最后这层 host-profile alias 误伤，当前代码已经在 lint 层被清掉了

### `practice_runs/20260419_231500/` 已启动：验证 enclosure seed 是否能越过这次 alias 误伤面

在上面这轮：

1. 根因复算
2. 新红测
3. 修复转绿
4. `142 passed` 宽回归

之后，我已经立即启动新的真实 Kimi rerun：

1. `practice_runs/20260419_231500/`

当前已经落盘的起始工件有：

1. `practice_runs/20260419_231500/practice_manifest.json`
2. `practice_runs/20260419_231500/hinged_pillbox_magnet_medium_v00_e34c3c99/prompt.txt`
3. `practice_runs/20260419_231500/hinged_pillbox_magnet_medium_v00_e34c3c99/prompts/round_01_request.json`
4. `practice_runs/20260419_231500/hinged_pillbox_magnet_medium_v00_e34c3c99/trace/events.jsonl`

这条 run 的验证目标现在也非常具体：

1. 不再只是看 round 1 能不能“少几个 error”
2. 而是看 enclosure seed 能不能真正越过：
   - `wall_thick/inner_w/inner_d` 这一层 host-profile alias 误伤
3. 如果能越过去，下一层更有价值的真实瓶颈大概率会收缩到：
   - `transform_context_manager`
   - detached hinge positive-solid lane
   - 或者更后面的 read / local-finish closure

### `231500` 结束后继续新增的两层通用修复：`mating faces` 语义归类 + `RectangleRounded` radius bounds 前移

`231500` 这条 run 虽然是旧代码启动的，但它又暴露出两条很有价值、而且明显属于共享 contract 的问题：

1. requirement 里同时出现：
   - `mating faces`
   - `front face`
   这种混合语义时，旧的 `_named_face_requirement_plane_groups(...)` 只会识别出 `front_back`，不会把 `mating faces` 归到 `top_bottom`
2. first write 里像：
   - `RectangleRounded(label_w, label_h, label_r)`
   如果 `2 * radius >= min(width, height)`，旧系统会直接让它进执行期，再在 sandbox 里抛：
   - `ValueError: width and height must be > 2*radius`

对应修复已经继续落地到：

1. `src/sub_agent_runtime/tool_runtime.py`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`

具体内容：

1. requirement-plane parser 现在新增：
   - `mating face`
   - `mating faces`
   - `mating surface`
   - `mating surfaces`
   统一映射到 `top_bottom`
2. 新增 `RectangleRounded` radius bounds lint：
   - `invalid_build123d_contract.rectanglerounded_radius_bounds`
   - 会在 preflight 阶段直接拦截“半径已经不可能成立”的 rounded rectangle profile

新的红绿证据：

1. `test_named_face_requirement_plane_groups_include_mating_faces_as_top_bottom`
2. `test_named_face_plane_family_mismatch_allows_xy_mating_face_edits_when_requirement_mentions_front_face_and_mating_faces`
3. `test_preflight_lint_rejects_rectanglerounded_radius_that_exceeds_half_of_height`

整份 lint 回归：

1. `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q`
2. 结果：`145 passed`

而且这两层修复都已经能改变 `231500` 的离线复算结果：

1. 对 `231500` round 3 代码重新跑 `_find_named_face_plane_family_mismatch_hits(...)`
   - 当前结果已经是 `[]`
2. 对 `231500` round 1 代码重新跑 `_preflight_lint_execute_build123d(...)`
   - 当前不再是执行期 `ValueError` 盲炸
   - 而是会提前给出：
     - `invalid_build123d_contract.rectanglerounded_radius_bounds`

### `practice_runs/20260419_234500/` 已启动：用最新代码一起验证这四层收口是否真正进入 live lane

在补完：

1. host-profile alias suppressor
2. `mating faces -> top_bottom`
3. `RectangleRounded` radius bounds lint
4. 之前已有的 clamshell transform/open-sketch closure

之后，我已经继续发起了最新真实 Kimi rerun：

1. `practice_runs/20260419_234500/`

当前已落盘的启动工件：

1. `practice_runs/20260419_234500/practice_manifest.json`
2. `practice_runs/20260419_234500/hinged_pillbox_magnet_medium_v00_e34c3c99/prompt.txt`
3. `practice_runs/20260419_234500/hinged_pillbox_magnet_medium_v00_e34c3c99/prompts/round_01_request.json`
4. `practice_runs/20260419_234500/hinged_pillbox_magnet_medium_v00_e34c3c99/trace/events.jsonl`

这条 run 的目标现在更具体了：

1. 先看 first write 是否还会在旧的：
   - host-profile alias
   - `mating faces` plane-family 误杀
   - `RectangleRounded` radius runtime 爆炸
   这三层浪费回合
2. 如果这三层都被前移或压掉，下一层真正需要继续优化的就更可能是：
   - detached hinge positive-solid 表达
   - clamshell front/back local cut 的 whole-part 收口
   - 以及后半段 local-finish / validation closure

### `234500` 结束后的新结论：主问题已经继续收缩到 detached hinge 的 canonical lane，而不是 plane-family alias 误伤

`practice_runs/20260419_234500/hinged_pillbox_magnet_medium_v00_e34c3c99/` 跑完以后，当前主矛盾已经很清楚：

1. round 1 不再出现 `224800` 那一类：
   - host-profile alias 误伤
   - `mating faces` 归类误伤
   - `RectangleRounded` 直接运行期盲炸
2. 真实失败已经继续收缩到 detached hinge family：
   - round 2 仍把 back-edge hinge barrel / pin 写成 `Plane.YZ`
   - round 8 在强预算压力下又回退成 `with Rot(...): Cylinder(...)`

这条收缩是有真实证据的：

1. `practice_runs/20260419_234500/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_02_response.json`
2. `practice_runs/20260419_234500/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_02_execute_build123d.json`
3. `practice_runs/20260419_234500/hinged_pillbox_magnet_medium_v00_e34c3c99/plans/round_08_response.json`
4. `practice_runs/20260419_234500/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_08_execute_build123d.json`

这说明当前系统已经不是“连 Build123d API 基本面都没压住”，而是进入了更窄的 clamshell / hinge repair lane 收敛阶段。

### 已继续落地：把 back-edge hinge 的 seam / axis 语义显式写进 skill 与 repair recipe

基于 `234500` 的真实失败代码，我继续补了一层 family-level canonical lane，目标不是写新 case rule，而是把 clamshell detached hinge 的公共几何语义讲清楚：

1. back edge 先定义的是 seam location：
   - 仍是 `y = ±depth/2`
2. cylinder axis 是另一件事：
   - 应该由 transform 决定
   - 不应该因为“hinge 在 back”就误写成 `Plane.YZ`
3. detached hinge 的 canonical lane 应该显式给出：
   - `Pos(0, ±depth/2, split_z) * (Rot(Y=90) * hinge_barrel.part)`

这层修复已经同步进入：

1. `src/sub_agent_runtime/skill_pack.py`
2. `src/sub_agent_runtime/tool_runtime.py`
3. `tests/unit/sub_agent_runtime/test_skill_pack.py`
4. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`

新增测试覆盖的要点：

1. `execute_build123d_clamshell_host_local_cut_contract` 必须明确：
   - 不要把 back-edge hinge seam 重解释为 `Plane.YZ`
   - seam location 与 hinge axis direction 是两件事
   - safe lane 是 `Pos(...) * (Rot(Y=90) * hinge_barrel.part)`
2. `execute_build123d_clamshell_transform_lane_contract` 也必须讲同样的 lane
3. `clamshell_host_local_cut_contract` repair recipe skeleton 也必须返回同样的 deterministic guidance

真实红绿证据：

1. 红测：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_skill_pack.py -q -k 'clamshell_host_local_cut_contract_guidance or frontloads_clamshell_host_local_cut_contract_on_first_turn or clamshell_transform_lane_guidance_after_rot_context_failure'`
   - 初始结果：`3 failed`
2. 绿测：
   - 同一条 `skill_pack` focused 回归
   - 结果：`3 passed`
3. recipe 绿测：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'prefers_clamshell_host_local_cut_recipe_for_named_face_plane_family_mismatch'`
   - 结果：`1 passed`
4. 合并 focused 回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'clamshell_host_local_cut_contract or clamshell_transform_lane_guidance_after_rot_context_failure or prefers_clamshell_host_local_cut_recipe_for_named_face_plane_family_mismatch'`
   - 结果：`4 passed`

### `practice_runs/20260419_184731/` 已启动：验证新的 hinge canonical lane 是否进入 live Kimi practice

在上面这层修复转绿之后，我已经立即继续发起新的真实 Kimi rerun：

1. `practice_runs/20260419_184731/`

当前已经落盘的启动工件：

1. `practice_runs/20260419_184731/practice_manifest.json`
2. `practice_runs/20260419_184731/hinged_pillbox_magnet_medium_v00_e34c3c99/prompt.txt`
3. `practice_runs/20260419_184731/hinged_pillbox_magnet_medium_v00_e34c3c99/prompts/round_01_request.json`
4. `practice_runs/20260419_184731/hinged_pillbox_magnet_medium_v00_e34c3c99/trace/events.jsonl`

这条 run 现在验证的不是“项目能不能跑”，而是更具体的 family 收敛目标：

1. round 2 是否还会把 detached hinge barrel / pin 写成 `Plane.YZ`
2. 后半段是否还会在预算压力下回退成 `with Rot(...):`
3. 如果这两层都被压下去，下一轮就更值得继续看：
   - clamshell whole-part completeness
   - local-finish / validation closure

### `practice_runs/20260419_220300/` 追加证明：plain `pin hinge` 的 detached-hardware 误解已经被明显压下去

围绕 clamshell highwater seed，我继续把真实 root cause 往前推，而不是只看最终 `PASS/FAIL`。这次先把一个共享语义收窄了：

1. `pin hinge` / `mechanical hinge` 不再默认放开 detached hinge hardware
2. 只有显式出现：
   - `removable pin`
   - `separate hinge parts`
   - `exposed hinge assembly`
   这一类表述时，才允许 detached hinge lane

这层 contract 已经同步进入四个面：

1. `src/sub_agent/prompts/codegen.md`
2. `src/sub_agent_runtime/context_manager.py`
3. `src/sub_agent_runtime/skill_pack.py`
4. `src/sub_agent_runtime/tool_runtime.py`

对应 focused 红绿测试：

1. 红测：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_codegen_prompt_contract.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'clamshell_split_axis_and_hinge_contract or system_prompt_surfaces_core_build123d_contracts or centered_clamshell_split_pose_and_boundaries or keeps_clamshell_contract_priority_when_slots_family_comes_from_kernel'`
   - 初始结果：`4 failed`
2. 绿测：
   - 同一条 focused 回归
   - 结果：`4 passed`
3. 扩展 clamshell/hinge 回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_codegen_prompt_contract.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'clamshell or detached or rectangle_rounded_outer_envelope or transform_context_manager or pin_hinge'`
   - 结果：`27 passed`

这层改动已经进入新的 live Kimi run：

1. `practice_runs/20260419_220300/clamshell_enclosure_highwater_v00_355620b2/`

最关键的 round 1 证据：

1. `plans/round_01_response.json`
   - 已不再规划 detached `hinge_pin` / detached hinge barrels / extra hinge solids
   - 直接走 `result = Compound([base.part, lid.part])`
2. `actions/round_01_execute_build123d.json`
   - `solids = 2`
   - `bbox = [72.0, 65.0, 14.0]`
3. `queries/round_01_validate_requirement_post_write.json`
   - `include_two_part_lid_base_separation` 已从 round 1 就被判为 `verified`
   - 不再出现旧 run 那类 `geometry_solids=5/7; expected_parts=2`
4. `queries/round_02_query_feature_probes.json`
   - `general_geometry.blockers = ["bbox_dimension_mismatch"]`
   - `half_shell.blockers = ["bbox_dimension_mismatch", "missing_hinge_like_cylindrical_evidence"]`
   - 说明主矛盾已经从“extra detached parts”收缩到“整体高度/hinge 几何不对”

这和旧 run 的差异是实质性的：

1. 旧证据：
   - `practice_runs/20260419_213645/clamshell_enclosure_highwater_v00_355620b2/queries/round_02_validate_requirement_post_write.json`
   - `include_two_part_lid_base_separation` blocker 证据是 `geometry_solids=5; expected_parts=2`
2. 新证据：
   - `practice_runs/20260419_220300/clamshell_enclosure_highwater_v00_355620b2/queries/round_01_validate_requirement_post_write.json`
   - `include_two_part_lid_base_separation` 在 round 1 已验证通过

这说明当前改动确实把 root cause 从 detached extra parts 前移压掉了，而不是只是在末尾 validator 上“解释得更好”。

### `220300` 暴露的下一层共享根因：centered clamshell 的 `extrude` 语义仍被误解

在 detached-hardware 被压掉之后，live run 继续暴露出更窄的新瓶颈：

1. round 1：
   - `actions/round_01_execute_build123d.json`
   - `bbox = [72.0, 65.0, 14.0]`
2. round 3：
   - `actions/round_03_execute_build123d.json`
   - `bbox = [72.0, 65.5, 16.9]`

两轮共同问题都是：

1. 模型把 sketch 放到“想象中的壳体中心平面”
2. 然后直接 `extrude(amount=h)`
3. 结果以为会得到围绕该平面对称展开的 shell interval
4. 实际上 Build123d 的 `extrude(amount=h)` 是从当前 sketch plane 单侧生长

也就是说，这不是 clamshell case 专属规则，而是一个更通用的 Build123d 语义误解：

1. `Locations((0, 0, center_z)) + extrude(amount=h)` 不会自动生成 centered shell
2. 如果要守住 lid/base 的目标 Z 区间，必须：
   - sketch 在真实起始面
   - 或者先生成 centered solid 再显式平移

因此我把这条语义也提升成共享 contract，同样同步到：

1. `src/sub_agent/prompts/codegen.md`
2. `src/sub_agent_runtime/context_manager.py`
3. `src/sub_agent_runtime/skill_pack.py`
4. `src/sub_agent_runtime/tool_runtime.py`

对应 focused 红绿测试：

1. 红测：
   - 仍是上面的 4 文件 focused 回归
   - 初始结果：`4 failed`
2. 绿测：
   - 同一条 focused 回归
   - 结果：`4 passed`
3. 扩展 clamshell/hinge 回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_codegen_prompt_contract.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'clamshell or detached or rectangle_rounded_outer_envelope or transform_context_manager or pin_hinge'`
   - 结果：`27 passed`

这条 centered-extrude contract 加进去以后，下一步要看的就很明确：

1. 新的 live rerun 是否还能在 round 1/3 出现 `72 x 65 x 14` / `72 x 65.5 x 16.9` 这种高度塌缩
2. 如果高度塌缩也被压下去，clamshell seed 的主矛盾就会继续收缩到：
   - hinge boundary cylindrical evidence
   - notch / local host grounding
   - magnet slot evidence coverage

### `230215` 暴露的更底层共享错误：back-edge hinge 的 seam 坐标与 cylinder 轴向被混淆

在 `practice_runs/20260419_230215/clamshell_enclosure_highwater_v00_355620b2/` 这轮里，detached-subtractive 已经不是主导问题，但 round 1 立即暴露出新的共享错误：

1. `plans/round_01_response.json`
   - 模型直接把 hinge barrels 和 pin 写成了未旋转 `Cylinder(...)`
   - 并把它们放在 `hinge_y = -depth/2` 的 back seam 上
2. `actions/round_01_execute_build123d.json`
   - `solids = 2`
   - `bbox = [72.0, 66.0, 76.0]`
   - 第二个 solid 的 `bbox.zlen = 76.0`
3. `queries/round_01_validate_requirement_post_write.json`
   - `create_a_snap_clamshell_enclosure_with_overall_dimensions_72mm_x_64mm_x_26mm` 被直接判成 `contradicted`
   - 证据是 `requested_dimensions=[72.0, 64.0, 26.0], realized_bbox=[72.0, 66.0, 76.0]`
   - `use_a_pin_hinge` 也被判成 `contradicted`
4. `queries/round_02_query_feature_probes.json`
   - `general_geometry.blockers = ["bbox_dimension_mismatch"]`
   - `slots.blockers = ["feature_notch_or_profile_cut", "need_topology_host_selection"]`

这个错误的本质不是“不会写 hinge”，而是把两件不同的语义混在了一起：

1. `y = -depth/2` 是 hinge seam 的位置约束
2. `Cylinder(...)` 默认沿 Z 轴生长
3. back-edge pin hinge 通常要求 cylinder 轴向沿 X/width

因此，单纯把未旋转 `Cylinder(...)` 放到 `(x, hinge_y, split_z)`，不会自动得到沿 X 的 hinge barrel，只会得到一个仍沿 Z 的实体，并且很容易把整体包围盒高度直接拉坏。

### 新增共享合同：`clamshell_hinge_unrotated_default_cylinder`

为了把这类错误前移成 preflight 阶段的结构化失败，而不是继续让它进入真实执行后再由 validator 兜底，本轮新增了一个 family-level lint contract：

1. 新规则：
   - `invalid_build123d_contract.clamshell_hinge_unrotated_default_cylinder`
2. 触发目标：
   - requirement 明确是 clamshell + pin/mechanical hinge
   - 代码把未旋转 `Cylinder(...)` 直接放到 back-edge hinge seam 的 `Locations((x, hinge_y, ...))` / `Locations((x, -depth/2, ...))` 上
3. repair lane：
   - 继续走 `clamshell_host_local_cut_contract`
   - 明确要求：
     - 不要把 seam 坐标误当成 axis 方向
     - 不要把未旋转默认 `Cylinder(...)` 直接当成 X-axis hinge barrel/pin
     - host-owned hinge 用 axis-correct host-native lane
     - detached hinge hardware 只有在 prompt 明确要求时才允许，并且必须 positive-first 后再旋转装配

对应代码改动：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 增加 `_find_clamshell_unrotated_default_hinge_cylinder_hits(...)`
   - 新增 lint rule `invalid_build123d_contract.clamshell_hinge_unrotated_default_cylinder`
   - 将该规则并入 `clamshell_host_local_cut_contract`
2. `src/sub_agent/prompts/codegen.md`
   - 增加“不要把未旋转默认 Cylinder 直接放到 `(x, hinge_y, split_z)` 就当成 X-axis hinge”提示
3. `src/sub_agent_runtime/context_manager.py`
   - 同步系统 prompt 合同
4. `src/sub_agent_runtime/skill_pack.py`
   - 同步 split-axis / host-local-cut 两条 skill guidance

对应红绿测试：

1. focused 红绿：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_codegen_prompt_contract.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'clamshell_hinge_unrotated_default_cylinder or centered_clamshell_split_pose_and_boundaries or system_prompt_surfaces_core_build123d_contracts or clamshell_split_axis_and_hinge_contract or unrotated_clamshell_hinge_cylinder_axis_mismatch'`
   - 结果：`4 passed`
2. 扩展 clamshell 相关回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sub_agent_runtime/test_context_manager.py tests/unit/sub_agent_runtime/test_codegen_prompt_contract.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'clamshell or detached or rectangle_rounded_outer_envelope or transform_context_manager or pin_hinge'`
   - 结果：`28 passed`

这条合同的价值在于，它不针对某个固定 case 写死 geometry，而是把一个共享的 Build123d 语义误解正式提升成了：

1. prompt-level 明示
2. runtime skill-level 强约束
3. preflight-lint 早期拦截

下一步 live 验证目标已经切清楚：

1. fresh rerun `practice_runs/20260419_231730/` 是否会在 round 1 直接避开 `bbox = [72, 66, 76]` 这一类 hinge-axis 失配
2. 如果它被成功拦下，后续主矛盾将继续集中到：
   - notch/local host realization
   - hinge topology evidence
   - magnet slot clause grounding

### 继续收敛 prompt 质量：重复 lint 去重 + `CenterArc end_angle` 前移

围绕 `practice_runs/20260419_231730/clamshell_enclosure_highwater_v00_355620b2/` 的真实产物，又暴露出两个共享问题：

1. `prompts/round_02_user_prompt.txt`
   - 同一个 `invalid_build123d_contract.clamshell_hinge_unrotated_default_cylinder` 在 `previous_tool_failure_summary.lint_hits` 里重复出现 3 次
   - 这会挤占 repair prompt 的上下文预算，让 distinct failure surface 露不出来
2. `actions/round_02_execute_build123d.json`
   - 实际报错是 `TypeError: CenterArc.__init__() got an unexpected keyword argument 'end_angle'`
   - 说明 `CenterArc(..., end_angle=...)` 仍然会漏过 preflight，直到 runtime exception 才暴露

因此本轮继续做了两条共享修复：

1. `src/sub_agent_runtime/context_manager.py`
   - `_summarize_failure_lint_hits(...)` 现在会按 `rule_id` 去重
   - 保留首次出现顺序
   - 同类重复项新增 `occurrence_count`
2. `src/sub_agent_runtime/tool_runtime.py`
   - 把 `CenterArc(..., end_angle=...)` 正式前移成 preflight lint：
     - `invalid_build123d_keyword.center_arc_end_angle_alias`
   - repair hint 统一要求：
     - 保留 `start_angle=...`
     - 把 `end_angle=` 改成 `arc_size=...`
   - 同时把 recipe 路由拆成：
     - path-sweep truly specific lint
     - arc signature shared lint
   - 避免 `CenterArc` 签名错误在非 sweep 语境里被错误路由到 `build123d_path_sweep_contract`

对应 focused 红绿测试：

1. 去重红测：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_context_manager.py -q -k summarize_failure_lint_hits_deduplicates_repeated_rule_ids`
   - 初始结果：`1 failed`
2. 去重绿测：
   - 同一条 focused 回归
   - 结果：`1 passed`
3. `CenterArc end_angle` 红测：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k center_arc_end_angle_alias`
   - 初始结果：payload 为 `None`
4. route-fix 绿测：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_context_manager.py -q -k 'center_arc_end_angle_alias or center_arc_arc_angle_alias or center_arc_missing_start_angle or summarize_failure_lint_hits_deduplicates_repeated_rule_ids'`
   - 结果：`3 passed`

文档合同也同步补到了：

1. `docs/cad_iteration/TOOL_SURFACE.md`
   - 增加 `CenterArc(..., end_angle=...)` 属于 preflight invalid surface
   - 增加 failure summary dedupe / `occurrence_count` 约束
2. `docs/cad_iteration/ITERATION_PROTOCOL.md`
   - 补充相同的 preflight / prompt compaction 合同

### 新 practice 证据：问题已经从 clamshell 轴向误解继续收缩到 named-face / transform lane

新产生的 practice run：

1. `practice_runs/20260419_234500/hinged_pillbox_magnet_medium_v00_e34c3c99/summary.json`
2. `practice_runs/20260419_234500/brief_report.md`

当前结果说明：

1. `query_topology = 1`
   - 说明 practice lane 已经真实进入 topology-aware read，而不是只做 whole-part rewrite
2. `hallucination_events = 10`
   - 比 `231730` 的 `14` 更低
3. 主导问题已经切到：
   - `invalid_build123d_contract.named_face_plane_family_mismatch`
   - `invalid_build123d_context.transform_context_manager`
4. `brief_report.md` 的 case issue 已经直接给出：
   - `Use Locations(...) for scoped placement, or apply the transform with Rot(...) * solid instead of with Rot(...):`

也就是说，新 practice lane 的主要价值正在显现：

1. 不再只是围着固定 benchmark family 补规则
2. 会不断暴露新的 Build123d canonical contract gap
3. 这些 gap 可以被正式沉淀成：
   - preflight governance
   - clearer repair lane routing
   - read-model / topology targeting 约束

### live 证明 failure-summary 去重已经生效，并继续补上 transform-wrapped primitive 漏检

新 clamshell rerun：

1. `practice_runs/20260419_221440/clamshell_enclosure_highwater_v00_355620b2/`

这轮最关键的不是最终还没 PASS，而是 prompt 质量和 failure family 的变化已经能被直接看到：

1. `actions/round_01_execute_build123d.json`
   - 第一轮主导失败变成：
     - `invalid_build123d_contract.detached_subtractive_builder_without_host`
   - 三个失败点分别是：
     - `base_magnets`
     - `lid_magnets`
     - `thumb_notch`
2. `prompts/round_02_user_prompt.txt`
   - `previous_tool_failure_summary.lint_hits` 已经不再展开成 3 条重复项
   - 现在只保留 1 条：
     - `rule_id = invalid_build123d_contract.detached_subtractive_builder_without_host`
     - `occurrence_count = 3`
3. `actions/round_02_execute_build123d.json`
   - 第二轮主导失败变成：
     - `invalid_build123d_context.transform_context_manager`
   - 同样只在 prompt 里保留 1 条并给出：
     - `occurrence_count = 2`

这说明前面做的 `previous_tool_failure_summary` 去重不是停留在 unit test，而是已经真实进入 live rerun prompt surface。

接着这轮又暴露出一个新的共享漏检：

1. `actions/round_03_execute_build123d.json`
   - round 3 已经能成功写出 geometry
   - 但输出变成：
     - `solids = 4`
     - `bbox = [72.0, 67.0, 23.0]`
2. round 3 代码里用了这一类模式：
   - `cutter = Pos(...) * Cylinder(...)`
   - `barrel = Rot(...) * Cylinder(...)`
   - 然后在活跃 builder 里继续：
     - `base.part = base.part - cutter`
     - `base.part = base.part + barrel`
3. `queries/round_03_validate_requirement_post_write.json`
   - 直接暴露出新的主导 blocker：
     - `create_a_snap_clamshell_enclosure_with_overall_dimensions_72mm_x_64mm_x_26mm`
     - `include_two_part_lid_base_separation`
     - `feature_notch_or_profile_cut`
   - 对应证据：
     - `realized_bbox = [72.0, 67.0, 23.0]`
     - `geometry_solids = 4; expected_parts = 2`

这里的根因是：

1. 仓库里原本已经能拦 `cone = Cone(...); cone = Pos(...) * cone; part.part = part.part - cone`
2. 但还漏掉了更直接的一种写法：
   - `cutter = Pos(...) * Cylinder(...)`
   - 也就是 transform 和 primitive 在同一条赋值里直接绑定

因此本轮继续补了一个更通用的 matcher 扩展：

1. `src/sub_agent_runtime/tool_runtime.py`
   - `_primitive_constructor_name(...)` 现在会识别 transform-wrapped primitive：
     - `Pos(...) * Cylinder(...)`
     - `Rot(...) * Cylinder(...)`
   - `_find_active_buildpart_temporary_primitive_transform_hits(...)`
     - 现在也能识别“直接赋值即 transform-wrapped primitive”的形态
2. 新增 focused 红绿测试：
   - `test_preflight_lint_rejects_active_builder_part_mutation_for_direct_transformed_primitive_assignment`
3. focused 回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'direct_transformed_primitive_assignment or active_builder_transform_rebind_for_temporary_primitives or keeps_clamshell_contract_priority_over_nested_hollow_slot_recipe or rejects_active_builder_part_mutation_inside_buildpart'`
   - 结果：`4 passed`

这条修复的意义是：

1. 下一轮再出现 `cutter = Pos(...) * Cylinder(...)` / `barrel = Rot(...) * Cylinder(...)`
2. runtime 会在 preflight 直接把它识别成：
   - `active_builder_part_mutation`
   - `active_builder_temporary_primitive_arithmetic`
   - `active_builder_temporary_primitive_transform_rebind`
3. 并继续优先路由到 `clamshell_host_local_cut_contract`
4. 而不是让这类 builder-authority 破坏先写出错误多实体，再由 validator 被动指出 `solids=4`
