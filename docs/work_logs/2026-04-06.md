# 2026-04-06

## 今日目标

1. 按 Claude-Code 风格 vNext 方案继续重构 V2。
2. 默认写入路径切到 code-first，减少把复杂需求拆成过细结构化动作的依赖。
3. 新增模型可自主调用的诊断工具，而不是继续在 runner 里堆 case patch。

## 当前判断

1. `20260406_013300` 相比上一轮“通过数下降”，本质上不是纯退化，而是：
   - validator 假阳性和 sketch-only 假 timeout 被压下去
   - 真实几何能力缺口暴露出来
2. 当前主要瓶颈不是基础模型不会写 CadQuery，而是：
   - 缺少 family-level probe 工具
   - 缺少模型可自主写的小型诊断脚本通道
   - 当前 loop 仍然偏向细碎结构化动作，导致每一步推理成本高、反馈密度低

## 本轮实施方向

1. 新增 `query_feature_probes`
   - 面向 hollow section / annular groove / explicit anchor hole / orthogonal union / axisymmetric profile
2. 新增 `execute_cadquery_probe`
   - 允许模型运行一次性诊断代码
   - 只写 `queries/` 侧产物，不污染主 session
3. 在 runtime/context/report 中显式暴露：
   - `primary_write_mode`
   - `feature_probe_count`
   - `probe_code_count`
   - `failure_cluster`

## 约束

1. 不再往 runner 增加新的 case-specific patch。
2. 不牺牲现有 inspectability：
   - `prompts/`
   - `plans/`
   - `actions/`
   - `queries/`
   - `outputs/`
   - `trace/`
3. 外部 contract 保持稳定。

## 本轮已完成

1. 新增 probe 工具的真实 MCP 链路已经打通：
   - `query_feature_probes`
   - `execute_cadquery_probe`
2. 修复了一个真实 server 层问题：
   - `build_unstructured_content()` 之前不支持 `QueryFeatureProbesOutput`
   - 导致 MCP `structuredContent=None`
   - 运行中表现为 `sandbox_mcp_invalid_query_feature_probes_payload`
3. `previous_tool_failure_summary` 现在会记录：
   - `consecutive_write_failure_count`
   - `same_tool_failure_count`
4. skill pack 新增“连续 whole-part code 失败后，下一轮必须先 probe 再重写”的 family-level guidance，而不是继续盲目重写。
5. 项目默认 reasoning 配置已重新对齐到：
   - provider: `kimi`
   - model: `kimi-k2.5-thinking`
   - timeout: `180s`

## 验证

1. focused unit tests:
   - `PYTHONPATH=src uv run --extra dev pytest -q tests/unit/sandbox/test_mcp_runner.py tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py tests/unit/sub_agent_runtime/test_v2_runtime.py`
   - 结果：`152 passed`
2. 真实 live probe：
   - run dir: `test_runs/20260406_191123`
   - 作用：首次暴露 `query_feature_probes` 的 MCP 内容构建错误
3. 修复后再次 live probe：
   - run dir: `test_runs/20260406_191645`
   - 结果：运行完成，新的 probe 工具链不再因为 server payload 丢失而中断
4. 单 case benchmark 对照：
   - `benchmark/runs/20260406_191843`
   - `L1_218` 在 `glm / 60s` 下表现为 `NO_STEP + llm timeout`
   - `benchmark/runs/20260406_192520`
   - `L1_218` 在 `kimi-k2.5-thinking / 180s` 下变为 `PASS`
   - 运行结果：
     - `validation_complete=true`
     - `evaluation_passed=true`
     - `score=0.9598178326010692`

## 当前判断

1. `provider/model/timeout` 不是旁枝，而是系统行为的一部分。
2. 这次对照证明：
   - 之前一些 “架构问题” 的外观，实际上被 `glm + 60s` 的配置放大了
   - 切回 `kimi-k2.5-thinking + 180s` 后，V2 code-first 路线在 `L1_218` 上已经能真正闭环
3. 下一步优先级：
   - 继续跑 L1 / sampled L2
   - 观察新的失败是否开始更多集中到 family skill / probe tool gap
   - 而不是继续停留在 provider timeout / validator disagreement / MCP payload breakage

## L1 Full（本轮最新）

1. 完整 run：
   - `benchmark/runs/20260406_193100`
2. 汇总结果：
   - `PASS=6`
   - `EVAL_FAIL=3`
   - `VALIDATOR_MISMATCH=1`
   - `generated_step_case_count=10`
   - `validator_evaluator_disagreement_case_count=1`
3. 相比此前 `PASS=4 / NO_STEP=2` 的阶段，当前最重要的改进是：
   - 10 个 case 都能稳定产出 STEP
   - 失败主要集中到少数 family/tool gap，而不是大面积 runtime/transport/timeout 崩坏
4. 剩余失败 case：
   - `L1_122`
   - `L1_159`
   - `L1_191`
   - `L1_218`
5. 共同特征：
   - `failure_cluster` 全部落到 `tool_gap`
   - `feature_probe_count=0`
   - 说明新 probe 工具已经可用，但模型在这些失败链路里还没有稳定切过去
6. 当前最新判断：
   - 下一刀不应该再是“补 skill 文案”
   - 应该把“连续 whole-part code 失败后先 probe 再重写”的要求提升为 runtime 级 turn policy / tool exposure policy

## 下一刀实施计划（runtime turn policy）

1. 在 V2 loop 引入显式 `turn_tool_policy`
   - 记录 `policy_id / mode / reason / allowed_tool_names / blocked_tool_names / preferred_probe_families`
2. 触发条件先做一条通用规则：
   - 最新失败写入是 `execute_cadquery`
   - 同一工具连续失败次数 `>= 2`
   - requirement 仍属于 family-driven code-first 几何族
   - 且自最近失败写入后还没有跑过 `query_feature_probes` 或 `execute_cadquery_probe`
3. 触发后下一轮不再暴露 broad rewrite surface，而是只暴露 probe-first 诊断工具面
4. 目标不是再给模型多一段提示词，而是直接改变当前 turn 可调用的工具集合
5. 验证顺序：
   - 先跑 focused unit tests
   - 再跑 `L1_218 / L1_191 / L1_159 / L1_122`
   - 如果 probe 使用率和结果变好，再重跑完整 `L1 full`
