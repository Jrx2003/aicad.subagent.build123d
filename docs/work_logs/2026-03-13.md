# Work Log - 2026-03-13

## Scope

1. Diagnose L1_122 generation drift using full evidence (`prompts/`, `plans/`, `queries/`, `trace/`).
2. Improve iterative planner/runtime behavior so edits are evidence-driven and window-complete.
3. Replace benchmark scoring with non-LLM STEP geometric-signature scoring.
4. Keep evaluation images for fast human comparison (generated vs ground-truth, four views each).

## Diagnosis Notes (L1_122, run `20260312_180817`)

1. Planner proposed sketch-edit sequence but one-action mode executed only `create_sketch`, leaving modification window incomplete.
2. Planner then drifted to unrelated finishing actions (`fillet`, `hole`) not requested by user prompt.
3. Requirement validation was too weak for free-text benchmark prompt (passed with only `solid_exists/no_issues`).

## Planned Fixes

1. Runtime policy: complete sketch modification window (`create_sketch` -> profile ops -> `cut_extrude`/`extrude`) in one round when safe.
2. Planner prompt hardening: avoid unrequested decorative features; force core-shape closure for notch/groove/U-shape tasks.
3. Benchmark evaluator rewrite: score by geometric signatures only; export generated/ground-truth four-view previews.

## Implemented Updates (Today)

1. Fixed iterative replay determinism in `IterativeSubAgentRunner`:
   - each full-prefix replay now runs with `clear_session=True` to prevent duplicated history accumulation across rounds.
2. Strengthened runtime action rewrites for notch/U-shape flows:
   - skip secondary sketch profiles before first solid in notch-like requirements.
   - align post-solid `create_sketch` plane with requirement extrude axis.
   - align post-solid `cut_extrude` distance with requirement extrude distance when clearly under-cut.
3. Fixed polygon action compatibility:
   - `add_polygon` now accepts `vertices` alias (in addition to `points`) so planner outputs remain executable.
4. Refined benchmark evaluator:
   - deterministic STEP signature scoring now emphasizes transform-insensitive geometry terms.
   - evaluation artifact set is cleaned to only keep `generated_preview_*` and `ground_truth_preview_*` (plus score JSON/summary and `model.step`).
5. Docs updated:
   - `docs/OBSERVABILITY.md`
   - `docs/RUNBOOK.md`
   - `docs/cad_iteration/ITERATION_PROTOCOL.md`
   - `benchmark/README.md`

## Verification Runs

1. Focused unit tests:
   - `uv run pytest -q tests/unit/sub_agent_runtime/test_runner_contracts.py tests/unit/sandbox_mcp_server/test_action_params_contract.py`
   - pass: `23 passed`
2. Real benchmark case (`L1_122`) reruns:
   - `20260313_113852`: pass (`score=0.8369`)
   - `20260313_115948`: fail (planner drift case, used for diagnosis)
   - `20260313_121139`: fail (multi-solid regression case, used for diagnosis)
   - `20260313_121537`: near miss (`score=0.7602`, under-cut volume)
   - `20260313_122017`: pass (`score=0.9445`) with corrected sketch-plane/cut-distance rewrite behavior.

## Late-Session Deep Fix (L1_122 mismatch vs score)

1. Issue observed:
   - generated model still deviated from U-shape intent in some runs, while score could remain high.
2. Root causes confirmed from `prompts/plans/queries/trace`:
   - post-solid notch rectangle sometimes had no `position`, causing center-cut instead of top-notch.
   - evaluator previously lacked a robust profile-position penalty.
3. New runtime fix:
   - added rewrite `add_rectangle_position_inferred_for_notch_top_cut`:
     infer top-notch position from requirement dimensions (e.g. `50x40` and `30x25` -> position `[0.0, 7.5]`).
4. New evaluation fix:
   - added ranked normalized-centroid profile metric (`centroid_ranked_rel_max`) to penalize wrong notch placement even when volume/bbox match.
   - normalized component weights to sum exactly `1.0`.
5. Final validation run:
   - `20260313_132623`:
     - key rewrites hit in trace:
       - `create_sketch_plane_aligned_with_primary_extrude_axis`
       - `add_rectangle_position_inferred_for_notch_top_cut`
       - `cut_extrude` executed with full required distance
     - generated stats aligned with GT (volume/surface/bbox_sorted/faces/edges).
     - benchmark eval output includes only generated/gt four-view previews + eval JSON artifacts.

## Full L1 Run (Requested by User)

1. Command executed:
   - `./benchmark/run_prompt_benchmark.sh --levels L1 --reasoning-provider kimi --reasoning-model moonshot-v1-128k --max-rounds 6`
2. Run root:
   - `benchmark/runs/20260313_135108`
3. Selection scope:
   - `selected_case_count=10`
   - `L1_20, L1_40, L1_79, L1_101, L1_122, L1_148, L1_157, L1_159, L1_191, L1_218`
4. Runtime completion:
   - case runtime success: `10/10` (all cases produced `summary.json` and `outputs/final_model.step`)
   - evaluation pass: `5/10` at threshold `0.78`
5. PASS cases:
   - `L1_20` (`0.8200`)
   - `L1_101` (`0.7936`)
   - `L1_122` (`1.0000`)
   - `L1_157` (`0.8294`)
   - `L1_191` (`0.8441`)
6. FAIL cases:
   - `L1_40` (`0.0400`)
   - `L1_79` (`0.5950`)
   - `L1_148` (`0.4403`)
   - `L1_159` (`0.6999`)
   - `L1_218` (`0.5621`)
7. Notable trace observation:
   - `L1_218` round 4 had a `revolve` failure (`BRep_API: command not done`), then the loop entered diagnostic inspection rounds; final case still completed but failed eval threshold.
8. Reports generated:
   - `benchmark/runs/20260313_135108/summary.json`
   - `benchmark/runs/20260313_135108/brief_report.tsv`
   - `benchmark/runs/20260313_135108/brief_report.md`

## Late Update (2026-03-13 PM, Generalization + Tool-Surface Stability)

1. New diagnosis focus:
   - checked whether prior fixes were over-fitted to `L1_122`.
   - re-read `L1` evidence (`prompts/plans/actions/queries/trace`) from `benchmark/runs/20260313_154253`.
2. Root causes confirmed:
   - `create_sketch` over-attached to existing solid faces for canonical planes (`XY/XZ/YZ`), causing axis drift in cross-axis tasks.
   - `add_circle` 3D position handling lacked robust plane-aware mapping in sketch-local coordinates.
   - stepped-profile `add_polygon(lengths+radiuses)` orientation was poor for revolved-shaft prompts.
   - planner outputs still produced corner-frame circle arrays in centered-array prompts.
3. Implemented upgrades:
   - `create_sketch` plane handling:
     - canonical planes stay global (`XY/XZ/YZ`);
     - face aliases (`Top/Bottom/Front/Back/Left/Right`) attach to corresponding solid faces when available.
   - `add_circle`:
     - supports `diameter` alias;
     - 3D `position/center` now maps by active sketch plane (`XY/XZ/YZ`);
     - safer default radius behavior.
   - `add_polygon(length_list/radius_list)`:
     - stepped profile rebuilt with radius-axial ordering more suitable for revolve profiles.
   - runtime rewrites:
     - centered-array circle position normalization (`add_circle_position_normalized_to_centered_array`);
     - centerline tuple inference for sketch placement (`create_sketch_centerline_position_inferred_from_requirement`);
     - subtractive-cylinder flow rewrite (`extrude` -> `cut_extrude`) when requirements indicate boolean-difference slot/groove.
4. Prompt policy updates:
   - added explicit constraints for subtractive cylinder flow and centered arrays.
5. Documentation updated first for behavior policy:
   - `docs/cad_iteration/TOOL_SURFACE.md`
   - `docs/cad_iteration/ITERATION_PROTOCOL.md`
   - `docs/cad_iteration/SYSTEM_RECORD.json` (`record_version=1.3.2`)
   - `docs/OBSERVABILITY.md` rewrite reason list expanded.
6. Test verification:
   - `uv run pytest -q tests/unit/sandbox_mcp_server/test_action_params_contract.py tests/unit/sub_agent_runtime/test_runner_contracts.py tests/unit/sub_agent/test_codegen_aci.py`
   - result: `45 passed`.
7. Runtime validation notes:
   - direct LLM benchmark retry (`benchmark/runs/20260313_164815`) was blocked by Kimi account quota (`429 exceeded_current_quota_error`).
   - trace-level rewrite verification still confirmed on replayed runs (e.g. `test_runs/20260313_165332/L1_79/trace/events.jsonl` includes:
     - `create_sketch_centerline_position_inferred_from_requirement`
     - `extrude_promoted_to_cut_extrude_for_subtractive_requirement`).

## Runtime Artifacts (Latest)

1. LLM full-L1 attempt (blocked by provider quota):
   - `benchmark/runs/20260313_164815`
2. Live probe attempts:
   - `test_runs/20260313_164609` (Kimi quota error)
   - `test_runs/20260313_164657` (GLM key not configured in env)
3. Replay regression run root (real sandbox replay with fixed runtime/tooling):
   - `test_runs/20260313_165332`
   - summary: `test_runs/20260313_165332/replay_regression_report.json`
