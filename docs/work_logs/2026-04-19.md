# 2026-04-19

## 今日简版汇报

### 三句话结论

1. 今天最重要的推进，不是继续围 benchmark 加规则，而是把 practice lane 补成了无 Docker 也能真实跑通的 live lane。
2. 两条真实 run `practice_runs/20260419_143759` 与 `practice_runs/20260419_144300` 已经证明：主矛盾重新收敛到 write-side Build123d contract。
3. `Plane(...).moved(...)` 这类共享 API 幻觉已经退出首轮主导面，当前下一层共享瓶颈是 `broad_shell_axis_fillet_on_fresh_host`。

### 最新追加结论

1. `practice_runs/20260419_154837 -> 20260419_155523 -> 20260419_155831` 三条真实 Kimi run 已经把问题逐层压缩：先解决 graph-refresh 卡死，再重开 round 4 的 code repair，最后第一次真实走到 `query_topology -> apply_cad_action(face_ref=...)`。
2. `practice_runs/20260419_155831/front_face_sketch_recess_focus_v00_194ed1dd/practice_analysis.md` 已证明：
   - `local_targeting_action_count = 1`
   - `fresh_targeting_action_count = 1`
   - `exact_ref_consumption_rate = 1.0`
3. 当前最明确剩余瓶颈已经收窄到 `explicit_anchor_hole` 的 countersink 几何承认链，而不是“前表面 ref 取不到”。
   - 直接证据：
     - `practice_runs/20260419_155831/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_06_validate_requirement.json`
4. 今天又补了一条 budget closure：
   - `repair_after_local_finish_semantic_refresh_under_budget`
   - 作用：successful local finish 之后如果又做过 semantic refresh，且只剩最后一轮，不再把最后一轮浪费在 `validate_requirement`，而是直接重开 actionable repair lane。
   - 代码：
     - `src/sub_agent_runtime/agent_loop_v2.py`
     - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
5. 新 rerun `practice_runs/20260419_160521/` 已进一步证明：最后一轮可以真实切回修复，而不是纯 validation。
   - 直接证据：
     - `practice_runs/20260419_160521/front_face_sketch_recess_focus_v00_194ed1dd/plans/round_06_response.json`
     - `practice_runs/20260419_160521/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_06_execute_build123d.json`
     - `practice_runs/20260419_160521/front_face_sketch_recess_focus_v00_194ed1dd/trace/failure_bundle.json`
6. 新增一条 family-level local-finish bias：
   - 当 exact `face_ref` 已经存在且目标就是 `hole/countersink` 这类直接特征时，优先 direct `apply_cad_action(hole|countersink, face_ref=...)`，不要先开 `create_sketch(face_ref=...)`。
   - 代码：
     - `src/sub_agent_runtime/context_manager.py`
   - 单测：
     - `tests/unit/sub_agent_runtime/test_context_manager.py::test_prompt_payload_adds_local_finish_contract_when_topology_refs_exist`
7. 今天又补齐了另一半尾段 closure：
   - `code_escape_after_local_finish_semantic_refresh_under_budget`
   - 作用：successful local finish 之后如果又做过 semantic refresh、只剩最后一轮、blocker 还在、但拿不到 actionable patch，就直接放行 `execute_build123d` 做 whole-part escape，而不是退回 `validate_after_local_finish_semantic_refresh`。
   - 代码：
     - `src/sub_agent_runtime/agent_loop_v2.py`
   - 红绿测：
     - `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py::test_last_round_after_successful_local_finish_semantic_refresh_falls_back_to_code_escape_without_patch`

### 今天做了什么

1. 把 validator 的 grounding 缺口从隐式状态补成结构化 surface。
   - 代码：
     - `src/sandbox_mcp_server/contracts.py`
     - `src/sandbox_mcp_server/validation_grounding.py`
     - `src/sandbox_mcp_server/service.py`
   - 新字段：
     - `grounding_gap_reasons`
     - `overclaim_guard`
     - `required_evidence_kinds`

2. 把 repair packet 的 recipe contract 校验正式前移到 runtime adapter。
   - 代码：
     - `src/sub_agent_runtime/tool_adapters.py`
   - 新返回面：
     - `repair_packet_contract_miss`
     - `missing_anchor_keys`
     - `missing_parameters`
     - `recipe_contract`

3. 把 practice lane 的 read-model 使用质量补成可统计指标，而不再只看“有没有 query_topology”。
   - 代码：
     - `src/sub_agent_runtime/practice_runner.py`
   - 新指标：
     - `fresh_targeting_action_count`
     - `stale_ref_action_count`
     - `nonconcrete_ref_action_count`
     - `candidate_label_ref_action_count`
     - `exact_ref_consumption_rate`

4. 补了 `local-process` sandbox fallback，让 practice lane 在本机没有 Docker daemon 时仍能真实执行 Build123d。
   - 代码：
     - `src/common/config.py`
     - `src/sandbox/__init__.py`
     - `src/sandbox/local_process_runner.py`
     - `src/sandbox/docker_runner.py`

5. 把 `Plane(...).moved(...)` 收进 API governance 和首轮 hygiene，作为共享 Build123d 幻觉处理，而不是继续靠 case 内自然碰撞。
   - 代码：
     - `src/sub_agent_runtime/tool_runtime.py`
     - `src/sub_agent_runtime/skill_pack.py`

### 如何证明有效

1. 今天新增与回归相关单测已经通过。
   - 执行：
     - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_practice_runner.py tests/unit/sub_agent_runtime/test_tool_adapters.py tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py tests/unit/sub_agent_runtime/test_skill_pack.py tests/unit/sandbox/test_local_process_runner.py tests/unit/sandbox/test_docker_runner.py tests/unit/sandbox_mcp_server/test_validate_requirement_contract.py tests/unit/sub_agent_runtime/test_hallucination.py -q`
   - 结果：
     - `238 passed`

2. `local-process` 不是只存在于单测，已经真实进入 practice live lane。
   - 直接证据：
     - `practice_runs/20260419_143759/practice_manifest.json`
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`
   - 关键信息：
     - round 1 stderr 已出现 `build123d-local-process-...`
     - run 最终成功落出 `model.step` 与四视图图片，而不是再次死在 MCP server 启动

3. 第一条真实 practice run 已经完整跑完，说明当前项目在 practice lane 上是可运行的。
   - run：
     - `practice_runs/20260419_143759/`
   - 汇总：
     - `practice_runs/20260419_143759/brief_report.md`
     - `practice_runs/20260419_143759/run_diagnostics.md`
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/practice_analysis.md`
   - 关键信息：
     - `status = partial_geometry`
     - `step_file_exists = True`
     - `hallucination.event_count = 3`
     - `hallucination.primary_layer = write_surface`
     - `local_targeting_action_count = 1`
     - `fresh_targeting_action_count = 1`
     - `stale_ref_action_count = 0`
     - `exact_ref_consumption_rate = 1.0`

4. 第二条真实 rerun 进一步证明：今天新增治理已经改变了首轮错误形态。
   - run：
     - `practice_runs/20260419_144300/`
   - 汇总：
     - `practice_runs/20260419_144300/brief_report.md`
     - `practice_runs/20260419_144300/run_diagnostics.md`
     - `practice_runs/20260419_144300/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`
   - 对比结论：
     - `hallucination.event_count: 3 -> 2`
     - `writes: 4 -> 3`
     - 首轮主失败从 `Plane.YZ.moved(...)` 变成 `invalid_build123d_contract.broad_shell_axis_fillet_on_fresh_host`
   - 解释：
     - 这不代表任务已经完成，而是说明今天新增的共享治理确实把错误从 API 猜错推进到了更窄的 shell host 组织问题。

5. 当前主瓶颈已经被进一步收窄，而不是停留在“环境不通”。
   - 直接证据：
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_02_execute_build123d.json`
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_03_execute_build123d.json`
   - 关键信息：
     - round 1：`Plane.YZ.moved(...)` 触发真实 Build123d API 幻觉
     - round 2：`SlotOverall(...)` 在 active `BuildPart` 中被 preflight lint 直接拦下
     - round 3：修完后成功产出 `model.step`

6. read-model 与 topology-aware local finish 不是空转，已经在真实 case 中被消费。
   - 直接证据：
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/queries/round_05_query_topology.json`
     - `practice_runs/20260419_143759/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_06_apply_cad_action.json`
   - 关键信息：
     - `preferred_ref_id = face:1:F_bf763cd5c387`
     - round 6 已真实消费这个 `face_ref`
     - `fresh_targeting = true`

### 当前有效结论

1. practice lane 现在已经能在无 Docker daemon 的开发机上跑出真实 Build123d 几何、render 图和完整工件链。
2. 当前复杂新 prompt 的主瓶颈不是 read-model 缺席，而是 write-side Build123d contract 仍有共享幻觉残差。
3. read-model 的提升方向现在更明确：不是继续加“有没有读”的统计，而是继续提升“读到的 ref 是否被新鲜、具体地消费”。
4. validator / repair / read-model 的分层改造已经不再只是设计，今天的 diff 已经把三条关键 surface 真正落盘。

### 下一步

1. 继续用同一个 seed rerun，验证新加的 `Plane(...).moved(...)` guidance / preflight 是否能把首轮失败再向后推。
   - 正在运行：
     - `practice_runs/20260419_144300/`

2. 继续压 `feature_notch_or_profile_cut` 这条共享 family，不针对单 case 写硬规则，而是继续增强：
   - `BuildSketch` / host-plane contract
   - host-owned local cut lane
   - topology-aware local finish closure

3. 继续把 today 的 surface 往汇报与诊断面贯通：
   - `grounding_gap_reasons`
   - `repair_packet_contract_miss`
   - `fresh_targeting_action_count`
   - `stale_targeting_cases`

## 补充入口

1. 汇报页：
   - `report-20260419-build123d-practice-progress/index.html`
   - `report-20260419-build123d-practice-progress/shareable-report.md`
2. 重点 run：
   - `practice_runs/20260419_143759/`
3. 进行中的 rerun：
   - `practice_runs/20260419_144300/`

## 2026-04-19 第二阶段追加进展

### `practice_runs/20260419_145502/` 证明：first-write 已继续收紧，但 read-model 仍有一层共享误导

这条 run 是今天后半段最关键的一条真实对照，因为它把新加的 rounded-envelope guidance 和 read-model 缺口都一起照出来了。

直接证据：

1. `practice_runs/20260419_145502/brief_report.md`
2. `practice_runs/20260419_145502/run_diagnostics.md`
3. `practice_runs/20260419_145502/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_01_execute_build123d.json`
4. `practice_runs/20260419_145502/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_02_execute_build123d.json`
5. `practice_runs/20260419_145502/hinged_pillbox_magnet_medium_v00_e34c3c99/queries/round_02_validate_requirement_post_write.json`
6. `practice_runs/20260419_145502/hinged_pillbox_magnet_medium_v00_e34c3c99/queries/round_04_query_topology.json`
7. `practice_runs/20260419_145502/hinged_pillbox_magnet_medium_v00_e34c3c99/actions/round_05_apply_cad_action.json`

这条 run 的三个关键信号：

1. round 1 的首轮主失败已不再是 `Plane(...).moved(...)`，也不再是 fresh-host broad fillet，而是更窄的：
   - `invalid_build123d_contract.detached_subtractive_builder_without_host`
2. round 2 whole-part 写已经真实成功：
   - `actions/round_02_execute_build123d.json -> success = true`
3. round 5 失败不是 write-side API 幻觉，而是 read-side target 误导：
   - `invalid_reference: face_ref 'face:1:F_1e5e00c2830d' is not planar (geom_type=CYLINDER)`

这条 run 的汇总结论：

1. `build123d_hallucination.event_count = 2`
2. `build123d_hallucination.weighted_score = 1.6`
3. 分层从只剩 `write_surface`，变成：
   - `write_surface = detached subtractive builder without host`
   - `read_surface = non-planar face_ref consumed by create_sketch`

这说明当前瓶颈已经进一步细化：

1. first-write 侧已经不再主要卡在最浅层 API 猜错
2. 但 `query_topology -> apply_cad_action` 之间仍可能因为 candidate 排序不够好，把圆柱面放到 sketch host 前面

### 针对 `145502` 已新增的 read-model 修复

为了避免 `front_faces` / `back_faces` / `left_faces` / `right_faces` / `top_faces` / `bottom_faces` 默认把不可 sketch 的曲面排到前面，本轮已补一条共享 read-model 排序修复：

1. `src/sandbox_mcp_server/service.py`
   - 方向面 candidate sets 现在默认 `planar first`，再按面积降序
2. `tests/unit/sandbox_mcp_server/test_topology_candidate_sets.py`
   - 新增：当 `front_faces` 同时含平面与圆柱面时，默认首选平面

对应回归：

1. `./.venv/bin/pytest tests/unit/sandbox_mcp_server/test_topology_candidate_sets.py -q`
   - `6 passed`
2. `./.venv/bin/pytest tests/unit/sandbox_mcp_server/test_action_contracts.py -q -k 'query_topology or face_ref'`
   - `4 passed, 6 deselected`

### 当前继续进行中的验证

1. 我已经基于这条 planar-first 排序修复继续发起下一条真实 rerun：
   - `practice_runs/<next timestamp>/...`
2. 目标不是提高单 case 分数，而是验证：
   - `query_topology` 是否会把可 sketch 的 planar `front_face` 排到前面
   - `apply_cad_action(create_sketch(face_ref=...))` 是否能少一次 read-surface invalid_reference

### `practice_runs/20260419_150602/` 进一步证明：focused local-finish 链路已经完整进入 live run，但末端 contract 还需继续收紧

这条 run 的价值比 `150141` 更高，因为它专门回答了 front-face local finish 这条链到底有没有真实跑起来。

直接证据：

1. `practice_runs/20260419_150602/brief_report.md`
2. `practice_runs/20260419_150602/run_diagnostics.md`
3. `practice_runs/20260419_150602/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_03_query_feature_probes.json`
4. `practice_runs/20260419_150602/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_04_query_topology.json`
5. `practice_runs/20260419_150602/front_face_sketch_recess_focus_v00_194ed1dd/plans/round_05_response.json`
6. `practice_runs/20260419_150602/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_05_apply_cad_action.json`
7. `practice_runs/20260419_150602/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_06_apply_cad_action.json`

这条 run 证明了四件事：

1. round 2 whole-part 写已经稳定成功。
2. round 3 已真实进入 `query_feature_probes`。
3. round 4 已真实进入 `query_topology`，而且 `Front Faces` 候选已经收缩成单一 ref。
4. round 5 / 6 没有再卡在“没有 topology evidence”，而是暴露出更末端的两个 local-finish contract：
   - round 5：`apply_cad_action preflight failed: get_history is not allowed while the turn_tool_policy is in local_finish mode`
   - round 6：`apply_cad_action preflight failed: cut_extrude requires an active profile sketch with non-empty profile_refs`

这说明 focused local-finish 主链其实已经打通：

1. `execute_build123d`
2. `validate_requirement`
3. `query_feature_probes`
4. `query_topology`
5. `apply_cad_action`

但末端还有两层 shared contract 需要继续收：

1. 当 exact refs 已经存在时，禁止把 `apply_cad_action` 浪费在 `get_history` 这类 session-control 逃逸动作上。
2. 当 local edit 需要 `cut_extrude` 时，必须先有 active sketch / profile_refs，而不是直接跳到 cut。

### 基于 `150602` 已新增的 local-finish contract 修复

针对 round 5 的 `get_history` 逃逸动作，本轮已继续前移一条共享 contract：

1. `src/sub_agent_runtime/context_manager.py`
   - local-finish instructions 新增：
     - `do not spend apply_cad_action on get_history or any other session-control escape action`
2. `tests/unit/sub_agent_runtime/test_context_manager.py`
   - 新增断言，确保这条约束进入 prompt payload

## 2026-04-19 第三阶段追加进展

### `practice_runs/20260419_151916/` 证明：exact topology ref 已经从“读得到”推进到“真实写得到”

这条 run 是今天最关键的 topology-targeting 证据，因为它第一次把之前修的两层改动同时跑进了真实 Kimi live lane：

1. `candidate_set.preferred_ref_id` 优先于杂乱 `matched_ref_ids`
2. local-finish prompt 里新增了可直接消费的 exact ref 文本提示

直接证据：

1. `practice_runs/20260419_151916/brief_report.md`
2. `practice_runs/20260419_151916/run_diagnostics.md`
3. `practice_runs/20260419_151916/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_03_query_topology.json`
4. `practice_runs/20260419_151916/front_face_sketch_recess_focus_v00_194ed1dd/plans/round_04_response.json`
5. `practice_runs/20260419_151916/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_04_apply_cad_action.json`
6. `practice_runs/20260419_151916/front_face_sketch_recess_focus_v00_194ed1dd/queries/round_05_query_sketch.json`

这条 run 给出了两个明确结论：

1. round 4 已真实消费平面 face ref：
   - `face_ref = face:1:F_4514e213348b`
   - `actions/round_04_apply_cad_action.json -> success = true`
2. 主瓶颈已经不再是“选错 non-planar 面”，而是“在只剩 1-2 轮时仍被困在 open sketch tail”

对应汇总面：

1. `hallucination.event_count = 1`
2. `hallucination.weighted_score = 0.6`
3. 唯一 hallucination 已不是 topology targeting 失败，而是：
   - `apply_cad_action preflight failed: clear_session is not allowed while the turn_tool_policy is in local_finish mode`

这说明今天这一轮 local-finish 改造已经把主问题继续前推了一层：

1. 从 “query_topology 之后仍选错曲面”  
2. 变成 “runtime 没给 sketch tail 一个正式的 budget escape lane”

### 针对 `151916` 已新增的 policy 修复

这条 run 直接促成了一个共享 policy 级改动：

1. `src/sub_agent_runtime/agent_loop_v2.py`
   - 新增 open sketch tail 的 budget 风险判断
   - 当 `remaining_rounds < min_write_steps_remaining` 时，退出 `continue_open_sketch_window_after_apply_action`
   - 正式切到：
     - `policy_id = code_escape_after_open_sketch_window_under_budget`
     - `mode = code_first`
2. `tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py`
   - 新增 critical budget 场景回归测试

对应回归：

1. `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q -k 'open_sketch_window_under_critical_budget_exits_to_code_first_escape or continue_open_sketch_window_after_apply_action or open_sketch_window_persists_after_query_sketch_when_profile_is_ready'`
   - 通过

### `practice_runs/20260419_152405/` 证明：critical-budget escape 已经进入真实 live lane

这条 run 的价值不在于最后完全收敛，而在于它给出了 policy 改动已经生效的真实证据。

直接证据：

1. `practice_runs/20260419_152405/brief_report.md`
2. `practice_runs/20260419_152405/run_diagnostics.md`
3. `practice_runs/20260419_152405/front_face_sketch_recess_focus_v00_194ed1dd/summary.json`
4. `practice_runs/20260419_152405/front_face_sketch_recess_focus_v00_194ed1dd/plans/round_06_response.json`
5. `practice_runs/20260419_152405/front_face_sketch_recess_focus_v00_194ed1dd/actions/round_06_execute_build123d.json`

这条 run 明确证明了两件事：

1. `forced_policy_chain` 已真实出现：
   - `code_escape_after_open_sketch_window_under_budget`
2. 第 6 轮不再退化成 `clear_session`，而是真的切回：
   - `execute_build123d`

也就是说，今天新增的 budget escape 不是只在单测里通过，而是已经进入真实 Kimi practice run。

### `152405` 暴露出来的下一层共享写侧瓶颈

第 6 轮虽然成功切回了 whole-part rewrite，但新的失败也很有价值：

1. `actions/round_06_execute_build123d.json`
   - `AttributeError: 'Plane' object has no attribute 'rotate'. Did you mean: 'rotated'?`

这不是 case-specific 问题，而是和更早的 `Plane(...).moved(...)` 同类型的共享 Build123d plane transform 幻觉。

因此本轮又继续补了一层共享 governance：

1. `src/sub_agent_runtime/tool_runtime.py`
   - 新增：
     - `invalid_build123d_api.plane_rotate_shape_method_guess`
   - 并把它纳入现有：
     - `build123d_plane_rotation_contract`
2. `tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py`
   - 新增对应单测

对应回归：

1. `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'plane_rotate_shape_method_guess or plane_rotated_origin_guess'`
   - 通过
2. `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_tool_runtime_preflight_lint.py -q -k 'preferred_face_refs or local_finish or plane_moved_shape_method_guess or plane_located_shape_method_guess'`
   - 通过

### 今天收口后的最新判断

到当前为止，4/19 的真实 practice 结论已经很明确：

1. `query_topology -> apply_cad_action(create_sketch(face_ref=...))` 这条 exact ref lane 已经真实打通。
2. open sketch tail 在 critical budget 下现在已经有了正式 code escape，不再只能退化成 `clear_session`。
3. 主瓶颈再次向前推进，已经变成更窄的共享 write-side plane transform 幻觉：
   - `Plane(...).moved(...)`
   - `Plane....rotate(...)`
4. 这正符合当前目标：不是围某个 benchmark case 写硬规则，而是持续把共享 hallucination family 收编进 governance / skill / policy / read-model 这几层明确边界里。
3. 回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_context_manager.py -q -k 'local_finish_contract or topology_targeting_summary or system_prompt_surfaces_core_build123d_contracts'`
   - `4 passed, 9 deselected`
4. 相关策略回归：
   - `./.venv/bin/pytest tests/unit/sub_agent_runtime/test_agent_loop_v2_policy.py -q -k 'local_finish or query_topology'`
   - `18 passed, 37 deselected`

### 下一条 focused rerun 的验证目标

1. 继续用 `front_face_sketch_recess_focus`
2. 验证刚新增的 local-finish contract 是否能让 round 5 直接消费 exact ref，而不再浪费在 `get_history`
